---
title: Глава 4. Описание служб GUIX
description: В этой главе приводится описание всех служб GUIX (перечислены ниже) в алфавитном порядке.
author: philmea
ms.author: philmea
ms.date: 05/19/2020
ms.topic: article
ms.service: rtos
ms.openlocfilehash: b07e275468484ccc905655dcd13197de42b2ac86
ms.sourcegitcommit: 4ebe7c51ba850951c6a9d0f15e22d07bb752bc28
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/20/2021
ms.locfileid: "110223415"
---
# <a name="chapter-4---description-of-guix-services"></a>Глава 4. Описание служб GUIX

В этой главе приводится описание всех служб GUIX (перечислены ниже) в алфавитном порядке.  

В разделе "Возвращаемые значения" для приведенных ниже описаний API значения, **выделенные полужирным шрифтом**, не затрагиваются определением **GX_DISABLE_ERROR_CHECKING**, используемым для отключения проверки ошибок API, в то время как значения, не выделенные полужирным шрифтом, полностью отключены. 

| **Служба GUIX**                      | **Описание**                                                                             |
| -------------------------------------- | -------------------------------------------------------------------------------------------- |
| gx_accordion_menu_create            | Создание меню "гармошка"                                                                        |
| gx_accordion_menu_draw              | Вывод меню "гармошка"                                                                          |
| gx_accordion_menu_event_process    | Обработка события меню "гармошка"                                                                 |
| gx_accordion_menu_position          | Размещение пунктов меню                                                                          |
| gx_animation_canvas_define          | Указание памяти для контроллера анимации для холста, которую следует использовать для последующих анимаций. |
| gx_animation_create                  | Создание контроллера анимации                                                               |
| gx_animation_delete                  | Удаление одного или нескольких контроллеров анимации |
| gx_animation_drag_disable           | Отключение перехватчика анимации перетаскивания экрана                                                           |
| gx_animation_drag_enable            | Включение перехватчика анимации перетаскивания экрана                                                            |
| gx_animation_landing_speed_set     | Задание стартовой скорости размещения для анимации перетаскивания экрана                                                  |
| gx_animation_start                   | Инициация последовательности анимации                                                               |
| gx_animation_stop                    | Приостановка последовательности анимации                                                                |
| gx_binres_language_count_get      |  Получение количества языков из файла двоичного ресурса                                          |
| gx_binres_language_info_load      |  Чтение сведений о названии и размере языка из файла двоичного ресурса.                           |
| gx_binres_language_table_load      | Загрузка таблицы языков из буфера данных двоичного ресурса (не рекомендуется)                          |
| gx_binres_language_table_load_ext | Загрузка таблицы языков из буфера данных двоичного ресурса                                       |
| gx_binres_theme_load                | Загрузка темы из буфера данных двоичного ресурса                                                |
| gx_brush_default                     | Инициализация значений по умолчанию для текущей кисти                                                         |
| gx_brush_define                      | Определение кисти                                                                                 |
| gx_button_background_draw           | Вывод фона кнопки                                                                       |
| gx_button_create                     | Кнопка "Создать"                                                                                |
| gx_button_deselect                   | Отмена выбора кнопки                                                                              |
| gx_button_draw                       | Вывод кнопки                                                                                  |
| gx_button_event_process            | Обработка события кнопки                              |
| gx_button_select                    | Кнопка выбора                                     |
| gx_canvas_alpha_set                | Задание значения альфа-смешения для холста                  |
| gx_canvas_arc_draw                 | Вывод дуги окружности                                   |
| gx_canvas_block_move               | Перемещение блока                                        |
| gx_canvas_circle_draw              | Вывод окружности                                       |
| gx_canvas_create                    | Создание холста                                   |
| gx_canvas_delete                    | Удаление холста                                   |
| gx_canvas_drawing_complete         | Завершение рисования на холсте                           |
| gx_canvas_drawing_initiate         | Начало рисования на холсте                        |
| gx_canvas_ellipse_draw             | Вывод эллипса                                   |
| gx_canvas_hardware_layer_bind     | Привязка холста к графическому слою                     |
| gx_canvas_hide                      | Сделать холст невидимым                           |
| gx_canvas_line_draw                | Вывод линии                                         |
| gx_canvas_memory_define            | Назначение адреса памяти холста                      |
| gx_canvas_offset_set               | Назначение смещения холста по осям X и Y                  |
| gx_canvas_pie_draw                 | Вывод круговой фигуры (секторной)                          |
| gx_canvas_pixel_draw               | Вывод одного пикселя                               |
| gx_canvas_pixelmap_blend           | Смешивание карты отображения пикселей с фоном                  |
| gx_canvas_pixelmap_get             | Получение карты отображения пикселей, указывающей на данные холста            |
| gx_canvas_pixelmap_draw            | Вывод карты отображения пикселей                                     |
| gx_canvas_pixelmap_rotate          | Поворот карты отображения пикселей                                   |
| gx_canvas_pixelmap_tile            | Замостить карту отображения пикселей                                     |
| gx_canvas_polygon_draw             | Вывод многоугольника                                      |
| gx_canvas_rectangle_draw           | Вывод прямоугольника                                    |
| gx_canvas_rotated_text_draw       | (Не рекомендуется.) Вывод текста, повернутого вокруг центральной точки |
| gx_canvas_rotated_text_draw_ext  | Вывод текста, повернутого вокруг центральной точки              |
| gx_canvas_shift                     | Сдвиг холста по осям X, Y                               |
| gx_canvas_show                      | Сделать холст видимым                             |
| gx_canvas_text_draw                | (Не рекомендуется.) Вывод текста                            |
| gx_canvas_text_draw_ext           | Вывод текста                                         |
| gx_checkbox_create                  | Создание флажка                                 |
| gx_checkbox_draw                    | Вывод флажка                                   |
| gx_checkbox_event_process          | Функция обработки событий флажка                   |
| gx_checkbox_pixelmap_set           | Назначение карты отображения пикселей флажка                          |
| gx_checkbox_select                  | Выбор флажка                                   |
| gx_circular_gauge_angle_get       | Получение угла стрелки для мини-приложения датчика                |
| gx_circular_gauge_angle_set       | Назначение угла стрелки для мини-приложения датчика                  |
| gx_circular_gauge_animation_set   | Определение анимации кругового датчика                   |
| gx_circular_gauge_background_draw | Вывод фона кругового датчика                    |
| gx_circular_gauge_create           | Создание мини-приложения кругового датчика                    |
| gx_circular_gauge_draw             | Вывод мини-приложения кругового датчика                      |
| gx_circular_gauge_event_process   | Обработка события кругового датчика                      |
| gx_context_brush_default            | Задание кисти текущего контекста                                      |
| gx_context_brush_define             | Определение кисти текущего контекста                                       |
| gx_context_brush_get                | Получение кисти текущего контекста                                          |
| gx_context_brush_pattern_set       | Задание шаблона кисти текущего контекста                           |
| gx_context_brush_set                | Задание кисти текущего контекста                                          |
| gx_context_brush_style_set         | Задание стиля кисти текущего контекста                                    |
| gx_context_brush_width_set         | Задание ширины кисти текущего контекста                                     |
| gx_context_color_get                | Разрешение идентификатора цвета в значение цвета                                     |
| gx_context_fill_color_set          | Задание цвета заливки текущего контекста                                     |
| gx_context_font_get                 | Разрешение идентификатора шрифта в значение указателя шрифта                               |
| gx_context_font_set                 | Задание шрифта текущего контекста                                           |
| gx_context_line_color_set          | Задание цвета линии текущего контекста                                     |
| gx_context_pixelmap_get             | Разрешение идентификатора карты отображения пикселей в значение указателя карты отображения пикселей                       |
| gx_context_pixelmap_set             | Назначение кисти карты отображения пикселей, используемой для заливки областей                            |
| gx_context_raw_brush_define        | Определение необработанной кисти текущего контекста                                   |
| gx_context_raw_fill_color_set     | Задание цвета необработанной заливки текущего контекста                                 |
| gx_context_raw_line_color_set     | Задание цвета необработанной линии текущего контекста                                 |
| gx_context_string_get               | Получение строки, связанной с текущим контекстом рисования (не рекомендуется) |
| gx_context_string_get_ext          | Получение строки, связанной с текущим контекстом рисования (не рекомендуется) |
| gx_display_active_language_set     | Назначение активного языка                                                |
| gx_display_color_set                | Замена одного значения цвета в таблице цветов дисплея                       |
| gx_display_color_table_set         | Назначение таблицы цветов, используемой дисплеем                              |
| gx_display_create                    | Создание дисплея                                                        |
| gx_display_delete                    | Удаление дисплея                                                        |
| gx_display_font_table_set          | Назначение таблицы шрифтов, используемой дисплеем                               |
| gx_display_language_table_get      | Получение таблицы языков, связанной с дисплеем (не рекомендуется)    |
| gx_display_language_table_get_ext | Получение таблицы языков, связанной с дисплеем                 |
| gx_display_language_table_set      | Назначение таблицы языков указанному дисплею (не рекомендуется)           |
| gx_display_language_table_set_ext | Назначение таблицы языков указанному дисплею.                       |
| gx_display_pixelmap_table_set      | Назначение таблицы карт отображения пикселей, используемой дисплеем                           |
| gx_display_string_get               | Получение строки, связанной с идентификатором строки (не рекомендуется)                |
| gx_display_string_get_ext          | Получение строки, связанной с идентификатором строки                             |
| gx_display_string_table_get             | Получение таблицы строк, связанной с указанным дисплеем (не рекомендуется) |
| gx_display_string_table_get_ext        | Получение таблицы строк, связанной с указанным дисплеем               |
| gx_display_theme_install                 | Установка тем для указанного дисплея                               |
| gx_drop_list_close                       | Закрытие раскрывающего списка                                                       |
| gx_drop_list_create                      | Создание раскрывающего списка                                                      |
| gx_drop_list_event_process               | Обработка событий раскрывающего списка                                            |
| gx_drop_list_open                        | Открытие раскрывающего списка                                                        |
| gx_drop_list_pixelmap_set               | Задание карты отображения пикселей для раскрывающего списка                                             |
| gx_drop_list_popup_set                  | Задание всплывающего окна для раскрывающегося списка                                                |
| gx_generic_scroll_wheel_children_position | Размещение дочерних элементов в универсальном колесе прокрутки |
| gx_generic_scroll_wheel_create| Создание мини-приложения универсального колеса прокрутки |
| gx_generic_scroll_wheel_draw | Отображение мини-приложения универсального колеса прокрутки |
| gx_generic_scroll_wheel_event_process| Обработка события универсального колеса прокрутки|
| gx_generic_scroll_wheel_row_height_set| Задание высоты строки для универсального колеса прокрутки|
| gx_generic_scroll_wheel_total_rows_set| Задание числа строк для универсального колеса прокрутки |
| gx_horizontal_list_children_position    | Размещение дочерних мини-приложений в горизонтальном списке                          |
| gx_horizontal_list_create                | Создание горизонтального списка                                                |
| gx_horizontal_list_event_process        | Обработка события в горизонтальном списке                                      |
| gx_horizontal_list_page_index_set      | Назначение индекса страницы горизонтального списка                                     |
| gx_horizontal_list_selected_index_get  | Получение индекса выбранного элемента                                           |
| gx_horizontal_list_selected_widget_get | Получение мини-приложения выбранного элемента                                          |
| gx_horizontal_list_selected_set         | Задание выбранного элемента                                                 |
| gx_horizontal_list_total_columns_set   | Изменение количества столбцов в списке после создания                          |
| gx_horizontal_scrollbar_create           | Создание горизонтальной полосы прокрутки                                           |
| gx_icon_button_create                    | Создание кнопки со значком                                                    |
| gx_icon_button_draw                      | Вывод кнопки со значком                                                   |
| gx_icon_button_pixelmap_set             | Задание карты отображения пикселей в кнопке со значком                                           |
| gx_icon_background_draw                  | Вывод фона значка                                                  |
| gx_icon_create                            | значок "Создать"                                                           |
| gx_icon_draw                              | Вывод значка                                                             |
| gx_icon_event_process                    | Создание функции обработки события значка                                        |
| gx_icon_pixelmap_set                     | Задание карты отображения пикселей для значка                                                 |
| gx_image_reader_create                   | Создание экземпляра модуля чтения изображений                                   |
| gx_image_reader_palette_set             | Задание палитры модуля чтения изображений                                           |
| gx_image_reader_start                    | Запуск процесса распаковки и преобразования                           |
| gx_line_chart_axis_draw                 | Вывод осей X, Y графика                                              |
| gx_line_chart_create                     | Создание экземпляра GX_LINE_CHART                                       |
| gx_line_chart_data_draw                 | Вывод линии данных графика                                             |
| gx_line_chart_draw                       | Рисование графика по умолчанию                                            |
| gx_line_chart_update                     | Принудительное обновление данных на графике                                       |
| gx_line_chart_y_scale_calculate        | Вычисление масштаба значений данных на оси Y до координат пикселя           |
| gx_menu_create                            | Создание меню                                                           |
| gx_menu_draw                              | Вывод меню                                                             |
| gx_menu_event_process                     | Обработка события меню                                                    |
| gx_menu_insert                                | Вставка нового пункта                                                               |
| gx_menu_remove                                | Удаление пункта                                                                  |
| gx_menu_text_draw                            | Вывод текста меню                                                                  |
| gx_menu_text_offset_set                     | Задание смещения вывода текста меню                                                       |
| gx_multi_line_text_button_create           | Создание кнопки с многострочным текстом                                                   |
| gx_multi_line_text_button_draw             | Вывод кнопки с многострочным текстом                                                     |
| gx_multi_line_text_button_event_process   | Задание шрифта для кнопки с многострочным текстом                                             |
| gx_multi_line_text_button_text_draw       | Часть для вывода текста                                                 |
| gx_multi_line_text_button_text_id_set    | Задание системной строки для кнопки с текстом                                                |
| gx_multi_line_text_button_text_set        | Назначение пользовательской строки кнопке с текстом (не рекомендуется)                          |
| gx_multi_line_text_button_text_set_ext   | Назначение пользовательской строки кнопке с текстом                                       |
| gx_multi_line_text_input_backspace         | Удаление символа до позиции курсора ввода многострочного текста               |
| gx_multi_line_text_input_buffer_get       | Извлечение сведений о буфере мини-приложения ввода текста                               |
| gx_multi_line_text_input_buffer_clear     | Удаление всех символов из буфера ввода текста                               |
| gx_multi_line_text_input_char_insert      | Вставка строки в формате UTF8 в позиции курсора ввода многострочного текста (не рекомендуется) |
| gx_multi_line_text_input_char_insert_ext | Вставка строки в формате UTF8 в позиции курсора ввода многострочного текста              |
| gx_multi_line_text_input_create            | Создание ввода многострочного текста                                                    |
| gx_multi_line_text_input_cursor_pos_get  | Получение позиции курсора ввода многострочного текста                                  |
| gx_multi_line_text_input_delete            | Удаление символа после курсора ввода многострочного текста                 |
| gx_multi_line_text_input_down_arrow       | Перемещение курсора ввода многострочного текста на следующую строку                              |
| gx_multi_line_text_input_end               | Перемещение курсора ввода многострочного текста в конец текущей строки                |
| gx_multi_line_text_input_event_process    | Обработка ввода многострочного текста                                              |
| gx_multi_line_text_input_fill_color_set  | Установка цветов заливки для ввода многострочного текста                                       |
| gx_multi_line_text_input_home              | Перемещение курсора ввода многострочного текста в начало текущей строки              |
| gx_multi_line_text_input_left_arrow       | Перемещение курсора ввода многострочного текста влево на один символ                         |
| gx_multi_line_text_input_right_arrow      | Перемещение курсора ввода многострочного текста вправо на один символ                        |
| gx_multi_line_text_input_style_add        | Добавление флагов стиля многострочного текста                                                 |
| gx_multi_line_text_input_style_remove     | Удаление флагов стиля многострочного текста                                              |
| gx_multi_line_text_input_style_set        | Назначение флагов стиля многострочного текста                                              |
| gx_multi_line_text_input_text_color_set  | Назначение цветов текста для ввода многострочного текста                                    |
| gx_multi_line_text_input_text_select      | Выбор текста для ввода многострочного текста                                               |
| gx_multi_line_text_input_text_set         | Присвоение текста вводу многострочного текста (не рекомендуется)                               |
| gx_multi_line_text_input_text_set_ext          | Присвоение текста вводу многострочного текста                         |
| gx_multi_line_text_input_up_arrow               | Перемещение курсора ввода многострочного текста на предыдущую строку       |
| gx_multi_line_text_view_create                   | Создание представления многострочного текста                                  |
| gx_multi_line_text_view_event_process           | Обработка события представления многострочного текста                           |
| gx_multi_line_text_view_font_set                | Задание шрифта, используемого в представлении многострочного текста                        |
| gx_multi_line_text_view_line_space_set         | Задание промежутка между строками представления многострочного текста                          |
| gx_multi_line_text_view_scroll_info_get        | Получение сведений о прокрутке представления многострочного текста                         |
| gx_multi_line_text_view_text_color_set         | Задание цвета текста в представлении многострочного текста                       |
| gx_multi_line_text_view_text_id_set            | Задание системной текстовой строки в представлении многострочного текста               |
| gx_multi_line_text_view_text_set                | Задание определяемой пользователем строки как представления многострочного текста (не рекомендуется) |
| gx_multi_line_text_view_text_set_ext           | Задание определяемой пользователем строки как представления многострочного текста              |
| gx_multi_line_text_view_whitespace_set          | Задание пробела в представлении многострочного текста                          |
| gx_numeric_pixelmap_prompt_create                 | Создание числового запроса карты отображения пикселей                               |
| gx_numeric_pixelmap_prompt_format_ function_set | Переопределение функции форматирования числового запроса карты отображения пикселей          |
| gx_numeric_pixelmap_prompt_value_set             | Задание значения числового запроса                                     |
| gx_numeric_prompt_create                           | Создание числового запроса                                        |
| gx_numeric_prompt_format_function_set            | Переопределение функции форматирования числового запроса                   |
| gx_numeric_prompt_value_set                       | Задание значения числового запроса                                     |
| gx_numeric_scroll_wheel_create                    | Создание числового мини-приложения для колесика прокрутки                           |
| gx_numeric_scroll_wheel_range_set                | Назначение диапазона значений для колесика прокрутки                              |
| gx_pixelmap_button_create                          | Создание кнопки карты отображения пикселей                                       |
| gx_pixelmap_button_draw                            | Вывод кнопки карты отображения пикселей                                         |
| gx_pixelmap_button_event_process                  | Обработка событий кнопки карты отображения пикселей                             |
| gx_pixelmap_button_pixelmap_set                   | Задание карты отображения пикселей в кнопке карты отображения пикселей                              |
| gx_pixelmap_prompt_create                          | Создание запроса карты отображения пикселей                                       |
| gx_pixelmap_prompt_draw                            | Вывод запроса карты отображения пикселей                                         |
| gx_pixelmap_prompt_pixelmap_set                   | Задание карты отображения пикселей в запросе карты отображения пикселей                              |
| gx_pixelmap_slider_create                          | Создание ползунка карты отображения пикселей                                       |
| gx_pixelmap_slider_draw                            | Вывод ползунка карты отображения пикселей                                         |
| gx_pixelmap_slider_event_process        | Обработка события ползунка карты отображения пикселей       |
| gx_pixelmap_slider_pixelmap_set         | Задание карты отображения пикселей в ползунке карты отображения пикселей        |
| gx_progress_bar_background_draw         | Вывод фона индикатора выполнения           |
| gx_progress_bar_create                   | Создание индикатора выполнения                  |
| gx_progress_bar_draw                     | Вывод индикатора выполнения                    |
| gx_progress_bar_event_process           | Обработка события индикатора выполнения           |
| gx_progress_bar_font_set                | Задание шрифта текста индикатора выполнения          |
| gx_progress_bar_info_set                | Задание структуры сведений об индикаторе выполнения |
| gx_progress_bar_pixelmap_set            | Задание карты отображения пикселей, используемой для рисования индикатора выполнения |
| gx_progress_bar_range_set               | Задание диапазона значений индикатора выполнения        |
| gx_progress_bar_text_color_set         | Задание цвета текста для индикатора выполнения            |
| gx_progress_bar_text_draw               | Вывод текста индикатора выполнения                 |
| gx_progress_bar_value_set               | Задание значения индикатора выполнения                 |
| gx_prompt_create                          | Создание запроса                          |
| gx_prompt_draw                            | Вывод запроса                            |
| gx_prompt_event_process                   | Обработка события запроса                   |
| gx_prompt_font_set                       | Задание шрифта запроса                        |
| gx_prompt_text_color_set                | Задание цвета текста запроса                  |
| gx_prompt_text_draw                      | Текстовая часть для вывода запроса    |
| gx_prompt_text_get                       | Получение текста запроса (не рекомендуется)           |
| gx_prompt_text_get_ext                  | Получение текста запроса                        |
| gx_prompt_text_id_set                   | Задание запроса с системной текстовой строкой     |
| gx_prompt_text_set                       | Задание текста запроса (не рекомендуется)           |
| gx_prompt_text_set_ext                  | Задание текста запроса                        |
| gx_radial_progress_bar_anchor_set      | Задание начального угла                     |
| gx_radial_progress_bar_background_draw | Вывод фона кругового индикатора выполнения    |
| gx_radial_progress_bar_create           | Создание кругового индикатора выполнения           |
| gx_radial_progress_bar_draw             | Вывод кругового индикатора выполнения             |
| gx_radial_progress_bar_event_process   | Обработка события кругового индикатора выполнения      |
| gx_radial_progress_bar_font_set        | Задание шрифта кругового индикатора выполнения           |
| gx_radial_progress_bar_info_set        | Задание сведений о круговом индикаторе выполнения    |
| gx_radial_progress_bar_text_color_set | Задание цвета текста для кругового индикатора выполнения     |
| gx_radial_progress_bar_text_draw       | Вывод текста кругового индикатора выполнения          |
| gx_radial_progress_bar_value_set       | Задание значения кругового индикатора выполнения          |
| gx_radio_button_create                   | Создание переключателя                    |
| gx_radio_button_draw                     | Вывод переключателя                      |
| gx_radio_button_pixelmap_set            | Задание карты отображения пикселей в переключателе           |
| gx_radial_slider_anchor_angles_set     | Задание списка углов привязки кругового ползунка    |
| gx_radial_slider_angle_set              | Задание угла круглого ползунка                |
| gx_radial_slider_animation_set          | Задание сведений об анимации круглого ползунка       |
| gx_radial_slider_animation_start               | Задание угла круглого ползунка с анимацией                      |
| gx_radial_slider_create                         | Создание круглого ползунка                                      |
| gx_radial_slider_draw                           | Вывод круглого ползунка                                        |
| gx_radial_slider_event_process                 | Обработка события круглого ползунка                               |
| gx_radial_slider_info_get                      | Получение указателя на сведения о круглом ползунке                  |
| gx_radial_slider_info_set                      | Задание сведений о круглом ползунке                               |
| gx_radial_slider_pixelmap_set                  | Задание карт отображения пикселей круглого ползунка                                 |
| gx_rich_text_view_create                       | Создание представления форматированного текста                                     |
| gx_rich_text_view_draw                         | Вывод представления форматированного текста                                         |
| gx_rich_text_view_set_fonts                    | Задание шрифтов для представления форматированного текста                                    |
| gx_rich_text_view_text_draw                    | Вывод текста представления форматированного текста                                    |
| gx_screen_stack_create                          | Создание блока управления стеком экранов GUIX и области памяти |
| gx_screen_stack_pop                             | Выделение верхнего экрана из стека экранов                   |
| gx_screen_stack_push                            | Отправка текущего экрана в стек экранов                |
| gx_screen_stack_reset                           | Сброс стека экранов                                      |
| gx_scroll_wheel_create                          | Создание базового мини-приложения колесика прокрутки                             |
| gx_scroll_wheel_event_process                  | Обработка событий колесика прокрутки                               |
| gx_scroll_wheel_gradient_alpha_set            | Изменение градиента наложения колесика прокрутки                        |
| gx_scroll_wheel_row_height_set                | Назначение высоты строки колесика прокрутки                              |
| gx_scroll_wheel_selected_background_set       | Назначение фонового изображения для выбранной строки                    |
| gx_scroll_wheel_selected_get                   | Получение индекса выбранной строки                                 |
| gx_scroll_wheel_selected_set                   | Назначение выбранного индекса строки                                   |
| gx_scroll_wheel_speed_set                      | Назначение скорости прокрутки                                      |
| gx_scroll_wheel_total_rows_set                | Назначение общего количества доступных строк                       |
| gx_scrollbar_draw                                | Вывод полосы прокрутки                                              |
| gx_scrollbar_event_process                      | Обработка события полосы прокрутки                                     |
| gx_scrollbar_limit_check                        | Проверка предела полосы прокрутки                                       |
| gx_scrollbar_reset                               | Сброс полосы прокрутки                                             |
| gx_scrollbar_value_set                          | Назначение значения полосы прокрутки                                      |
| gx_single_line_text_input_backspace           | Обработка символа возврата                                  |
| gx_single_line_text_input_buffer_clear       | Очистка буфера символов                                  |
| gx_single_line_text_input_buffer_get         | Получение указателя буфера                                     |
| gx_single_line_text_input_character_delete   | Удаление символа                                            |
| gx_single_line_text_input_character_insert   | Вставка символа                                            |
| gx_single_line_text_input_create              | Создание ввода однострочного текста                               |
| gx_single_line_text_input_draw                | Вывод мини-приложения ввода однострочного текста                          |
| gx_single_line_text_input_draw_position_get | Получение начальной точки вывода текста                           |
| gx_single_line_text_input_end                 | Перемещение курсора в конец                                          |
| gx_single_line_text_input_event_process      | Обработка событий ввода текста                                 |
| gx_single_line_text_input_fill_color_set    | Задание цвета заливки для ввода однострочного текста                  |
| gx_single_line_text_input_home                | Перемещение курсора в начало                                         |
| gx_single_line_text_input_left_arrow         | Обработка нажатия клавиши со стрелкой влево                                       |
| gx_single_line_text_input_position_get       | Получение новой позиции курсора                                         |
| gx_single_line_text_input_right_arrow        | Обработка нажатия клавиши со стрелкой право                                      |
| gx_single_line_text_input_style_add          | Добавление флагов стиля                                             |
| gx_single_line_text_input_style_remove       | Удаление флагов стиля                                          |
| gx_single_line_text_input_style_set          | Назначение флагов стиля                                          |
| gx_single_line_text_input_text_color_set  | Задание цвета текста для ввода однострочного текста                      |
| gx_single_line_text_input_text_select     | Выбор текста для ввода однострочного текста                              |
| gx_single_line_text_input_text_set        | Задание текста для ввода однострочного текста (не рекомендуется)                    |
| gx_single_line_text_input_text_set_ext    | Задание текста для ввода однострочного текста                                 |
| gx_slider_create                          | Создание ползунка                                                   |
| gx_slider_draw                            | Вывод ползунка                                                     |
| gx_slider_event_process                   | Обработка события ползунка                                            |
| gx_slider_info_set                        | Задание блока сведений о ползунке                                    |
| gx_slider_needle_draw                     | Вывод стрелки ползунка                                              |
| gx_slider_needle_position_get             | Получение стрелки ползунка                                      |
| gx_slider_tickmarks_draw                  | Вывод делений ползунка                                           |
| gx_slider_travel_get                      | Получение перемещения ползунка                                               |
| gx_slider_value_calculate                 | Вычисление значения ползунка                                          |
| gx_slider_value_set                       | Задание значения ползунка                                                |
| gx_sprite_create                          | Создание мини-приложения GX_SPRITE                                         |
| gx_sprite_current_frame_set               | Назначение текущего отображаемого фрейма для мини-приложения спрайта                  |
| gx_sprite_frame_list_set                  | Назначение или изменение списка кадров спрайта                            |
| gx_sprite_start                           | Запуск последовательности спрайтов                                         |
| gx_sprite_stop                            | Завершение последовательности спрайтов                                          |
| gx_string_scroll_wheel_create             | Создание мини-приложения `GX_STRING_SCROLL_WHEEL`                          |
| gx_string_scroll_wheel_event_process      | Обработка события колесика прокрутки строки                               |
| gx_string_scroll_wheel_string_id_list_set | Назначение массива идентификаторов строк                                      |
| gx_string_scroll_wheel_string_list_set    | Изменение отображаемого массива строк                                   |
| gx_string_scroll_wheel_text_get           | Получение текста для строки колесика прокрутки                              |
| gx_studio_widget_create                   | Создание мини-приложения, определенного в Studio                             |
| gx_studio_named_widget_create             | Создание экрана, определенного в Studio                             |
| gx_studio_display_configure               | Создание и инициализация `GX_DISPLAY`, `GX_CANVAS` и `GX_WINDOW_ROOT` |
| gx_system_active_language_set             | Назначение идентификатора активного языка                                       |
| gx_system_canvas_refresh                  | Принудительное обновление (рисование) "грязных" холстов                       |
| gx_system_dirty_mark                      | Пометка области как грязной                                                 |
| gx_system_dirty_partial_add               | Пометка частичной области как грязной                                         |
| gx_system_draw_context_get                | Получение контекста рисования                                             |
| gx_system_event_fold                      | Событие свертывания                                                       |
| gx_system_event_send                      | Событие отправки                                                      |
| gx_system_focus_claim                     | Запрос фокуса                                                     |
| gx_system_initialize                      | Инициализация GUIX                                                 |
| gx_system_language_table_get              | Получение таблицы языков                                         |
| gx_system_language_table_set              | Назначение таблицы языков                                           |
| gx_system_memory_allocator_set            | Назначение распределителя/дераспределителя памяти                            |
| gx_system_pen_configure                  | Задание конфигурации пера                                  |
| gx_system_screen_stack_create           | Создание элемента управления стека экранов                            |
| gx_system_screen_stack_get              | Получение указателей стека экрана                         |
| gx_system_screen_stack_pop              | Выделение верхнего экрана из стека экранов                       |
| gx_system_screen_stack_push             | Отправка указанного экрана в стек экранов              |
| gx_system_screen_stack_reset            | Сброс стека экранов                                 |
| gx_system_scroll_appearance_get         | Получение внешнего вида прокрутки                                  |
| gx_system_scroll_appearance_set         | Задание внешнего вида прокрутки                                  |
| gx_system_start                           | Запуск GUIX                                             |
| gx_system_string_get                     | Получение строки                                             |
| gx_system_string_table_get              | Получение таблицы строк                                       |
| gx_system_string_table_set              | Задание таблицы строк                                       |
| gx_system_string_width_get              | Получение ширины строки (не рекомендуется)                          |
| gx_system_string_width_get_ext         | Получение ширины строки                                       |
| gx_system_theme_install                  | Установка таблиц шрифтов/цветов/карт отображения пикселей                     |
| gx_system_timer_start                    | Запуск таймера                                            |
| gx_system_timer_stop                     | Остановка таймера                                             |
| gx_system_version_string_get            | Получение строки версии библиотеки GUIX (не рекомендуется)      |
| gx_system_version_string_get_ext       | Получение строки версии библиотеки GUIX                   |
| gx_system_widget_find                    | Поиск мини-приложения                                            |
| gx_text_button_create                    | Создание кнопки с текстом                                     |
| gx_text_button_draw                      | Вывод кнопки с текстом                                       |
| gx_text_button_event_process             | Обработка события кнопки с текстом                              |
| gx_text_button_font_set                 | Задание шрифта для кнопки с текстом                               |
| gx_text_button_text_color_set          | Задание цвета для кнопки с текстом                                  |
| gx_text_button_text_draw                | Текстовая часть для вывода кнопки                 |
| gx_text_button_text_get                 | Получение текста, используемого в кнопке с текстом (не рекомендуется)              |
| gx_text_button_text_get_ext            | Получение текста, используемого в кнопке с текстом                           |
| gx_text_button_text_id_set             | Назначение системной строки для кнопки с текстом                    |
| gx_text_button_text_set                 | Назначение пользовательской строки кнопке с текстом (не рекомендуется) |
| gx_text_button_text_set_ext            | Назначение пользовательской строки кнопке с текстом              |
| gx_text_scroll_wheel_callback_set      | Назначение обратного вызова для получения строки (не рекомендуется)          |
| gx_text_scroll_wheel_callback_set_ext | Назначение обратного вызова получения строки                       |
| gx_text_scroll_wheel_create             | Создание базового колесика прокрутки текста                          |
| gx_text_scroll_wheel_draw               | Функция рисования колесика прокрутки текста                  |
| gx_text_scroll_wheel_event_process      | Обработка события колесика прокрутки текста                        |
| gx_text_scroll_wheel_font_set          | Назначение шрифтов для колесика прокрутки текста                         |
| gx_text_scroll_wheel_text_color_set   | Назначение цветов текста для колесика прокрутки текста                   |
| gx_tree_view_create                    | Создание представления в виде дерева                          |
| gx_tree_view_draw                      | Вывод представления в виде дерева                              |
| gx_tree_view_event_process            | Обработка события представления в виде дерева                     |
| gx_tree-view_indentation_set           | Задание отступа для представления в виде дерева                   |
| gx_tree_view_position                  | Размещение элементов представления в виде дерева                    |
| gx_tree_view_root_line_color_set    | Задание цвета корневой линии представления в виде дерева               |
| gx_tree_view_root_pixelmap_set       | Задание карт отображения пикселей корня представления в виде дерева                |
| gx_tree_view_selected_get             | Получение выбранного элемента                      |
| gx_tree_view_selected_set             | Задание выбранного элемента                           |
| gx_utility_canvas_to_bmp              | Преобразование памяти холста в формат точечного рисунка      |
| gx_utility_circle_point_get           | Вычисление точки на окружности               |
| gx_utility_gradient_create             | Создание градиентной карты отображения пикселей                  |
| gx_utility_gradient_delete              | Удаление градиентной карты отображения пикселей                  |
| gx_utility_ltoa                         | Преобразование длинного целого числа в ASCII               |
| gx_utility_math_acos                   | Вычисление арккосинуса                          |
| gx_utility_math_asin                   | Вычисление арксинуса                             |
| gx_utility_math_cos                    | Вычисление косинуса                              |
| gx_utility_math_sin                    | Вычисление синуса                                |
| gx_utility_math_sqrt                   | Вычисление квадратного корня                         |
| gx_utility_bidi_paragraph_reorder         | Переупорядочение двунаправленного текста в порядке его отображения|
| gx_utility_bidi_resolved_text_info_delete | Удаление переупорядоченного двунаправленного текста               |
| gx_utility_pixelmap_resize             | Изменение размера карты отображения пикселей                             |
| gx_utility_pixelmap_rotate             | Поворот карты отображения пикселей на произвольный угол          |
| gx_utility_pixelmap_simple_rotate      | Поворот карты отображения пикселей на 90, 180, 270 градусов     |
| gx_utility_rectangle_center            | Центрирование прямоугольника внутри другого прямоугольника   |
| gx_utility_rectangle_center_find      | Поиск центра прямоугольника                    |
| gx_utility_rectangle_combine           | Объединение двух прямоугольников в первый           |
| gx_utility_rectangle_compare           | Сравнение двух прямоугольников                      |
| gx_utility_rectangle_define            | Определение прямоугольника                            |
| gx_utility_rectangle_resize            | Изменение размера прямоугольника                            |
| gx_utility_rectangle_overlap_detect   | Обнаружение наложения прямоугольников                |
| gx_utility_rectangle_point_detect     | Определение того, находится ли точка в прямоугольнике        |
| gx_utility_rectangle_shift             | Смещение прямоугольника                             |
| gx_utility_string_to_alphamap         | Преобразование текстовой строки в альфа-карту (не рекомендуется) |
| gx_utility_string_to_alphamap_ext    | Преобразование текстовой строки в альфа-карту              |
| gx_vertical_list_children_position    | Размещение дочерних элементов в вертикальном списке          |
| gx_vertical_list_create                | Создание вертикального списка                        |
| gx_vertical_list_event_process        | Обработка события вертикального списка                 |
| gx_vertical_list_selected_index_get  | Получение индекса выбранного элемента                     |
| gx_vertical_list_selected_widget_get | Получение выбранного мини-приложения                         |
| gx_vertical_list_selected_set         | Задание записи в вертикальном списке                  |
| gx_vertical_list_total_rows_set      | Изменение количества строк в списке после создания   |
| gx_vertical_scrollbar_create           | Создание вертикальной полосы прокрутки                   |
| gx_widget_allocate                      | Динамическое выделение мини-приложения               |
| gx_widget_attach                        | Присоединение мини-приложения к родительскому элементу                     |
| gx_widget_background_draw              | Вывод фона мини-приложения                      |
| gx_widget_back_attach                  | Присоединение мини-приложения на заднем плане                       |
| gx_widget_back_move                    | Перемещение мини-приложения на задний план                         |
| gx_widget_block_move                   | Перемещение блока пикселей                        |
| gx_widget_border_draw                  | Вывод границы мини-приложения                          |
| gx_widget_border_style_set            | Задание стиля границы мини-приложения                     |
| gx_widget_border_width_get            | Задание ширины границы мини-приложения                     |
| gx_widget_canvas_get               | Получение холста мини-приложения                                                         |
| gx_widget_child_detect             | Обнаружение дочернего элемента мини-приложения                                                       |
| gx_widget_children_draw            | Вывод дочернего элемента мини-приложения                                                      |
| gx_widget_client_get               | Получение клиентской области мини-приложения                                                    |
| gx_widget_color_get                | Разрешение идентификатора цвета в значение цвета для отображаемого мини-приложения                      |
| gx_widget_create                    | Создание мини-приложения                                                             |
| gx_widget_created_test             | Проверка создания мини-приложения                                                    |
| gx_widget_delete                    | Удаление мини-приложения                                                             |
| gx_widget_detach                    | Отсоединение мини-приложения от родительского элемента                                                 |
| gx_widget_draw                      | Вывод мини-приложения                                                               |
| gx_widget_draw_set                 | Задание функции рисования мини-приложения                                               |
| gx_widget_event_generate           | Создание события мини-приложения                                                     |
| gx_widget_event_process            | Обработка события мини-приложения                                                      |
| gx_widget_event_process_set       | Задание функции обработки событий мини-приложения                                   |
| gx_widget_event_to_parent         | Отправка события родительскому элементу мини-приложения                                             |
| gx_widget_fill_color_set          | Назначение цвета заливки мини-приложения                                                  |
| gx_widget_find                      | Поиск мини-приложения                                                               |
| gx_widget_first_child_get         | Возврат указателя к первому дочернему элементу                                             |
| gx_widget_focus_next               | Перемещение фокуса ввода на следующее мини-приложение                                           |
| gx_widget_focus_previous           | Перемещение фокуса ввода на предыдущее мини-приложение                                       |
| gx_widget_font_get                 | Разрешение идентификатора шрифта в указатель шрифта для отображаемого мини-приложения                    |
| gx_widget_free                      | Освобождение памяти блока управления мини-приложением                                          |
| gx_widget_front_move               | Перемещение мини-приложения на передний план                                                      |
| gx_widget_height_get               | Получение высоты мини-приложения                                                         |
| gx_widget_hide                      | Скрытие мини-приложения                                                               |
| gx_widget_last_child_get          | Возврат указателя к последнему дочернему элементу                                              |
| gx_widget_next_sibling_get        | Возврат указателя к следующему одноуровневому элементу                                            |
| gx_widget_parent_get               | Возврат указателя на родительское мини-приложение                                           |
| gx_widget_pixelmap_get             | Разрешение идентификатора карты отображения пикселей в указатель карты отображения пикселей для отображаемого мини-приложения            |
| gx_widget_previous_sibling_get    | Возврат указателя к предыдущему одноуровневому элементу                                        |
| gx_widget_resize                    | Изменение размера мини-приложения                                                             |
| gx_widget_shift                     | Смещение мини-приложения                                                              |
| gx_widget_show                      | Отображение мини-приложения                                                               |
| gx_widget_status_add               | Добавление состояния мини-приложения                                                         |
| gx_widget_status_get               | Получение флагов состояния мини-приложения                                              |
| gx_widget_status_remove            | Удаление состояния мини-приложения                                                      |
| gx_widget_string_get               | Получение строки, связанной с ИД строки для отображаемого мини-приложения (не рекомендуется) |
| gx_widget_string_get_ext          | Получение строки, связанной с ИД строки для отображаемого мини-приложения             |
| gx_widget_status_test              | Тестирование состояния мини-приложения                                                        |
| gx_widget_style_add                | Добавление стиля мини-приложения                                                          |
| gx_widget_style_get                | Получение флагов стиля мини-приложения                                               |
| gx_widget_style_remove             | Удаление стиля мини-приложения                                                       |
| gx_widget_style_set                | Задание стиля мини-приложения                                                          |
| gx_widget_text_blend               | Отрисовка смешанного текста поверх мини-приложения (не рекомендуется)                              |
| gx_widget_text_blend_ext          | Отрисовка смешанного текста поверх мини-приложения                                           |
| gx_widget_text_draw                | Отрисовка текста поверх мини-приложения (не рекомендуется)                                      |
| gx_widget_text_draw_ext           | Отрисовка текста поверх мини-приложения                                            |
| gx_widget_text_id_draw            | Отрисовка текста, идентифицированного по ИД строки, поверх мини-приложения                           |
| gx_widget_top_visible_child_find | Возврат указателя к видимому дочернему элементу, рисуемому в верхней части Z-порядка   |
| gx_widget_type_find                | Поиск типа мини-приложения                                                          |
| gx_widget_width_get                | Получение ширины мини-приложения                                                          |
| gx_window_canvas_set               | Задание холста окна                                                         |
| gx_window_client_height_get       | Получение высоты клиента окна                                                  |
| gx_window_client_scroll            | Прокрутка клиентов окна                                                     |
| gx_window_client_width_get        | Получение ширины клиента окна                                                   |
| gx_window_close                     | Завершение выполнения модального окна                                          |
| gx_window_create                    | Создание окна                                                             |
| gx_window_draw                      | Вывод окна                                                               |
| gx_window_event_process            | Обработка события окна                                                      |
| gx_window_execute                   | Выполнение модального окна                                                    |
| gx_window_root_create              | Создание корневого окна                                                        |
| gx_window_root_delete              | Удаление корневого окна                                                       |
| gx_window_root_event_process      | Обработка события для корневого окна                                             |
| gx_window_root_find                | Поиск корневого окна                                                          |
| gx_window_scroll_info_get         | Получение сведений о прокрутке окна                                                    |
| gx_window_scrollbar_find           | Поиск полосы прокрутки окна                                                     |
| gx_window_wallpaper_get            | Получение фонового рисунка окна                                                      |
| gx_window_wallpaper_set            | Задание фонового рисунка окна                                                      |

## <a name="gx_accordion_menu_create"></a>gx_accordion_menu_create

Создание меню "гармошка"

### <a name="prototype"></a>Прототип

```C
UINT gx_accordion_menu_create(
    GX_ACCORDION_MENU *accordion,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    ULONG style ,
    USHORT accordion_menu_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает меню "гармошка", как указано, и присоединяет меню "гармошка" к заданному родительскому мини-приложению. Меню "гармошка" — это мини-приложение для развертывания/свертывания меню. Оно принимает все типы мини-приложений в качестве дочерних элементов меню. Меню "гармошка" могут быть вложенными, то есть можно создать несколько уровней вложения в меню.

Чтобы вставить дочерний элемент в мини-приложение пункта меню, в качестве родительского пункта меню рекомендуется использовать мини-приложение типа GX_MENU.

Советы по созданию меню "гармошка" с одним уровнем вложения

1.  Создайте меню "гармошка".

2.  Присоедините мини-приложения типа GX_MENU к меню "гармошка".

3.  Присоедините дочерние мини-приложения к родительскому элементу типа GX_MENU. Тип дочернего элемента может быть любым типом мини-приложения GUIX.

Советы по созданию меню "гармошка" с несколькими уровнями вложения

1.  Создайте меню "гармошка".

2.  Присоедините мини-приложения типа GX_MENU к меню "гармошка".

3.  Присоедините мини-приложение типа GX_ACCORDION_MENU к родительскому элементу типа GX_MENU.

4.  Присоедините пункты меню к родительскому элементу типа GX_ACCORDION_MENU, как описано в разделе "Советы по созданию меню "гармошка" с одним уровнем вложения".

### <a name="parameters"></a>Параметры

- **accordion**: указатель на блок управления меню "гармошка"
- **name**: название меню "гармошка"
- **parent**: указатель на родительское мини-приложение
- **style**: стиль мини-приложения Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **accordion_menu_id**: определяемый приложением идентификатор меню "гармошка"
- **size**: размер меню "гармошка"

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — меню "гармошка" успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

```C
GX_ACCORDION_MENU my_accordion;
GX_MENU menu_1;
GX_MENU item_1;
GX_RECTANGLE size;

gx_utility_rectangle_define(&size, 100, 100, 300, 150);

status = gx_accordion_menu_create(&my_accordion, “my_accordion”,
                                  parent, GX_STYLE_ENABLED,
                                  MY_ACCORDION_ID, &size);

gx_menu_create(&menu_1, “menu_1”, &my_accordion,
               GX_STRING_ID_MENU_1, GX_ID_NONE,
               GX_STYLE_ENABLED | GX_TYLE_BORDER_THIN, 0, &size);

gx_menu_create(&item_1, “item_1”, &my_accordion,
               GX_STRING_ID_ITEM_1, GX_ID_NONE,
               GX_STYLE_ENABLED | GX_STYLE_BORDER_THIN, 0, &size);

gx_text_offset_set(&item_1, 30, 0);

gx_menu_insert(&menu_1, &item_1);

/* If status is GX_SUCCESS the accordion menu was successfully created. */

```

Демонстрационное приложение demo_guix_widget_types в составе GUIX Studio содержит полный пример использования мини-приложения представления меню "гармошка".

### <a name="see-also"></a>См. также:

- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_accordion_menu_draw"></a>gx_accordion_menu_draw

Вывод меню "гармошка"

### <a name="prototype"></a>Прототип

```C
VOID gx_accordion_menu_draw(GX_ACCORDION_MENU *accordion);
```

### <a name="description"></a>Описание

Эта служба рисует указанное меню "гармошка". Эта служба обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений меню "гармошка".

### <a name="parameters"></a>Параметры

- **accordion**: указатель на блок управления меню "гармошка"

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Define a custom accordion menu draw function */

VOID my_accordion_menu_draw(GX_ACCORDION_MENU *accordion)
{
    /* Call default accordion menu draw. */
    gx_accordion_menu_draw(accordion);

    /* Add custom drawing here. */
}

```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_accordion_menu_event_process"></a>gx_accordion_menu_event_process

Обработка события меню "гармошка"

### <a name="prototype"></a>Прототип

```C
UINT gx_accordion_menu_event_process(
    GX_ACCORDION_MENU *accordion, 
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного меню "гармошка". Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий меню "гармошка".

Эта служба обрабатывает события GX_EVENT_PEN_DOWN и GX_EVENT_PEN_UP, чтобы развернуть или свернуть пункт меню.

### <a name="parameters"></a>Параметры

- **accordion**: указатель на блок управления меню "гармошка"
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие меню "гармошка" успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic accordion menu event processing as part of custom event processing function. */

UINT custom_accordion_event_process( 
    GX_ACCORDION_MENU *accordion,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default accordion menu
            event processing */
        status =
        gx_accordion_menu_event_process(accordion, event);
        break;
    }
    return status;
}

```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_accordion_menu_position"></a>gx_accordion_menu_position

Размещение пунктов меню

### <a name="prototype"></a>Прототип

```C
UINT gx_accordion_menu_position(GX_ACCORDION_MENU *accordion);
```

### <a name="description"></a>Описание

Эта служба размещает пункты меню для меню "гармошка". Эта функция обычно вызывается внутри, когда меню "гармошка" становится видимым. Если вы хотите вставить или удалить элементы в меню "гармошка" или изменить стили развертывания дочернего элемента, эту функцию следует вызвать для изменения расположения дочерних элементов.

### <a name="parameters"></a>Параметры

- **accordion**: указатель на блок управления меню "гармошка"

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — меню "гармошка" успешно размещено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Position menu items in the accordion menu “my_accordion” */
status = gx_accordion_menu_position (&my_accordion);

/* If status is GX_SUCCESS the children in the accordion menu
“my_accordion” are positioned. */

```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_menu_create
- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_animation_canvas_define"></a>gx_animation_canvas_define

Предоставление памяти холста для контроллера анимации

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_canvas_define(
    GX_ANIMATION *animation, 
    GX_CANVAS *canvas);
```

### <a name="description"></a>Описание

Эта служба предоставляет память холста для контроллера анимации, используемого для реализации последовательности анимации. Размер холста должен быть достаточно большим, чтобы вместить мини-приложение анимации.

При определении холста анимации целевое мини-приложение рисуется один раз на этом холсте анимации, а перетаскивание экрана или эффект исчезания достигаются путем изменения смещения холста и/или альфа-значения холста. При наличии аппаратной поддержки нескольких графических слоев определение холста анимации, привязанного к аппаратному слою наложения, может ***значительно*** повысить производительность анимации перетаскивания и исчезания.

Диспетчер анимации требует наличия холста анимации для выполнения эффектов появления и исчезновения, если выполняется при глубине цвета меньше 16 бит.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией
- **canvas**: холст памяти для реализации анимации перехода

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — холст анимации успешно определен.
- **GX_INVALID_STATUS** (0x10) — недопустимый статус анимации.
- **GX_INVALID_MEMORY_SIZE** (0x29) — размера указанного блока памяти недостаточно для создания холста.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION animation;
GX_CANVAS animation_canvas;
GX_ROOT_WINDOW animation_root;
/* Create animation canvas. */
status = gx_canvas_create(
        &animation_canvas, /* Canvas control block. */
        “animation_canvas”, /* Name of canvas. */
        display, /* Display control block. */
        GX_ANIMATION_MANAGED, /* Type of canvas. */
        width, /* Width of canvas. */
        height, /* Height of canvas. */
        memory_area, /* Memory area of canvas. */
        memory_size /* Size of canvas memory. */
        );
if (status == GX_SUCCESS)
{
    /* Create the root window for the canvas. */
    status = gx_window_root_create(
        &animation_root, /* Root window control block. */
        “animation_root”, /* Name of root window. */
        &animation_canvas, /* Canvas of the root window. */ GX_STYLE_NONE, /* Style of the window. */
        GX_ID_NONE, /* Root window ID. */
        &root_size /* Window size. */
        );
}
if (status == GX_SUCCESS)
{
    /* Define canvas for the animation. */
    status = gx_animation_canvas_define(&animation,
                &animation_canvas);
}
/* If status is GX_SUCCESS the new canvas was successfully set to the animation manager. */

```

### <a name="see-also"></a>См. также:

- gx_animation_create
- gx_animation_delete
- gx_animation_drag_disable
- gx_animation_drag_enable
- gx_animation_landing_speed_set
- gx_animation_start
- gx_animation_stop

## <a name="gx_animation_create"></a>gx_animation_create

Создание контроллера анимации

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_create(GX_ANIMATION *animation);
```

### <a name="description"></a>Описание

Эта служба создает контроллер анимации. Контроллер инициализируется в состоянии бездействия. Невозможно запустить анимацию, если она не находится в состоянии IDLE. Указатель блока управления GX_ANIMATION можно получить с помощью gx_system_animation_get(). Также это может быть статически определенный блок управления.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией


### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — контроллер анимации успешно создан.
- **GX_ALREADY_CREATED** (0x13) — блок управления уже инициализирован.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION *animation;

/* Allocate an animaton control from system pool */
gx_system_animation_get(&animation);

/* Initialize the control block */

if (animation)
{
    status = gx_animation_create(animation);
}

/* If status is GX_SUCCESS the new animation controller was successfully created and initialized. */

```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define
- gx_animation_delete
- gx_animation_drag_disable
- gx_animation_drag_enable
- gx_animation_start
- gx_animation_landing_speed_set
- gx_animation_stop
- gx_system_animation_get
- gx_system_animation_free

## <a name="gx_animation_delete"></a>gx_animation_delete

Удаление одного или нескольких контроллеров анимации

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_delete(GX_ANIMATION *animation, GX_WIDGET *parent);
```

### <a name="description"></a>Описание

Эта служба удаляет последовательность анимации, если задан заголовок ввода, а если он не задан, то удаляет все элементы анимации, принадлежащие указанному родительскому мини-приложению.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией
- **parent**: указатель на родительское мини-приложение


### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — контроллеры анимации успешно удалены.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

- Удаление одного элемента анимации

```C
GX_ANIMATION *animation;

/* Allocate an animaton control from system pool */
gx_system_animation_get(&animation);

if (animation)
{
    /* Create an animation.  */
    gx_animation_create(animation);

    /* Delete an animation.  */
    status = gx_animation_delete(animation, GX_NULL);
}

/* If status is GX_SUCCESS the animation controller was successfully deleted and returned back to system animation pool. */

```

- Удаление множества элементов анимации
```C

status = gx_animation_delete(GX_NULL, parent);

/* If status is GX_SUCCESS all the animations belong to the parent were successfully deleted. */

```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define,
- gx_animation_create
- gx_animation_drag_disable,
- gx_animation_drag_enable
- gx_animation_start
- gx_animation_landing_speed_set,
- gx_animation_stop
- gx_system_animation_get
- gx_system_animation_free

## <a name="gx_animation_drag_disable"></a>gx_animation_drag_disable

Отключение перехватчика анимации перетаскивания экрана

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_drag_disable(
    GX_ANIMATION *animation, 
    GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба удаляет процедуру перехвата анимации перетаскивания экрана из функции процесса события по умолчанию мини-приложения и останавливает последовательность анимации. Процедура перехвата анимации перетаскивания экрана обрабатывает события для анимации перетаскивания экрана.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией
- **widget**: указатель на блок управления мини-приложением

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION animation;
GX_WIDGET *animation_parent;

/* Disable screen drag animation of “animation_parent”. */
status = gx_animation_drag_disable(&animation, animation_parent);

/* If status is GX_SUCCESS the screen drag hook has been removed
    from the event process of “animation_parent”. */

```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define
- gx_animation_create
- gx_animation_delete
- gx_animation_drag_enable
- gx_animation_landing_speed_set
- gx_animation_start
- gx_animation_stop
- gx_system_animation_get
- gx_system_animation_free

## <a name="gx_animation_drag_enable"></a>gx_animation_drag_enable

Включение перехватчика анимации перетаскивания экрана

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_drag_enable(
    GX_ANIMATION *animation,
    GX_WIDGET *widget, 
    GX_ANIMATION_INFO *info);
```

### <a name="description"></a>Описание

Эта служба задает определенную внутри функцию обработки события анимации перетаскивания экрана в качестве процедуры перехвата функции процесса события по умолчанию мини-приложения. Функция обработки события анимации перетаскивания экрана обрабатывает события для анимации перетаскивания экрана.

Процедура перехвата перетаскивания экрана становится обработчиком по умолчанию для событий ввода с помощью пера, отправляемых в целевое мини-приложение. Исходная функция обработки событий мини-приложения вызывается последовательно после проверки типов событий перетаскивания экрана.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией
- **widget**: указатель на блок управления мини-приложением
- **info**: сведения об анимации

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_STATUS** (0x26) — недопустимое состояние анимации.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.
- **GX_INVALID_WIDGET** (0x12) — не предоставлен список слайдов.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION animation;
GX_ANIMATION_INFO info;
GX_WIDGET *animation_parent;
GX_WIDGET *screen_list[] = {
    screen_1,
    screen_2,
    screen_3,
    GX_NULL
}

memset(&info, 0, sizeof(GX_ANIMATION_INFO);
info.gx_animation_parent = animation_parent;

/* If GX_STYLE_ANIMATION_WRAP is set, the screen list will wrap itself. */
info.gx_animation_style = GX_ANIMATION_SCREEN_DRAG |
                          GX_ANIMATION_HORIZONTAL |
                          GX_STYLE_ANIMATION_WRAP;
info.gx_animation_frame_interval = 1;
info.gx_animation_slide_screen_list = screen_list;

status = gx_animation_drag_enable(&animation, animation_parent,
                                  info);

/* If status is GX_SUCCESS the screen slide animinatin event
    process function has been set as a hook procedure of
    “animation_parent”. */
```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define
- gx_animation_create
- gx_animation_delete
- gx_animation_drag_disable
- gx_animation_landing_speed_set
- gx_animation_start
- gx_animation_stop
- gx_system_animation_get
- gx_system_animation_free

## <a name="gx_animation_landing_speed_set"></a>gx_animation_landing_speed_set

Задание стартовой скорости размещения для анимации перетаскивания экрана

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_landing_speed_set(
    GX_ANIMATION *animation, 
    USHORT shift_per_step);
```

### <a name="description"></a>Описание

Эта служба задает стартовую скорость размещения для анимации перетаскивания экрана.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией
- **shift_per_step**: расстояние сдвига для каждого шага

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set landing speed of “my_animation” to 20. */
status = gx_animation_landing_peed_set(&my_animation, 20);

/* If status is GX_SUCCESS the landing speed is successfully set to 20. */
```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define
- gx_animation_create
- gx_animation_delete
- gx_animation_slide_disable
- gx_animation_slide_enable
- gx_animation_start
- gx_animation_stop
- gx_system_animation_get
- gx_system_animation_free

## <a name="gx_animation_start"></a>gx_animation_start

Запуск анимации, управляемой таймером

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_start(
    GX_ANIMATION *animation, 
    GX_ANIMATION_INFO *params);
```

### <a name="description"></a>Описание

Эта служба инициирует последовательность анимации с помощью созданного ранее экземпляра анимации и нового набора параметров анимации. Эта функция создает локальную копию параметров, то есть структура параметра не обязательно должна быть определена статически.

Структура элемента управления GX_ANIMATION может быть определена статически приложением или может быть получена с помощью API gx_system_animation_get().

Структура GX_ANIMATION_INFO определяет параметры анимации для выполнения. Полное описание этой структуры и значения каждого поля см. в разделе "Компонент анимации GUIX" главы 3 этого руководства.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией
- **params**: указатель на структуру параметра

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимый целевой объект анимации.
- **GX_INVALID_STATUS** (0x26) — недопустимое состояние анимации.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст анимации.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION_INFO params;
GX_ANIMATION *animation;

/* obtain an animation control block pointer */
gx_system_animation_get(&animation);
if (animation)
{
    /* define a slide down and to the right */
    params.gx_animation_start_position.gx_point_x = 0;
    params.gx_animation_start_position.gx_point_y = 0;
    params.gx_animation_end_position.gx_point_x = 100;
    params.gx_animation_end_position.gx_point_y = 200;
    params.gx_animation_style= GX_ANIMATION_TRANSLATE;
    params.gx_animation_target = &my_window;
    params.gx_animation_parent = root_window;
    params.gx_animation_start_alpha = 255;
    params.gx_animation_end_alpha = 255;

    params.gx_animation_delay_before = 0;
    params.gx_animation_steps = 10;
    params.gx_animation_tick_rate = 2;

    status = gx_animation_start(&animation, &params);
}

/* If status is GX_SUCCESS the animation is initiated. */
```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define,
- gx__animation_create
- gx_animation_slide_disable,
- gx__animation_slide_enable
- gx_animation_landing_speed_set,
- gx__animation_stop
- gx_system_animation_get,
- gx__system_animation_free

## <a name="gx_animation_stop"></a>gx_animation_stop

Остановка активной анимации, управляемой таймером

### <a name="prototype"></a>Прототип

```C
UINT gx_animation_stop(GX_ANIMATION *animation);
```

### <a name="description"></a>Описание

Останавливает запущенную ранее анимацию. Если указатель на блок управления анимацией был выделен с помощью gx_system_animation_get, приложение может повторно использовать блок управления или вернуть его в системный пул с помощью gx_system_animation_free().

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления анимацией

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно. GX_PTR_ERROR (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_STATUS** (0x26) — недопустимое состояние контроллера.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION animation;

status = gx_animation_stop(&animation);

/* If status is GX_SUCCESS the animation is stopped */

```

### <a name="see-also"></a>См. также:

- gx_animation_canvas_define
- gx_animation_create
- gx_animation_delete
- gx_animation_drag_disable
- gx_animation_drag_enable
- gx_animation_start
- gx_system_animation_get
- gx_system_animation_free

## <a name="gx_binres_language_count_get"></a>gx_binres_language_count_get

Возврат количества языков, имеющихся в двоичных данных ресурса

### <a name="prototype"></a>Прототип

```C
UINT gx_binres_language_count_get(
    GX_UBYTE *root_address, 
    GX_VALUE *returned_count);
```

### <a name="description"></a>Описание

Эта служба анализирует заголовок двоичных данных ресурса, чтобы получить количество языков, содержащихся в двоичных данных. Это полезно для приложений, которые должны отображать список выбора для пользователя, в котором пользователь может выбрать нужный язык.

### <a name="parameters"></a>Параметры

- **root_address**: адрес двоичных данных ресурса в памяти
- **return_count**: расположение для хранения возвращенного количества языков

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_FORMAT** (0x24) — недопустимый двоичный ресурс.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE language_count = 0;

/* Specify address that binary resource data is located. */
GX_UBYTE    *root_address = 0x60000000;

status = gx_binres_language_count_get(root_address,
    &language_count);

/* If status is GX_SUCCESS, the language table was successfully loaded. */

```

### <a name="see-also"></a>См. также:

- gx_binres_language_info_load

## <a name="gx_binres_language_info_load"></a>gx_binres_language_info_load

Загрузка сведений о таблице языков

### <a name="prototype"></a>Прототип

```C
UINT gx_binres_language_info_load(
    GX_UBYTE *root_address, 
    GX_LANGUAGE_HEDAER *put_info);
```

### <a name="description"></a>Описание

Эта служба анализирует большой двоичный объект данных двоичного ресурса для заполнения массива структур GX_LANGUAGE_HEADER и уведомляет приложение о названиях языков и размере таблицы строк для каждого языка, содержащегося в двоичных данных. Приложение должно сначала вызвать метод gx_binres_language_count_get(), чтобы определить количество языков в двоичных данных, и убедиться, что указатель put_info, переданный этой функции, указывает на массив структур language_count GX_LANGUAGE_HEADER.

Эта служба используется приложением для определения содержимого блока данных двоичного ресурса во время выполнения.

Структура GX_LANGUAGE_HEADER определяется следующим образом.

```c
typedef struct GX_LANGUAGE_HEADER_STRUCT{
    USHORT gx_language_header_magic_number;
    USHORT gx_language_header_index;
    UCHAR gx_language_header_name[GX_LANGUAGE_HEADER_NAME_SIZE];
    ULONG  gx_language_header_data_size;
} GX_LANGUAGE_HEADER;
```

- Поле *magic_number* используется для внутренней проверки формата данных двоичного ресурса.
- Поле *header_index* указывает порядок, в котором языки определены в двоичных данных.
- Поле *header_name* содержит название языка.
- Поле *header_data_size* содержит размер данных для таблицы строк языков.

### <a name="parameters"></a>Параметры

- **root_address**: адрес двоичных данных ресурса в памяти
- **put_info**: указатель на массив структур GX_LANGUAGE_HEADER

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_FORMAT** (0x24) — недопустимый двоичный ресурс.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_LANGUAGE_HEADER language_info[4];

/* Specify address that binary resource data is located. */
GX_UBYTE    *root_address = 0x60000000;

status = gx_binres_language_info_load(root_address,
    language_info);
/* If status is GX_SUCCESS, the language table was successfully loaded. */

```

### <a name="see-also"></a>См. также:

- gx_binres_language_count_get

## <a name="gx_binres_language_table_load"></a>gx_binres_language_table_load

Загрузка ресурса таблицы языка (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_binres_language_table_load(
    GX_UBYTE *root_address, 
    GX_UBYTE **returned_language_table);
```

### <a name="description"></a>Описание

Этот устаревший API позволяет приложениям загружать данные таблицы строк из более старых (до выпуска 5.6) файлов данных двоичных ресурсов.

В новых приложениях следует использовать gx_binres_language_table_load_ext().

Эта служба создает структуру таблицы языков, содержащей указатели на ресурсы таблицы. Сгенерированные структуры данных указывают на данные ресурса "на месте", но не копируют данные ресурсов. Данные ресурсов следует поместить в область памяти для общего доступа, а базовый адрес этого расположения в памяти передается этому API.

Для этой службы требуется выделенный блок памяти среды выполнения достаточного размера для хранения структуры таблицы языков, поэтому API gx_system_memory_allocator_set необходимо вызвать один раз перед запросом этой службы.

В возвращенной таблице языков определена одна таблица строк или несколько, каждая из которых содержит указатели на строковые ресурсы в памяти данных ресурсов.

### <a name="parameters"></a>Параметры

- **root_address**: адрес двоичных данных ресурса в памяти
- **returned _language_table**: указатель на загруженную таблицу языков

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_FORMAT** (0x24) — недопустимый двоичный ресурс.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_SYSTEM_MEMPRY_ERROR** (0x30) — не определены распределитель памяти или функция Free.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_UBYTE ***language_table = GX_NULL;

/* Specify address that binary resource data is located. */
GX_UBYTE *root_address = 0x60000000;

status = gx_binres_language_table_load(root_address,
                                        &language_table);

/* If status is GX_SUCCESS, the language table was successfully loaded. */

```

### <a name="see-also"></a>См. также:

- gx_binres_language_table_load_ext

## <a name="gx_binres_language_table_load_ext"></a>gx_binres_language_table_load_ext

Загрузка ресурса таблицы языка

### <a name="prototype"></a>Прототип

```C
UINT gx_binres_language_table_load_ext(
    GX_UBYTE *root_address, 
    GX_STRING **returned_language_table);
```

### <a name="description"></a>Описание

Эта служба создает структуру таблицы языков, содержащей указатели на ресурсы таблицы. Сгенерированные структуры данных указывают на данные ресурса "на месте", но не копируют данные ресурсов. Данные ресурсов следует поместить в область памяти для общего доступа, а базовый адрес этого расположения в памяти передается этому API.

Для этой службы требуется выделенный блок памяти среды выполнения достаточного размера для хранения структуры таблицы языков, поэтому API gx_system_memory_allocator_set необходимо вызвать один раз перед запросом этой службы.

В возвращенной таблице языков определена одна таблица строк или несколько, каждая из которых содержит указатели на строковые ресурсы в памяти данных ресурсов.

### <a name="parameters"></a>Параметры

- **root_address**: адрес двоичных данных ресурса в памяти
- **returned _language_table**: указатель на загруженную таблицу языков

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_FORMAT** (0x24) — недопустимый двоичный ресурс.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_SYSTEM_MEMPRY_ERROR** (0x30) — не определены распределитель памяти или функция Free.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING **language_table = GX_NULL;

/* Specify address that binary resource data is located. */
GX_UBYTE *root_address = 0x60000000;

status = gx_binres_language_table_load_ext(root_address,
                                            &language_table);

/* If status is GX_SUCCESS, the language table was successfully loaded. */

```

### <a name="see-also"></a>См. также:

- gx_binres_theme_load

## <a name="gx_binres_theme_load"></a>gx_binres_theme_load

Загрузка ресурса темы

### <a name="prototype"></a>Прототип

```C
UINT gx_binres_theme_load(
    GX_UBYTE *root_address, 
    INT theme_id, GX_THEME **returned_theme);
```

### <a name="description"></a>Описание

Эта служба создает структуру GX_THEME, содержащую указатели на таблицы ресурсов для запрошенной темы. Созданные структуры данных указывают на данные ресурса "на месте", но не копируют данные ресурсов. Поэтому данные ресурсов следует поместить в область памяти для общего доступа, а базовый адрес этого расположения в памяти передается этому API.

Для этой службы требуется выделенный блок памяти среды выполнения достаточного размера для хранения структуры таблицы тем, поэтому API gx_system_memory_allocator_set необходимо вызвать один раз перед запросом этой службы.

### <a name="parameters"></a>Параметры

- **root_address**: адрес двоичных данных ресурса в памяти
- **theme_id**: идентификатор темы
- **returned_theme**: указатель на загруженную тему

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_FORMAT** (0x24) — недопустимый двоичный ресурс.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимый идентификатор темы.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определены распределитель памяти или функция Free.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *theme = GX_NULL;
GX_UBYTE *root_address = 0x60000000;
INT theme_id = 0;

status = gx_binres_theme_load(root_address, theme_id, &theme);

/* If status is GX_SUCCESS, the theme was successfully loaded. */
```

### <a name="see-also"></a>См. также:

- gx_binres_language_table_read

## <a name="gx_brush_default"></a>gx_brush_default
Задание кисти по умолчанию

### <a name="prototype"></a>Прототип

```C
UINT gx_brush_default(GX_BRUSH *brush);
```

### <a name="description"></a>Описание

Эта служба задает для кисти текущего контекста системное значение по умолчанию.

### <a name="parameters"></a>Параметры
- **brush**: указатель на блок управления кистью

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кисть успешно определена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель кисти.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/*Reset the brush to its default value. */
status = gx_brush_default(&my_brush);

/* If status is GX_SUCCESS the brush was successfully reset to its default value. */

```

### <a name="see-also"></a>См. также:

- gx_brush_define


## <a name="gx_brush_define"></a>gx_brush_define
Определение кисти

### <a name="prototype"></a>Прототип

```C
UINT gx_brush_define(
    GX_BRUSH *brush, 
    GX_COLOR line_color, 
    GX_COLOR fill_color, 
    UINT style);
```

### <a name="description"></a>Описание

Эта служба определяет кисть с указанными цветом линии, цветом заливки и стилем.

### <a name="parameters"></a>Параметры

- **brush**: указатель на блок управления кистью
- **line_color**: цвет линии кисти В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **fill_color**: цвет заливки кисти В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **стиль**: стиль кисти В **приложении D** описаны поддерживаемые стили кисти. Стили кисти можно объединить в одну переменную с помощью побитовой операции OR.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кисть успешно определена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель кисти.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define a brush. */
status = gx_brush_define(&my_brush, GX_COLOR_BLACK, GX_COLOR_BLACK,
                         GX_STYLE_BORDER_NONE);

/* If status is GX_SUCCESS the brush was successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_brush_default

## <a name="gx_button_background_draw"></a>gx_button_background_draw

 Вывод фона кнопки

###<a name="prototype"></a>Прототип

```C
VOID gx_button_background_draw(GX_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует фон кнопки. Эта функция обычно вызывается внутри функции gx_button_draw, но предоставляется приложению для помощи в написании пользовательских функций рисования.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
VOID custom_button_draw(GX_BUTTON *button)
{
    /* Draw button background. */
    gx_button_background_draw(button);

    /* Add custom drawing here. */

    /* Draw child widgets. */
    gx_widget_children_draw((GX_WIDGET *)button);
}
```

### <a name="see-also"></a>См. также:

- gx_button_create,
- gx__button_deselect,
- gx__button_draw
- gx_button_event_process,
- gx__button_select
- gx_icon_button_create,
- gx__pixelmap_button_create
- gx_pixelmap_button_draw,
- gx__radio_button_create
- gx_radio_button_draw,
- gx__text_button_create
- gx_text_button_color_set,
- gx__text_button_draw

## <a name="gx_button_create"></a>gx_button_create

Кнопка "Создать"

### <a name="prototype"></a>Прототип

```C
UINT gx_button_create(
    GX_BUTTON *button, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent, 
    ULONG style, 
    USHORT button_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает кнопку, как указано, и связывает кнопку с указанным родительским мини-приложением.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки
- **name**: логическое имя кнопки
- **parent**: указатель на родительское мини-приложение кнопки
- **style**: стиль кнопки Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **button_id**: определяемый приложением идентификатор кнопки
- **size**: размер кнопки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BUTTON my_top_button;

/* Create a stop button. */
status = gx_button_create(&my_stop_button, "my stop button",
                            &my_parent_window,
                            GX_STYLE_BUTTON_TOGGLE,
                            MY_STOP_BUTTON_ID, &size);

/* If status is GX_SUCCESS the stop button was successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw,
- gx_button_deselect,
- gx_button_draw
- gx_button_event_process,
- gx_button_select
- gx_radio_button_create,
- gx_radio_button_draw
- gx_icon_button_create,
- gx_pixelmap_button_create
- gx_pixelmap_button_draw,
- gx_text_button_create
- gx_text_button_color_set,
- gx_text_button_draw

## <a name="gx_button_deselect"></a>gx_button_deselect


Отмена выбора кнопки

### <a name="prototype"></a>Прототип

```C
UINT gx_button_deselect(
    GX_BUTTON *button, 
    GX_BOOL gen_event);
```

### <a name="description"></a>Описание

Эта служба отменяет выбор указанной кнопки и создает событие сигнала в зависимости от стилей кнопок.


| Стиль кнопки              | Сигнал                     |
|---------------------------|----------------------------|
| Нет                      | GX_EVENT_CLICKED         |
| GX_STYLE_BUTTON_RADIO  | GX_EVENT_RADIO_DESELECT |
| GX_STYLE_BUTTON_TOGGLE | GX_EVENT_TOGGLE_OFF     |



### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки
- **gen_event**: если GX_TRUE, кнопка сгенерирует событие GX_EVENT_CLICKED, GX_EVENT_DESELECT или GX_EVENT_TOGGLE_OFFSET (в зависимости от стиля кнопки), Если GX_FALSE, кнопка не будет создавать никаких событий более высокого уровня, даже если обычно она это делает

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — выбор кнопки успешно отменен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Deselect button. */
status = gx_button_deselect(&my_stop_button, GX_TRUE);

/* If status is GX_SUCCESS the stop button was successfully deselected. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw,
- gx_button_create,
- gx_button_draw
- gx_button_event_process,
- gx_button_select
- gx_radio_button_create,
- gx_radio_button_draw
- gx_icon_button_create,
- gx_pixelmap_button_create
- gx_pixelmap_button_draw,
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_button_draw"></a>gx_button_draw

Вывод кнопки

### <a name="prototype"></a>Прототип

```C
VOID gx_button_draw(GX_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует указанную кнопку. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых кнопок.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom button draw function. */
VOID custom_button_draw(GX_BUTTON *button)
{
    /* Call default button draw. */
    gx_button_draw(button);

    /* Add custom drawing here. */
}

```

### <a name="see-also"></a>См. также:

- gx_button_background_draw,
- gx_button_create
- gx_button_deselect,
- gx_button_event_process,
- gx_button_select
- gx_radio_button_create,
- gx_radio_button_draw
- gx_icon_button_create,
- gx_pixelmap_button_create
- gx_pixelmap_button_draw,
- gx_text_button_create
- gx_text_button_color_set,
- gx_text_button_draw

## <a name="gx_button_event_process"></a>gx_button_event_process


Обработка события кнопки

### <a name="prototype"></a>Прототип

```C
UINT gx_button_event_process(
    GX_BUTTON *button, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанной кнопки.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие кнопки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic button event processing as part of custom event processing function. */

UINT custom_button_event_process(GX_BUTTON *button,
                                 GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;
    default:
        /* Pass all other events to the default button
            event processing */
        status = gx_button_event_process(button, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw,
- gx_button_create
- gx_button_deselect,
- gx_button_draw,
- gx_button_select
- gx_radio_button_create,
- gx_radio_button_draw
- gx_icon_button_create,
- gx_pixelmap_button_create
- gx_pixelmap_button_draw,
- gx_text_button_create
- gx_text_button_color_set,
- gx_text_button_draw

## <a name="gx_button_select"></a>gx_button_select

Кнопка выбора

### <a name="prototype"></a>Прототип

```C
UINT gx_button_select(GX_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба выбирает указанную кнопку и создает событие сигнала в зависимости от стилей кнопок.

Отменяет выбор одноуровневых элементов для группы переключателей.

| Стиль кнопки                       | Сигнал                   |
|------------------------------------|--------------------------|
| GX_STYLE_BUTTON_RADIO           | GX_EVENT_RADIO_SELECT |
| GX_STYLE_BUTTON_EVENT_ON_PUSH | GX_EVENT_CLICKED       |
| GX_STYLE_BUTTON_TOGGLE          | GX_EVENT_TOGGLE_ON    |


### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка успешно выбрана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Select button. */
status = gx_button_select(&my_stop_button);

/* If status is GX_SUCCESS the stop button was successfully selected. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw,
- gx_button_create
- gx_button_deselect,
- gx_button_draw,
- gx_button_event_process
- gx_radio_button_create,
- gx_radio_button_draw
- gx_icon_button_create,
- gx_pixelmap_button_create
- gx_pixelmap_button_draw,
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_canvas_alpha_set"></a>gx_canvas_alpha_set

Задание значения альфа-смешения для холста

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_alpha_set(
    GX_CANVAS *canvas, 
    GX_UBYTE alpha);
```

### <a name="description"></a>Описание

Эта служба задает значение альфа-смешения для указанного холста. Альфа-значения холста могут находиться в диапазоне от 0 (прозрачно) до 255 (полностью непрозрачно).

Для смешивания накладывающихся холстов требуется поддержка аппаратного уровня графики или поддержка программного обеспечения посредством создания составного холста.

Поддержка аппаратного смешивания холста включается путем вызова API gx_canvas_hardware_layer_bind() перед установкой альфа-значения холста. Когда холст привязывается к аппаратному уровню графики, вызов API gx_canvas_alpha_set() напрямую вызывает службы смешивания аппаратного уровня графики.

Чтобы использовать поддержку программного обеспечения для смешивания холстов, приложение должно создать холст со стилем GX_CANVAS_COMPOSITE, в котором все другие управляемые холсты являются составными до окончательного отображения. Поддержка программного обеспечения для смешивания холстов обеспечивается только при работе с видеодрайвером с глубиной цвета 16 бит или выше.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **alpha**: значение альфа-смешивания в диапазоне от 0 (прозрачно) до 255 (непрозрачно)

### <a name="return--values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение альфа-смешивания успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_ERROR** (0x20) — недопустимый холст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the alpha-blend value of “my_canvas”. */
status = gx_canvas_alpha_set(&my_canvas, GX_ALPHA_VALUE_OPAQUE);

/* If status is GX_SUCCESS the alpha-blend value was successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_create,
- gx_canvas_drawing_complete
- gx_canvas_drawing_initiate,
- gx_canvas_offset_set
- gx_canvas_shift,
- gx_canvas_hardware_layer_bind,
- gx_canvas_show
- gx_canvas_hide

## <a name="gx_canvas_arc_draw"></a>gx_canvas_arc_draw

Вывод дуги

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_arc_draw(
    INT xcenter, 
    INT ycenter, 
    UINT r,
    INT start_angle, 
    INT end_angle);
```

### <a name="description"></a>Описание

Эта служба рисует дугу окружности на холсте с помощью текущей кисти. Дуга окружности обрезается до недопустимого региона холста. Для этой службы требуется определить GX_ARC_DRAWING_SUPPORT.

### <a name="parameters"></a>Параметры

- **xcenter**: положение центра дуги окружности по оси X
- **ycenter**: положение центра дуги окружности по оси Y
- **r**: радиус дуги окружности
- **start_angle**: начальный угол дуги окружности
- **end_angle**: конечный угол дуги окружности

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — дуга успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Draw a circle arc from 0 degree to 90 degree in clockwise. */
status = gx_canvas_arc_draw(100, 100, 50, 0, 90);

/* If status is GX_SUCCESS the arc has been drawn on “my_canvas”. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move,
- gx_canvas_circle_draw
- gx_display_create,
- gx_canvas_ellipse_draw
- gx_canvas_line_draw,
- gx_canvas_pie_draw
- gx_canvas_pixelmap_draw,
- gx_canvas_pixelmap_tile
- gx_canvas_polygon_draw
- gx_canvas_rectangle_draw,
- gx_canvas_text_draw

## <a name="gx_canvas_block_move"></a>gx_canvas_block_move

Перемещение блока пикселей на холсте

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_block_move(GX_RECTANGLE *block,
    GX_VALUE x_shift, GX_VALUE y_shift, GX_RECTANGLE *dirty);
```

### <a name="description"></a>Описание

Эта служба перемещает блок данных пикселей на холсте в указанном направлении. Эта служба используется внутренне GUIX для выполнения быстрой прокрутки, но может также использоваться программным обеспечением приложения.

### <a name="parameters"></a>Параметры

- **block**: координаты области для перемещения
- **x_shift**: количество пикселей для смещения по оси X
- **y_shift**: количество пикселей для смещения по оси Y
- **dirty**: если блок перемещен успешно, эта функция возвращает часть исходного прямоугольника, которая все еще грязная, вызывающему объекту в этом параметре

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — блок успешно перемещен.
- **GX_FAILURE** (0x10) — не удалось переместить блок.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
GX_RECTANGLE invalid;
GX_RECTANGLE move;

/* define 100 x 100 pixel rectangle */
gx_utility_rectangle_define(&move, 0, 0, 99, 99);

/* Move this rectangle 10 pixels to the right”. */
status = gx_canvas_block_move(&move, 10, 0, &invalid);

/* If status is GX_SUCCESS, then ‘invalid’ marks the area that needs to be redrawn after the block move. */

```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_draw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile,
- gx_canvas_polygon_draw
- gx_canvas_rectangle_draw,
- gx_canvas_text_draw

## <a name="gx_canvas_circle_draw"></a>gx_canvas_circle_draw

Вывод окружности

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_circle_draw(
    INT xcenter, 
    INT ycenter, 
    UINT r);
```

### <a name="description"></a>Описание

Эта служба рисует окружность на холсте с помощью текущей кисти. Окружность обрезается до недопустимого региона холста. Для этой службы требуется определить GX_ARC_DRAWING_SUPPORT.

### <a name="parameters"></a>Параметры

- **xcenter**: координата центра окружности по оси X
- **ycenter**: координата центра окружности по оси Y
- **r**: радиус окружности

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — эллипс успешно нарисован.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_VALUE** (0x22) — недопустимый радиус окружности.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C

/* Draw a circle of radius 10 centered at (100, 100). */
status = gx_canvas_circle_draw(100, 100, 50);

/* If status is GX_SUCCESS the circle has been drawn on “my_canvas”. */

```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_darw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile,
- gx_canvas_polygon_draw
- gx_canvas_rectangle_draw,
- gx_canvas_text_draw


## <a name="gx_canvas_create"></a>gx_canvas_create

Создание холста

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_create(
    GX_CANVAS *canvas, 
    GX_CONST GX_CHAR *name,
    GX_DISPLAY *display, 
    UINT type, 
    UINT width, 
    UINT height,
    GX_COLOR *memory_area, 
    ULONG memory_size);
```

### <a name="description"></a>Описание

Эта служба создает холст с указанными свойствами и связанной памятью.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **name**: логическое имя холста
- **display**: указатель на созданный ранее дисплей
- **type**: тип холста. Типы холста включают следующие.
- **GX_CANVAS_SIMPLE**: холст памяти, используемый для рисования за пределами экрана.
- **GX_CANVAS_MANAGED**: холст, который автоматически отправляется на активный экран либо как часть составного процесса сборки, либо как часть операции переключения буфера для архитектур с одним холстом.
- **GX_CANVAS_VISIBLE**: этот флаг можно использовать для включения и выключения холста без потери нарисованного содержимого.
- **GX_CANVAS_MODIFIED**: зарезервировано для последующего использования.
- **GX_CANVAS_COMPOSITE**: этот флаг используется приложением при настройке системы с несколькими холстами, которая будет объединять несколько управляемых холстов в один составной холст, после чего тот отправляется в аппаратный буфер кадров.
- **width**: ширина в пикселях
- **height**: высота в пикселях
- **memory_area**: область памяти холста Это значение может быть
- **GX_NULL** во время создания холста
- **и** в дальнейшем инициализироваться с помощью gx_canvas_memory_define.
- **memory_size**: размер области памяти в байтах или 0, если память холста будет определена после создания холста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — холст успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_CANVAS_SIZE** (0x1C) — недопустимый размер блока управления холста.
- **GX_INVALID_TYPE** (0x1B) — недопустимый тип холста.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define global canvas memory area. */
GX_COLOR my_canvas_memory[272*480];

…

/* Create “my_canvas”. */
status = gx_canvas_create(&my_canvas, "my canvas", &my_display,
                    (GX_CANVAS_MANAGED | GX_CANVAS_VISIBLE),
                    272, 480,
                    my_canvas_memory,
                    sizeof(default_canvas_memory));

/* If status is GX_SUCCESS the 272 x 480 canvas was successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_delete,
- gx_canvas_hardware_layer_bind
- gx_canvas_memory_define

## <a name="gx_canvas_delete"></a>gx_canvas_delete

Удаление холста

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_delete(GX_CANVAS *canvas);
```

### <a name="description"></a>Описание

Эта служба удаляет холст. Холст удаляется из внутреннего связанного списка, который хранится в GUIX.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — холст успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete “my_canvas”. */
status = gx_canvas_delete (&my_canvas);

/* If status is GX_SUCCESS my_canvas was deleted. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_drawing_complete
- gx_canvas_create,
- gx_canvas_drawing_initiate
- gx_canvas_offset_set,
- gx_canvas_shift

## <a name="gx_canvas_drawing_complete"></a>gx_canvas_drawing_complete

Завершение рисования на холсте

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_drawing_complete(
    GX_CANVAS *canvas, 
    GX_BOOL flush);
```

### <a name="description"></a>Описание

Эта служба дает GUIX понять, что рисование приложения на указанном холсте завершено.

Приложение может использовать эту службу для принудительного немедленного рисования на холсте. При этом холст записывается в видимый буфер кадров и/или активируется операция переключения буфера (в зависимости от архитектуры системной памяти).

Эта служба должна вызываться приложением только для закрытия последовательности прорисовки, начатой функцией gx_canvas_drawing_initiate().

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **flush**: если **_GX_TRUE_**, изменения на холсте отправляются на дисплей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — рисование успешно завершено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Complete drawing on “my_canvas” and flush to display. */
status = gx_canvas_drawing_complete(&my_canvas, GX_TRUE);

/* If status is GX_SUCCESS the canvas drawing was successfully completed. */
```


### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_create
- gx_canvas_drawing_initiate,
- gx_canvas_offset_set
- gx_canvas_shift

## <a name="gx_canvas_drawing_initiate"></a>gx_canvas_drawing_initiate

Начало рисования на холсте

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_drawing_initiate(
    GX_CANVAS *canvas,
    GX_WIDGET *who, 
    GX_RECTANGLE *dirty_area);
```

### <a name="description"></a>Описание

Эта служба инициирует рисование на указанном холсте. Эта служба вызывается внутри как часть отложенной операции рисования, выполняемой GUIX автоматически при необходимости обновить холст. Однако приложению разрешается обойти алгоритм отложенного рисования GUIX, а также немедленно выполнить рисование прямо на холсте путем сначала вызова gx_canvas_drawing_inititate, вызова нужных функций рисования, а затем вызова gx_canvas_drawing_complete().

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **who**: указатель на блок управления мини-приложением вызывающего объекта Этот параметр используется для инициализации обрезки рисования изображения и параметров представления для последующих операций рисования.
- **dirty_area**: область для рисования Этот параметр передается вызывающим объектом, чтобы указать область, до которой вызывающий объект хочет обрезать все операции рисования. Обычно это область, которая ранее была помечена как грязная, однако вызывающий объект может расширить область обрезки или сузить ее.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — рисование успешно начато.
- **GX_DRAW_NESTING_EXCEEDED** (0x05) — превышено максимальное количество вложений.
- **GX_NO_VIEW** (0x03) — нет окон просмотра для вызывающего объекта.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Initiate drawing on “my_canvas”, my_widget.gx_widget_size
specify the area the application wants GUIX to redraw. */
status = gx_canvas_drawing_initiate(&my_canvas, &my_widget,
    &my_widget.gx_widget_size);

/* If status is GX_SUCCESS the canvas drawing was successfully initiated. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_create
- gx_canvas_drawing_complete,
- gx_canvas_offset_set
- gx_canvas_shift


## <a name="gx_canvas_ellipse_draw"></a>gx_canvas_ellipse_draw

Вывод эллипса

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_ellipse_draw(
    INT xcenter, 
    INT ycenter, 
    INT a, 
    dINT b);
```

### <a name="description"></a>Описание

Эта служба рисует эллипс на холсте с помощью текущей кисти. Эллипс обрезается до недопустимого региона холста. Для этой службы требуется определить GX_ARC_DRAWING_SUPPORT.

### <a name="parameters"></a>Параметры

- **xcenter**: координата центра эллипса по оси X
- **ycenter**: координата центра эллипса по оси Y
- **a**: длина большой полуоси
- **b**: длина малой полуоси

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — эллипс успешно нарисован.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Draw an ellipse of semi-major radius 100, semi-minor radius 50
    and centered at (200, 200). */
status = gx_canvas_ellipse_draw(200, 200, 100, 50);

/* If status is GX_SUCCESS the ellipse has been drawn on
    “my_canvas”. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create,
- gx_canvas_line_darw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile,
- gx_canvas_polygon_draw
- gx_canvas_rectangle_draw,
- gx_canvas_text_draw

## <a name="gx_canvas_hardware_layer_bind"></a>gx_canvas_hardware_layer_bind

Привязка холста к аппаратному уровню графики

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_hardware_layer_bind(
    GX_CANVAS *canvas, 
    INT layer);
```

### <a name="description"></a>Описание

Эта служба привязывает холст рисования GUIX к аппаратному уровню графики. Эта служба необходима только для аппаратных устройств, поддерживающих несколько аппаратных уровней графики.

Привязка холста к аппаратному уровню графики приводит к реализации интерфейсов API gx_canvas_show(), gx_canvas_hide(), gx_canvas_alpha_set() и gx_canvas_offset_set() непосредственно с помощью служб драйверов оборудования дисплея.

Если аппаратный драйвер дисплея не поддерживает несколько уровней графики, эта служба не сможет возвратить GX_INVALID_DISPLAY.

### <a name="parameters"></a>Параметры

- **canvas**: холст для реализации с помощью оборудования
- **layer**: аппаратный уровень графики

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — привязка выполнена успешно.
- **GX_INVALID_DISPLAY** (0x1D) — не определена служба уровня дисплея.
- **GX_PTR_ERROR** (0x17) — недопустимые указатели.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.
- **GX_NOT_SUPPORTED** (0x28) — не поддерживается.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Binds the canvas to the hardware graphics layer 1. */
status = gx_canvas_hardware_layer_bind(&my_canvas, 1);

/* If status is GX_SUCCESS, the drawing canvas is bound to the
    hardware graphics. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_create,
- gx_canvas_memory_define

## <a name="gx_canvas_hide"></a>gx_canvas_hide

Скрытие холста, придание ему невидимости

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_hide(GX_CANVAS *canvas);
```

### <a name="description"></a>Описание

Эта служба скрывает холст GUIX. Если холст привязан к аппаратному уровню графики с помощью gx_canvas_hardware_layer_bind(), эта служба реализуется с помощью аппаратной поддержки.

### <a name="parameters"></a>Параметры

- **canvas**: холст для скрытия

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.
- **GX_PTR_ERROR** (0x17) — недопустимые указатели.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Make my_canvas invisible. */
status = gx_canvas_hide(&my_canvas);

/* If status is GX_SUCCESS, the canvas has been hidden. */

```

### <a name="see-also"></a>См. также:

- gx_canvas_create,
- gx_canvas_drawing_complete
- gx_canvas_drawing_initiate,
- gx_canvas_offset_set
- gx_canvas_shift,
- gx_canvas_hardware_layer_bind,
- gx_canvas_show
- gx_canvas_hide

## <a name="gx_canvas_line_draw"></a>gx_canvas_line_draw

Вывод линии

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_line_draw(
    GX_VALUE x_start, 
    GX_VALUE y_start,
    GX_VALUE x_end, 
    GX_VALUE y_end);
```

### <a name="description"></a>Описание

Эта служба рисует линию на холсте с помощью текущей кисти. Линия обрезается до недопустимого региона холста.

### <a name="parameters"></a>Параметры

- **x_start**: начальная координата линии по оси X
- **y_end**: начальная координата линии по оси Y
- **x_end**: конечная координата линии по оси X
- **y_end**: конечная координата линии по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — линия успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.
- **GX_INVALID_WIDTH** (0x1E) — недопустимая ширина кисти.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Draw line on canvas. */
status = gx_canvas_line_draw(0, 1, 320, 480);

/* If status is GX_SUCCESS, the line has been drawn to canvas. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_pie_draw
- gx_canvas_pixelmap_draw,
- gx_canvas_pixelmap_tile
- gx_canvas_polygon_draw,
- gx_canvas_rectangle_draw
- gx_canvas_text_draw

## <a name="gx_canvas_memory_define"></a>gx_canvas_memory_define

Определение памяти холста

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_memory_define(
    GX_CANVAS *canvas, 
    GX_COLOR *memory,
    ULONG memsize);
```

### <a name="description"></a>Описание

Эта служба может использоваться для назначения адреса памяти холста после создания холста.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на созданный ранее холст
- **memory**: адрес памяти холста
- **memsize**: размер блока памяти холста (в байтах)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно назначено.
- **GX_INVALID_CANVAS** (0x20) — недопустимый блок управления.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Assign canvas memory block. */
status = gx_canvas_memory_define(canvas,
    (GX_COLOR *) DRAM_MEMORY,
    (640 * 480 * 2));

/* If status is GX_SUCCESS, the canvas memory pointer has be
reassigned. */

```

### <a name="see-also"></a>См. также:

- gx_canvas_create,
- gx_canvas_hardware_layer_bind

## <a name="gx_canvas_mouse_define"></a>gx_canvas_mouse_define

Определение изображения курсора мыши

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_mouse_define(GX_CANVAS *canvas,
    GX_MOUSE_CURSOR_INFO *info);
```

### <a name="description"></a>Описание

Эта служба определяет сведения о мыши для указанного холста. Для этой службы требуется определить GX_MOUSE_SUPPORT.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **info**: указатель на сведения о курсоре мыши Определение структуры GX_MOUSE_CURSOR_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о курсоре мыши успешно заданы.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set mouse cursor info. */
GX_MOUSE_CURSOR_INFO mouse_cursor;
mouse_cursor.gx_mouse_cursor_image_id = GX_PIXELMAP_ID_MOUSE;
mouse_cursor.gx_mouse_cursor_hotspot_x = 0;
mouse_cursor.gx_mouse_cursor_hotspot_y = 0;

status = gx_canvas_mouse_define(&my_canvas, &mouse_cursor);

/* If status is GX_SUCCESS the mouse info of “my_canvas” has been
set successfully. */

```

### <a name="see-also"></a>См. также:

- gx_canvas_mouse_show,
- gx_canvas_mouse_hide

## <a name="gx_canvas_mouse_hide"></a>gx_canvas_mouse_hide


Отключение курсора мыши

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_mouse_hide(GX_CANVAS *canvas);
```

### <a name="description"></a>Описание

Эта служба скрывает курсор мыши на указанном холсте. Для этой службы требуется определить GX_MOUSE_SUPPORT.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор мыши успешно скрыт.
- **GX_FAILURE** (0x10) — не удалось скрыть курсор мыши.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.


### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Hide the mouse cursor. */
status = gx_canvas_mouse_hide(&my_canvas);

/* If status is GX_SUCCESS the mouse cursor of “my_canvas” has been
hidden successfully. */

```

### <a name="see-also"></a>См. также:

- gx_canvas_mouse_show,
- gx_canvas_mouse_define

## <a name="gx_canvas_mouse_show"></a>gx_canvas_mouse_show


Включение курсора мыши

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_mouse_show(GX_CANVAS *canvas);
```

### <a name="description"></a>Описание

Эта служба делает курсор мыши видимым на указанном холсте. Для этой службы требуется определить GX_MOUSE_SUPPORT. Необходимо вызвать API gx_canvas_mouse_define, чтобы определить изображение курсора мыши до запроса этой службы.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о курсоре мыши успешно заданы.
- **GX_FAILURE** (0x10) — не удалось отобразить курсор мыши.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Make mouse cursor hidden ”. */
status = gx_canvas_mouse_show(&my_canvas);

/* If status is GX_SUCCESS the mouse of “my_canvas” has been
hidden successfully. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_mouse_show,
- gx_canvas_mouse_define

## <a name="gx_canvas_offset_set"></a>gx_canvas_offset_set


Назначение смещения холста по осям X и Y

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_offset_set(
    GX_CANVAS *canvas,
    GX_VALUE x, 
    GX_VALUE y);
```

### <a name="description"></a>Описание

Эта служба назначает смещение отображения указанного холста по осям X и Y. Это позволяет управлять положением холста в видимом буфере кадров и часто используется, когда размер холста меньше размеров физического дисплея.

Если холст привязан к аппаратному уровню графики с помощью API gx_canvas_hardware_layer_bind(), служба gx_canvas_offset_set реализуется напрямую с помощью аппаратной поддержки.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **x**: координата смещения по оси X
- **y**: координата смещения по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — смещение успешно назначено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set display offset for “my_canvas”. */
status = gx_canvas_offset_set(&my_canvas, 20, 30);

/* If status is GX_SUCCESS the canvas drawing is now offset from
position 20,30. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_create
- gx_canvas_drawing_complete,
- gx_canvas_initiate,
- gx_canvas_shift
- gx_canvas_show,
- gx_canvas_hide,
- gx_canvas_hardware_layer_bind

## <a name="gx_canvas_pie_draw"></a>gx_canvas_pie_draw


Вывод круговой фигуры

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pie_draw(
    INT xcenter, 
    INT ycenter,
    UINT r, 
    INT start_angle, 
    INT end_angle);
```

### <a name="description"></a>Описание

Эта служба рисует круговую диаграмму на холсте, используя кисть текущего контекста рисования. Круговая фигура обрезается до недопустимого региона холста. Для этой службы необходимо определить параметр конфигурации GX_ARC_DRAWING_SUPPORT.

### <a name="parameters"></a>Параметры

- **xcenter**: положение центра круговой фигуры по оси X
- **ycenter**: положение центра круговой фигуры по оси Y
- **r**: радиус круговой фигуры
- **start_angle**: начальный угол круговой фигуры
- **end_angle**: конечный угол круговой фигуры

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — дуга успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw a pie from 0 degree to 90 degree in clockwise. */
status = gx_canvas_pie_draw(100, 100, 50, 0, 90);

/* If status is GX_SUCCESS the pie has been drawn to canvas. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_draw
- gx_canvas_pixelmap_draw,
- gx_canvas_pixelmap_tile
- gx_canvas_polygon_draw,
- gx_canvas_rectangle_draw
- gx_canvas_text_draw

## <a name="gx_canvas_pixel_draw"></a>gx_canvas_pixel_draw


Вывод пикселя

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pixel_draw(GX_POINT position);
```

### <a name="description"></a>Описание

Эта служба рисует пиксель на холсте, используя цвет линий кисти текущего контекста рисования. Если определен параметр конфигурации GX_BRUSH_ALPHA_SUPPORT, выполняется смешивание пикселя, в противном случае пиксель рисуется как полностью непрозрачный.

- **point**: положение пикселя для рисования на осях X и Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_POINT point; /* the x,y position you want to draw to */
GX_RECTANGLE drawto; /* the rectangle bounding your drawing */
GX_CANVAS *mycanvas; /* the canvas you want to draw to */

/* calculate 1x1 pixel drawing area: */
gx_utility_rectangle_define(&drawto,
    point.gx_point_x, point.gx_point_y,
    point.gx_point_x, point.gx_point_y);

/* get my canvas: */
gx_widget_canvas_get(win, &mycanvas);

/* open my canvas for drawing: */
gx_canvas_drawing_initiate(mycanvas, win, &drawto);

/* setup my brush colors. Use any color ID in your resources: */
gx_context_line_color_set(GX_COLOR_ID_WINDOW_BORDER);

/* draw a pixel: */
status = gx_canvas_pixel_draw(point);

/* close the canvas: */
gx_canvas_drawing_complete(mycanvas, GX_TRUE);

/* If status is GX_SUCCESS, the pixel was successfully drawn to mycanvas. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move,
- gx_canvas_pixelmap_tile
- gx_canvas_pixelmap_blend

## <a name="gx_canvas_pixelmap_blend"></a>gx_canvas_pixelmap_blend

Смешивание карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pixelmap_blend(
    GX_VALUE x_position,
    GX_VALUE y_position, 
    GX_PIXELMAP *pixelmap, 
    GX_UBYTE alpha);
```

### <a name="description"></a>Описание

Эта служба смешивает карту отображения пикселей с фоном холста. Коэффициент смешивания определяется вызывающим объектом. Альфа-значение может находиться в диапазоне от 0 (полностью прозрачно) до 255 (полностью непрозрачно). Карта отображения пикселей также может включать внутренний альфа-канал, который объединяется с входящим значением смешивания. Эта служба поддерживается только драйверами дисплея, работающими с глубиной цвета 16 бит и выше.

### <a name="parameters"></a>Параметры

- **x_start**: начальная координата карты отображения пикселей по оси X
- **y_end**: конечная координата карты отображения пикселей по оси X
- **pixelmap**: указатель на карту отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_NOT_SUPPORTED** (0x28) — не поддерживается.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw pixelmap on active canvas */

GX_PIXELMAP *map;
gx_system_pixelmap_get(ID_MY_PIXELMAP, &map);

status = gx_canvas_pixelmap_blend(10, 20, map, 128);

/* If status is GX_SUCCESS the pixelmap has been blended onto the current canvas. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move,
- gx_canvas_pixelmap_get
- gx_canvas_pixelmap_tile
- gx_canvas_pixelmap_draw

## <a name="gx_canvas_pixelmap_draw"></a>gx_canvas_pixelmap_draw

Вывод карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pixelmap_draw(
    GX_VALUE x_position, 
    GX_VALUE y_position,
    GX_PIXELMAP *pixelmap);
```

### <a name="description"></a>Описание

Эта служба рисует карту отображения пикселей на холсте.

### <a name="parameters"></a>Параметры

- **x_start**: начальная координата карты отображения пикселей по оси X
- **y_end**: конечная координата карты отображения пикселей по оси X
- **pixelmap**: указатель на карту отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw pixelmap on canvas. */
status = gx_canvas_pixelmap_draw(10, 20, &my_pixelmap);

/* If status is GX_SUCCESS the pixelmap “my_pixelmap” has been drawn. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move,
- gx_canvas_pixelmap_get
- gx_canvas_pixelmap_tile
- gx_canvas_pixelmap_blend

## <a name="gx_canvas_pixelmap_get"></a>gx_canvas_pixelmap_get

Получение карты отображения пикселей холста

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pixelmap_get(GX_PIXELMAP *pixelmap);
```

### <a name="description"></a>Описание

Эта служба возвращает структуру GX_PIXELMAP, указывающую на данные холста. Для формата карты отображения пикселей задан формат текущего отображения цветов.

### <a name="parameters"></a>Параметры

- **pixelmap**: возвращенная карта отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw pixelmap on active canvas */

GX_PIXELMAP *map;

status = gx_canvas_pixelmap_get(map);

/* If status is GX_SUCCESS the pixelmap has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_pixelmap_blend,
- gx_canvas_pixelmap_tile
- gx_canvas_pixelmap_draw

## <a name="gx_canvas_pixelmap_rotate"></a>gx_canvas_pixelmap_rotate


Вывод повернутой карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pixelmap_rotate(
    GX_VALUE x_position, 
    GX_VALUE y_position,
    GX_PIXELMAP *pixelmap, 
    INT angle,
    INT rot_cx, 
    INT rot_cy);
```

### <a name="description"></a>Описание

Эта служба поворачивает карту отображения пикселей на указанный угол и отрисовывает карту отображения пикселей на холсте непосредственно по мере выполнения вращения. Эта служба отличается от gx_utility_pixelmap_rotate тем, что выходные данные вращения отрисовываются непосредственно в памяти холста, а повернутая карта отображения пикселей не возвращается вызывающему объекту.

Преимущество этой службы перед gx_utility_pixelmap_rotate заключается в том, что для размещения повернутой карты отображения пикселей не требуется дополнительная память. Недостаток же заключается в том, что код вращения должен выполняться при каждой отрисовке карты отображения пикселей.

Проверка обрезки и окна просмотра применяется во время отрисовки повернутой карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **x_position**: начальная координата карты отображения пикселей по оси X
- **y_position**: начальная координата карты отображения пикселей по оси Y
- **pixelmap**: указатель на карту отображения пикселей
- **angle**: угол поворота
- **rot_cx**: координата центра вращения по оси X Если это значение равно -1, в качестве центра вращения используется центр изображения.
- **rot_cy**: координата центра вращения по оси Y. Если это значение равно –1, в качестве центра вращения используется центр изображения.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* rotate “src_pixelmap” by 30 degree in clockwise direction and draw in on canvas. */
status = gx_canvas_pixelmap_rotate(10, 20, &my_pixelmap, 30,
    -1, -1);

/* If status is GX_SUCCESS the rotated pixelmap “my_pixelmap” has been drawn. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move,
- gx_canvas_pixelmap_get
- gx_canvas_pixelmap_tile,
- gx_canvas_pixelmap_blend

## <a name="gx_canvas_pixelmap_tile"></a>gx_canvas_pixelmap_tile

Замостить карту отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_pixelmap_tile(
    GX_RECTANGLE *fill,
    GX_PIXELMAP *pixelmap);
```

### <a name="description"></a>Описание

Эта служба заполняет прямоугольник на холсте запрошенной картой отображения пикселей.

### <a name="parameters"></a>Параметры

- **fill**: область для заполнения картой отображения пикселей
- **pixelmap**: указатель на карту отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — область успешно заполнена картой отображения пикселей.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер заливки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Tile pixelmap on canvas */
status = gx_canvas_pixelmap_tile(&tile_area, &my_pixelmap);

/* If status is GX_SUCCESS the pixelmap “my_pixelmap” has been tiled on canvas */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move,
- gx_canvas_pixelmap_get
- gx_canvas_pixelmap_blend,
- gx_canvas_pixelmap_draw

## <a name="gx_canvas_polygon_draw"></a>gx_canvas_polygon_draw


Вывод многоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_polygon_draw(
    GX_POINT *point_array,
    INT number_of_points);
```

### <a name="description"></a>Описание

Эта служба рисует многоугольник на холсте с помощью кисти текущего контекста рисования.

### <a name="parameters"></a>Параметры

- **point_array**: массив точек многоугольника
- **number_of_points**: количество точек многоугольника

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — многоугольник успешно нарисован.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_POINT my_polygon[4] =
{
    { 208, 63 },
    { 274, 63 },
    { 274, 163 },
    { 208, 163 }
};

/* Draw polygon “my_polygon” on canvas. */
status = gx_canvas_polygon_draw(&my_polygon, 4);

/* If status is GX_SUCCESS the polygon “my_polygon” has been drawn. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_draw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile,
- gx_canvas_rectangle_draw
- gx_canvas_text_draw

## <a name="gx_canvas_rectangle_draw"></a>gx_canvas_rectangle_draw


Вывод прямоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_rectangle_draw(GX_RECTANGLE *rectangle);
```

### <a name="description"></a>Описание

Эта служба рисует прямоугольник на холсте.

### <a name="parameters"></a>Параметры

- **rectangle**: прямоугольник для рисования

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — прямоугольник успешно нарисован.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw rectangle “my_rectangle” on canvas. */
status = gx_canvas_rectangle_draw(&my_rectangle);

/* If status is GX_SUCCESS the rectangle “my_rectangle” has been drawn. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_draw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile,
- gx_canvas_polygon_draw
- gx_canvas_text_draw

## <a name="gx_canvas_rotated_text_draw"></a>gx_canvas_rotated_text_draw


Вывод текста, повернутого вокруг центральной точки (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_rotated_text_draw(
    const GX_CHAR *text,
    GX_VALUE xCenter, 
    GX_VALUE yCenter, 
    INT angle);
```

### <a name="description"></a>Описание

Этот API является устаревшим. Вместо него используется gx_canvas_rotated_text_draw_ext(). Хотя он и поддерживается, новые приложения должны использовать gx_canvas_rotated_text_draw_ext() вместо этого API.

Эта служба рисует текст на холсте. Текст отображается повернутым относительно запрошенной центральной точки. Для отрисовки текста используются шрифт и цвет линий текущего контекста рисования.

Эта служба использует функцию gx_utility_string_to_alphamap для преобразования текстовой строки во временную карту отображения пикселей 8bpp, содержащую только альфа-значение. Затем служба поворачивает альфа-карту с помощью функции gx_utility_pixelmap_rotate. После отрисовки окончательной альфа-карты на холсте эта служба освобождает временную альфа-карту и связанную память.

Поскольку для отрисовки повернутого текста требуется временная альфа-карта, приложение должно настроить gx_system_memory_allocator путем вызова API gx_system_memory_allocator_set(), прежде чем попытаться нарисовать повернутый текст.

Эта служба должна использоваться только для отображения повернутого текста один раз. Если одна и та же текстовая строка будет нарисована несколько раз в разных местах или под разными углами вращения, более эффективно будет использовать служебную функцию gx_utility_string_to_alphamap() для создания текста альфа-карты один раз, а затем использовать gx_utility_pixelmap_rotate несколько раз для повторного поворота полученной альфа-карты.

### <a name="parameters"></a>Параметры

- **text**: текстовая строка для вывода
- **xCenter**: положение центра, вокруг которого будет выполняться поворот текста
- **yCenter**: положение центра, вокруг которого будет выполняться поворот текста
- **angle**: требуемый угол поворота текста (в градусах)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно отрисован.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — недостаточно доступной памяти, или gx_system_memory_allocator не назначен.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
void my_window_draw(GX_WINDOW *window)
{

    GX_VALUE xpos = 100;
    GX_VALUE ypos = 100;
    INT dynamic_count = 1234567;
    GX_CHAR dynamic_text[10];

    /* Call default window draw routine. */
    gx_window_draw(window);

    /* Set font. */
    gx_context_font_set(GX_FONT_ID_SMALL_BOLD);

    /* Convert int value to string. */
    gx_utility_ltoa(dynamic_count, dynamic_text, 20);

    /* Draw rotate text. */
    gx_canvas_rotated_text_draw(dynamic_text, xpos, ypos, 45);
}
```

### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_drawing_complete
- gx_canvas_create,
- gx_canvas_drawing_initiate
- gx_canvas_offset_set,
- gx_canvas_shift

## <a name="gx_canvas_rotated_text_draw_ext"></a>gx_canvas_rotated_text_draw_ext


Вывод текста, повернутого вокруг центральной точки

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_rotated_text_draw_ext(
    GX_CONST GX_STRING *text,
    GX_VALUE xCenter, 
    GX_VALUE yCenter, 
    INT angle);
```

### <a name="description"></a>Описание

Эта служба рисует текст на холсте. Текст отображается повернутым относительно запрошенной центральной точки. Для отрисовки текста используются шрифт и цвет линий текущего контекста рисования.

Эта служба использует функцию gx_utility_string_to_alphamap для преобразования текстовой строки во временную карту отображения пикселей 8bpp, содержащую только альфа-значение. Затем служба поворачивает альфа-карту с помощью функции gx_utility_pixelmap_rotate. После отрисовки окончательной альфа-карты на холсте эта служба освобождает временную альфа-карту и связанную память.

Поскольку для отрисовки повернутого текста требуется временная альфа-карта, приложение должно настроить gx_system_memory_allocator путем вызова API ***gx_system_memory_allocator_set***, прежде чем попытаться нарисовать повернутый текст.

Эта служба должна использоваться только для отображения повернутого текста один раз. Если одна и та же текстовая строка будет нарисована несколько раз в разных местах или под разными углами вращения, более эффективно будет использовать служебную функцию gx_utility_string_to_alphamap() для создания текста альфа-карты один раз, а затем использовать gx_utility_pixelmap_rotate несколько раз для повторного поворота полученной альфа-карты.

### <a name="parameters"></a>Параметры

- **text**: текстовая строка для вывода
- **xCenter**: положение центра, вокруг которого будет выполняться поворот текста
- **yCenter**: положение центра, вокруг которого будет выполняться поворот текста
- **angle**: требуемый угол поворота текста (в градусах)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно отрисован.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_CONTEXT** (0x06) — недопустимый контекст рисования.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — недостаточно доступной памяти, или gx_system_memory_allocator не назначен.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
void my_window_draw(GX_WINDOW *window)
{

    GX_VALUE xpos = 100;
    GX_VALUE ypos = 100;
    INT dynamic_count = 1234567;
    GX_CHAR dynamic_text[10];
    GX_STRING string;

    /* Call default window draw routine. */
    gx_window_draw(window);

    /* Set font. */
    gx_context_font_set(GX_FONT_ID_SMALL_BOLD);

    /* Convert int value to string. */
    gx_utility_ltoa(dynamic_count, dynamic_text, 20);

    string.gx_string_ptr = dynamic_text;
    string.gx_string_length = strlen(dynamic_text);

    /* Draw rotate text. */
    gx_canvas_rotated_text_draw_ext(&string, xpos, ypos, 45);
}
```

### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_drawing_complete
- gx_canvas_create,
- gx_canvas_drawing_initiate
- gx_canvas_offset_set,
- gx_canvas_shift

## <a name="gx_canvas_shift"></a>gx_canvas_shift


Сдвиг холста по осям X, Y

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_shift(
    GX_CANVAS *canvas, 
    GX_VALUE x, GX_VALUE y);
```

### <a name="description"></a>Описание

Эта служба сдвигает указанное смещение холста на указанную величину. Это влияет на расположение, в котором холст отображается в видимом буфере кадров.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **x**: пиксели для смещения по оси X
- **y**: пиксели для смещения по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — холст успешно смещен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Shift canvas “my_canvas”. */
status = gx_canvas_shift(&my_canvas, 10, 15);

/* If status is GX_SUCCESS the canvas has been shifted by 10 pixels
    on the X axis and 15 on the Y axis. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_drawing_complete,
- gx_canvas_initiate
- gx_canvas_alpha_set,
- gx_canvas_create
- gx_canvas_offset_set

## <a name="gx_canvas_show"></a>gx_canvas_show


Сделать холст видимым

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_show(GX_CANVAS *canvas);
```

### <a name="description"></a>Описание

Эта служба делает холст видимым. Если холст был ранее привязан к аппаратному уровню графики с помощью API gx_canvas_hardware_layer_bind(), служба gx_canvas_show реализуется напрямую с помощью аппаратной поддержки.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — смещение успешно назначено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Make this canvas visible. */
status = gx_canvas_show(&my_canvas);

/* If status is GX_SUCCESS the canvas drawing is now visible */
```

### <a name="see-also"></a>См. также:

- gx_canvas_alpha_set,
- gx_canvas_create
- gx_canvas_drawing_complete,
- gx_canvas_initiate,
- gx_canvas_shift,
- gx_canvas_hide,
- gx_canvas_hardware_layer_bind

## <a name="gx_canvas_text_draw"></a>gx_canvas_text_draw

Вывод текста (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_text_draw(
    GX_VALUE x_start, 
    GX_VALUE y_start,
    GX_CONST GX_CHAR *string, 
    INT length);
```

### <a name="description"></a>Описание

Эта служба рисует текст на холсте. Этот API, хотя и поддерживается, является устаревшим, и новые приложения должны использовать gx_canvas_text_draw_ext().

### <a name="parameters"></a>Параметры

- **x_start**: начальная координата на оси X для текста
- **y_start**: начальная координата на оси Y для текста
- **string**: указатель на строку для вывода
- **length**: если length >= 0, количество выводимых символов ограничивается до значения length Если length < 0, выводится вся строка до NULL в конце.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно нарисован.
- **GX_FAILURE** (0x1E) — не удалось нарисовать текст.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw text “example” on current canvas”. */
status = gx_canvas_text_draw(10, 20, “example”, 7);

/* Draw all of a string of unknown length on the current canvas */
status = gx_canvas_text_draw(10, 40, string_ptr, -1);

/* If status is GX_SUCCESS the text “example” has been drawn. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_draw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile
- gx_canvas_polygon_draw,
- gx_canvas_rectangle_draw

## <a name="gx_canvas_text_draw_ext"></a>gx_canvas_text_draw_ext


Вывод текста

### <a name="prototype"></a>Прототип

```C
UINT gx_canvas_text_draw_ext(
    GX_VALUE x_start, 
    GX_VALUE y_start,
    GX_CONST GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба рисует текст на холсте.

### <a name="parameters"></a>Параметры

- **x_start**: начальная координата на оси X для текста
- **y_start**: начальная координата на оси Y для текста
- **string**: указатель на строку для вывода

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно нарисован.
- **GX_FAILURE** (0x1E) — не удалось нарисовать текст.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_CONTEXT** (0x06) — нет открытого контекста рисования.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.
- **GX_INVALID_FONT** (0x16) — недопустимый шрифт.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING string;
string.gx_string_ptr = “example”;
string.gx_string_length = 7;

/* Draw text “example” on current canvas”. */
status = gx_canvas_text_draw_ext(10, 20, &string);

/* If status is GX_SUCCESS the text “example” has been drawn. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_arc_draw,
- gx_canvas_block_move
- gx_canvas_circle_draw,
- gx_display_create
- gx_canvas_ellipse_draw,
- gx_canvas_line_draw
- gx_canvas_pie_draw,
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile
- gx_canvas_polygon_draw,
- gx_canvas_rectangle_draw

## <a name="gx_checkbox_create"></a>gx_checkbox_create


Создание флажка

### <a name="prototype"></a>Прототип

```C
UINT gx_checkbox_create(
    GX_CHECKBOX *checkbox, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent, 
    GX_RESOURCE_ID text_id,
    ULONG style, 
    USHORT checkbox_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение флажка с указанными свойствами. GX_CHECKBOX является производным от GX_TEXT_BUTTON, и все службы gx_text_button могут использоваться с мини-приложениями GX_CHECKBOX.

### <a name="parameters"></a>Параметры

- **checkbox**: указатель на имя блока управления флажком. Логическое имя мини-приложения флажка
- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса текста флажка
- **style**: стиль флажка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **checkbox_id**: определяемый приложением идентификатор флажка
- **size**: размеры флажка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — флажок успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_checkbox”. */
status = gx_checkbox_create(&my_checkbox, “my_checkbox”,
    &my_parent,
    MY_CHECKBOX_TEXT_RESOURCE_ID, GX_STYLE_BORDER_RAISED,
    MY_CHECKBOX_ID,
    &size);

/* If status is GX_SUCCESS the checkbox “my_checkbox” has been created. */
```
### <a name="see-also"></a>См. также:

- gx_checkbox_draw,
- gx_checkbox_event_process,
- gx_checkbox_select

## <a name="gx_checkbox_draw"></a>gx_checkbox_draw

Вывод флажка

### <a name="prototype"></a>Прототип

```C
VOID gx_checkbox_draw(GX_CHECKBOX *checkbox);
```

### <a name="description"></a>Описание

Эта служба рисует указанный флажок. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых флажков.

### <a name="parameters"></a>Параметры

- **checkbox**: указатель на блок управления флажком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom checkbox draw function. */
VOID custom_checkbox_draw(GX_CHECKBOX *checkbox)
{
    /* Call default checkbox draw. */
    gx_checkbox_draw(checkbox);

    /* Add custom drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_checkbox_create,
- gx_checkbox_event_process,
- gx_checkbox_select

## <a name="gx_checkbox_event_process"></a>gx_checkbox_event_process


Обработка события флажка

### <a name="prototype"></a>Прототип

```C
UINT gx_checkbox_event_process(
    GX_CHECKBOX *checkbox, 
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного флажка. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий флажка.

### <a name="parameters"></a>Параметры

- **checkbox**: указатель на блок управления флажком
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие флажка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic checkbox event processing as part of custom event processing function. */

UINT custom_checkbox_event_process(GX_CHECKBOX *checkbox,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default checkbox
            event processing */
        status = gx_checkbox_event_process(checkbox, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_checkbox_create,
- gx_checkbox_draw,
- gx_checkbox_select

## <a name="gx_checkbox_pixelmap_set"></a>gx_checkbox_pixelmap_set


Задание карты отображения пикселей для флажка

### <a name="prototype"></a>Прототип

```C
UINT gx_checkbox_pixelmap_set(
    GX_CHECKBOX *checkbox,
    GX_RESOURCE_ID unchecked_id, 
    GX_RESOURCE_ID checked_id,
    GX_RESOURCE_ID unchecked_disabled_id, 
    GX_RESOURCE_ID checked_disabled_id);
```

### <a name="description"></a>Описание

Эта служба назначает карты отображения пикселей для отображения указанным флажком для каждого состояния флажка. Идентификаторы ресурсов могут дублироваться.

### <a name="parameters"></a>Параметры

- **checkbox**: указатель на блок управления флажком
- **unchecked_id**: карта отображения пикселей, используемая для состояния, когда флажок снят
- **checked_id**: карта отображения пикселей, используемая для состояния, когда флажок установлен
- **unchecked_disabled_id**: карта отображения пикселей, используемая для состояния, когда флажок отключен и снят
- **checked_disabled_id**: карта отображения пикселей, используемая для состояния, когда флажок отключен и установлен

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — флажок успешно установлен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Select “my_checkbox”. */
status = gx_checkbox_pixelmap_set(&my_checkbox,
    PIXELMAP_UNCHECKED_ID,
    PIXELMAP_CHECKED_ID, PIXELMAP_UNCHECKED_DISABLED_ID,
    PIXELMAP_CHECKED_SIABLED_ID));

/* If status is GX_SUCCESS the pixelmaps are assigned to the checkbox “my_checkbox” */
```

### <a name="see-also"></a>См. также:

- gx_checkbox_create,
- gx_checkbox_draw,
- gx_checkbox_event_process

## <a name="gx_checkbox_select"></a>gx_checkbox_select


Выбор флажка

### <a name="prototype"></a>Прототип

```C
UINT gx_checkbox_select(GX_CHECKBOX *checkbox);
```

### <a name="description"></a>Описание

Эта служба принудительно переводит флажок в выбранное состояние.

### <a name="parameters"></a>Параметры

- **checkbox**: указатель на блок управления флажком

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — флажок успешно установлен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Select “my_checkbox”. */
status = gx_checkbox_select(&my_checkbox);

/* If status is GX_SUCCESS the checkbox “my_checkbox” has been toggled. */
```

### <a name="see-also"></a>См. также:

- gx_checkbox_create,
- gx_checkbox_draw,
- gx_checkbox_event_process

## <a name="gx_circular_gauge_angle_get"></a>gx_circular_gauge_angle_get


Получение текущего угла

### <a name="prototype"></a>Прототип

```C
UINT gx_circular_gauge_angle_get(
    GX_CIRCULAR_GAUGE *gauge, 
    INT *angle);
```

### <a name="description"></a>Описание

Эта служба получает текущий угол стрелки для мини-приложения кругового датчика.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления круговым датчиком
- **angle**: текущий угол стрелки для получения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — угол стрелки кругового датчика успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
INT current_angle;

/* Retrieve the current needle angle of “my_gauge”. */
status = gx_circular_gauge_angle_get(&my_gauge, &current_angle);

/* If status is GX_SUCCESS the current needle angle of “my_gauge” has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_set,
- gx_circular_gauge_animation_set
- gx_circular_gauge_background_draw,
- gx_circular_gauge_create
- gx_circular_gauge_draw,
- gx_circular_gauge_event_process

## <a name="gx_circular_gauge_angle_set"></a>gx_circular_gauge_angle_set


Задание целевого угла

### <a name="prototype"></a>Прототип

```C
UINT gx_circular_gauge_angle_set(
    GX_CIRCULAR_GAUGE *gauge,
    INT angle);
```

### <a name="description"></a>Описание

Эта служба задает целевой угол для мини-приложения кругового датчика.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления круговым датчиком
- **angle**: целевой угол стрелки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — угол успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set target angle of “my_gauge” to 180. */
status = gx_circular_gauge_angle_set(&my_gauge, 180);

/* If status is GX_SUCCESS the circular gauge of “my_gauge” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_get,
- gx_circular_gauge_animation_set
- gx_circular_gauge_background_draw,
- gx_circular_gauge_create
- gx_circular_gauge_draw,
- gx_circular_gauge_event_process

## <a name="gx_circular_gauge_animation_set"></a>gx_circular_gauge_animation_set


Задание параметров анимации

### <a name="prototype"></a>Прототип

```C
UINT gx_circular_gauge_animation_set(
    GX_CIRCULAR_GAUGE *gauge,
    INT steps, 
    INT delay);
```

### <a name="description"></a>Описание

Эта служба задает шаги анимации и время задержки для мини-приложения кругового датчика.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления круговым датчиком
- **steps**: общее количество шагов для одного вращения
- **delay**: время задержки для каждого шага

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — флажок успешно установлен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set animation steps and delay time of circular gauge “my_gauge”
    to 30 and 1, the needle of “my_gauge” will rotate from
    current angle to target angle by 30 steps with 1 tick delay between every step. */
status = gx_circular_gauge_animation_set(&my_gauge, 30, 1);

/* If status is GX_SUCCESS the steps and delay time of “my_gauge” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_get,
- gx_circular_gauge_angle_set
- gx_circular_gauge_background_draw,
- gx_circular_gauge_create
- gx_circular_gauge_draw,
- gx_circular_gauge_event_process

## <a name="gx_circular_gauge_background_draw"></a>gx_circular_gauge_background_draw


Вывод фона кругового датчика

### <a name="prototype"></a>Прототип

```C
VOID gx_circular_gauge_background_draw(GX_CIRCULAR_GAUGE *gauge);
```

### <a name="description"></a>Описание

Эта служба рисует фон указанного кругового датчика. Эта служба обычно вызывается внутри функции gx_circular_gauge_draw, но предоставляется приложению для помощи в написании пользовательских функций рисования.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления круговым датчиком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Draw circular gauge background. */
gx_circular_gauge_background_draw(&my_circular_gauge);
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_get,
- gx_circular_gauge_angle_set
- gx_circular_gauge_animation_set,
- gx_circular_gauge_create
- gx_circular_gauge_draw,
- gx_circular_gauge_event_process

## <a name="gx_circular_gauge_create"></a>gx_circular_gauge_create


Создание кругового датчика

### <a name="prototype"></a>Прототип

```C
UINT gx_circular_gauge_create(
    GX_CIRCULAR_GAUGE *gauge,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_CIRCULAR_GAUGE_INFO *info,
    GX_RESOURCE_ID background_id,
    ULONG style,
    USHORT circular_gauge_id,
    GX_VALUE xpos,
    GX_VALUE ypos);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение кругового датчика с указанными свойствами.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления круговым датчиком
- **name**: логическое имя мини-приложения кругового датчика
- **parent**: указатель на родительское мини-приложение
- **info**: указатель на информационную структуру датчика Определение структуры GX_CIRCULAR_GAUGE_INFO см. в **приложении I**.
- **background_id**: ИД ресурса карты отображения пикселей фона кругового датчика
- **style**: стиль кругового датчика Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **circular_gauge_id**: определяемый приложением идентификатор кругового датчика
- **xpos**: координата положения датчика по оси X
- **ypos**: координата положения датчика по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — флажок успешно установлен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CIRCULAR_GAUGE gauge_info;

gauge_info.gx_circular_gauge_info_animation_steps = 30;
gauge_info.gx_circular_gauge_info_animation_delay = 1;
gauge_info.gx_circular_gauge_info_needle_xpos = 140;
gauge_info.gx_circular_gauge_info_needle_ypos = 140;
gauge_info.gx_circular_gauge_info_neelde_xcor = 20;
gauge_info.gx_circular_gauge_info_neelde_ycor = 88;
gauge_info.gx_circular_gauge_info_needle_pixelmap = GX_PIXELMAP_ID_NEEDLE;

/* Create “my_gauge”. */
status = gx_circular_gauge_create(&my_gauge, “my_gauge”,
            &my_parent,
            &gauge_info, MY_PIXELMAP_RESOURCE_ID, GX_NULL,
            MY_ICON_ID, 5, 30);

/* If status is GX_SUCCESS the circular gauge “my_gauge” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_get,
- gx_circular_gauge_angle_set
- gx_circular_gauge_animation_set
- gx_circular_gauge_background_draw,
- gx_circular_gauge_draw
- gx_circular_gauge_event_process

## <a name="gx_circular_gauge_draw"></a>gx_circular_gauge_draw

Вывод кругового датчика

### <a name="prototype"></a>Прототип

```C
VOID gx_circular_gauge_draw(GX_CIRCULAR_GAUGE *gauge);
```

### <a name="description"></a>Описание

Эта служба рисует указанный круговой датчик. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых датчиков.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления круговым датчиком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom circular gauge draw function. */
VOID custom_gauge_draw(GX_CIRCULAR_GAUGE *gauge)
{
    /* Call default circular gauge draw. */
    gx_circular_gauge_draw(gauge);

    /* Add custom drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_get,
- gx_circular_gauge_angle_set
- gx_circular_gauge_animation_set
- gx_circular_gauge_background_draw
- gx_circular_gauge_create,
- gx_circular_gauge_event_process

## <a name="gx_circular_gauge_event_process"></a>gx_circular_gauge_event_process


Обработка события кругового датчика

### <a name="prototype"></a>Прототип

```C
UINT gx_circular_gauge_event_process(
    GX_CIRCULAR_GAUGE *gauge,
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного кругового датчика.

### <a name="parameters"></a>Параметры

- **gauge**: указатель на блок управления датчиком
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие датчика успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic circular gauge event processing as part of custom event processing function. */
UINT custom_gauge_event_process(GX_CIRCULAR_GAUGE *gauge,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default circular gauge
             event processing */
        status = gx_circular_gauge_event_process(gauge, event);
        break;
}
return status;
```

### <a name="see-also"></a>См. также:

- gx_circular_gauge_angle_get,
- gx_circular_gauge_angle_set
- gx_circular_gauge_animation_set
- gx_circular_gauge_background_draw,
- gx_circular_gauge_create
- gx_circular_gauge_draw

## <a name="gx_context_brush_default"></a>gx_context_brush_default


Задание кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_default(GX_DRAW_CONTEXT *context);
```

### <a name="description"></a>Описание

Эта служба задает для кисти указанного контекста рисования значение по умолчанию.

### <a name="parameters"></a>Параметры

- **context**: указатель на блок управления контекстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель контекста.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the brush of “my_context” to default. */
status = gx_context_brush_default(&my_context);

/* If status is GX_SUCCESS the brush of “my_context” has been set to default. */

```

### <a name="see-also"></a>См. также:

- gx_context_brush_define,
- gx_context_brush_get
- gx_context_brush_set,
- gx_context_brush_style_set
- gx_context_brush_pattern_set,
- gx_context_brush_width_set
- gx_context_fill_color_set,
- gx_context_font_set
- gx_context_line_color_set,
- gx_context_pixelmap_set
- gx_context_raw_brush_define,
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_brush_define"></a>gx_context_brush_define


Определение кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_define(
    GX_RESOURCE_ID line_color_id,
    GX_RESOURCE_ID fill_color_id, 
    UINT style);
```

### <a name="description"></a>Описание

Эта служба определяет кисть текущего контекста рисования.

### <a name="parameters"></a>Параметры

- **line_color_id**: идентификатор ресурса для цвета линии В **приложении B** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **fill_color_id**: идентификатор ресурса цвета заливки В **приложении B** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **style**: стиль кисти В **приложении D** описаны поддерживаемые стили кисти. Стили кисти можно объединить в одну переменную с помощью побитовой операции OR.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кисть контекста успешно определена.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define the brush of the current context. */
status = gx_context_brush_define(GX_COLOR_BLACK_ID,
    GX_COLOR_BLACK_ID,
    GX_STYLE_BORDER_NONE);

/* If status is GX_SUCCESS the brush of the current context has been defined. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default,
- gx_context_brush_get
- gx_context_brush_set,
- gx_context_brush_pattern_set
- gx_context_brush_style_set,
- gx_context_brush_width_set
- gx_context_fill_color_set,
- gx_context_font_set
- gx_context_line_color_set,
- gx_context_pixelmap_set
- gx_context_raw_brush_define,
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_brush_get"></a>gx_context_brush_get


Получение кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_get(GX_BRUSH **return_brush);
```

### <a name="description"></a>Описание

Эта служба возвращает указатель на активную кисть в текущем контексте рисования. Если активный контекст рисования отсутствует, происходит сбой службы и возвращается указатель NULL.

### <a name="parameters"></a>Параметры

- **return_brush**: указатель на назначение для кисти

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кисть для контекста успешно получена.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BRUSH *my_brush;

/* Get the brush of the current context. */
status = gx_context_brush_get(&my_brush);

/* If status is GX_SUCCESS the brush of the current context has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_set,
- gx_context_brush_style_set
- gx_context_brush_default,
- gx_context_brush_define
- gx_context_brush_pattern_set,
- gx_context_brush_width_set
- gx_context_fill_color_set,
- gx_context_font_set
- gx_context_line_color_set,
- gx_context_pixelmap_set
- gx_context_raw_brush_define,
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_brush_pattern_set"></a>gx_context_brush_pattern_set


Задание узора кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_pattern_set(ULONG pattern);
```

### <a name="description"></a>Описание

Эта служба задает узор кисти текущего контекста рисования.

Узор кисти используется для рисования пунктирных горизонтальных и вертикальных линий. При вызове gx_canvas_line_draw(), если линия горизонтальная или вертикальная, а значение в поле brush.gx_brush_line_pattern не равно нулю, рисуется узорная линия.

Маска узора кисти в настоящее время поддерживается только для горизонтальных и вертикальных линий.

### <a name="parameters"></a>Параметры

- **pattern**: узор, используемый для кисти Это простой узор шестнадцатеричного включения или отключения, используемый для рисования узорной линии.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кисть контекста успешно задана.
- **GX_INVALID_CONTEXT** (0x06) — недопустимый контекст рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the brush pattern for the current contex. */
status = gx_context_brush_pattern_set(0x80808080);

/* If status is GX_SUCCESS the brush pattern of the current context
has been set to the specified pattern. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default,
- gx_context_brush_define
- gx_context_brush_get,
- gx_context_brush_style_set
- gx_context_brush_width_set,
- gx_context_fill_color_set
- gx_context_font_set,
- gx_context_line_color_set
- gx_context_pixelmap_set,
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set,
- gx_context_raw_line_color_set

## <a name="gx_context_brush_set"></a>gx_context_brush_set


Задание кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_set(GX_BRUSH *brush);
```

### <a name="description"></a>Описание

Эта служба задает кисть текущего контекста рисования.

### <a name="parameters"></a>Параметры

- **brush**: указатель на кисть, используемую для текущего контекста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кисть контекста успешно задана.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BRSUH my_brush;
GX_FONT *font;
GX_COLOR fill_color;
GX_COLOR line_color;

/* Retrieve the font that associated with the specified font ID. */
gx_context_font_get(MY_FONT_ID, &font);

/* Retrieve the color that associated with the specified color ID. */
gx_context_color_get(MY_FILL_COLOR_ID, &fill_color);
gx_context_color_get(MY_LINE_COLOR, &line_color);

my_brush.gx_brush_pixelmap = MY_PIXELMAP_ID;
my_brush.gx_brush_font = font;
my_brush.gx_brush_line_pattern = 0x80808080;
my_brush.gx_brush_pattern_mask = 0x80000000;
my_brush.gx_brush_fill_color = fill_color;
my_brush.gx_brush_line_color = line_color;
my_brush.gx_brush_style = GX_BRSUH_SOLID_FILL | GX_BRUSH_ALIAS |
                          GX_BRUSH_PIXELMAP_FILL | GX_BRUSH_ROUND
my_brush.gx_brush_width = 2;
my_brush.gx_brush_alpha = 255;

/* Set the brush of the current context. */
status = gx_context_brush_set(my_brush);

/* If status is GX_SUCCESS the brush of the current context has been set. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default,
- gx_context_brush_define
- gx_context_brush_get,
- gx_context_brush_pattern_set
- gx_context_brush_style_set,
- gx_context_brush_width_set
- gx_context_fill_color_set,
- gx_context_font_set
- gx_context_line_color_set,
- gx_context_pixelmap_set
- gx_context_raw_brush_define,
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_brush_style_set"></a>gx_context_brush_style_set


Задание стиля кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_style_set(UINT style);
```

### <a name="description"></a>Описание

Эта служба задает стиль кисти текущего контекста рисования.

### <a name="parameters"></a>Параметры

- **style**: стиль кисти текущего контекста Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль кисти контекста успешно задан.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the brush style of the current context. */
status = gx_context_brush_style_set(GX_BRUSH_ALIAS);

/* If status is GX_SUCCESS the brush style of the current context has been set. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default,
- gx_context_brush_define
- gx_context_brush_get,
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_width_set,
- gx_context_fill_color_set
- gx_context_font_set,
- gx_context_line_color_set
- gx_context_pixelmap_set,
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set,
- gx_context_raw_line_color_set

## <a name="gx_context_brush_width_set"></a>gx_context_brush_width_set


Задание ширины кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_brush_width_set(UINT width);
```

### <a name="description"></a>Описание

Эта служба задает ширину активной кисти в текущем контексте рисования.

### <a name="parameters"></a>Параметры

**width**: ширина кисти текущего контекста (в пикселях)

### <a name="return-values"></a>Возвращаемые значения

**GX_SUCCESS** (0x00) — ширина кисти контекста успешно задана.

**GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the brush width of the current context to 10 pixels. */
status = gx_context_brush_width_set(10); /*

If status is GX_SUCCESS the brush width of the current context has been set to 10. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_color_get"></a>gx_context_color_get


Получение значения цвета, связанного с идентификатором цвета в текущем контексте рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_color_get(
    GX_RESOURCE_ID color_id,
    GX_COLOR *return_color);
```

### <a name="description"></a>Описание

Эта служба получает значение цвета, связанное с указанным идентификатором цвета. Значение цвета возвращается в формате цвета активного контекста. Эту службу следует вызывать только внутри активной операции рисования.

### <a name="parameters"></a>Параметры

**color_id**: идентификатор запрошенного цвета

**return_color**: адрес переменной для хранения возвращенного значения цвета

### <a name="return-values"></a>Возвращаемые значения

**GX_SUCCESS** (0x00) — значение цвета успешно получено.

**GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.

**GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

**GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_COLOR color_value;

/* Get the color vaue. */
status = gx_context_color_get(MY_BLACK_COLOR_ID, &color_value);
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_fill_color_set"></a>gx_context_fill_color_set

Задание цвета заливки текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_fill_color_set(GX_RESOURCE_ID fill_color_id);
```

### <a name="description"></a>Описание

Эта служба задает цвет заливки активной кисти в текущем контексте рисования.

### <a name="parameters"></a>Параметры

- **fill_color_id**: идентификатор ресурса цвета заливки текущего контекста В **приложении B** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет контекста успешно задан.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the fill color of the current context to black. */
status = gx_context_fill_color_set(MY_BLACK_COLOR_ID);

/* If status is GX_SUCCESS the fill color of the current context has been set to black. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_font_get"></a>gx_context_font_get

Получение шрифта, связанного с идентификатором шрифта в текущем контексте рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_font_get(
    GX_RESOURCE_ID font_id,
    GX_FONT **return_font);
```

### <a name="description"></a>Описание

Эта служба получает указатель на шрифт, связанный с указанным идентификатором шрифта. Эту службу следует вызывать только внутри активной операции рисования.

### <a name="parameters"></a>Параметры

- **font_id**: идентификатор ресурса запрошенного шрифта
- **return_font**: адрес переменной для хранения возвращенного указателя шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт успешно получен.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_FONT *my_font;

/* Get the font pointer. */
status = gx_context_font_get(MY_MIDSIZE_FONT, &my_font);

/* If status is GX_SUCCESS, the font that indicated with MY_MIDSIZE_FONT has been successfully retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_font_set"></a>gx_context_font_set

Задание шрифта текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_font_set(GX_RESOURCE_ID font_id);
```

### <a name="description"></a>Описание

Эта служба задает шрифт в активной кисти текущего контекста рисования.

### <a name="parameters"></a>Параметры

- **font_id**: идентификатор ресурса шрифта текущего контекста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт контекста успешно задан.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the font of the current context to MY_FONT_ID. */
status = gx_context_font_set(MY_FONT_ID);

/* If status is GX_SUCCESS the font of the current context has been set. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_line_color_set"></a>gx_context_line_color_set

Задание цвета линии текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_line_color_set(GX_RESOURCE_ID line_color_id);
```

### <a name="description"></a>Описание

Эта служба задает цвет линии активной кисти в текущем контексте рисования.

### <a name="parameters"></a>Параметры

- **line_color_id**: цвет линии текущего контекста В **приложении B** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — контекстный цвет линии успешно задан.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the line color of the current context to black. */
status = gx_context_line_color_set(GX_COLOR_BLACK_ID);

/* If status is GX_SUCCESS the line color of the current context has been set to black. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_pixelmap_get"></a>gx_context_pixelmap_get

Получение карты отображения пикселей, связанной с идентификатором карты отображения пикселей в текущем контексте рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_pixelmap_get(
    GX_RESOURCE_ID pixelmap_id,
    GX_PIXELMAP **return_map);
```

### <a name="description"></a>Описание

Эта служба получает указатель карты отображения пикселей, связанной с указанным идентификатором карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **pixelmap_id**: запрашиваемый идентификатор ресурса карты отображения пикселей
- **return_map**: адрес переменной для хранения возвращенного адреса карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно получена.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель карты отображения пикселей.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PIXELMAP *map;

/* Get the pixelmap pointer. */
status = gx_context_pixelmap_get(MY_PIXELMAP_ID, &map);

/* If status is GX_SUCCESS, the pixelmap was successfully retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_pixelmap_set"></a>gx_context_pixelmap_set

Задание карты отображения пикселей текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_pixelmap_set(GX_RESOURCE_ID pixelmap_id);
```

### <a name="description"></a>Описание

Эта служба назначает карту отображения пикселей активной кисти в текущем контексте рисования.

### <a name="parameters"></a>Параметры

- **pixelmap_id**: идентификатор ресурса карты отображения пикселей для использования в текущем контексте

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно задана.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVLAID_CONTEXT** (0x06) — недопустимый контекст.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set pixelmap of the current context to MY_PIXELMAP_ID. */
status = gx_context_pixelmap_set(MY_PIXELMAP_ID);

/* If status is GX_SUCCESS the pixelmap of the current context has been set. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_raw_brush_define"></a>gx_context_raw_brush_define

Определение необработанной кисти текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_raw_brush_define(
    GX_COLOR line_color,
    GX_COLOR fill_color, 
    UINT style);
```

### <a name="description"></a>Описание

Эта служба определяет необработанную кисть текущего контекста экрана. Необработанные определения используются, когда в кисть вместо идентификаторов цвета передаются 32-разрядные значения цвета ARGB. Определения необработанных цветов полезны в том случае, если нужный цвет отсутствует в текущей таблице системных цветов или если значение цвета RGB вычисляется во время выполнения.

### <a name="parameters"></a>Параметры

- **line_color**: цвет линии в формате 32-разрядного необработанного значения цвета ARGB В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **fill_color**: цвет заливки в формате 32-разрядного необработанного значения цвета ARGB В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **style**: стиль кисти Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — необработанная кисть контекста успешно определена.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define the raw brush of the current context. */
status = gx_context_raw_brush_define(GX_COLOR_BLACK,
    GX_COLOR_BLACK, GX_STYLE_BORDER_NONE);

/* If status is GX_SUCCESS the raw brush of the current context has been defined. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_fill_color_set
- gx_context_raw_line_color_set

## <a name="gx_context_raw_fill_color_set"></a>gx_context_raw_fill_color_set

Задание необработанного цвета заливки текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_raw_fill_color_set(GX_COLOR line_color);
```

### <a name="description"></a>Описание

Эта служба задает необработанный цвет заливки для контекста текущего экрана. Параметр line_color — это 32-разрядное значение необработанного цвета в формате ARGB, а не значение идентификатора цвета. Определения необработанных цветов полезны в том случае, если нужный цвет отсутствует в текущей таблице системных цветов или если значение цвета RGB вычисляется во время выполнения.

### <a name="parameters"></a>Параметры

- **line_color**: цвет линии В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — необработанный цвет заливки контекста успешно задан.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the raw fill color of the current context. */
status = gx_context_raw_fill_color_set(GX_COLOR_BLACK);

/* If status is GX_SUCCESS the raw fill color of the current context has been set. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_line_color_set

## <a name="gx_context_raw_line_color_set"></a>gx_context_raw_line_color_set

Задание необработанного цвета линии текущего контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_context_raw_line_color_set(GX_COLOR line_color);
```

### <a name="description"></a>Описание

Эта служба задает цвет линии активной кисти в текущем контексте рисования. Параметр line_color — это 32-разрядное значение необработанного цвета в формате ARGB, а не значение идентификатора цвета. Определения необработанных цветов полезны в том случае, если нужный цвет отсутствует в текущей таблице системных цветов или если значение цвета RGB вычисляется во время выполнения.

### <a name="parameters"></a>Параметры

- **line_color**: цвет значения линии В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — необработанный цвет линии контекста успешно задан.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

```C
/* Set the raw line color of the current context. */
status = gx_context_raw_line_color_set(GX_COLOR_BLACK);

/* If status is GX_SUCCESS the raw line color of the current context has been set. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set

## <a name="gx_context_string_get"></a>gx_context_string_get

Получение строки, связанной с идентификатором строки (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_context_string_get(GX_RESOURCE_ID string_id,
    GX_CONST GX_CHAR **return_string);
```

### <a name="description"></a>Описание

Этот устаревший API возвращает строку, связанную с заданным идентификатором строки. Новые приложения должны использовать gx_context_string_get_ext().

### <a name="parameters"></a>Параметры

- **string_id**: идентификатор строки, созданный приложением GUIX Studio
- **return_string**: адрес переменной для возврата указателя строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — необработанный цвет линии контекста успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *text;

status = gx_context_string_get(GX_ID_ERROR, &text);

/* If status is GX_SUCCESS the string pointer has been returned. */
```

### <a name="see-also"></a>См. также:

- gx_context_string_get_ext

## <a name="gx_context_string_get_ext"></a>gx_context_string_get_ext

Получение строки, связанной с указанным идентификатором строки

### <a name="prototype"></a>Прототип

```C
UINT gx_context_string_get_ext(
    GX_RESOURCE_ID string_id, 
    GX_STRING *return_string);
```

### <a name="description"></a>Описание

Эта служба возвращает строку, связанную с указанным идентификатором строки. Эта служба может вызываться только при наличии активного контекста рисования, т. е. из функции рисования мини-приложения. Эта служба определяет активные холст и дисплей, а также отображает и получает строку из найденного экземпляра дисплея.

### <a name="parameters"></a>Параметры

- **string_id**: идентификатор строки, используемый для идентификации строки, который создан GUIX Studio в файле заголовка ресурса приложения
- **return_string**: адрес переменной GX_STRING, в которую будут возвращены указатель строки и длина строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — необработанный цвет линии контекста успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_CONTEXT** (0x06) — нет активного контекста рисования.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример


```C
GX_STRING string;

/* Set the raw line color of the current context. */
status = gx_context_string_get_ext(ID_ERROR, &string);

/* If status is GX_SUCCESS the string.gx_string_ptr and
string.gx_string_length values have been returned. */
```

### <a name="see-also"></a>См. также:

- gx_context_brush_default
- gx_context_brush_define
- gx_context_brush_get
- gx_context_brush_set
- gx_context_brush_pattern_set
- gx_context_brush_style_set
- gx_context_brush_width_set
- gx_context_fill_color_set
- gx_context_font_set
- gx_context_line_color_set
- gx_context_pixelmap_set
- gx_context_raw_brush_define
- gx_context_raw_fill_color_set

## <a name="gx_display_active_language_set"></a>gx_display_active_language_set

Назначение активного языка для отображения

### <a name="prototype"></a>Прототип

```C
UINT gx_display_active_language_set(
    GX_DISPLAY *display, 
    GX_UBYTE language);
```

### <a name="description"></a>Описание

Эта служба назначает текущий активный язык для указанного дисплея. Индекс языков соответствует языкам, определенным в таблице языка интерфейса, и не является идентификатором языка ANSI.

Различные дисплеи в системе с несколькими дисплеями могут использовать разные активные языки. Перед использованием этого API следует назначить таблицу языков интерфейса. Когда дисплей инициализируется с помощью gx_studio_display_configure, автоматически устанавливается таблица языков и приложение передает индекс активного языка.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **language**: индекс активного языка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — язык успешно назначен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на отображение.
- **GX_INVALID_VALUE** (0x22) — недопустимый индекс языка.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Change value of color MY_COLOR_ID. */
status = gx_display_active_language_set(&my_display,
    LANGUAGE_ENGLISH);

/* If status is GX_SUCCESS the active language has been assigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_language_table_set
- gx_studio_display_configure

## <a name="gx_display_color_set"></a>gx_display_color_set

Повторное назначение значения одного цвета

### <a name="prototype"></a>Прототип

```C
UINT gx_display_color_set(
    GX_DISPLAY *display,
    GX_RESOURCE_ID color_id, 
    GX_COLOR new_color);
```

### <a name="description"></a>Описание

Эта служба переназначает значение цвета, связанное с указанным идентификатором цвета. Ее можно использовать для изменения таблицы цветов для дисплея без предоставления полностью новой таблицы цветов. Указанное значение цвета должно иметь собственный формат, поддерживаемый дисплеем.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **color_id**: идентификатор цвета для повторного назначения
- **new_color**: значение цвета, присваиваемое этому слоту color_id

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет успешно переназначен.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый ИД цвета.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_DISPLAY** (0x1D) — недопустимый дисплей.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Change value of color MY_COLOR_ID. */
status = gx_display_color_set(&my_display, MY_COLOR_ID, 0x5454);

/* If status is GX_SUCCESS the color has been reassigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_table_set

## <a name="gx_display_color_table_set"></a>gx_display_color_table_set

Назначение таблицы цветов для дисплея

### <a name="prototype"></a>Прототип

```C
UINT gx_display_color_table_set(
    GX_DISPLAY *display,
    GX_COLOR *color_table, 
    INT color_count);
```

### <a name="description"></a>Описание

Эта служба переназначает таблицу цветов, которая будет использоваться дисплеем. Эта служба обычно вызывается функцией конфигурации дисплея, созданной GUIX Studio, но также может вызываться программным обеспечением приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **color_table**: массив значений цветов в собственном формате дисплея
- **color_count** обозначает количество записей в таблице цветов

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица цветов успешно задана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_DISPLAY** (0x1D) — недопустимый дисплей.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_COLOR default_table[32] = { … };

/* Change the color table */
status = gx_display_color_table_set(&my_display, default_table, 32);

/* If status is GX_SUCCESS the color table has been reassigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_set

## <a name="gx_display_create"></a>gx_display_create

Создание дисплея

### <a name="prototype"></a>Прототип

```C
UINT gx_display_create(
    GX_DISPLAY *display, 
    GX_CONST CHAR *name,
    UINT (*display_driver_setup)(GX_DISPLAY *),
    GX_VALUE width, 
    GX_VALUE height);
```

### <a name="description"></a>Описание

Эта служба создает дисплей и вызывает функцию установки драйвера дисплея. GUIX принимает этот дисплей и добавляет его во внутренний список дисплеев.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **name**: имя дисплея
- **display_driver_setup**: указатель на функцию установки драйвера дисплея
- **optional_driver_info**: указатель на дополнительные сведения о драйвере
- **color_format**: формат цвета, как определено в **приложении C**
- **width**: количество пикселей по оси X
- **height**: количество пикселей по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — отображение успешно создано.
- **GX_SYSTEM_ERROR** (0xFE) — не удалось настроить отображение.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_SIZE** (0x23) — недопустимый размер блока управления отображением.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_DISPLAY my_display;

UINT my_driver_setup_callback(GX_DISPLAY *display)
{
    …
}

/* Create screen “my_display”. */
status = gx_display_create(&my_display, “my display”,
    my_driver_setup_callback, 320, 480);

/* If status is GX_SUCCESS, the screen “my_display” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_display_delete

## <a name="gx_display_delete"></a>gx_display_delete


Уничтожение отображения

### <a name="prototype"></a>Прототип

```C
UINT gx_display_delete(
    GX_DISPLAY *display,
    VOID (*display_driver_cleanup)(GX_DISPLAY *));
```

### <a name="description"></a>Описание

Эта служба завершает отображение и очищает выделенные ресурсы.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **display_driver_cleanup**: указатель на функцию очистки драйвера дисплея

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — отображение успешно удалено.
- **GX_FAILURE** (0x10) — созданный список дисплеев имеет значение NULL.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
VOID driver_cleanup_callback(GX_DISPLAY *display)
{
    …
}

/* Delete a display “my_display”. */
status = gx_display_delete(&my_display, driver_cleanup_callback);

/* If status is GX_SUCCESS the screen “my_display” has been destroyed. */
```

### <a name="see-also"></a>См. также:

- gx_canvas_block_move
- gx_canvas_line_draw
- gx_canvas_pixelmap_draw
- gx_canvas_pixelmap_tile
- gx_canvas_polygon_draw
- gx_canvas_rectangle_draw
- gx_canvas_text_draw
- gx_display_create

## <a name="gx_display_font_table_set"></a>gx_display_font_table_set

Назначение таблицы шрифтов для отображения

### <a name="prototype"></a>Прототип

```C
UINT gx_display_font_table_set(
    GX_DISPLAY *display,
    GX_FONT **font_table, 
    INT table_size);
```

### <a name="description"></a>Описание

Эта служба переназначает таблицу шрифтов, которая будет использоваться дисплеем. Эта служба обычно вызывается функцией конфигурации дисплея, созданной GUIX Studio, но также может вызываться программным обеспечением приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **font_table**: массив указателей GX_FONT
- **table_size**: количество шрифтов в таблице

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_FONT *font_table[32] = { … };

/* Assign font table */
status = gx_display_font_table_set(&my_display, font_table, 32);

/* If status is GX_SUCCESS, the font table has been reassigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_set
- gx_display_color_table_set
- gx_display_pixelmap_table_set

## <a name="gx_display_language_table_get"></a>gx_display_language_table_get

Получение таблицы языков интерфейса (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_display_language_table_get(
    GX_DISPLAY *display,
    GX_CHAR **table, 
    GX_UBYTE *language_count,
    UINT *string_table_size);
```

### <a name="description"></a>Описание

Эта служба получает таблицу языков из указанного интерфейса. Эта служба может использоваться приложением для изменения таблицы языков интерфейса в среде выполнения с использованием динамически определяемых строк.

Этот API является устаревшим и поддерживается только для приложений, использующих старую таблицу языков (т. е. файл ресурсов, созданный Studio для библиотеки, версия которой предшествует версии 5.6). Новые приложения должны использовать gx_display_language_table_get_ext().

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **table**: адрес для получения указателя таблицы
- **language_count**: адрес для получения количества языков
- **string_table_size**: адрес для получения размера таблицы строк

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR **language_table;
GX_UBYTE language_count;
UINT string_count;

/* Retrieve language table */
status = gx_display_language_table_get(&my_display,
        &language_table, &language_count, &string_count);

/* If status is GX_SUCCESS, the language table has been retrieved.
```

### <a name="see-also"></a>См. также:

- gx_display_language_table_set
- gx_display_active_langauge_set
- gx_display_string_get
- gx_display_language_table_get_ext

## <a name="gx_display_language_table_get_ext"></a>gx_display_language_table_get_ext

Получение таблицы языков интерфейса

### <a name="prototype"></a>Прототип

```C
UINT gx_display_language_table_get_ext(
    GX_DISPLAY *display,
    GX_STRING **table, 
    GX_UBYTE *language_count, 
    UINT *string_table_size);
```

### <a name="description"></a>Описание

Эта служба получает таблицу языков из указанного интерфейса. Эта служба может использоваться приложением для изменения таблицы языков интерфейса в среде выполнения с использованием динамически определяемых строк.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **table**: адрес для получения указателя таблицы
- **language_count**: адрес для получения количества языков
- **string_table_size**: адрес для получения размера таблицы строк

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING **language_table;
GX_UBYTE language_count;
UINT string_count;

/* Retrieve language table */
status = gx_display_language_table_get_ext(&my_display,
    &language_table, &language_count, &string_count);

/* If status is GX_SUCCESS, the language table has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_display_language_table_set_ext
- gx_display_active_langauge_set
- gx_display_string_get_ext

## <a name="gx_display_language_table_set"></a>gx_display_language_table_set

Назначение таблицы языков интерфейса (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_display_language_table_set(
    GX_DISPLAY *display,
    GX_CHAR **table, 
    GX_UBYTE number_of_languages, 
    UINT number_of_strings);
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и новые приложения должны использовать gx_display_language_table_set_ext(). Эта служба поддерживается только для обеспечения совместимости с файлами ресурсов, созданными Studio, которые предназначены для версий библиотеки, предшествующих версии 5.6.

Эта служба назначает таблицу языков, используемую дисплеем. Эта служба обычно вызывается функцией gx_studio_display_configure, созданной GUIX Studio, но также может вызываться программным обеспечением приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **table**: таблица языков
- **number_of_languages**: количество столбцов в указанной таблице
- **number_of_strings**: количество строк в указанной таблице

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR ***language_table= my_app_language_table;

/* Assign language table */
status = gx_display_language_table_set(&my_display, language_table,
5, 232);

/* If status is GX_SUCCESS, the language table has been reassigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_active_language_set
- gx_display_string_get

## <a name="gx_display_language_table_set_ext"></a>gx_display_language_table_set_ext

Назначение таблицы языков интерфейса

### <a name="prototype"></a>Прототип

```C
UINT gx_display_language_table_set_ext(
    GX_DISPLAY *display,
    GX_STRING **table, 
    GX_UBYTE number_of_languages,
    UINT number_of_strings);
```

### <a name="description"></a>Описание

Эта служба назначает таблицу языков, используемую дисплеем. Эта служба обычно вызывается функцией gx_studio_display_configure, созданной GUIX Studio, но также может вызываться программным обеспечением приложения.

Назначение таблицы языков среды выполнения обычно выполняется, когда языки загружаются из двоичного файла ресурсов с помощью gx_binres_language_table_load().

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **table**: таблица языков
- **number_of_languages**: количество столбцов в указанной таблице
- **number_of_strings**: количество строк в указанной таблице

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING **language_table = my_app_language_table;

/* Assign the language table */
status = gx_display_language_table_set_ext(&my_display,
    language_table, 5, 132);

/* If status is GX_SUCCESS, the language table has been reassigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_active_language_set
- gx_display_string_get

## <a name="gx_display_pixelmap_table_set"></a>gx_display_pixelmap_table_set

Назначение таблицы шрифтов для отображения

### <a name="prototype"></a>Прототип

```C
UINT gx_display_pixelmap_table_set(
    GX_DISPLAY *display,
    GX_PIXELMAP **pixelmap_table, 
    INT table_size);
```

### <a name="description"></a>Описание

Эта служба повторно назначает таблицу карты отображения пикселей для использования на дисплее. Эта служба обычно вызывается функцией конфигурации дисплея, созданной GUIX Studio, но также может вызываться программным обеспечением приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **pixelmap_table**: массив указателей GX_PIXELMAP
- **table_size**: количество карт отображения пикселей в таблице

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PIXELMAP *pixelmap_table[32] = { … };

/* Assign pixelmap table */
status = gx_display_pixelmap_table_set(&my_display, pixelmap_table, 32);

/* If status is GX_SUCCESS the pixelmap table has been reassigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_set
- gx_display_color_table_set
- gx_display_font_table_set

## <a name="gx_display_string_get"></a>gx_display_string_get

Получение строки из активной таблицы строк (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_display_string_get(
    GX_DISPLAY *display,
    GX_RESOURCE_ID string_id, 
    GX_CONST GX_CHAR **string);
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_display_string_get_ext().

Эта служба получает строку из активной таблицы строк для указанного дисплея. Для выбора строки из таблицы языков, назначенной для отображения, используется активный язык.

Идентификаторы строк создаются GUIX Studio и находятся в файле заголовков resources.h приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **string_id**: ИД строки, созданный GUIX Studio
- **string**: адрес переменной указателя строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор строки.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на отображение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *string;

/* Retrieve string value */
status = gx_display_string_get(&my_display,
    GX_STRING_ID_MONDAY, &string);

/* If status is GX_SUCCESS, the string has been retrieved. */
```


### <a name="see-also"></a>См. также:

- gx_display_active_language_set
- gx_display_language_table_set

## <a name="gx_display_string_get_ext"></a>gx_display_string_get_ext

Получение строки из активной таблицы строк

### <a name="prototype"></a>Прототип

```C
UINT gx_display_string_get_ext(
    GX_DISPLAY *display,
    GX_RESOURCE_ID string_id,
    GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба получает строку из активной таблицы строк для указанного дисплея. Для выбора строки из таблицы языков, назначенной для отображения, используется активный язык.

Идентификаторы строк создаются GUIX Studio и находятся в файле заголовков resources.h приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **string_id**: ИД строки, созданный GUIX Studio
- **string**: адрес переменной GX_STRING
- **which**:будут возвращены string.gx_string_ptr и
- **string.gx_string_length**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор строки.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на отображение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING string;

/* Retrieve string value */
status = gx_display_string_get_ext(&my_display,
    GX_STRING_ID_MONDAY, &string);

/* If status is GX_SUCCESS, the string has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_display_active_language_set
- gx_display_language_table_set


## <a name="gx_display_string_table_get"></a>gx_display_string_table_get


Получение активной таблицы строк (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_display_string_table_get(
    GX_DISPLAY *display,
    GX_UBYTE language, 
    GX_CHAR ***table,
    UINT *table_size);
```

### <a name="description"></a>Описание

Эта служба является устаревшей. Вместо нее используется gx_display_string_table_get_ext().

Эта служба извлекает таблицу строк, связанную с активным языком. Эта служба используется нечасто, но предоставляется для полноты приложений, которым может потребоваться внести изменения во время выполнения в таблицу строк.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **language**: столбец таблицы для извлечения
- **table**: адрес переменной для возврата указателя
- **table_size**: адрес переменной для возврата размера таблицы

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_NOT_FOUND** (0x09) — недопустимый индекс языка.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR **string_table;
UINT table_size;

/* Retrieve string table */
status = gx_display_string_table_get(&my_display, LANGUAGE_ENGLISH,
    &string_table, &table_size);

/* If status is GX_SUCCESS, the string table has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_set
- gx_display_color_table_set
- gx_display_pixelmap_table_set

## <a name="gx_display_string_table_get_ext"></a>gx_display_string_table_get_ext


Получение активной таблицы строк

### <a name="prototype"></a>Прототип

```C
UINT gx_display_string_table_get(
    GX_DISPLAY *display,
    GX_UBYTE language, 
    GX_STRING **table, 
    UINT *table_size);
```

### <a name="description"></a>Описание

Эта служба извлекает таблицу строк, связанную с активным языком. Эта служба используется нечасто, но предоставляется для полноты приложений, которым может потребоваться внести изменения во время выполнения в таблицу строк.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **language**: столбец таблицы для извлечения
- **table**: адрес переменной для возврата указателя
- **table_size**: адрес переменной для возврата размера таблицы

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица шрифтов успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_NOT_FOUND** (0x09) — недопустимый индекс языка.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING *string_table;
UINT table_size;

/* Retrieve string table */
status = gx_display_string_table_get_ext(&my_display,
    LANGUAGE_ENGLISH, &string_table, &table_size);

/* If status is GX_SUCCESS, the string table has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_set
- gx_display_color_table_set
- gx_display_pixelmap_table_set

## <a name="gx_display_theme_install"></a>gx_display_theme_install


Установка тем для указанного дисплея

### <a name="prototype"></a>Прототип

```C
UINT gx_display_theme_install(
    GX_DISPLAY *display, 
    GX_THEME *theme_table);
```

### <a name="description"></a>Описание

Эта служба устанавливает темы для указанного дисплея Эта служба обычно вызывается функцией конфигурации дисплея, созданной GUIX Studio, но также может вызываться программным обеспечением приложения.

### <a name="parameters"></a>Параметры

- **display**: указатель на блок управления отображением
- **theme_table**: таблица тем для установки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица тем успешно установлена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на таблицу темы.
- **GX_INVALID_DISPLAY** (0x1D) — недопустимый дисплей.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_THEME theme_1;

&theme_table[1] = {
    &theme_1,
    …
}

/* Define resource tables. */
GX_COLOR color_table[32] = {…};
GX_FONT *font_table[32] = {…};
GX_PIXELMAP *pixelmap_table[32] = { … };

/* Define scroll appearance. */
GX_SCROLLBAR_APPEARANCE scroll_appearance;

memset(&scroll_appearance, 0, sizeof(GX_SCROLLBAR_APPEARANCE));

scroll_appearance.gx_scroll_width = 20;
scroll_appearance.gx_scroll_thumb_width = 18;
scroll_appearance.gx_scroll_thumb_color =
    GX_COLOR_ID_SCROLL_BUTTON;
scroll_appearance.gx_scroll_thumb_border_color =
    GX_COLOR_ID_SCROLL_BUTTON;
scroll_appearance.gx_scroll_button_color =
    GX_COLOR_ID_SCROLL_BUTTON;
scroll_appearance.gx_scroll_thumb_travel_min = 20;
scroll_appearance.gx_scroll_thumb_travel_max = 20;
scroll appearance.gx_scroll_thumb_border_style =
    GX_STYLE_BORDER_THIN;

theme_1.theme_color_table = color_table;
theme_1.theme_font_table = font_table;
theme_1.theme_pixlemap_table = pixelmap_table;
theme_1.theme_palette = GX_NULL;
theme_1.theme_vertical_scrollbar_appearance = scroll_appearance;
theme_1.theme_horizontal_scrollbar_appearance = scroll_appearance;
theme_1.theme_vertical_scroll_style = GX_SCROLLBAR_RELATIVE_THUMB;
theme_1.theme_horizontal_scroll_style =
    GX_SCROLLBAR_RELATIVE_THUMB;
theme_1.theme_color_table_size = 32;
theme_1.theme_font_table_size = 32;
theme_1.theme_pixelmap_table_size = 32;
theme_1.theme_palette_size = 0;

/* Install theme table. */
status = gx_display_theme_install(&my_display, theme_table);

/* If status is GX_SUCCESS the theme table has been installed. */
```

### <a name="see-also"></a>См. также:

- gx_display_color_set
- gx_display_color_table_set
- gx_display_font_table_set

## <a name="gx_drop_list_close"></a>gx_drop_list_close


Закрытие раскрывающего списка

### <a name="prototype"></a>Прототип

```C
UINT gx_drop_list_close(GX_DROP_LIST *drop_list);
```

### <a name="description"></a>Описание

Эта служба закрывает всплывающий список указанного раскрывающегося списка.

### <a name="parameters"></a>Параметры

- **drop_list**: указатель на блок управления раскрывающимся списком

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — раскрывающийся список успешно закрыт.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Close a drop list. */
status = gx_drop_list_close(&drop_list);

/* If status is GX_SUCCESS, the drop list is closed. */
```

### <a name="see-also"></a>См. также:

- gx_drop_list_create
- gx_drop_list_event_process
- gx_drop_list_open

gx_drop_list_pixelmap_set, gx_drop_list_popup_get

## <a name="gx_drop_list_create"></a>gx_drop_list_create


Создание раскрывающегося списка

### <a name="prototype"></a>Прототип

```C
UINT gx_drop_list_create(
    GX_DROP_LIST *drop_list, 
    GX_CONST
    GX_CHAR *name, 
    GX_WIDGET *parent,
    INT total_rows, 
    INT open_height,
    VOID (*callback)(GX_VERTICAL_LIST *, 
    GX_WIDGET *, INT),
    ULONG style, 
    USHORT drop_list_id,
    GX_CONST GX_RECTANGLE *size)
```

### <a name="description"></a>Описание

Эта служба создает раскрывающийся список. Раскрывающийся список представляет собой сочетание мини-приложения раскрывающегося списка и всплывающего вертикального списка, который отображается при открытии раскрывающегося списка. Всплывающий вертикальный список создается автоматически при создании мини-приложения раскрывающегося списка и отображается или скрывается при открытии или закрытии мини-приложения раскрывающегося списка соответственно.

Мини-приложение раскрывающегося списка поддерживает две связанные карты отображения пикселей. Первая, указываемая как"Фон списка" в представлении свойств Studio, является необязательной картой отображения пикселей, отображаемой в качестве фона вертикального списка, который отображается при открытии мини-приложения раскрывающегося списка. Вторая карта отображения пикселей, указываемая как "Фоновое изображение" в представлении свойств Studio, является необязательным изображением, отображаемым в качестве фона самого раскрывающегося списка.

Мини-приложение раскрывающегося списка может иметь (но не обязательно) дочернее мини-приложение, которое используется для открытия и закрытия раскрывающегося списка. Это настраиваемое мини-приложение значка или кнопки, но даже настраиваемое мини-приложение можно использовать в качестве переключателя открытия и закрытия для родительского раскрывающегося списка. Ключевая настройка, которая делает дочернее мини-приложение работоспособным, заключается в том, что у этого дочернего мини-приложения должен быть предварительно определенный идентификатор мини-приложения ID_DROP_LIST_BUTTON.

Чтобы определить дочернее мини-приложение, которое будет открывать и закрывать раскрывающийся список, сначала добавьте в раскрывающийся список мини-приложение и сделайте его дочерним, а затем поместите этот дочерний элемент в раскрывающийся список требуемым образом.

![Снимок экрана: раскрывающийся список](./media/guix/image6.jpg)

Затем в представлении "Свойства" в Studio назначьте этому дочернему мини-приложению значение идентификатора ID_DROP_LIST_BUTTON, которое является внутренним значением идентификатора, распознаваемым родительским раскрывающимся списком.

![Снимок экрана: представление "Свойства" в Studio](./media/guix/image7.jpg)

### <a name="parameters"></a>Параметры
- **drop_list**: указатель на блок управления раскрывающимся списком
- **name**: имя раскрывающегося списка
- **parent**: указатель на родительское мини-приложение
- **total_rows**: общее количество строк в раскрывающемся списке
- **open_height**: высота вертикального списка, отображаемого при открытии раскрывающегося списка
- **callback**: функция, вызываемая вертикальным списком при прокрутке списка Дополнительные сведения см. в документации по GX_VERTICAL_LIST.
- **style**: стиль границы раскрывающегося списка
- **drop_list_id**: определяемый приложением идентификатор раскрывающегося списка
- **size**: размеры раскрывающегося списка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — раскрывающийся список успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

```C
GX_DROP_LIST my_drop_list;
VOID list_row_create(GX_VERTICAL_LIST *list,
                     GX_WIDGET *row, INT index);
{
    …
}

GX_RECTANGLE size;

gx_utility_rectangle_define(&size, 10, 10, 80, 40);

status = gx_drop_list_create(&my_drop_list,
    "my drop list", GX_NULL,
    10, 100, list_row_create,
    GX_STYLE_BORDER_RECESSED | GX_STYLE_ENABLED,
    ID_DROP_LIST, &size)

/* Is status is GX_SUCCESS, the drop list was successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_drop_list_close
- gx_drop_list_event_process
- gx_drop_list_open
- gx_drop_list_pixelmap_set
- gx_drop_list_popup_get

## <a name="gx_drop_list_event_process"></a>gx_drop_list_event_process


Обработка события раскрывающегося списка

### <a name="prototype"></a>Прототип

```C
UINT gx_drop_list_event_process(
    GX_DROP_LIST *list, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для раскрывающегося списка.

### <a name="parameters"></a>Параметры

- **drop_list**: блок управления мини-приложением раскрывающегося списка
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие раскрывающегося списка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic drop list event processing as part of custom event processing function. */

UINT custom_drop_list_event_process(GX_DROP_LIST *drop_list,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;
    default:
        /* Pass all other events to the default drop list
            event processing */
        status = gx_drop_list_event_process(drop_list, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_drop_list_close
- gx_drop_list_create
- gx_drop_list_open
- gx_drop_list_pixelmap_set
- gx_drop_list_popup_get

## <a name="gx_drop_list_open"></a>gx_drop_list_open


Открытие раскрывающегося списка

### <a name="prototype"></a>Прототип

```C
UINT gx_drop_list_open(GX_DROP_LIST *drop_list);
```

### <a name="description"></a>Описание

Эта служба открывает созданный ранее раскрывающийся список.

### <a name="parameters"></a>Параметры

- **drop_list**: указатель на блок управления раскрывающимся списком

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — раскрывающийся список успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_DROP_LIST mylist;

/* Open the popup list of “mylist”. */
status = gx_drop_list_open(&mylist);

/* If status == GX_SUCCESS, the popup list of “mylist” has been displayed. */
```

### <a name="see-also"></a>См. также:

- gx_drop_list_close
- gx_drop_list_create
- gx_drop_list_event_process
- gx_drop_list_pixelmap_set
- gx_drop_list_popup_get

## <a name="gx_drop_list_pixelmap_set"></a>gx_drop_list_pixelmap_set


Назначение фонового изображения для раскрывающегося списка

### <a name="prototype"></a>Прототип

```C
UINT gx_drop_list_pixelmap_set(
    GX_DROP_LIST *drop_list,
    GX_RESOURCE_ID id);
```

### <a name="description"></a>Описание

Назначение фонового изображения для раскрывающегося списка. Эта карта отображения пикселей используется как фон самого мини-приложения раскрывающегося списка, а не для всплывающего вертикального списка, отображаемого при открытии списка. Чтобы назначить карту отображения пикселей всплывающему раскрывающемуся списку, необходимо сначала вызвать gx_drop_list_popup_get, чтобы получить указатель на всплывающий список, а затем использовать gx_window_wallpaper_set() для назначения карты отображения пикселей этому всплывающему списку.

### <a name="parameters"></a>Параметры

- **drop_list**: указатель на блок управления раскрывающимся списком
- **id**: идентификатор ресурса для карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей раскрывающегося списка успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор карты отображения пикселей.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

```C
GX_DROP_LIST mylist;

/* create the drop list here */

/* Assign a pixelmap id, which will turn on the list button */
status = gx_drop_list_pixelmap_set(&mylist,
    GX_PIXELMAP_ID_DROP_LIST_BACKGROND);

/* If status is GX_SUCCESS, the pixelmap was successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_drop_list_close
- gx_drop_list_create
- gx_drop_list_event_process
- gx_drop_list_open
- gx_drop_list_popup_get

## <a name="gx_drop_list_popup_get"></a>gx_drop_list_popup_get


Получение указателя на всплывающий вертикальный список

### <a name="prototype"></a>Прототип

```C
UINT gx_drop_list_popup_get(
    GX_DROP_LIST *drop_list,
    GX_VERTICAL_LIST **return_list);
```

### <a name="description"></a>Описание

Мини-приложение раскрывающегося списка, состоящее из самого мини-приложения раскрывающегося списка и всплывающего вертикального списка, который отображается при открытии мини-приложения раскрывающегося списка. Эта служба получает указатель на компонент вертикального списка в раскрывающемся списке, позволяя приложению вызывать службы API непосредственно в этом вертикальном списке.

### <a name="parameters"></a>Параметры

- **drop_list**: указатель на блок управления раскрывающимся списком
- **return_list**: указатель на вертикальный список, хранящийся в раскрывающемся списке

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — всплывающий вертикальный список успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

```C
GX_DROP_LIST drop_list;
GX_VERTICAL_LIST *vertical_list;

/* Retrieve the popup list of “drop_list”. */
status = gx_drop_list_popup_get(&drop_list, &vertical_list)

/* If status is GX_SUCCESS, the popup list was successfully retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_drop_list_close
- gx_drop_list_create
- gx_drop_list_open
- gx_drop_list_pixelmap_set

## <a name="gx_generic_scroll_wheel_children_position"></a>gx_generic_scroll_wheel_children_position
### <a name="position-children-for-the-generic-scroll-wheel"></a>Размещение дочерних элементов для универсального колеса прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_generic_scroll_wheel_children_position(
    GX_GENERIC_SCROLL_WHEEL *wheel)
```

### <a name="description"></a>Описание

Эта функция размещает дочерние элементы для универсального колеса прокрутки в соответствии с заданной для него высотой строки. Функция вызывается по умолчанию при отображении такого колеса.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колеса прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — дочерние элементы для универсального колеса прокрутки успешно размещены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Position children in the generic scroll wheel. */
status = gx_generic_scroll_wheel_children_position (&wheel);

/* If status is GX_SUCCESS the children in the generic scroll wheel are positioned. */
```

### <a name="see-also"></a>См. также:

- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_generic_scroll_wheel_create
- gx_generic_scroll_wheel_draw
- gx_generic_scroll_wheel_event_process
- gx_generic_scroll_wheel_row_height_set
- gx_generic_scroll_wheel_total_rows_set

## <a name="gx_generic_scroll_wheel_create"></a>gx_generic_scroll_wheel_create


Создание универсального колеса прокрутки.

### <a name="prototype"></a>Прототип

```C
UINT gx_generic_scroll_wheel_create(
    GX_GENERIC_SCROLL_WHEEL *wheel,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    INT total_rows,
    VOID (*callback)(GX_GENERIC_SCROLL_WHEEL *, GX_WIDGET *, INT),
    ULONG style,
    USHORT id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает универсальное мини-приложение колесика прокрутки.

Универсальное колесо прокрутки — это тип мини-приложения колеса прокрутки, которое содержит дочерние мини-приложения. Также доступны другие типы мини-приложений колесика прокрутки. Дополнительные сведения об иерархии мини-приложений колеса прокрутки, их типах и производных вариантах см. в справке по API-интерфейсу gx_scroll_wheel_create().

Метод GX_GENERIC_SCROLL_WHEEL является производным от GX_SCROLL_WHEEL и поддерживает все службы gx_scroll_wheel.

Все типы колесика прокрутки создают события GX_EVENT_LIST_SELECT для их родительских элементов при прокрутке колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **name**: логическое имя мини-приложения универсального колеса прокрутки
- **parent**: указатель на родительское мини-приложение
- **total_rows**: общее число строк колеса прокрутки
- **callback**: функция обратного вызова для создания строки колеса прокрутки. Она может иметь значение GX_NULL, если общее число строк совпадает с числом дочерних элементов. Ее необходимо предоставить для повторного использования мини-приложения, когда число дочерних элементов меньше общего числа строк или когда задан стиль GX_STYLE_WRAP. В этом случае число дочерних элементов должно быть на единицу больше, чем число видимых строк.
- **style**: стиль универсального колеса прокрутки. Предварительно заданные общие стили для всех и для конкретных мини-приложений см. в **Приложении D**.
- **id**: определяемый приложением идентификатор универсального колеса прокрутки
- **size**: размеры мини-приложения универсального колеса прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — универсальное колесо прокрутки успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_VALUE** (0x22) — недопустимое число строк.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C

/* Define visible rows.  */
#define VISIBLE_ROWS 5

/* Define row memory.  */
GX_NUMERIC_PROMPT row_memory[VISIBLE_ROWS + 1];

/* Define callback function.  */
VOID row_create(GX_GENERIC_SCROLL_WHEEL *wheel, GX_WIDGET *widget, INT index)
{
GX_NUMERIC_PROMPT *row = (GX_PROMPT *)widget;
GX_BOOL result;
GX_RECTANGLE size;

    gx_widget_created_test(widget, &result);

    if(!result)
    {
        gx_numeric_prompt_create(row, "", wheel, 0, GX_STYLE_ENABLED, 0, &size);
    }

    gx_numeric_prompt_value_set(row, index);
}

/* Create "generic_wheel” with 20 rows.  */
status = gx_generic_scroll_wheel_create(&generic_wheel, “generic_wheel”, &parent, 20, row_create,
                                       GX_STYLE_ENABLED, WHEEL_ID, &size);

/* If status is GX_SUCCESS the generic scroll wheel "generic_wheel”" has been created. */

/* Create children for generic scroll wheel.  */
for(index = 0; index <= VISIBLE_ROWS; index++)
{
    row_create(generic_wheel, (GX_WIDGET *)&row_memory[index], index);
}
```

### <a name="see-also"></a>См. также:

- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_generic_scroll_wheel_children_position
- gx_generic_scroll_wheel_draw
- gx_generic_scroll_wheel_event_process
- gx_generic_scroll_wheel_row_height_set
- gx_generic_scroll_wheel_total_rows_set

## <a name="gx_generic_scroll_wheel_draw"></a>gx_generic_scroll_wheel_draw
### <a name="draw-window"></a>Вывод окна

### <a name="prototype"></a>Прототип

```C
VOID gx_generic_scroll_wheel_draw(GX_GENERIC_SCROLL_WHEEL *wheel);
```

### <a name="description"></a>Описание

Эта служба отображает универсальное колесо прокрутки. Обычно она вызывается изнутри при обновлении холста, но ее также можно вызывать и из пользовательских функций отображения универсального колеса прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom generic scroll wheel draw function. */

VOID my_custom_generic_scroll_wheel_draw(GX_GENERIC_SCROLL_WHEEL *wheel)
{
    /* Call default generic scroll wheel draw. */
    gx_generic_scroll_wheel_draw(wheel);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_generic_scroll_wheel_children_position
- gx_generic_scroll_wheel_create
- gx_generic_scroll_wheel_event_process
- gx_generic_scroll_wheel_row_height_set
- gx_generic_scroll_wheel_total_rows_set

## <a name="gx_generic_scroll_wheel_event_process"></a>gx_generic_scroll_wheel_event_process
### <a name="process-generic-scroll-wheel-event"></a>Обработка события универсального колеса прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_generic_scroll_wheel_event_process(
    GX_GENERIC_SCROLL_WHEEL *wheel, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для окна.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие универсального колеса прокрутки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic generic scroll wheel event processing as part of custom event processing function. */

UINT custom_generic_scroll_wheel_event_process(GX_GENERIC_SCROLL_WHEEL *wheel,
                                               GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
        case xyz:

            /* Insert custom event handling here */
            break;

        default:

            /* Pass all other events to the default generic scroll wheel
            event processing */
            status = gx_generic_scroll_wheel_event_process(wheel, event);
            break;
        }
        return status;
}
```

### <a name="see-also"></a>См. также:

- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_generic_scroll_wheel_children_position
- gx_generic_scroll_wheel_create
- gx_generic_scroll_wheel_draw
- gx_generic_scroll_wheel_row_height_set
- gx_generic_scroll_wheel_total_rows_set

## <a name="gx_generic_scroll_wheel_row_height_set"></a>gx_generic_scroll_wheel_row_height_set


Назначение высоты строки для каждой строки колесика

### <a name="prototype"></a>Прототип

```C
UINT gx_generic_scroll_wheel_row_height_set(
    GX_GENERIC_SCROLL_WHEEL *wheel,
    GX_VALUE row_height);
```

### <a name="description"></a>Описание

Эта служба назначает высоту для каждой строки колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колеса прокрутки
- **row_height**: значение высоты строки (в пикселях)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — высота строк колесика прокрутки успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимая высота строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_generic_scroll_wheel_row_height_set(&wheel, 40);

/* if status == GX_SUCCESS the wheel row height has been set to 40
pixels. */
```

### <a name="see-also"></a>См. также:

- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_generic_scroll_wheel_children_position
- gx_generic_scroll_wheel_create
- gx_generic_scroll_wheel_draw
- gx_generic_scroll_wheel_event_process
- gx_generic_scroll_wheel_total_rows_set

## <a name="gx_generic_scroll_wheel_total_rows_set"></a>gx_generic_scroll_wheel_total_rows_set


Назначение общего числа доступных строк колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_generic_scroll_wheel_total_rows_set(
    GX_GENERIC_SCROLL_WHEEL *wheel,
    INT total_rows);
```

### <a name="description"></a>Описание

Эта служба назначает или изменяет общее число строк универсального колеса прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колеса прокрутки
- **total_rows**: общее число строк колесика, которое будет представлено пользователю

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — общее число строк универсального колеса прокрутки успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое общее число строк.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_generic_scroll_wheel_total_rows_set(&wheel, 20);

/* if status == GX_SUCCESS the scroll wheel has been changed to
display 20 total rows */
```
### <a name="see-also"></a>См. также:

- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_generic_scroll_wheel_children_position
- gx_generic_scroll_wheel_create
- gx_generic_scroll_wheel_draw
- gx_generic_scroll_wheel_event_process
- gx_generic_scroll_wheel_row_height_set

## <a name="gx_horizontal_list_children_position"></a>gx_horizontal_list_children_position


Размещение дочерних элементов для горизонтального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_children_position(GX_HORIZONTAL_LIST *horizontal_list);
```

### <a name="description"></a>Описание

Эта функция размещает дочерние элементы для горизонтального списка. Эта функция вызывается автоматически, когда список получает событие GX_EVENT_SHOW, но должна вызываться напрямую, если список был изменен после того, как он был сделан видимым.

### <a name="parameters"></a>Параметры

- **horizontal_list**: указатель на блок управления горизонтальным списком

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — дочерние элементы для горизонтального списка успешно размещены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Position children in the horizontal list */
status = gx_horizontal_list_children_position (&horizontal_list);

/* If status is GX_SUCCESS the children in the horizontal list are positioned. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_create
- gx_horizontal_list_event_process
- gx_horizontal_list_page_index_set
- gx_horizontal_list_selected_index_get
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_selected_set
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_create"></a>gx_horizontal_list_create


Создание горизонтального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_create(
    GX_HORIZONTAL_LIST *horizontal_list,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent,
    INT total_columns,
    VOID (*callback)(GX_HORIZONTAL_LIST *, 
    GX_WIDGET *, INT),
    ULONG style, 
    USHORT horizontal_list_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает горизонтальный список.

### <a name="parameters"></a>Параметры

- **horizontal_list**: блок управления мини-приложением горизонтального списка
- **name**: имя горизонтального списка
- **parent**: указатель на родительское мини-приложение
- **total_columns**: общее количество столбцов в горизонтальном списке
- **callback**: указатель на функцию обратного вызова, предоставляемую приложением Функция обратного вызова вызывается при прокрутке горизонтального списка, чтобы создать новые видимые элементы списка. Таким образом, в качестве элемента списка в горизонтальном списке может отображаться любой определяемый пользователем тип мини-приложения.
- **style**: стиль мини-приложения полосы прокрутки Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **horizontal_list_id**: определяемый приложением идентификатор горизонтального списка
- **size**: размеры горизонтального списка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — горизонтальный список успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_VALUE** (0x22) — недопустимое количество столбцов.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create horizontal list “my_list” with 5 columns. */
status = gx_horizontal_list_create(&my_list, “my_list”, &my_parent,
    5, callback, GX_STYLE_WRAP, MY_LIST_ID, &size);

/* If status is GX_SUCCESS the horizontal list “my_list” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_event_process
- gx_horizontal_list_page_index_set
- gx_horizontal_list_selected_index_get
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_selected_set
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_event_process"></a>gx_horizontal_list_event_process


Обработка события горизонтального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_event_process(
    GX_HORIZONTAL_LIST *list,
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для горизонтального списка.

### <a name="parameters"></a>Параметры

- **list**: блок управления мини-приложением горизонтального списка
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие горизонтального списка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Call generic horizontal list event processing as part of custom event processing function. */

UINT custom_list_event_process(
    GX_HORIZONTAL_LIST *list,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;
    default:
        /* Pass all other events to the default horizontal
        list event processing */
        status =
        gx_horizontal_list_event_process(list, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_create
- gx_horizontal_list_page_index_set
- gx_horizontal_list_selected_index_get
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_selected_set
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_page_index_set"></a>gx_horizontal_list_page_index_set


Задание начального индекса страницы

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_page_index_set(
    GX_HORIZONTAL_LIST *list,
    INT *index);
```

### <a name="description"></a>Описание

Эта служба задает начальный индекс для горизонтального списка.

### <a name="parameters"></a>Параметры

- **list**: блок управления мини-приложением горизонтального списка
- **index**: новый начальный индекс

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — начальный индекс страницы для горизонтального списка успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Sets the starting page index of horizontal list “my_list” as 1. */
status = gx_horizontal_list_page_index_set(&my_list, 1);

/* If status is GX_SUCCESS the starting page index of “my_list” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_create
- gx_horizontal_list_event_process
- gx_horizontal_list_selected_index_get
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_selected_set
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_selected_index_get"></a>gx_horizontal_list_selected_index_get


Получение выбранного индекса записи из горизонтального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_selected_index_get(
    GX_horizobntal_LIST *horizontal_list,
    INT *return_index);
```

### <a name="description"></a>Описание

Эта служба возвращает выбранный индекс записи в горизонтальном списке.

### <a name="parameters"></a>Параметры

- **horizontal_list**: блок управления мини-приложением горизонтального списка
- **return_index**: назначение для возвращаемого индекса списка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запись горизонтального списка успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
INT current_index_entry;

/* Get the list entry at the current index of horizontal list “my_list”. */
status = gx_horizontal_list_selected_index_get(&my_list,
    &current_index_entry);

/* If status is GX_SUCCESS, “current_index_entry” contains the current list selection index. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_create
- gx_horizontal_list_event_process
- gx_horizontal_list_page_index_set
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_selected_set
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_selected_set"></a>gx_horizontal_list_selected_set


Назначение выбранной записи в горизонтальном списке

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_selected_set(
    GX_HORIZONATAL_LIST *horizontal_list,
    INT index);
```

### <a name="description"></a>Описание

Эта служба назначает выбранную запись в горизонтальном списке. При необходимости горизонтальный список будет прокручиваться, чтобы сделать выбранную запись видимой.

### <a name="parameters"></a>Параметры

- **horizontal_list**: блок управления мини-приложением горизонтального списка
- **index**: позиция новой записи в списке на основе индекса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запись горизонтального списка успешно задана.
- **GX_FAILURE** (0x10) — индекс вне допустимого диапазона.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the list entry of “my_list” to the child in line 12. */
status = gx_horizontal_list_selected_set(&my_list, 12);

/* If status is GX_SUCCESS, the list entry of “my_list” has been successfully set to 12. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_create
- gx_horizontal_list_event_process
- gx_horizontal_list_page_index_set
- gx_horizontal_list_selected_index_get
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_selected_widget_get"></a>gx_horizontal_list_selected_widget_get


Получение выбранной записи из горизонтального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_selected_widget_get(
    GX_horizobntal_LIST *horizontal_list,
    GX_WIDGET **return_list_entry);
```

### <a name="description"></a>Описание

Эта служба возвращает выбранную запись из горизонтального списка. Обратите внимание, что, если горизонтальный список содержит больше строк, чем дочерние мини-приложения, и выбранная запись была прокручена за пределы представления, этот API возвратит GX_NULL, поскольку дочерние мини-приложения повторно используются при прокрутке содержимого списка. Функция gx_horizontal_list_selected_index_get будет надежно возвращать индекс выбранного элемента, даже если этот элемент был прокручен за пределы представления.

### <a name="parameters"></a>Параметры

- **horizontal_list**: блок управления мини-приложением горизонтального списка
- **return_list_entry**: назначение для возвращаемого мини-приложения записи списка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запись горизонтального списка успешно получена.
- **GX_FAILURE** (0x10) — выбранное мини-приложение было прокручено за пределы представления в списке, в котором больше строк, чем у дочернего элемента клиента.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get the list entry at the current index of horizontal list “my_list”. */
status = gx_horizontal_list_selected_widget_get(&my_list, &current_list_entry);

/* If status is GX_SUCCESS, “current_list_entry” contains a pointer to the currently selected widget. */

    
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_create
- gx_horizontal_list_event_process
- gx_horizontal_list_page_index_set
- gx_horizontal_list_selected_index_get
- gx_horizontal_list_selected_set
- gx_horizontal_list_total_columns_set

## <a name="gx_horizontal_list_total_columns_set"></a>gx_horizontal_list_total_columns_set


Назначение общего числа столбцов списка.

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_list_total_columns_set(
    GX_HORIZONTAL_LIST *horizontal_list,
    INT count);
```

### <a name="description"></a>Описание

Эта служба назначает общее число столбцов, отображаемых в горизонтальном списке.

### <a name="parameters"></a>Параметры

- **horizontal_list**: блок управления мини-приложением горизонтального списка
- **count**: число столбцов для отображения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — число успешных назначенных столбцов.
- **GX_CALLER_ERROR** (0x10) — недопустимый вызывающий объект для этой функции
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение количества.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Tell my list to display 20 total columns. */
status = gx_horizontal_list_total_columns_set(&my_list, 20);

/* If status is GX_SUCCESS, the total colums of “my_list” was successfully set to 20. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_list_children_position
- gx_horizontal_list_create
- gx_horizontal_list_event_process
- gx_horizontal_list_page_index_set
- gx_horizontal_list_selected_index_get
- gx_horinzontal_list_selected_widget_get
- gx_horizontal_list_selected_set

## <a name="gx_horizontal_scrollbar_create"></a>gx_horizontal_scrollbar_create


Создание горизонтальной полосы прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_horizontal_scrollbar_create(
    GX_SCROLLBAR *scrollbar,
    GX_CONST GX_CHAR *name,
    GX_WINDOW *parent,
    GX_SCROLLBAR_APPEARANCE *appearance ULONG style);
```

### <a name="description"></a>Описание

Эта служба создает горизонтальную полосу прокрутки. Идентификатор горизонтальной полосы прокрутки определен предварительно (поскольку окно должно уметь перехватывать события из него), а размер — автоматически (поскольку он должен заполнять ширину клиента родительского окна). Если вы решили разрешить полосы прокрутки клиентской области, необходимо добавить еще одну функцию create с параметрами id и size.

### <a name="parameters"></a>Параметры

- **scrollbar**: блок управления мини-приложением полосы прокрутки
- **name**: имя полосы прокрутки
- **parent**: указатель на родительское мини-приложение
- **appearance**: структура внешнего вида определяет внешний вид полосы прокрутки Если это значение равно GX_NULL, то полоса прокрутки будет использовать стандартный вид полосы прокрутки, определенный gx_system_scroll_appearance_get. Определение структуры GX_SCROLLBAR_APPEARANCE см. в **приложении I**.
- **style**: стиль мини-приложения полосы прокрутки Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — горизонтальная полоса прокрутки успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create horizontal scrollbar “my_scrollbar”. */
status = gx_horizontal_scrollbar_create(&my_scrollbar,
    “my_horizontal_scrollbar”, &my_parent, GX_NULL,
    GX_STYLE_SCROLLBAR_END_BUTTONS);

/* If status is GX_SUCCESS the horizontal scrollbar “my_scrollbar” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_scrollbar_draw
- gx_scrollbar_event_process
- gx_scrollbar_limit_check
- gx_scrollbar_reset
- gx_vertical_scrollbar_create

## <a name="gx_icon_button_create"></a>gx_icon_button_create


Создание кнопки со значком

### <a name="prototype"></a>Прототип

```C
UINT gx_icon_button_create(
    GX_ICON_BUTTON *button,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent,
    GX_RESOURCE_ID icon_id,
    ULONG style,
    USHORT icon_button_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает указанное мини-приложение кнопки со значком.

GX_ICON_BUTTON является производным от GX_BUTTON и поддерживает все службы gx_button.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой со значком
- **name**: логическое имя мини-приложения кнопки со значком
- **parent**: указатель на родительское мини-приложение
- **icon_id**: идентификатор ресурса значка
- **style**: стиль значка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **icon_button_id**: определяемый приложением идентификатор кнопки со значком
- **size**: размеры кнопки со значком

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка со значком успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_icon_button”. */
status = gx_icon_button_create(&my_icon_button, “my_icon_button”,
    &my_parent,
    MY_ICON_RESOURCE_ID, GX_STYLE_BORDER_RAISED, MY_ICON_BUTTON_ID,
    &size);

/* If status is GX_SUCCESS the icon button “my_icon_button” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_create
- gx_icon_draw
- gx_icon_pixelmap_set
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_radio_button_create
- gx_radio_button_draw gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_icon_button_draw"></a>gx_icon_button_draw


Вывод кнопки со значком

### <a name="prototype"></a>Прототип

```C
VOID gx_icon_button_draw(GX_ICON_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует кнопку со значком. Эта функция обычно вызывается GUIX внутри как часть операции обновления холста, но она также предоставляется приложению, которому может потребоваться предоставить настраиваемую функцию рисования и вызвать кнопку со значком по умолчанию в качестве настраиваемой базы для рисования.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой со значком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom icon draw function. */
void MyIconButtonDraw(GX_ICON_BUTTON *button)
{
    /* Do the normal drawing first */
    gx_icon_button_draw(button);

    /* Add custom drawing here */
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_create
- gx_icon_draw
- gx_icon_pixelmap_set
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_radio_button_create
- gx_radio_button_draw gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_icon_button_pixelmap_set"></a>gx_icon_button_pixelmap_set


Задание карты отображения пикселей для мини-приложения кнопки со значком

### <a name="prototype"></a>Прототип

```C
UINT gx_icon_button_pixelmap_set(
    GX_ICON_BUTTON *button,
    GX_RESOURCE_ID icon_id);
```

### <a name="description"></a>Описание

Эта служба назначает новую карту отображения пикселей мини-приложению кнопки со значком.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой со значком
- **icon_id**: идентификатор ресурса карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей для кнопки со значком успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set pixelmap for “my_icon_button”. */
status = gx_icon_button_pixelmap_set(&my_icon_button,
    GX_PIXELMAP_ID_ICON);

/* If status is GX_SUCCESS, the pixelmap of the icon button “my_icon_button” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_create
- gx_icon_draw
- gx_icon_pixelmap_set
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_radio_button_create
- gx_radio_button_draw gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_icon_background_draw"></a>gx_icon_background_draw


Вывод фона значка

### <a name="prototype"></a>Прототип

```C
VOID gx_icon_background_draw(GX_ICON *icon);
```

### <a name="description"></a>Описание

Эта служба рисует фон указанного мини-приложения значка. Эта служба обычно вызывается внутри функции gx_icon_button_draw, но предоставляется приложению для помощи в написании пользовательских функций рисования.

### <a name="parameters"></a>Параметры

- **icon**: указатель на блок управления мини-приложением значка

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom icon draw function. */
void MyIconButtonDraw(GX_ICON *icon)
{
    /* Call icon background draw. */
    gx_icon_background_draw(icon);

    /* Add custom drawing here */

    /* Draw child widgets. */
    gx_widget_children_draw(icon);
}
```

### <a name="see-also"></a>См. также:

- gx_icon_create
- gx_icon_draw
- gx_icon_event_process
- gx_icon_pixelmap_set

## <a name="gx_icon_create"></a>gx_icon_create


значок "Создать"

### <a name="prototype"></a>Прототип

```C
UINT gx_icon_create(
    GX_ICON *icon, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RESOURCE_ID pixelmap_id, 
    ULONG style,
    USHORT icon_id, 
    GX_VALUE x, 
    GX_VALUE y);
```

### <a name="description"></a>Описание

Эта служба создает указанное мини-приложение значка.

### <a name="parameters"></a>Параметры

- **icon**: указатель на блок управления значком
- **name**: логическое имя мини-приложения значка
- **parent**: указатель на родительское мини-приложение
- **pixelmap_id**: идентификатор ресурса карты отображения пикселей
- **style**: стиль значка
- **icon_id**: определяемый приложением идентификатор значка
- **x**: координата начального положения по оси X
- **y**: координата начального положения по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значок успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_icon”. */
status = gx_icon_create(&my_icon, “my_icon”, &my_parent,
    MY_PIXELMAP_RESOURCE_ID, GX_STYLE_BORDER_RAISED, MY_ICON_ID,
    5, 30);

/* If status is GX_SUCCESS the icon “my_icon” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_icon_button_create
- gx_icon_draw
- gx_icon_pixelmap_set

## <a name="gx_icon_draw"></a>gx_icon_draw


Вывод значка

### <a name="prototype"></a>Прототип

```C
VOID gx_icon_draw(GX_ICON *icon);
```

### <a name="description"></a>Описание

Эта служба рисует указанное мини-приложение значка. Эта служба обычно вызывается GUIX внутри как часть операции обновления холста, но она также предоставляется приложению, которому может потребоваться предоставить настраиваемую функцию рисования и вызвать рисование значка по умолчанию в качестве настраиваемой базы для рисования.

### <a name="parameters"></a>Параметры

- **icon**: указатель на блок управления мини-приложением значка

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom icon drawing function. */

VOID my_icon_draw(GX_MENU *menu)
{
    /* Call default icon draw. */
    gx_icon_draw(menu);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_icon_button_create
- gx_icon_create
- gx_icon_pixelmap_set

## <a name="gx_icon_event_process"></a>gx_icon_event_process

Обработка события мини-приложения значка

### <a name="prototype"></a>Прототип

```C
UINT gx_icon_event_process(
    GX_ICON *icon, 
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает события, отправленные мини-приложению GX_ICON.

### <a name="parameters"></a>Параметры

- **icon**: указатель на блок управления мини-приложением значка
- **event_ptr**: указатель на структуру GX_EVENT

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие значка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
switch(event_ptr->gx_event_type)
{
case GX_EVENT_SHOW:
    /* Do default handling. */
    status = gx_icon_event_process(icon, event_ptr);

    /* add your own handling here */
    break;
}
```

### <a name="see-also"></a>См. также:

- gx_icon_button_create
- gx_icon_create
- gx_icon_pixelmap_set

## <a name="gx_icon_pixelmap_set"></a>gx_icon_pixelmap_set


Задание карты отображения пикселей для значка

### <a name="prototype"></a>Прототип

```C
UINT gx_icon_pixelmap_set(
    GX_ICON *icon, 
    GX_RESOURCE_ID normal_id,
    GX_RESOURCE_ID selected_id);
```

### <a name="description"></a>Описание

Эта служба задает карту отображения пикселей для указанного мини-приложения значка.

### <a name="parameters"></a>Параметры

- **icon**: указатель на блок управления мини-приложением значка
- **normal_id**: идентификатор ресурса нормального состояния
- **selected_id**: идентификатор ресурса состояния выбора

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей значка успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set pixelmap for “my_icon”. */
status = gx_icon_pixelmap_set(&my_icon,
    MY_NOT_SELECTED_RESOURCE_ID,
    MY_SELECTED_ID);

/* If status is GX_SUCCESS, the icon “my_icon” has a pixelmap set. */
```

### <a name="see-also"></a>См. также:

- gx_icon_button_create
- gx_icon_create
- gx_icon_draw

## <a name="gx_image_reader_create"></a>gx_image_reader_create


Создание экземпляра модуля чтения изображений

### <a name="prototype"></a>Прототип

```C
UINT gx_image_reader_create(
    GX_IMAGE_READER *image_reader,
    GX_CONST GX_UBYTE *data, 
    INT data_size,
    GX_UBYTE color_format, 
    GX_UBYTE mode);
```

### <a name="description"></a>Описание

Эта функция создает модуль чтения или декодер необработанных изображений для среды выполнения. В настоящее время поддерживаются только типы необработанных изображений в формате JPEG и PNG. Для этой службы требуется определить GX_SOFTWARE_DECODER_SUPPORT.

### <a name="parameters"></a>Параметры

- **image_reader**: блок управления модулем чтения изображений
- **data**: указатель на необработанные входные данные
- **data_size**:размер необработанных входных данных
- **color_format**: запрашиваемый формат выходного цвета
- **mode**: флаги режимов сжатия, сглаживания и альфа-режима

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — модуль чтения изображений успешно создан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер данных.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_IMAGE_READER my_image_reader;

“color format” could be set to:
    GX_COLOR_FORMAT_32ARGB
    GX_COLOR_FORMAT_24RGB
    GX_COLOR_FORMAT_565RGB
    GX_COLOR_FORMAT_8BIT_PALETTE

/* Create image reader “my_image_reader”. */
status = gx_image_reader_create(&my_image_reader, decoder_data,
    decoder_data_size,
    GX_COLOR_FORMAT_32ARGB,
    GX_IMAGE_READER_MODE_COMPRESS)

/* If status is GX_SUCCESS, “my_image_reader” has been successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_image_reader_start
- gx_image_reader_palette_set

## <a name="gx_image_reader_palette_set"></a>gx_image_reader_palette_set


Задание палитры модуля чтения изображений

### <a name="prototype"></a>Прототип

```C
UINT gx_image_reader_palette_set(
    GX_IMAGE_READER *image_reader,
    GX_COLOR *pal,
    UINT palsize);
```

### <a name="description"></a>Описание

Эта служба задает палитру для блока управления модулем чтения изображений. Для этой службы требуется определить GX_SOFTWARE_DECODER_SUPPORT.

### <a name="parameters"></a>Параметры

- **image_reader**: блок управления модулем чтения изображений
- **pal**: указатель на палитру
- **palsize**: размер палитры

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — палитра модуля чтения изображения успешно задана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер палитры.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set “my_palette” as the palette of “my_image_reader”. */
status = gx_image_reader_palette_set(&my_image_reader, my_palette,
    my_palette_size);

/* If status is GX_SUCCESS the palette of “my_image_reader” has been set to “my_palette”. */
```

### <a name="see-also"></a>См. также:

- gx_image_reader_create
- gx_image_reader_start

## <a name="gx_image_reader_start"></a>gx_image_reader_start

Запуск процесса распаковки и преобразования

### <a name="prototype"></a>Прототип

```C
UINT gx_image_reader_start(
    GX_IMAGE_READER *image_reader, 
    GX_PIXELMAP *outmap);
```

### <a name="description"></a>Описание

Эта служба декодирует необработанное изображение в заданный цветовой формат. В настоящее время поддерживаются только типы необработанных изображений в формате JPEG и PNG. Для этого необходимо определить GX_SOFTWARE_DECODER_SUPPORT.

### <a name="parameters"></a>Параметры

- **image_reader**: блок управления модулем чтения изображений
- **pixelmap**: выходная карта отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — изображение успешно декодировано.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.
- **GX_NOT_SUPPORTED** (0x28) — входной тип изображения или выходной формат цвета не поддерживается.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_IMAGE_READER my_image_reader;
GX_PIXELMAP output_map;

/* Create my_image_reader here. */

/* Decoding a raw image according to the settings of “my_image_reader”. */
status = gx_image_reader_start(&my_image_reader, output_map)

/* If status is GX_SUCCESS “output_map” have been loaded with the requested pixelmap. */
```

### <a name="see-also"></a>См. также:

- gx_image_reader_create
- gx_image_reader_palette_set

## <a name="gx_line_chart_axis_draw"></a>gx_line_chart_axis_draw


Вывод осей X, Y графика

### <a name="prototype"></a>Прототип

```C
VOID gx_line_chart_axis_draw(GX_LINE_CHART *chart);
```

### <a name="description"></a>Описание

Эта служба рисует оси X, Y графика. Цвета оси и параметры толщины линии извлекаются из структуры данных графика.

Эта служба обычно вызывается внутри функции gx_line_chart_draw, но предоставляется приложению для помощи в написании пользовательских функций рисования.

### <a name="parameters"></a>Параметры

- **chart**: блок управления графиком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom chart drawing function. */

VOID my_chart_draw(GX_LINE_CHART *chart)
{
    /* Call default window draw. */
    gx_window_draw((GX_WINDOW *)chart);

    /* Draw the line chart axis. */
    gx_line_chart_axis_draw(&chart->gx_line_chart_info);

    /* Draw the data line */
    gx_line_chart_data_draw(&chart->gx_line_chart_info);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_line_chart_create
- gx_line_chart_data_draw
- gx_line_chart_draw
- gx_line_chart_update
- gx_line_chart_y_scale_calculate

## <a name="gx_line_chart_create"></a>gx_line_chart_create


Создание мини-приложения GX_LINE_CHART

### <a name="prototype"></a>Прототип

```C
UINT gx_line_chart_create(
    GX_LINE_CHART *chart,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_LINE_CHART_INFO *info,
    ULONG style,
    USHORT chart_id, GX_RECTANGLE *size)
```

### <a name="description"></a>Описание

Эта служба создает окно с графиком. Параметры рисования графика и данные графика передаются через структуру GX_LINE_CHART_INFO.

GX_LINE_CHART основан на GX_WINDOW и поддерживает все API GX_WINDOW.

### <a name="parameters"></a>Параметры

- **chart**: указатель на блок управления GX_LINE_CHART
- **name**: имя графика (необязательно)
- **parent**: родительское мини-приложение (или GX_NULL)
- **info**: структура, определяющая параметры рисования графика Определение структуры GX_LINE_CHART_INFO см. в **приложении I**.
- **style**: флаги стиля мини-приложения
- **chart_id**: логическое значение идентификатора графика
- **size**: прямоугольник, ограничивающий окно графика

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — график успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create a line chart */
GX_LINE_CHART chart;
GX_RECTANGLE chart_size;
GX_LINE_CHART_INFO gx_chart_info;
GX_WINDOW_ROOT *root_window;

chart_size = root_window->gx_widget_size;

/* Initialize params for the GUIX base chart. */
gx_chart_info.gx_line_chart_min_val = DATA_MIN_VAL;
gx_chart_info.gx_line_chart_max_val = DATA_MAX_VAL;
gx_chart_info.gx_line_chart_max_data_count = MAX_DATA_COUNT;
gx_chart_info.gx_line_chart_active_data_count = 0;
gx_chart_info.gx_line_chart_axis_line_width = AXIS_LINE_WIDTH;
gx_chart_info.gx_line_chart_data_line_width = DATA_LINE_WIDTH;
gx_chart_info.gx_line_chart_data = chart_data;
gx_chart_info.gx_line_chart_line_color = GX_COLOR_ID_DATA_LINE;
gx_chart_info.gx_line_chart_axis_color = GX_COLOR_ID_AXIS_LINE;

/* Leave room for labels on bottom and right. */
gx_chart_info.gx_line_chart_left_margin = 0;
gx_chart_info.gx_line_chart_top_margin = 0;
gx_chart_info.gx_line_chart_right_margin = 80;
gx_chart_info.gx_line_chart_bottom_margin = 32;

status = gx_line_chart_create(&chart, “Line Chart”, root_window,
    &gx_chart_info, GX_STYLE_NONE, &chart_size);

/* If status is GX_SUCCESS, the “chart” has been successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_line_chart_create
- gx_line_chart_data_draw
- gx_line_chart_draw
- gx_line_chart_update
- gx_line_chart_y_scale_calculate

## <a name="gx_line_chart_data_draw"></a>gx_line_chart_data_draw


Вывод линии данных графика

### <a name="prototype"></a>Прототип

```C
VOID gx_line_chart_data_draw(GX_LINE_CHART *chart);
```

### <a name="description"></a>Описание

Эта служба рисует линию данных графика. Цвета линии и параметры толщины линии извлекаются из структуры данных графика.

Эта служба обычно вызывается внутри функции gx_line_chart_draw, но предоставляется приложению для помощи в написании пользовательских функций рисования.

### <a name="parameters"></a>Параметры

- **chart**: блок управления графиком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom chart drawing function. */

VOID my_chart_draw(GX_LINE_CHART *chart)
{
    /* Call default window draw. */
    gx_window_draw((GX_WINDOW *)chart);

    /* Draw the line chart axis. */
    gx_line_chart_axis_draw(chart);

    /* Draw the data line. */
    gx_line_chart_data_draw(chart);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_line_chart_create
- gx_line_chart_draw
- gx_line_chart_update
- gx_line_chart_y_scale_calculate

## <a name="gx_line_chart_draw"></a>gx_line_chart_draw


Вывод графика

### <a name="prototype"></a>Прототип

```C
UINT gx_line_chart_draw(GX_LINE_CHART *chart);
```

### <a name="description"></a>Описание

Это функция рисования графика по умолчанию, которая рисует ось графика и линию данных. Обычно приложения предоставляют настраиваемую функцию рисования взамен функции рисования по умолчанию, чтобы добавить такие элементы, как деления, масштаб или другие данные, на ось графика и линию данных, нарисованные базовым мини-приложением графика.

### <a name="parameters"></a>Параметры

- **chart**: указатель на блок управления графиком

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom chart drawing function. */

VOID my_chart_draw(GX_LINE_CHART *chart)
{
    /* Call default line chart draw. */
    gx_line_chart_draw(chart);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_line_chart_create
- gx_line_chart_draw
- gx_line_chart_update
- gx_line_chart_y_scale_calculate

## <a name="gx_line_chart_update"></a>gx_line_chart_update


Обновление линии данных графика

### <a name="prototype"></a>Прототип

```C
UINT gx_line_chart_update(
    GX_LINE_CHART *chart, 
    INT *data,
    INT data_count)
```

### <a name="description"></a>Описание

Эта служба обновляет массив данных, построенный в окне графика, и вызывает перерисовку окна графика.

### <a name="parameters"></a>Параметры

- **chart**: блок управления графиком
- **data**: массив данных для отображения
- **data_count**: размер массива данных

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка с текстом успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
INT chart_data[100];
GX_LINE_CHART chart;

/* Update the data array associated with “chart”. */
status = gx_line_chart_update(&chart, chart_data, 100);

/* If status is GX_SUCCESS, the line chart data has been updated. */
```

### <a name="see-also"></a>См. также:

- gx_line_chart_create
- gx_line_chart_data_draw
- gx_line_chart_draw
- gx_line_chart_y_scale_calculate

## <a name="gx_line_chart_y_scale_calculate"></a>gx_line_chart_y_scale_calculate


Вычисление значения масштабирования по оси Y с фиксированной запятой

### <a name="prototype"></a>Прототип

```C
UINT gx_line_chart_y_scale_calculate(
    GX_LINE_CHART *chart,
    INT *return_val);
```

### <a name="description"></a>Описание

Эта служба вычисляет значение масштабирования с фиксированной запятой, используемое для построения значений данных на оси Y графика. Для вычисления этого значения масштабирования используются параметры chart_info и ограничивающий прямоугольник графика.

### <a name="parameters"></a>Параметры

- **chart**: блок управления графиком
- **return_val**: адрес значения для хранения возвращаемого значения с фиксированной запятой

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение масштабирования успешно вычислено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_LINE_CHART chart;
INT y_scale;

/* Caluclate y scale value of “chart”. */
status = gx_line_chart_y_scale_calculate(&chart, &y_scale);

/* If status is GX_SUCCESS, y scale value of “chart” has been calculated. */
```

### <a name="see-also"></a>См. также:

- gx_line_chart_create
- gx_line_chart_data_draw
- gx_line_chart_draw
- gx_line_chart_update

## <a name="gx_menu_create"></a>gx_menu_create


Создание меню

### <a name="prototype"></a>Прототип

```C
UINT gx_menu_create(
    GX_MENU *menu,GX_CONST GX_CHAR *name,
    GX_WIDGET *parent, 
    GX_RESOURCE_ID text_id,
    GX_RESOURCE_ID fill_id, 
    ULONG style, 
    USHORT menu_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает меню, как указано, и связывает меню с указанным родительским мини-приложением. Она принимает все типы мини-приложений в качестве дочерних элементов меню. Чтобы вставить мини-приложение в качестве дочернего пункта меню, вызовите **gx_menu_insert**.

GX_MENU является производным от GX_PIXELMAP_PROMPT и поддерживает все службы API gx_pixelmap_prompt.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню
- **name**: имя меню
- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса текста
- **fill_id**: идентификатор ресурса заливки
- **style**: стиль мини-приложения Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **menu_id**: определяемый приложением идентификатор меню
- **size**: размер меню

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — меню успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_MENU my_menu;

/* Create “my_menu”. */
status = gx_menu_create(&my_menu, “my_menu”, parent, MY_TEXT_ID,
    MY_FILL_ID, GX_STYLE_ENABLED, MY_MENU_ID,
    &size);

/* If status is GX_SUCCESS, the menu was successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_accordion_meu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_draw
- gx_menu_event_process
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_menu_draw"></a>gx_menu_draw


Вывод меню

### <a name="prototype"></a>Прототип

```C
VOID gx_menu_draw(GX_MENU *menu);
```

### <a name="description"></a>Описание

Эта служба рисует указанное меню. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых меню.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom menu drawing function. */

VOID my_menu_draw(GX_MENU *menu)
{
    /* Call default menu draw. */
    gx_menu_draw(menu);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_event_process
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_menu_event_process"></a>gx_menu_event_process

Обработка события меню

### <a name="prototype"></a>Прототип

```C
UINT gx_menu_event_process(GX_MENU *menu, GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного меню. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий меню.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие меню успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x23) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic menu event processing as part of custom event processing function. */

UINT custom_menu_event_process(GX_MENU *menu, GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */

        /* Call default event process if this is a system event such as GX_EVENT_SHOW. */
        status = gx_menu_event_process(menu, event);
        break;

    default:
        /* Pass all other events to the default menu
           event processing */
        status = gx_menu_event_process(menu, event);
        break;
    }
    return status;
}

```

### <a name="see-also"></a>См. также:

- gx_menu_create
- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_menu_insert"></a>gx_menu_insert


Вставка нового пункта

### <a name="prototype"></a>Прототип

```C
UINT gx_menu_insert(
    GX_MENU *menu, 
    GX_WIDGET *insert);
```

### <a name="description"></a>Описание

Эта служба вставляет новый пункт в меню.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню
- **widget**: указатель на мини-приложение для вставки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — новый пункт успешно вставлен в меню.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Insert new item “my_widget” to the menu “my_menu”. */
status = gx_menu_insert(&my_menu, &my_widget);

/* If status is GX_SUCCESS the new item “my_widget” has been inserted to the menu “my_menu”. */
```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_event_process
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_menu_remove"></a>gx_menu_remove


Удаление пункта

### <a name="prototype"></a>Прототип

```C
UINT gx_menu_remvoe(
    GX_MENU *menu, 
    GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба удаляет пункт из меню.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню
- **widget**: указатель на мини-приложение для удаления

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — пункт меню успешно удален.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Remove item “my_widget” from menu “my_menu” */
status = gx_menu_remove(&my_menu, &my_widget);

/* If status is GX_SUCCESS the item “my_widget” has been removed from menu “my_menu”. */
```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_event_process
- gx_menu_insert
- gx_menu_text_draw
- gx_menu_text_offset_set

## <a name="gx_menu_text_draw"></a>gx_menu_text_draw


Вывод текста меню

### <a name="prototype"></a>Прототип

```C
VOID gx_menu_text_draw(GX_MENU *menu);
```

### <a name="description"></a>Описание

Эта служба рисует текст меню. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых меню.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom menu drawing function. */

VOID my_menu_draw(GX_MENU *menu)
{
    /* Call default menu background draw. */
    gx_pixelmap_prompt_background_draw(
                            (GX_PIXELMAP_PROMPT *)menu);

    /* Draw menu text. */
    gx_menu_text_draw(menu);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_event_process
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_offset_set

## <a name="gx_menu_text_offset_set"></a>gx_menu_text_offset_set


Задание смещения вывода текста меню

### <a name="prototype"></a>Прототип

```C
UINT gx_menu_text_offset_set(
    GX_MENU *menu, 
    GX_VALUE x_offset,
    GX_VALUE y_offset);
```

### <a name="description"></a>Описание

Эта служба устанавливает смещение для текста меню по осям X и Y.

### <a name="parameters"></a>Параметры

- **menu**: указатель на блок управления меню
- **x_offset**: координата смещения по оси X
- **y_offset**: координата смещения по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текста меню успешно смещен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set text draw offset of menu “my_menu” to (20, 10). */
status = gx_menu_text_offset_set(&my_menu, 20, 10);

/* If status is GX_SUCCESS the text draw offset of menu “my_menu” has been set to (20, 10). */
```

### <a name="see-also"></a>См. также:

- gx_accordion_menu_create
- gx_accordion_menu_draw
- gx_accordion_menu_event_process
- gx_accordion_menu_position
- gx_menu_create
- gx_menu_draw
- gx_menu_event_process
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw

## <a name="gx_multi_line_text_button_create"></a>gx_multi_line_text_button_create


Создание кнопки с многострочным текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_button_create(
    GX_MULTI_LINE_TEXT_BUTTON *text_button,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RESOURCE_ID text_id,
    ULONG style,
    USHORT text_button_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение кнопки с многострочным текстом. На кнопке с многострочным текстом отображается текст на 1-n строках. Максимальное число строк определяется константой GX_MULTI_LINE_TEXT_BUTTON_MAX_LINES, значение по умолчанию — 4. Разрывы строк задаются символами возврата каретки и/или возврата каретки + перевода строки в текстовой строке, назначенной кнопке с многострочным текстом.

GX_MULTI_LINE_TEXT_BUTTON является производным от GX_TEXT_BUTTON и поддерживает все службы API gx_text_button.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **name**: логическое имя кнопки с текстом
- **parent**: указатель на родительское мини-приложение кнопки
- **text_id**: идентификатор ресурса текста
- **style**: стиль кнопки текста Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **text_button_id**: определяемый приложением идентификатор кнопки с текстом
- **size**: размер кнопки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка с многострочным текстом успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create multi-line text button “my_text_button”. */
status = gx_multi_line_text_button_create(&my_text_button, "my text button",
    &my_parent_window, MY_TEXT_RESOURCE_ID,
    GX_STYLE_BUTTON_TOGGLE, MY_TEXT_BUTTON_ID,
    &size);

/* If status is GX_SUCCESS, the multi-line text button “my_text_button” was created. */
```

### <a name="see-also"></a>См. также:

- gx_text_button_create
- gx_button_create
- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set

## <a name="gx_multi_line_text_button_draw"></a>gx_multi_line_text_button_draw


Вывод кнопки с многострочным текстом

### <a name="prototype"></a>Прототип

```C
VOID gx_multi_line_text_button_draw(GX_MULTI_LINE_TEXT_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует кнопку с многострочным текстом. Эта функция обычно вызывается GUIX внутри как часть операции обновления холста, но она также предоставляется приложению, которому может потребоваться предоставить настраиваемую функцию рисования и вызвать кнопку с многострочным текстом по умолчанию в качестве настраиваемой базы для рисования.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой с текстом

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Draw the text button “my_text_button”. */
void MyButtonDraw(GX_MULTI_LINE_TEXT_BUTTON *button)
{
    /* Do the normal drawing first */
    gx_multi_line_text_button_draw(&my_text_button);

    /* Add custom drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_text_button_create
- gx_button_create
- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set

## <a name="gx_multi_line_text_button_event_process"></a>gx_multi_line_text_button_event_process


Обработка событий по умолчанию для кнопки с многострочным текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_button_event_process(
    GX_MULTI_LINE_TEXT_BUTTON *button,
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба является функцией обработки событий по умолчанию для мини-приложения кнопки с многострочным текстом. Эта функция становится доступной для приложений, которые хотят обеспечить настраиваемую обработку событий для мини-приложения кнопки с текстом.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой с текстом
- **event_ptr**: обрабатываемое событие

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT MyEventHandler(GX_MULTI_LINE_TEXT_BUTTON *button,
    GX_EVENT *event_ptr)
{
    switch(event->gx_event_type)
    {
    case GX_EVENT_SHOW:
        gx_multi_line_text_button_event_process(button, event_ptr);
        /* add custom actions here */
        break;
    default:
        gx_multi_line_text_button_event_process(button, event_ptr);
        break;
    }
    return GX_SUCCESS;
}
```

### <a name="see-also"></a>См. также:

- gx_text_button_create
- gx_button_create
- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set

## <a name="gx_multi_line_text_button_text_draw"></a>gx_multi_line_text_button_text_draw


Вывод функции поддержки

### <a name="prototype"></a>Прототип

```C
VOID gx_multi_line_text_button_text_draw(GX_MULTI_LINE_TEXT_BUTTON *text_button);
```

### <a name="description"></a>Описание

Эта функция поддержки рисует текстовую часть кнопки с многострочным текстом. Эта функция вызывается внутри функцией gx_multi_line_text_button_draw() и предоставляется как отдельный API для удобства работы приложений, которые определяют настраиваемую функцию рисования кнопок с многострочным текстом. Приложения, которым требуется настроить фоновый рисунок кнопки, могут предоставить свою настраиваемую функцию рисования и вызвать службу multi_line_text_button_text_draw в составе настраиваемого рисования, чтобы нарисовать текст на кнопке поверх фона.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define a custom drawing function */
VOID my_button_draw(GX_MULTI_LINE_TEXT_BUTTON *button)
{
    /* insert code here to draw button background */

    /* call support function to do text drawing */
    gx_multi_line_text_button_text_draw();

    /* draw child widgets */
    gx_widget_children_draw((GX_WIDGET *) button);
}
```

### <a name="see-also"></a>См. также:

- gx_text_button_create
- gx_button_create
- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set


## <a name="gx_multi_line_text_button_text_id_set"></a>gx_multi_line_text_button_text_id_set


Задание идентификатора ресурса текста для кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_button_text_id_set(
    GX_MULTI_LINE_TEXT_BUTTON *text_button,
    RESOURCE_ID string_id)
```

### <a name="description"></a>Описание

Эта служба присваивает указанный идентификатор ресурса строки кнопке с текстом. Строка может содержать символы новой строки, которые выводят текст в нескольких строках в области кнопки.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **string_id**: идентификатор ресурса строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — идентификатор ресурса строки успешно присвоен кнопке с текстом.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the string ID ”MY_STRING_ID” to the text button “my_text_button”. */
status = gx_multi_line_text_button_text_id_set(
    &my_text_button, MY_STRING_ID);

/* If status is GX_SUCCESS, the string ID MY_STRING_ID was set to “my_text_button”. */
```

### <a name="see-also"></a>См. также:

- gx_text_button_create
- gx_button_create
- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set

## <a name="gx_multi_line_text_button_text_set"></a>gx_multi_line_text_button_text_set


Назначение текста для кнопки с текстом (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_mult_line_text_button_text_set(
    GX_MULTI_LINE_TEXT_BUTTON *text_button,
    GX_CHAR *text);
```

### <a name="description"></a>Описание

Эта служба не рекомендуется. Вместо нее следует использовать gx_multi_line_text_button_text_set_ext().

Эта служба назначает указанную строку для кнопки с текстом. Если мини-приложение text_button было создано со стилем GX_STYLE_TEXT_COPY, то мини-приложение создает частную копию назначенной текстовой строки, поэтому перед запросом этой службы необходимо один раз вызвать API gx_system_memmory_allocate_set. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **text**: указатель на строку, завершающуюся NULL

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно присвоен кнопке.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_MEMORY_ERROR** (0x30) — не определен распределитель памяти.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
static GX_CHAR text[] = “myrstring”;

/* Set text to the text button “my_text_button”. */
status = gx_multi_line_text_button_text_set(&my_text_button, text);

/* If status is GX_SUCCESS, the text of “my_text_button” was set. */
```

### <a name="see-also"></a>См. также:

- gx_text_button_create
- gx_button_create
- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set

## <a name="gx_multi_line_text_button_text_set_ext"></a>gx_multi_line_text_button_text_set_ext


Назначение текста для кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_mult_line_text_button_text_set_ext(
    GX_MULTI_LINE_TEXT_BUTTON *text_button,
    GX_STRING *string)
```

### <a name="description"></a>Описание

Эта служба назначает указанную строку для кнопки с текстом. Если мини-приложение text_button было создано со стилем GX_STYLE_TEXT_COPY, то мини-приложение создает частную копию назначенной текстовой строки, поэтому перед запросом этой службы необходимо один раз вызвать API gx_system_memmory_allocate_set. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **string**: указатель на переменную GX_STRING

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно присвоен кнопке.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_MEMORY_ERROR** (0x30) — не определен распределитель памяти.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
static GX_CHAR text[] = “myrstring”;
GX_STRING string;

string.gx_string_ptr = text;
string.gx_string_length = strlen(text);

/* Set text to the text button “my_text_button”. */
status = gx_multi_line_text_button_text_set_ext(&my_text_button, string);

/* If status is GX_SUCCESS, the text of “my_text_button” was set. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_button_draw
- gx_multi_line_text_button_event_process
- gx_multi_line_text_button_text_set
- gx_multi_line_text_button_text_id_set

## <a name="gx_multi_line_text_input_backspace"></a>gx_multi_line_text_input_backspace


Удаление символа до позиции курсора ввода многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_backspace(
    GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба удаляет символ перед позицией курсора ввода многострочного текста. Эта служба вызывается внутри при получении события нажатия клавиши BACKSPACE, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — удаление ввода многострочного текста успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x23) — недопустимое мини-приложение.
- **GX_FAILURE** (0x10) — недопустимый шрифт.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete a character before the cursor of “my_text_input”. */
status = gx_multi_line_text_input_backspace(&my_text_input);

/* If status is GX_SUCCESS the character before the cursor has been deleted. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_buffer_clear"></a>gx_multi_line_text_input_buffer_clear


Удаление всех символов из буфера ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_buffer_clear(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба удаляет все символы из буфера ввода текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — буфер ввода многострочного текста успешно очищен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* clear input buffer of “my_text_input”. */
status = gx_multi_line_text_input_clear(&my_text_input);

/* If status is GX_SUCCESS the text input widget has emptied its input buffer. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_buffer_get"></a>gx_multi_line_text_input_buffer_get


Извлечение сведений о буфере мини-приложения ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_buffer_get(
    GX_MULTI_LINE_TEXT_INPUT *text_input, 
    GX_CHAR **buffer_address,
    UINT *content_size, 
    UINT *buffer_size);
```

### <a name="description"></a>Описание

Эта служба извлекает сведения о буфере мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **buffer_address**: адрес буфера ввода
- **content_size**: число байтов входных данных
- **buffer_size**: размер входных данных

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — многострочный текст успешно получен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *buffer_address;
UINT context_size;
UINT buffer_size;

/* Retrieves buffer information of “my_text_input” widget. */
status = gx_multi_line_text_input_buffer_get(&my_text_input,
    &buffer_address, &string_size,
    &buffer_size);

/* If status is GX_SUCCESS the value of buffer_address, string_size and buffer_size has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_char_insert"></a>gx_multi_line_text_input_char_insert


Вставка строки символов в текущей позиции курсора ввода многострочного текста (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_char_insert(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_UBYTE *insert_str,
    UINT insert_size);
```

### <a name="description"></a>Описание

Этот API является устаревшим. Вместо него используется gx_multi_line_text_input_char_insert_ext().

Эта служба вставляет символьную строку в буфер строки ввода многострочного текста в текущей позиции курсора. Эта служба вызывается внутри при получении события нажатия определенной клавиши со стрелкой, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **insert_str**: строка символов в формате UTF-8 для вставки
- **insert_size**: число байтов для вставки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка символов успешно вставлена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер строки.
- **GX_FAILURE** (0x10) — недопустимый шрифт или недостаточный размер буфера.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Insert characters at current cursor position. */
GX_CHAR insert_text[10] = “insert”;
status = gx_multi_line_text_input_char_insert(&my_text_input,
    insert_text,
    GX_STRLEN(insert_text));

/* If status is GX_SUCCESS the multi line text input widget has successfully insert the string. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_char_insert_ext

## <a name="gx_multi_line_text_input_char_insert_ext"></a>gx_multi_line_text_input_char_insert_ext


Вставка строки символов в текущей позиции курсора ввода многострочного текста (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_char_insert_ext(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_CONST GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба вставляет символьную строку в буфер строки ввода многострочного текста в текущей позиции курсора. Эта служба вызывается внутри при получении событий нажатия определенной клавиши со стрелкой, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **string**: строка символов в кодировке UTF-8 для вставки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка символов успешно вставлена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер строки.
- **GX_FAILURE** (0x10) — недопустимый шрифт или недостаточный размер буфера.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Insert characters at current cursor position. */
GX_CHAR insert_text[10] = “insert”;
GX_STRING string;

string.gx_string_ptr = insert_text;
string.gx_string_length = strlen(insert_text);

status = gx_multi_line_text_input_char_insert_ext(&my_text_input, &string);

/* If status is GX_SUCCESS the multi line text input widget has successfully inserted the string. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_create"></a>gx_multi_line_text_input_create


Создание ввода многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_create(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_CONST GX_CHAR *name, GX_WINDOW *parent,
    GX_CHAR *input_buffer, UINT buffer_size,
    ULONG style, USHORT text_input_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение ввода многострочного текста.

GX_MULTI_LINE_TEXT_INPUT является производным от GX_MULTI_LINE_TEXT_VIEW и поддерживает все службы gx_multi_line_text_view.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **name**: имя мини-приложения ввода текста
- **parent**: указатель на родительское мини-приложение
- **input_buffer**: указатель на буфер ввода текста
- **buffer_size**: размер буфера ввода (в байтах)
- **style**: стиль мини-приложения ввода текста Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **text_input_id**: определяемый приложением идентификатор ввода текста
- **size**: размеры мини-приложения ввода текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ввод многострочного текста успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_MULTI_LINE_TEXT_INPUT my_text_input;
GX_CHAR my_buffer[100];
GX_RECTANGLE size;

/* Define widget size. */
gx_utility_rectangle_define(&size, 10, 10, 100, 200);

/* Create multi-line text input widget “my_text_input”. */
status = gx_multi_line_text_input_create(&my_text_input,
    “my_text_input”, &my_parent,
    my_buffer, 100, GX_STYLE_BORDER_RAISED,
    MY_TEXT_INPUT_ID, &size);

/* If status is GX_SUCCESS, the text input “my_text_input” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_cursor_pos_get"></a>gx_multi_line_text_input_cursor_pos_get


Получение позиции курсора ввода многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_cursor_pos_get(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_POINT cursor_pos);
```

### <a name="description"></a>Описание

Эта служба получает позицию курсора ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **cursor_pos**: полученное расположение курсора

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — положение курсора успешно получено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Retrieve the cursor position of “my_text_input”. */
GX_POINT cursor_pos;
status = gx_multi_line_text_input_cursor_pos_get(&my_text_input,
    &cursor_pos);

/* If status is GX_SUCCESS the cursor position has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_delete"></a>gx_multi_line_text_input_delete


Удаление символа в позиции курсора ввода многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_delete(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба удаляет символ после позиции курсора ввода многострочного текста. Эта служба вызывается внутри при получении события нажатия клавиши DELETE, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — символ после курсора успешно удален.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_FAILURE** (0x10) — недопустимый шрифт.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete the character after the cursor of “my_text_input”. */
status = gx_multi_line_text_input_delete(&my_text_input);

/* If status is GX_SUCCESS the character after the cursor has been deleted. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_down_arrow"></a>gx_multi_line_text_input_down_arrow


Перемещение курсора ввода многострочного текста на следующую строку

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_down_arrow(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба помещает курсор мини-приложения ввода многострочного текста на следующую строку. Эта служба вызывается внутри при получении события нажатия клавиши СТРЕЛКА ВНИЗ, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор ввода текста успешно перемещен на следующую строку.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_FAILURE** (0x10) — недопустимый шрифт или высота строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move input cursor to the next line. */
status = gx_multi_line_text_input_down_arrow(&my_text_input);

/* If status is GX_SUCCESS, text text input cursor has been moved to the next line. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_end"></a>gx_multi_line_text_input_end


Перемещение курсора ввода многострочного текста в конец текущей строки

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_end(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба помещает курсор мини-приложения ввода многострочного текста в конец текущей строки. Эта служба вызывается внутри при получении события нажатия клавиши END, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор ввода текста успешно перемещен в конец текущей строки.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move input cursor to the end of current line. */
status = gx_multi_line_text_input_end(&my_text_input);

/* If status is GX_SUCCESS, the multi line text input cursor has been moved to the end of the current line. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_event_process"></a>gx_multi_line_text_input_event_process


Обработка событий по умолчанию для ввода многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_event_process(
    GX_MULTI_LINE_TEXT_INPUT *input,
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба является функцией обработки событий по умолчанию для мини-приложения ввода многострочного текста. Эта функция становится доступной для приложений, которые хотят обеспечить настраиваемую обработку событий для мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **button**: блок управления вводом многострочного текста
- **event_ptr**: обрабатываемое событие

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT MyEventHandler(GX_MULTI_LINE_TEXT_INPUT *input,
    GX_EVENT *event_ptr)
{
    switch(event->gx_event_type)
    {
    case xyz:
        /* insert custom event handling here */
        break;
    default:
        /* pass all other events to the generic multi line text
            input event processing */
        gx_multi_line_text_input_event_process(input, event_ptr);
        break;
    }
    return GX_SUCCESS;
}
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_fill_color_set"></a>gx_multi_line_text_input_fill_color_set


Задание цвета фона для ввода многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_fill_color_set(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_RESOURCE_ID normal_fill_color_id,
    GX_RESOURCE_ID selected_fill_color_id,
    GX_RESOURCE_ID disabled_fill_color_id,
    GX_RESOURCE_ID readonly_fill_color_id);
```

### <a name="description"></a>Описание

Эта служба задает цвета заливки для мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **normal_fill_color_id**: идентификатор ресурса обычного цвета заливки, который используется в нормальном состоянии
- **selected_fill_color_id**: идентификатор ресурса выбранного цвета заливки, который используется, когда фокус перемещается на мини-приложение
- **disabled_fill_color_id**: идентификатор ресурса цвета заливки неактивного элемента, используемого GX_STYLE_ENABLED
- **readonly_fill_color_id**: идентификатор ресурса цвета заливки объекта, доступного только для чтения, который используется, когда активны GX_STYLE_ENABLED и GX_STYLE_INPUT_READONLY

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвета для ввода многострочного текста успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set fill colors for the multi-line text input widget “my_text_input”. */

status = gx_multi_line_text_input_fill_color_set(&my_text_input,
    GX_COLOR_ID_NORMAL_FILL,
    GX_COLOR_ID_SELECTED_FILL,
    GX_COLOR_ID_DISABLED_FILL,
    GX_COLOR_ID_READONLY_FILL);

/* If status is GX_SUCCESS, the fill color of “my_text_input” has been successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_home"></a>gx_multi_line_text_input_home


Перемещение курсора ввода текста в начало текущей строки

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_home(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает позицию курсора текстового ввода в начало текущей строки. Эта служба вызывается внутри при получении события нажатия клавиши HOME, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен в начало текущей строки.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move cursor to the start of the current line. */
status = gx_multi_line_text_input_home(&my_text_input);

/* If status is GX_SUCCESS the cursor has been moved to the start of the current line. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_left_arrow"></a>gx_multi_line_text_input_left_arrow


Перемещение курсора ввода многострочного текста на один символ влево

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_left_arrow(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает курсор ввода многострочного текста на один символ влево. Эта служба вызывается внутри при получении события нажатия клавиши СТРЕЛКА ВЛЕВО, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен влево.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_FAILURE** (0x10) — недопустимый шрифт.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move the cursor one character to the left. */
status = gx_multi_line_text_input_left_arrow(&my_text_input);

/* If status is GX_SUCCESS the multi line text input cursor has been moved one character to the left. */

```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_right_arrow"></a>gx_multi_line_text_input_right_arrow


Перемещение курсора ввода многострочного текста на один символ вправо

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_right_arrow(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает курсор ввода многострочного текста на один символ вправо. Эта служба вызывается внутри при получении события нажатия клавиши СТРЕЛКА ВПРАВО, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен вправо.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move cursor one character to the right. */
status = gx_multi_line_text_input_right_arrow(&my_text_input);

/* If status is GX_SUCCESS the text input cursor has been moved one character to the right. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_style_add"></a>gx_multi_line_text_input_style_add


Добавление стилей многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_style_add(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба добавляет стили для мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **style**: стиль для добавления Предварительно заданные общие стили для всех мини-приложений см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль ввода многострочного текста успешно добавлен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Add style GX_STYTLE_CURSOR_ALWAYS to multi-line text input widget “my_text_input”. */
status = gx_multi_line_text_input_style_add(&my_text_input,
    GX_STYLE_CURSOR_ALWAYS);

/* If status is GX_SUCCESS the style GX_STYLE_CURSOR_ALWAYS has been successfully added. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_style_remove"></a>gx_multi_line_text_input_style_remove


Удаление стилей

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_remove(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба удаляет указанные стили из мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **style**: стили для удаления Предварительно заданные общие стили для всех мини-приложений см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ввод многострочного текста успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Remove style GX_STYLE_CURSOR_ALWAYS from text input widget “my_text_input”. */
status = gx_multi_line_text_input_style_remove(&my_text_input,
    GX_STYLE_CURSOR_ALWAYS);

/* If status is GX_SUCCESS, the style GX_STYLE_CURSOR_ALWAYS has been successfully removed. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_style_set"></a>gx_multi_line_text_input_style_set

Задание стилей многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_style_set(
    GX_MULTI_LINE_TEXT_INPUT *text_input, 
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба задает стили для мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **style**: стиль для задания Предварительно заданные общие стили для всех мини-приложений см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль ввода многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set style GX_STYLE_CURSOR_ALWAYS for text input widget “my_text_input”. */
status = gx_multi_line_text_input_style_set(&my_text_input,
    GX_STYLE_CURSOR_ALWAYS);

/* If status is GX_SUCCESS the text input style has been set */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_text_color_set"></a>gx_multi_line_text_input_text_color_set


Задание цвета многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_text_color_set(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_RESOURCE_ID normal_text_color_id,
    GX_RESOURCE_ID selected_text_color_id,
    GX_RESOURCE_ID disabled_text_color_id,
    GX_RESOURCE_ID readonly_text_color_id);
```

### <a name="description"></a>Описание

Эта служба назначает цвета текста для мини-приложения ввода многострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода многострочного текста
- **normal_fill_color_id**: идентификатор ресурса обычного цвета текста, который используется в нормальном состоянии
- **selected_text_color_id**: идентификатор ресурса цвета выбранного текста, который используется, когда фокус перемещается на мини-приложение
- **disabled_text_color_id**: идентификатор ресурса цвета неактивного текста, используемого GX_STYLE_ENABLED
- **readonly_text_color_id**: идентификатор ресурса цвета текста, доступного только для чтения, который используется, когда активны GX_STYLE_ENABLED и GX_STYLE_TEXT_INPUT_READONLY

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвета для ввода многострочного текста успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set text colors for the multi-line text input widget “my_text_input”. */
status = gx_multi_line_text_input_text_color_set(&my_text_input,
    GX_COLOR_ID_NORMAL_TEXT,
    GX_COLOR_ID_SELECTED_TEXT,
    GX_COLOR_ID_DISABLED_TEXT,
    GX_COLOR_ID_READONLY_TEXT);

/* If status is GX_SUCCESS, the fill colors of “my_text_view” has been successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_text_select"></a>gx_multi_line_text_input_text_select


Выделение текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_text_select(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    UINT start_index, UINT end_index);
```

### <a name="description"></a>Описание

Эта служба выбирает введенный многострочный текст с указанной начальной и конечной метками и выделяет выбранный текст, используя выбранные цвета заливки и текст.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом многострочного текста
- **start_index**: индекс первого выбранного символа
- **end_index**: индекс последнего выбранного символа

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — многострочный текст успешно выбран.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение индекса.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Select text between index [0, 9]. */
status = gx_multi_line_text_input_text_select(&my_text_input,
    0,9);

/* If status is GX_SUCCESS, the text between 
    index [0, 9] “my_text_input” was selected. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_text_set"></a>gx_multi_line_text_input_text_set


Назначение текста для ввода (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_mult_line_text_input_text_set(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_CHAR *text);
```

### <a name="description"></a>Описание

Этот API является устаревшим. Вместо него используется gx_multi_line_text_input_text_set_ext().

Эта служба назначает указанную строку для ввода многострочного текста. Если размер входного буфера мини-приложения multi_line_text_input меньше длины строки, строка будет усечена.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом многострочного текста
- **text**: указатель на строку, завершающуюся NULL

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст для ввода многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the string “my string” to the text button “my_text_input”. */
status = gx_multi_line_text_input_text_set(&my_text_input,
    “myrstring”);

/* If status is GX_SUCCESS, the content of “my_text_input” bas been reset. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_text_set_ext

## <a name="gx_multi_line_text_input_text_set_ext"></a>gx_multi_line_text_input_text_set_ext


Назначение текста для ввода

### <a name="prototype"></a>Прототип

```C
UINT gx_mult_line_text_input_text_set(
    GX_MULTI_LINE_TEXT_INPUT *text_input,
    GX_CONST GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба назначает указанную строку для ввода многострочного текста. Если размер входного буфера мини-приложения multi_line_text_input меньше длины строки, строка будет усечена.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом многострочного текста
- **string**: указатель на GX_STRING для назначения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст для ввода многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the string “my string” to the text button “my_text_input”. */
GX_STRING string;
string.gx_string_ptr = “myrstring”;
string.gx_string_length = strlen(string.gx_string_ptr);

status = gx_multi_line_text_input_text_set_ext(&my_text_input,
    &string);

/* If status is GX_SUCCESS, the content of “my_text_input” bas been reset. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_input_up_arrow"></a>gx_multi_line_text_input_up_arrow


Перемещение курсора ввода многострочного текста на предыдущую строку

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_input_up_arrow(GX_MULTI_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает курсор ввода многострочного текста на предыдущую строку текста. Эта служба вызывается внутри при получении события нажатия клавиши СТРЕЛКА ВНИЗ/ВВЕРХ, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен на предыдущую строку.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move the cursor to the previous line. */
status = gx_multi_line_text_input_up_arrow(&my_text_input);

/* If status is GX_SUCCESS the multi line text input cursor has been moved to the previous line. */

```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_create"></a>gx_multi_line_text_view_create


Создание представления многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_create(
    GX_MULTI_LINE_TEXT_VIEW *text_view,
    GX_CONST GX_CHAR *name, 
    GX_WINDOW *parent, 
    GX_RESOURCE_ID text_id,
    ULONG style, 
    USHORT text_view_id, 
    GX_CONST GX_RECTANGLE *size);

```

### <a name="description"></a>Описание

Эта служба создает мини-приложение GX_MULTI_LINE_TEXT_VIEW. Этот тип мини-приложения является производным от GX_WINDOW, поэтому все службы API gx_window могут также использоваться с этим типом мини-приложения.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **name**: имя мини-приложения представления текста
- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса текстовой строки
- **style**: стиль мини-приложения представления текста Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **text_view_id**: определяемый приложением идентификатор представления текста
- **size**: размеры мини-приложения представления текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение представления текста успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create multi-line text view widget “my_text_view”. */
status = gx_multi_line_text_view_create(&my_text_view,
    “my_text_view”, &my_parent,
    TEXT_STRING_ID, GX_STYLE_BORDER_RAISED,
    MY_TEXT_VIEW_ID, &size);

/* If status is GX_SUCCESS the text view “my_text_view” has been successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_draw"></a>gx_multi_line_text_view_draw


Вывод мини-приложения представления многострочного текста

### <a name="prototype"></a>Прототип

```C
VOID gx_multi_line_text_view_draw(GX_MULTI_LINE_TEXT_VIEW * text_view);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение представления многострочного текста. Эта служба обычно вызывается внутри во время обновления холста, но также может вызываться из настраиваемых функций рисования представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom multi line text view drawing function. */

VOID my_multi_line_text_view_draw(GX_MULTI_LINE_TEXT_VIEW *view)
{
    /* Call default multi line text view draw. */
    gx_multi_line_text_view_draw(view);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_event_process"></a>gx_multi_line_text_view_event_process


Обработка события представления многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_event_process(
    GX_MULTI_LINE_TEXT_VIEW *text_view, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для мини-приложения представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие представления многострочного текста успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom event handler. */
UINT my_event_handler(GX_MULTI_LINE_TEXT_VIEW *view, GX_EVENT *event_ptr)
{
    switch(event->gx_event_type)
    {
    case GX_EVENT_SHOW:
        gx_multi_line_text_view_event_process(view, event_ptr);
        /* Add custom actions here. */
        break;
    default:
        gx_multi_line_text_view_event_process (view, event_ptr);
        break;
    }
    return GX_SUCCESS;
}
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_font_set"></a>gx_multi_line_text_view_font_set


Задание шрифта, используемого в представлении многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_text_id_set(
    GX_MULTI_LINE_TEXT_VIEW *text_view, 
    GX_RESOURCE_ID font_id);
```

### <a name="description"></a>Описание

Эта служба задает шрифт мини-приложения представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **font_id**: идентификатор ресурса для шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт для представления многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set font ID FONT_ID to the multi-line text view widget “my_text_view”. */
status = gx_multi_line_text_view_font_set(&my_text_view, FONT_ID);

/* If status is GX_SUCCESS, the text view “my_text_view” will use the specified font to display its text. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_line_space_set"></a>gx_multi_line_text_view_line_space_set


Задание промежутка между строками представления многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_line_space_set(
    GX_MULTI_LINE_TEXT_VIEW *text_view, 
    GX_BYTE line_space);
```

### <a name="description"></a>Описание

Эта служба задает интервал между строками текста для мини-приложения представления многострочного текста.

### <a name="parameters"></a>Параметры

- **view**: блок управления мини-приложением представления многострочного текста
- **line_space**: значение для задания

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — интервал между строками для представления многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

```C
/* Set line space of “my_text_view” to 2. */
status = gx_multi_line_text_view_line_space_set(&my_text_view, 2);

/* If status is GX_SUCCESS, the line space of “my_text_view” has been successfully set to 2. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_scroll_info_get"></a>gx_multi_line_text_view_scroll_info_get

Получение сведений о прокрутке представления многострочного текста


### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_scroll_info_get(
    GX_MULTI_LINE_TEXT_VIEW *text_view, ULONG style,
    GX_SCROLL_INFO *info);
```

### <a name="description"></a>Описание

Эта служба получает сведения о прокрутке представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **Style**: GX_SCROLLBAR_HORIZONTAL или GX_SCROLLBAR_VERTICAL
- **Info**: указатель на назначение для сведений о прокрутке Определение структуры GX_SCROLL_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о прокрутке представления многострочного текста успешно получены.
- **GX_FAILURE** (0x10) — мини-приложение не отображается, или недопустимый идентификатор шрифта представления текста.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SCROLL_INFO scroll_info;

/* Get scroll information for multi-line text view “my_text_view”. */
status = gx_multi_line_text_view_scroll_info_get(&my_text_view,
    &scroll_info);

/* If status is GX_SUCCESS the “scroll_info” contains the scroll information for multi-line text view “my_text_view”. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_text_color_set"></a>gx_multi_line_text_view_text_color_set


Задание цвета текста для представления многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_text_color_set(
    GX_MULTI_LINE_TEXT_VIEW *text_view,
    GX_RESOURCE_ID normal_text_color_id,
    GX_RESOURCE_ID selected_text_color_id,
    GX_RESOURCe_ID disabled_text_color_id);
```

### <a name="description"></a>Описание

Эта служба назначает цвет текста для мини-приложения представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **normal_text_color_id**: идентификатор ресурса обычного цвета текста, который используется в нормальном состоянии
- **selected_text_color_id**: идентификатор ресурса цвета выбранного текста, который используется, когда фокус перемещается на мини-приложение
- **disabled_text_color_id**: идентификатор ресурса цвета неактивного текста, используемого GX_STYLE_ENABLED

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвета для представления многострочного текста успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set text colors for the multi-line text view widget “my_text_view”. */
status = gx_multi_line_text_view_text_color_set(&my_text_view,
    GX_COLOR_ID_NORMAL_TEXT,
    GX_COLOR_ID_SELECTED_TEXT,
    GX_COLOR_ID_DISABLED_TEXT);

/* If status is GX_SUCCESS the text color of “my_text_view” has been successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_text_id_set"></a>gx_multi_line_text_view_text_id_set


Задание системной текстовой строки в представлении многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_text_id_set(
    GX_MULTI_LINE_TEXT_VIEW *text_view, 
    GX_RESOURCE_ID text_id);
```

### <a name="description"></a>Описание

Эта служба задает идентификатор ресурса строки для мини-приложения представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **text_id**: идентификатор ресурса текстовой строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — идентификатор строки для представления многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set string ID STRING_ID to the multi-line text view widget “my_text_view”. */
status = gx_multi_line_text_view_text_id_set(&my_text_view, STRING_ID);

/* If status is GX_SUCCESS the text id of “my_text_view” has been successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_text_set"></a>gx_multi_line_text_view_text_set


Задание определяемой пользователем строки как представления многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_text_set(
    GX_MULTI_LINE_TEXT_VIEW *text_view, 
    GX_CONST GX_CHAR *text);
```

### <a name="description"></a>Описание

Эта служба назначает текстовую строку для мини-приложения представления многострочного текста. Если мини-приложение text_view было создано со стилем GX_STYLE_TEXT_COPY, то мини-приложение создает частную копию назначенной текстовой строки, поэтому перед запросом этой службы необходимо один раз вызвать API gx_system_memory_allocate_set. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **text**: текстовая строка, заканчивающаяся NULL

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка для представления многострочного текста успешно задана.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set string “my string” to the multi-line text view widget “my_text_view”. */
status = gx_multi_line_text_view_text_set(&my_text_view, “my string”);

/* If status is GX_SUCCESS the text of “my_text_view” has been successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_whitespace_set

## <a name="gx_multi_line_text_view_whitespace_set"></a>gx_multi_line_text_view_whitespace_set


Задание пробела в представлении многострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_multi_line_text_view_whitespace_set(
    GX_MULTI_LINE_TEXT_VIEW *text_view, 
    GX_UBYTE whitespace);
```

### <a name="description"></a>Описание

Эта служба задает промежуток между контурами мини-приложения и клиентской областью для мини-приложения представления многострочного текста.

### <a name="parameters"></a>Параметры

- **text_view**: блок управления мини-приложением представления многострочного текста
- **whitespace**: ширина промежутка между мини-приложением text_view и отображаемым текстом (в пикселях)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — пробел для представления многострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set whitespace of “my_text_view” to 2. */
status = gx_multi_line_text_view_whitespace_set(&my_text_view, 2);

/* If status is GX_SUCCESS the whitespace of “my_text_view” has been successfully set to 2. */
```

### <a name="see-also"></a>См. также:

- gx_multi_line_text_input_backspace
- gx_multi_line_text_input_buffer_clear
- gx_multi_line_text_input_buffer_get
- gx_multi_line_text_input_char_insert
- gx_multi_line_text_input_create
- gx_multi_line_text_input_cursor_pos_get
- gx_multi_line_text_input_delete
- gx_multi_line_text_input_down_arrow
- gx_multi_line_text_input_end
- gx_multi_line_text_input_event_process
- gx_multi_line_text_input_fill_color_set
- gx_multi_line_text_input_home
- gx_multi_line_text_input_left_arrow
- gx_multi_line_text_input_right_arrow
- gx_mutli_line_text_input_style_add
- gx_multi_line_text_input_style_remove
- gx_multi_line_text_input_style_set
- gx_multi_line_text_input_text_color_set
- gx_multi_line_text_input_text_select
- gx_multi_line_text_input_text_set
- gx_multi_line_text_input_up_arrow
- gx_multi_line_text_view_create
- gx_multi_line_text_view_draw
- gx_multi_line_text_view_event_process
- gx_multi_line_text_view_font_set
- gx_multi_line_text_view_line_space_set
- gx_multi_line_text_view_scroll_info_get
- gx_multi_line_text_view_text_color_set
- gx_multi_line_text_view_text_id_set
- gx_multi_line_text_view_text_set

## <a name="gx_numeric_pixelmap_prompt_create"></a>gx_numeric_pixelmap_prompt_create


Создание числового запроса карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_numeric_pixelmap_prompt_create(
    GX_NUMERIC_PIXELMAP_PROMPT *prompt,
    GX_CONST GX_CHAR name, GX_WIDGET *parent,
    GX_RESOURCE_ID text_id, GX_RESOURCE_ID fill_id,
    ULONG style, USHORT pixelmap_prompt_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает цифровое мини-приложение числового запроса карты отображения пикселей. numeric_pixelmap_prompt — это просто pixelmap_prompt, который имеет собственный буфер и предоставляет API gx_numeric_pixelmap_prompt_value_set(INT); размер буфера определяется константой GX_NUMERIC_PROMPT_BUFFER_SIZE, значение по умолчанию которой — 16.

GX_NUMERIC_PIXELMAP_PROMPT является производным от GX_PIXELMAP_PROMPT и поддерживает все службы API gx_pixelmap_prompt.

### <a name="parameters"></a>Параметры

- **prompt**: блок управления числовым запросом карты отображения пикселей
- **name**: имя запроса
- **parent**: блок управления родительским мини-приложением
- **text_id**: идентификатор строки ресурса
- **fill_id**: идентификатор карты отображения пикселей для области заполнения
- **style**: стиль числового запроса карты отображения пикселей. Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**
- **pixelmap_prompt_id**: идентификатор запроса, определяемый приложением
- **size**: размеры числового запроса карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — числовой запрос карты отображения пикселей успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_numeric_pix_prompt”. */
status = gx_numeric_pixelmap_prompt_create(&my_numeric_pix_prompt,
    “my_numeric_pix_prompt”, &my_parent,
    MY_TEXT_RESOURCE_ID, MY_FEEL_RESOURCE_ID,
    GX_STYLE_BORDER_RAISED, MY_NUMERIC_PIXELMAP_PROMPT_ID,
    &size);

/* If status is GX_SUCCESS the numeric pixelmap prompt “my_numeric_pix_prompt” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_pixelmap_format_function_set
- gx_numeric_pixelmap_prompt_value_set

## <a name="gx_numeric_pixelmap_prompt_format_function_set"></a>gx_numeric_pixelmap_prompt_format_function_set


Переопределение функции форматирования числового запроса карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_numeric_pixelmap_format_function_set(
    GX_NUMERIC_PIXELMAP_PROMPT *prompt,
    OID (*format_func)(GX_NUMERIC_PIXELMAP_PROMPT *, INT));
```

### <a name="description"></a>Описание

Эта служба переопределяет функцию форматирования по умолчанию для мини-приложения числового запроса карты отображения пикселей. Функция форматирования по умолчанию преобразует значение числового запроса карты отображения пикселей в строку и сохраняет ее в частном буфере мини-приложения. Эта служба позволяет приложению определить собственную функцию форматирования для форматирования и сохранения значения числового запроса карты отображения пикселей в частном буфере мини-приложения.

### <a name="parameters"></a>Параметры

- **prompt**: блок управления числовым запросом карты отображения пикселей
- **format_func**: задаваемая функция форматирования

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — функция форматирования числового запроса карты отображения пикселей успешно задана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define my numeric pixelmap format function. */
VOID my_format_function(GX_NUMERIC_PIXELMAP_PROMPT *prompt,
    INT value)
{
    /* If the value is “1234”, the new format will be “12.34”. */

    INT length;
    gx_utility_ltoa(value / 100,
        prompt->gx_numeric_pixelmap_prompt_buffer,
        GX_NUMERIC_PROMPT_ BUFFER_SIZE);
    Length = GX_STRLEN(prompt->gx_numeric_pixelmap_prompt_buffer);
    prompt->gx_numeric_pixelmap_prompt_buffer[length++] = ‘.’;
    gx_utility_ltoa(value % 100,
        prompt->gx_numeric_pixelmap_prompt_buffer + length,
        GX_NUMERIC_PROMPT_BUFFER_SIZE - length);
}

/* Override default format function of “my_numeric_pix_prompt”. */
status = gx_numeric_pixelmap_prompt_format_function_set(
    &my_numeric_pix_prompt,
    my_format_function);

/* If status is GX_SUCCESS the format function of “my_numeric_pix_prompt” has been override. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_pixelmap_prompt_create
- gx_numeric_pixelmap_prompt_value_set

## <a name="gx_numeric_pixelmap_prompt_value_set"></a>gx_numeric_pixelmap_prompt_value_set


Задание числового значения запроса карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_numeric_pixelmap_prompt_value_set(
    GX_NUMERIC_PIXELMAP_PROMPT *prompt,
    INT value);
```

### <a name="description"></a>Описание

Эта служба задает целочисленное значение для числового запроса карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **prompt**: блок управления числовым запросом карты отображения пикселей
- **value**: задаваемое целочисленное значение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение числового запроса карты отображения пикселей успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set a value to “my_numeric_pix_prompt”. */
status = gx_numeric_pixelmap_prompt_value_set(&my_numeric_pix_prompt, 1000);

/* If status is GX_SUCCESS the value of the numeric pixelmap prompt “my_numeric_pix_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_pixelmap_prompt_create
- gx_numeric_pixelmap_format_function_set

## <a name="gx_numeric_prompt_create"></a>gx_numeric_prompt_create


Создание числового запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_numeric_prompt_create( 
    GX_NUMERIC_PROMPT *prompt,
    GX_CONST GX_CHAR name, GX_WIDGET *parent,
    GX_RESOURCE_ID text_id,
    ULONG style, USHORT prompt_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение числового запроса. numeric_prompt — это просто запрос, который имеет собственный буфер и предоставляет API gx_numeric_prompt_value_set(INT); размер буфера определяется константой GX_NUMERIC_PROMPT_BUFFER_SIZE, значение по умолчанию которой — 16.

GX_NUMERIC_PROMPT является производным от GX_PROMPT и поддерживает все службы API gx_prompt.

### <a name="parameters"></a>Параметры

- **prompt**: блок управления числовым запросом
- **name**: имя запроса
- **parent**: блок управления родительским мини-приложением
- **text_id**: идентификатор строки ресурса
- **style**: стиль числового запроса. Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**
- **prompt_id**: идентификатор запроса, определяемый приложением
- **size**: размеры числового запроса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — числовой запрос успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_numeric _prompt”. */
status = gx_numeric_prompt_create(&my_numeric_prompt,
    “my_numeric_prompt”, &my_parent,
    MY_TEXT_RESOURCE_ID, GX_STYLE_BORDER_RAISED, MY_NUMERIC_PROMPT_ID,
    &size);

/* If status is GX_SUCCESS the numeric prompt “my_numeric_prompt” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_format_function_set
- gx_numeric_prompt_value_set

## <a name="gx_numeric_prompt_format_function_set"></a>gx_numeric_prompt_format_function_set


Переопределение функции форматирования числового запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_numeric_format_function_set(
    GX_NUMERIC_PROMPT *prompt,
    VOID (*format_func)(GX_NUMERIC_PROMPT *, INT));
```

### <a name="description"></a>Описание

Эта служба переопределяет функцию форматирования по умолчанию для мини-приложения числового запроса. Функция форматирования по умолчанию преобразует значение числового запроса в строку и сохраняет ее в частном буфере мини-приложения. Эта служба позволяет приложению определить собственную функцию форматирования для форматирования и сохранения значения числового запроса в частном буфере мини-приложения.

### <a name="parameters"></a>Параметры

- **prompt**: блок управления числовым запросом
- **format_func**: задаваемая функция форматирования

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — функция форматирования числового запроса успешно задана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define my numeric format function. */
VOID my_format_function(GX_NUMERIC_PROMPT *prompt, INT value)
{
    /* If the value is “1234”, the new format will be “12.34”. */

    INT length;
    gx_utility_ltoa(value / 100, prompt->gx_numeric_prompt_buffer,
        GX_NUMERIC_PROMPT_ BUFFER_SIZE);
    Length = GX_STRLEN(prompt->gx_numeric_prompt_buffer);
    prompt->gx_numeric_prompt_buffer[length++] = ‘.’;
    gx_utility_ltoa(value % 100,
        prompt->gx_numeric_prompt_buffer + length,
        GX_NUMERIC_PROMPT_BUFFER_SIZE - length);
}

/* Override the default format function of “my_numeric_prompt”. */
status = gx_numeric_prompt_format_function_set(&my_numeric_prompt,
    my_format_function);

/* If status is GX_SUCCESS, the format function of “my_numeric_prompt” has been override. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_prompt_create
- gx_numeric_prompt_value_set

## <a name="gx_numeric_prompt_value_set"></a>gx_numeric_prompt_value_set


Задание значения числового запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_numeric_prompt_value_set(
    GX_NUMERIC_PROMPT *prompt, 
    INT value);
```

### <a name="description"></a>Описание

Эта служба задает целочисленное значение для числового запроса.

### <a name="parameters"></a>Параметры

- **prompt**: блок управления числовым запросом
- **value**: задаваемое целочисленное значение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение числового запроса успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set a value to “my_numeric_prompt”. */
status = gx_numeric_prompt_value_set(&my_numeric_prompt, 1000);

/* If status is GX_SUCCESS the value of the numeric prompt “my_numeric_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_prompt_create
- gx_numeric_format_function_set

## <a name="gx_numeric_scroll_wheel_create"></a>gx_numeric_scroll_wheel_create


Создание числового колесика прокрутки

### <a name="prototype"></a>Прототип


UINT gx_numeric_scroll_wheel_create( GX_NUMERIC_SCROLL_WHEEL *wheel, GX_CONST GX_CHAR *name, GX_WIDGET *parent, INT start_val, INT end_val, ULONG style, USHORT wheel_id, GX_CONST GX_RECTANGLE *size);

### <a name="description"></a>Описание

Эта служба создает мини-приложение числового колесика прокрутки.

Числовое колесико прокрутки — это тип мини-приложения колесика прокрутки, который специально используется для отображения диапазона чисел. Также доступны другие типы мини-приложений колесика прокрутки. Дополнительные сведения об иерархии мини-приложения колесика прокрутки, типах мини-приложений и производных мини-приложения см. в разделе, посвященном API gx_scroll_wheel_create().

GX_NUMERIC_SCROLL_WHEEL является производным от GX_TEXT_SCROLL_WHEEL и поддерживает все службы gx_text_scroll_wheel и gx_scroll_wheel.

Все типы колесика прокрутки создают события GX_EVENT_LIST_SELECT для их родительских элементов при прокрутке колесика прокрутки.

Количество строк числового колесика прокрутки по умолчанию будет составлять abs(end_val – start_val) + 1. Иными словами, колесо прокрутки будет отображать каждое значение между start_val и end_val, увеличивая или уменьшая его на 1 с каждой строкой. Обратите внимание, что start_val может быть больше или меньше end_val, в зависимости от того, каким образом приложение хочет отобразить диапазон.

Если приложению требуется изменить шаг приращения строки, это можно сделать, вызвав gx_scroll_wheel_total_rows_set() после создания числового колесика прокрутки. Например, приложение, желающее создать колесо прокрутки, которое отображает значения в годах с 1980 г. по 2020 г. с шагом 5 лет, может сделать следующее.

```C
gx_numeric_scroll_wheel_create(&wheel, GX_NULL, parent, 1980,
    2020, style, id, &size);

/* the years 1980 through 2020, inclusive, incrementing by 5 years,
yields 9 total rows */

gx_scroll_wheel_total_rows_set(&wheel, 9);
```

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления числового колесика прокрутки
- **name**: логическое имя мини-приложения кнопки карты отображения пикселей
- **parent**: указатель на родительское мини-приложение
- **start_val**: начальное числовое значение
- **end_val**: конечное числовое значение
- **style**: стиль флажка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **wheel_id**: определяемый приложением идентификатор колесика прокрутки
- **size**: размеры мини-приложения колесика прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — числовое колесико прокрутки успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “year_wheel”. */
status = gx_numeric_scroll_wheel_create(&year_wheel,
    “year_selector””, &parent,
    1980, 2040,
    GX_STYLE_ENABLED | GX_STYLE_TEXT_CENTER |
    GX_STYLE_TRANSPARENT | GX_STYLE_WRAP |
    GX_STYLE_TEXT_SCROLL_WHEEL_ROUND,
    YEAR_WHEEL_ID,
    &size);

/* If status is GX_SUCCESS the scroll wheel “year_wheel” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_text_get

## <a name="gx_numeric_scroll_wheel_range_set"></a>gx_numeric_scroll_wheel_range_set


Назначение диапазона значений для числового колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT
gx_numeric_scroll_wheel_range_set(
    GX_NUMERIC_SCROLL_WHEEL *wheel,
    INT start_val, INT end_val);
```

### <a name="description"></a>Описание

Эта служба изменяет диапазон значений, допустимый и отображаемый мини-приложением числового колесика прокрутки.

Числовое колесико прокрутки — это тип мини-приложения колесика прокрутки, который специально используется для отображения диапазона чисел. Также доступны другие типы мини-приложений колесика прокрутки. Дополнительные сведения об иерархии мини-приложения колесика прокрутки, типах мини-приложений и производных мини-приложения см. в разделе, посвященном API gx_scroll_wheel_create().

Вызов этого API приводит к сбросу общего числа строк колесика прокрутки до abs (end_val – start_val) + 1, что означает, что колесико прокрутки увеличивается на 1 для каждой строки. Чтобы изменить это, приложение может вызвать gx_scroll_wheel_total_rows_set() для изменения общего числа строк, фактически изменив значение шага между строками.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления числового колесика прокрутки
- **start_val**: начальное числовое значение 
- **end_val**: конечное числовое значение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — диапазон для числового колесика прокрутки успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки 

### <a name="example"></a>Пример

```C
/* Change range of “rate” scroll wheel. */

status = gx_numeric_scroll_wheel_range_set(&year_wheel, 0, 200);

/* If status is GX_SUCCESS the scroll wheel range has been modified. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_pixelmap_button_create"></a>gx_pixelmap_button_create


Создание кнопки карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_button_create(
    GX_PIXELMAP_BUTTON *button,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RESOURCE_ID normal_id,
    GX_RESOURCE_ID selected_id,
    GX_RESOURCE_ID disabled_id,
    ULONG style,
    USHORT pixelmap_button_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение кнопки карты отображения пикселей.

GX_PIXELMAP_BUTTON является производным от GX_BUTTON и поддерживает все службы gx_button.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки карты отображения пикселей
- **name**: логическое имя мини-приложения кнопки карты отображения пикселей
- **parent**: указатель на родительское мини-приложение
- **normal_id**: идентификатор ресурса нормального состояния
- **selected_id**: идентификатор ресурса состояния выбора
- **disabled_id**: идентификатор ресурса отключенного состояния
- **style**: стиль флажка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **pixelmap_button_id**: определяемый приложением идентификатор кнопки карты отображения пикселей
- **size**: размеры кнопки карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка карты отображения пикселей успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_pixelmap_button”. */
status = gx_pixelmap_button_create(&my_pixelmap_button,
    “my_pixelmap_button”, &my_parent,
    MY_NORMAL_RESOURCE_ID, MY_SELECTED_RESOURCE_ID,
    MY_DESELECTED_RESOURCE_ID, GX_STYLE_BORDER_RAISED,
    MY_PIXELMAP_BUTTON_ID,
    &size);

/* If status is GX_SUCCESS the pixelmap button “my_pixelmap_button” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_pixelmap_button_draw
- gx_pixelmap_button_pxielmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_radio_button_create
- gx_radio_button_draw
- gx_icon_button_create
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_pixelmap_button_draw"></a>gx_pixelmap_button_draw


Вывод кнопки карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
VOID gx_pixelmap_button_draw(GX_PIXELMAP_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение карты отображения пикселей. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых кнопок карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom pixelmap button drawing function. */

VOID my_pixelmap_button_draw(GX_PIXELMAP_BUTTON *button)
{
    /* Call default pixelmap button draw. */
    gx_pixelmap_button_draw(button);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_pixelmap_button_create
- gx_pixelmap_button_pxielmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_radio_button_create
- gx_radio_button_draw
- gx_icon_button_create
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_pixelmap_button_event_process"></a>gx_pixelmap_button_event_process


Обработка событий кнопки карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_button_event_process(
    GX_PIXELMAP_BUTTON *button,
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обеспечивает обработку событий по умолчанию для типа мини-приложения кнопки карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки карты отображения пикселей
- **event_ptr**: указатель на структуру GX_EVENT

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка карты отображения пикселей успешно нарисована.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
switch(event_ptr->gx_event_type)
{
    case GX_EVENT_SHOW:
        /* Do default handling. */
        status = gx_pixelmap_button_event_process(icon, event_ptr);
    
        /* add my own handling here */
        break;
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_pixelmap_button_create
- gx_pixelmap_button_pxielmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_radio_button_create
- gx_radio_button_draw
- gx_icon_button_create
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_pixelmap_button_pixelmap_set"></a>gx_pixelmap_button_pixelmap_set


Назначение карты отображения пикселей кнопке

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_button_pixelmap_set(
    GX_PIXELMAP_BUTTON *button, 
    GX_RESOURCE_ID normal_id,
    GX_RESOURCE_ID selected_id, 
    GX_RESOURCE_ID disabled_id);
```

### <a name="description"></a>Описание

Эта служба задает для карты отображения пикселей кнопке карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопки карты отображения пикселей
- **normal_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться как нормальное состояние
- **selected_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться при выборе кнопки
- **disabled_id** идентификатор ресурса карты отображения пикселей, который будет использоваться для отключенной кнопки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно задана для кнопки.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw “my_pixelmap_button”. */
status = gx_pixelmap_button_pixelmap_set (&my_pixelmap_button,
    NORMAL_ID, SELECTED_ID,
    DISABLED_ID);

/* If status is GX_SUCCESS the pixelmap button is properly configured with the specified pixelmaps. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_radio_button_create
- gx_radio_button_draw
- gx_icon_button_create
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_pixelmap_prompt_create"></a>gx_pixelmap_prompt_create


Создание запроса карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_prompt_create(
    GX_PIXELMAP_PROMPT *prompt,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RESOURCE_ID text_id,
    GX_RESOURCE_ID fill_pixelmap_id,
    ULONG style,
    USHORT pixelmap_prompt_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение числового запроса карты отображения пикселей. Запрос карты отображения пикселей отличается от стандартного GX_PROMPT тем, что он заполняет фон запроса с помощью карт отображения пикселей. Функция создания принимает один идентификатор карты отображения пикселей — карту отображения пикселей заливки в нормальном состоянии. Для запроса карты отображения пикселей можно назначить до шести карт отображения пикселей.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления запросом карты отображения пикселей
- **name**: логическое имя мини-приложения запроса карты отображения пикселей
- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса текста
- **fill_id**: идентификатор ресурса заливки
- **style**: стиль флажка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **pixelmap_prompt_id**: идентификатор запроса карты отображения пикселей, определяемый приложением
- **size**: размеры ползунка карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запрос карты отображения пикселей успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_pixelmap_prompt”. */
status = gx_pixelmap_prompt_create(&my_pixelmap_prompt,
    “my_pixelmap_prompt”, &my_parent,
    MY_TEXT_RESOURCE_ID, MY_LEFT_RESOURCE_ID,
    MY_FILL_RESOURCE_ID, MY_RIGHT_RESOURCE_ID,
    GX_STYLE_BORDER_RAISED, MY_PIXELMAP_PROMPT_ID,
    &size);

/* If status is GX_SUCCESS the pixelmap prompt “my_pixelmap_prompt” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_button_pixelmap_set
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_get
- gx_prompt_text_set

## <a name="gx_pixelmap_prompt_draw"></a>gx_pixelmap_prompt_draw


Вывод запроса карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
VOID gx_pixelmap_prompt_draw(GX_PIXELMAP_PROMPT *prompt);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение запроса карты отображения пикселей. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых запросов карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления запросом карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom pixelmap prompt drawing function. */

VOID my_pixelmap_button_draw(GX_PIXELMAP_PROMPT *prompt)
{
    /* Call default pixelmap prompt draw. */
    gx_pixelmap_prompt_draw(prompt);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_button_pixelmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_get
- gx_prompt_text_set

## <a name="gx_pixelmap_prompt_pixelmap_set"></a>gx_pixelmap_prompt_pixelmap_set


Назначение карт отображения пикселей запросу

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_prompt_pixelmap_set(
    GX_PIXELMAP_PROMPT *prompt,
    GX_RESOURCE_ID normal_left_pixelmap,
    GX_RESOURCE_ID normal_fill_pixelmap,
    GX_RESOURCE_ID normal_right_pixelmap,
    GX_RESOURCE_ID selected_left_pixelmap,
    GX_RESOURCE_ID selected_fill_pixelmap,
    GX_RESOURCE_ID selected_right_pixelmap);
```

### <a name="description"></a>Описание

Эта служба назначает идентификаторы карты отображения пикселей запросу карты отображения пикселей. Идентификаторы левой, правой карт отображения пикселей, а также карта отображения пикселей для заполнения используются для того, чтобы приложение использовало один набор карт отображения пикселей для запросов различной ширины, но с одинаковой высотой, чтобы сэкономить место. Если идентификаторы левой и правой карт отображения пикселей не используются, им следует присвоить значение 0. Если при получении фокуса ввода запрос должен отрисовываться по-другому, для этой цели используются выбранные идентификаторы карты отображения пикселей. Если выбранные идентификаторы не используются или совпадают с обычными идентификаторами, задайте для них значение 0.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления запросом карты отображения пикселей
- **normal_left_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться в левой части в нормальном состоянии
- **normal_fill_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться в качестве мозаичного заполнения в нормальном состоянии
- **normal_right_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться в правой части в нормальном состоянии
- **selected_left_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться в левой части в состоянии выбора
- **selected_fill_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться в качестве мозаичного заполнения в состоянии выбора
- **selected_right_id**: идентификатор ресурса карты отображения пикселей, который будет использоваться в правой части в состоянии выбора

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно задана для запроса.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Assign pixelmap IDs to “my_prompt”. Only the normal state pixelmaps are used in this case */
status = gx_pixelmap_prompt_pixelmap_set (&my_prompt,
    normal_left_id, normal_fill_id,
    normal_right_id, 0, 0, 0);

/* If status is GX_SUCCESS the pixelmap prompt is properly configured with pixelmaps. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_button_pixelmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_radio_button_create
- gx_radio_button_draw
- gx_icon_button_create
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_pixelmap_slider_create"></a>gx_pixelmap_slider_create


Создание ползунка карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_slider_create(
    GX_PIXELMAP_SLIDER *slider,
    GX_CONST GX_CHAR *name, GX_WIDGET *parent,
    GX_SLIDER_INFO *info,
    GX_PIXELMAP_SLIDER_INFO *pixelmap_info, ULONG style,
    USHORT pixelmap_slider_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение ползунка карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления ползунка карты отображения пикселей
- **name**: логическое имя мини-приложения ползунка карты отображения пикселей
- **parent**: указатель на родительское мини-приложение
- **info**: указатель на структуру GX_SLIDER_INFO, которая содержит значения, определяющие минимальное, максимальное, текущее значения ползунка и ограничения стрелки Определение структуры GX_SLIDER_INFO см. в **приложении I**.
- **pixelmap_info**: указатель на структуру GX_PIXELMAP_SLIDER_INFO, которая определяет карты отображения пикселей, используемые для рисования фона и стрелки Определение структуры GX_PIXELMAP_SLIDER_INFO см. в **приложении I**. Фон ползунка может использовать одну или две карты отображения пикселей. Если фон один, по мере перемещения стрелки фон не меняется. Если заданы два фона, для фона до стрелки используется первая карта отображения пикселей фона, а для фона после стрелки используется вторая карта отображения пикселей фона.
- **style**: стиль ползунка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **pixelmap_slider_id**: определяемый приложением идентификатор ползунка карты отображения пикселей
- **size**: размеры ползунка карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ползунок карты отображения пикселей успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SLIDER_INFO info;
GX_PIXELMAP_SLIDER_INFO pixelmap_info;

/* Initiate slider information structure. */
info.gx_slider_info_min_val = 0;
info.gx_slider_info_max_val = 100;
info.gx_slider_info_current_val = 50;
info.gx_slider_info_min_travel = 10;
info.gx_slider_info_max_tralvel = 10;
info.gx_slider_info_needle_width = 5;
info.gx_slider_info_needle_height = 10;
info.gx_slider_info_needle_inset = 5;
info.gx_slider_info_needle_hotspot_offset;

/* Initiate pixelmap slider information structure. */
pixelmap_info.gx_pixelmap_slider_info_lower_background_pixelmap =
                                        GX_PIXELMAP_ID_ORANGE;
pixelmap_info.gx_pixelmap_slider_info_upper_background_pixelmap =
                                        GX_PIXELMAP_ID_EMPTY;
pixelmap_info.gx_pixelmap_slider_info_needle_pixelmap =
                                        GX_PIXELMAP_ID_NEEDLE;

/* Create “my_pixelmap_slider”. */
status = gx_pixelmap_slider_create(&my_pixelmap_slider,
                            “my_pixelmap_slider”, &my_parent,
                            &info, &pixelmap_info,
                            GX_STYLE_BORDER_RAISED,
                            MY_PIXELMAP_SLIDER_ID, &size);

/* If status is GX_SUCCESS the pixelmap slider “my_pixelmap_slider” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_button_pixelmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_pixelmap_slider_draw"></a>gx_pixelmap_slider_draw


Вывод ползунка карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
VOID gx_pixelmap_slider_draw(GX_PIXELMAP_SLIDER *slider);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение ползунка карты отображения пикселей. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых ползунков карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления ползунка карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom pixelmap slider drawing function. */

VOID my_pixelmap_slider_draw(GX_PIXELMAP_SLIDER *pixelmap_slider)
{
    /* Call default pixelmap slider draw. */
    gx_pixelmap_slider_draw(pixelmap_slider);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_button_pixelmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_pixelmap_slider_event_process"></a>gx_pixelmap_slider_event_process


Обработка события ползунка карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_slider_event_process(
    GX_PIXELMAP_SLIDER *slider,
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного мини-приложения ползунка карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **slider**: указатель на карту отображения пикселей
- **slider control block event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие карты отображения пикселей ползунка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom event processing function. */
UINT my_event_hanlder(GX_PIXELMAP_SLIDER *pixelmap_slider, GX_EVENT *event_ptr)
{
    switch(event_ptr->gx_event_type)
    {
    case GX_EVENT_SHOW:
        /* Do default handling. */
        status = gx_pixelmap_slider_event_process(pixelmap_slider,
                                                    event_ptr);

        /* add my own handling here */
        break;
    default:
        status = gx_pixelmap_slider_event_process(pixelmap_slider,
                                                  event_ptr);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_button_pixelmap_set
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_pixelmap_slider_pixelmap_set"></a>gx_pixelmap_slider_pixelmap_set


Назначение карт отображения пикселей ползунку

### <a name="prototype"></a>Прототип

```C
UINT gx_pixelmap_slider_pixelmap_set(
    GX_PIXELMAP_SLIDER *slider,
    GX_PIXELMAP_SLIDER_INFO *pixinfo);
```

### <a name="description"></a>Описание

Эта служба задает карты отображения пикселей ползунку карты отображения пикселей.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления ползунка карты отображения пикселей
- **pixinfo**: указатель на структуру GX_PIXELMAP_SLIDER_INFO, которая определяет карты отображения пикселей, используемые для рисования фона и стрелки Определение структуры GX_PIXELMAP_SLIDER_INFO см. в **приложении I**. Фон ползунка может использовать одну или две карты отображения пикселей. Если фон один, по мере перемещения стрелки фон не меняется. Если заданы два фона, для фона до стрелки используется первая карта отображения пикселей фона, а для фона после стрелки используется вторая карта отображения пикселей фона.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей для ползунка успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PIXELMAP_SLIDER_INFO pixelmap_info;

/* Initiate pixelmap slider information structure. */
pixelmap_info.gx_pixelmap_slider_info_lower_background_pixelmap =
                                        GX_PIXELMAP_ID_ORANGE;
pixelmap_info.gx_pixelmap_slider_info_upper_background_pixelmap =
                                        GX_PIXELMAP_ID_EMPTY;
pixelmap_info.gx_pixelmap_slider_info_needle_pixelmap =
                                        GX_PIXELMAP_ID_NEEDLE;

/* Draw “my_pixelmap_button”. */
status = gx_pixelmap_slider _pixelmap_set (&my_pixelmap_slider,
                                &pixelmap_info);

/* If status is GX_SUCCESS the pixelmap slider is properly configured with “pixelmap_info”. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_radio_button_create
- gx_radio_button_draw
- gx_icon_button_create
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw

## <a name="gx_progress_bar_background_draw"></a>gx_progress_bar_background_draw


Вывод фона индикатора выполнения

### <a name="prototype"></a>Прототип

```C
VOID gx_progress_bar_background_draw(GX_PROGRESS_BAR *progress_bar)
```

### <a name="description"></a>Описание

Эта служба рисует фон указанного индикатора выполнения. Эта функция вызывается внутри как часть gx_progress_bar_draw(), но предоставляется приложению для поддержки в случаях, когда приложение определяет настраиваемую функцию рисования индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления индикатором выполнения

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom progress bar drawing function. */

VOID my_progress_bar_draw(GX_PROGRESS_BAR *progress_bar)
{
    /* Call default progress bar background draw. */
    gx_progress_bar_background_draw(progress_bar);

    /* Call default progress bar text draw. */
    gx_progress_bar_text_draw(progress_bar);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_value_set

## <a name="gx_progress_bar_create"></a>gx_progress_bar_create


Создание индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_create(
    GX_PROGRESS_BAR *progress_bar,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_PROGRESS_BAR_INFO *progress_bar_info,
    ULONG style, 
    USHORT progress_bar_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **name**: логическое имя
- **parent**: указатель на родительское мини-приложение
- **progress_bar_info**: указатель на структуру GX_PROGRESS_BAR_INFO Определение структуры GX_PROGRESS_BAR_INFO см. в **приложении I**.
- **style**: стиль индикатора выполнения Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **progress_bar_id**: определяемый приложением идентификатор индикатора выполнения
- **size**: размеры индикатора выполнения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — индикатор выполнения успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PROGRESS_BAR_INFO info;
GX_RECTANGLE size;

info.gx_progress_bar_info_min_val = 0;
info.gx_progress_bar_info_max_val = 100;
info.gx_progress_bar_info_current_val = 0;
info.gx_progress_bar_font_id = GX_FONT_ID_SYSTEM_FONT;
info.gx_progress_bar_normal_text_color = GX_COLOR_ID_WHITE;
info.gx_progress_bar_selected_text_color = GX_COLOR_ID_BLUE;
info.gx_progress_bar_fill_pixelmap = 0;

size.gx_rectangle_left = 10;
size.gx_rectangle_top = 10;
size.gx_rectangle_right = 110;
size.gx_rectangle_bottom = 140;

/* Create a progress bar with the specified information. */
status = gx_progress_bar_create(&my_progress_bar, GX_NULL, GX_NULL,
                        &info, GX_STYLE_BORDER_THIN,
                        0, &size);

/* If status is GX_SUCSESS the progress bar “my_progress_bar” has been successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_draw"></a>gx_progress_bar_draw


Вывод индикатора выполнения

### <a name="prototype"></a>Прототип

```C
VOID gx_progress_bar_draw(GX_PROGRESS_BAR *progress_bar);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение индикатора выполнения. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых индикаторов выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom progress bar drawing function. */

VOID my_progress_bar_draw(GX_PROGRESS_BAR *progress_bar)
{
    /* Call default progress bar draw. */
    gx_progress_bar_draw(progress_bar);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_create
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_event_process"></a>gx_progress_bar_event_process


Обработка события индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_event_process(
    GX_PROGRESS_BAR *progress_bar,
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **event_ptr**: указатель на структуру GX_EVENT

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запрос успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom event processing function. */

UINT my_event_process (GX_PROGRESS_BAR *progress_bar, GX_EVENT *event_ptr)
{
    switch(event_ptr->gx_event_type)
    {
    case GX_EVENT_SHOW:
        /* Do default handling. */
        status = gx_progress_bar_event_process(progress_bar,
                                                event_ptr);
        /* add my own handling here */
        break;
    default:
        status = gx_progress_bar_event_process(progress_bar,
                                                event_ptr);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_create
- gx_progress_bar_event_draw
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_font_set"></a>gx_progress_bar_font_set


Задание шрифта текста индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_font_set(
    GX_PROGRESS_BAR *progress_bar,
    GX_RESOURCE_ID font_id);
```

### <a name="description"></a>Описание

Эта служба задает шрифт для мини-приложения индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **font_id**: идентификатор ресурса шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт для индикатора выполнения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status = gx_progress_bar_font_set(&progress_bar,
                                        GX_FONT_ID_MEDIUM);

/* if status is GX_SUCCESS, the font was successfully assigned. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_info_set"></a>gx_progress_bar_info_set


Задание структуры сведений об индикаторе выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_info_set(
    GX_PROGRESS_BAR *progress_bar,
    GX_PROGRESS_BAR_INFO *info);
```

### <a name="description"></a>Описание

Эта служба сбрасывает структуру сведений о мини-приложении индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **info**: указатель на структуру GX_PROGRESS_BAR_INFO Определение структуры GX_PROGRESS_BAR_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения об индикаторе выполнения успешно сброшены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PROGRESS_BAR_INFO info;

info.gx_progress_bar_info_min_val = 0;
info.gx_progress_bar_info_max_val = 100;
info.gx_progress_bar_info_current_val = 0;
info.gx_progress_bar_font_id = GX_FONT_ID_SYSTEM_FONT;
info.gx_progress_bar_normal_text_color = GX_COLOR_ID_WHITE;
info.gx_progress_bar_selected_text_color = GX_COLOR_ID_BLUE;
info.gx_progress_bar_fill_pixelmap = 0;

status = gx_progress_bar_info_set(&progress_bar, &info);

/* if status == GX_SUCCESS the progress bar info was re-assigned. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_info_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_pixelmap_set"></a>gx_progress_bar_pixelmap_set


Задание карты отображения пикселей, используемой для рисования индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_pixelmap_set(
    GX_PROGRESS_BAR *progress_bar,
    GX_RESOURCE_ID pixelmap_id);
```

### <a name="description"></a>Описание

Эта служба задает карту отображения пикселей, используемую для заполнения фона индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **pixelmap_id**: идентификатор ресурса карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей для индикатора выполнения успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status = gx_progress_bar_pixelmap_set(&progress_bar,
                                GX_PIXELMAP_ID_PROGRESS_FILL);

/* if status is GX_SUCCESS, the pixelmap was successfully assigned. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_pielmap_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_range_set"></a>gx_progress_bar_range_set


Задание диапазона значений индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_range_set(
    GX_PROGRESS_BAR *progress_bar,
    INT min_value, 
    INT max_value);
```

### <a name="description"></a>Описание

Эта служба задает диапазон значений индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **MIN_VALUE**: минимальное значение индикатора выполнения
- **MAX_VALUE**: максимальное значение индикатора выполнения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — диапазон значений для индикатора выполнения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status = gx_progress_bar_range_set(progress_bar, 0, 100);

/* if status is GX_SUCCESS, the progress bar range was successfully assigned. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_range_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_text_color_set"></a>gx_progress_bar_text_color_set


Задание цвета текста для индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_text_color_set(
    GX_PROGRESS_BAR *progress_bar,
    GX_RESOURCE_ID normal_text_color,
    GX_RESOURCE_ID selected_text_color,
    GX_RESOURCE_ID disabled_text_color);
```

### <a name="description"></a>Описание

Эта служба задает цвет текста для мини-приложения индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **normal_text_color**: идентификатор ресурса обычного цвета текста, который используется в нормальном состоянии
- **selected_text_color**: идентификатор ресурса цвета выбранного текста, который используется, когда фокус перемещается на мини-приложение
- **disabled_text_color**: идентификатор ресурса цвета неактивного текста, используемого GX_STYLE_ENABLED

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста для индикатора выполнения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set text colors for the progress bar “my_progress_bar”*/
UINT status = gx_progress_bar_text_color_set(&my_progress_bar,
                        GX_COLOR_ID_NORMAL_TEXT,
                        GX_COLOR_ID_SELECTED_TEXT,
                        GX_COLOR_ID_DISABLED_TEXT);

/* if status is GX_SUCCESS, the progress bar text colors were successfully assigned. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_draw
- gx_progress_bar_value_set

## <a name="gx_progress_bar_text_draw"></a>gx_progress_bar_text_draw


Вывод текста индикатора выполнения

### <a name="prototype"></a>Прототип

```C
VOID gx_progress_bar_text_draw(GX_PROGRESS_BAR *progress_bar)
```

### <a name="description"></a>Описание

Эта служба рисует текст указанного индикатора выполнения. Эта функция вызывается внутри как часть gx_progress_bar_draw(), но предоставляется приложению для поддержки в случаях, когда приложение определяет настраиваемую функцию рисования индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления индикатором выполнения

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom progress bar drawing function. */

VOID my_progress_bar_draw(GX_PROGRESS_BAR *progress_bar)
{
    /* Call default progress bar background draw. */
    gx_progress_bar_background_draw(progress_bar);

    /* Call default progress bar text draw. */
    gx_progress_bar_text_draw(progress_bar);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_value_set

## <a name="gx_progress_bar_value_set"></a>gx_progress_bar_value_set


Задание текущего значения индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_progress_bar_value_set(
    GX_PROGRESS_BAR *progress_bar,
    INT value);
```

### <a name="description"></a>Описание

Эта служба задает текущее значение индикатора выполнения. Мини-приложение индикатора выполнения будет автоматически становиться недействительным и перерисовываться при изменении значения индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress_bar**: блок управления индикатором выполнения
- **value**: текущее значение индикатора выполнения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение индикатора выполнения успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status = gx_progress_bar_value_set(progress_bar, 50);

/* if status == GX_SUCCESS the progress bar value was successfully assigned. */
```

### <a name="see-also"></a>См. также:

- gx_progress_bar_value_create
- gx_progress_bar_draw
- gx_progress_bar_event_process
- gx_progress_bar_font_set
- gx_progress_bar_info_set
- gx_progress_bar_pielmap_set
- gx_progress_bar_range_set
- gx_progress_bar_text_color_set
- gx_progress_bar_text_draw

## <a name="gx_prompt_create"></a>gx_prompt_create


Создание запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_create(
    GX_PROMPT *prompt, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent, 
    GX_RESOURCE_ID text_id,
    ULONG style, 
    USHORT prompt_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение запроса.

GX_PROMPT является производным от GX_WIDGET и поддерживает все службы gx_widget.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса текста запроса
- **style**: стиль запроса Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **prompt_id**: идентификатор запроса, определяемый приложением
- **size**: размеры запроса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запрос успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_prompt”. */
status = gx_prompt_create(&my_prompt, “my_promPt”, &my_parent,
                    MY_PROMPT_TEXT_RESOURCE_ID,
                    GX_STYLE_BORDER_RAISED, MY_PROPMT_ID, &size);

/* If status is GX_SUCCESS the prompt “my_prompt” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_get
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_draw"></a>gx_prompt_draw


Вывод запроса

### <a name="prototype"></a>Прототип

```C
VOID gx_prompt_draw(GX_PROMPT *prompt);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение запроса. Эта служба вызывается внутри при обновлении холста GUIX, но также может вызываться настраиваемыми функциями рисования.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom prompt drawing function. */

VOID my_prompt_draw(GX_PROMPT *prompt)
{
    /* Call default prompt draw. */
    gx_prompt_draw(prompt);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_get
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_event_process"></a>gx_prompt_event_process


Обработка события запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_event_process(GX_PROMPT *prompt, GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного запроса. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий запроса.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления запросом
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие запроса успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic prompt event processing as part of custom event processing function. */

UINT custom_prompt_event_process(GX_PROMPT *prompt, GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default prompt event processing */
        status = gx_prompt_event_process(prompt, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_get
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_font_set"></a>gx_prompt_font_set


Задание шрифта запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_font_set(
    GX_PROMPT *prompt, 
    GX_RESOURCE_ID font_id);
```

### <a name="description"></a>Описание

Эта служба задает шрифт мини-приложения запроса.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **font_id**: идентификатор ресурса шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт запроса успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the font of “my_prompt”. */
status = gx_prompt_font_set(&my_prompt, MY_PROMPT_FONT_ID);

/* If status is GX_SUCCESS the font for prompt “my_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_text_color_set
- gx_prompt_text_get
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_text_color_set"></a>gx_prompt_text_color_set


Задание цвета текста запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_text_color_set(
    GX_PROMPT *prompt,
    GX_RESOURCE_ID normal_color,
    GX_RESOURCE_ID selected_color,
    GX_RESOURCE_ID disabled_color);
```

### <a name="description"></a>Описание

Эта служба задает цвет текста для мини-приложения запроса.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **normal_color**: идентификатор ресурса цвета для обычного текста В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **selected_color**: идентификатор ресурса цвета для выбранного текста, который используется, когда фокус перемещается на мини-приложение В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **disabled_color**: идентификатор ресурса цвета для неактивного текста, используемого, когда GX_STYLE_ENABLED неактивен В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста для запроса успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET_SIZE** (0x14) — недопустимый размер мини-приложения

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the text color of “my_prompt”. */
status = gx_prompt_text_color_set(&my_prompt,
                    GX_COLOR_ID_BLACK,
                    GX_COLOR_ID_LIGHTGRAY,
                    GX_COLOR_ID_DISABLED_TEXT);

/* If status is GX_SUCCESS the text color for prompt “my_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_get
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_text_draw"></a>gx_prompt_text_draw


Вывод функции поддержки

### <a name="prototype"></a>Прототип

```C
VOID gx_prompt_text_draw(GX_PROMPT *prompt);
```

### <a name="description"></a>Описание

Эта функция поддержки рисует текстовую часть запроса. Эта функция вызывается внутри функцией gx_prompt_draw() и предоставляется как отдельный API для удобства работы приложений, которые определяют настраиваемую функцию рисования запросов. Приложения, которым требуется настроить фоновый рисунок запроса, могут предоставить свою настраиваемую функцию рисования и вызвать службу gx_prompt_text_draw в составе настраиваемого рисования, чтобы нарисовать текст запроса поверх фона.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления запросом

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Define a custom drawing function */
VOID my_prompt_draw(GX_PROMPT *prompt)
{
    /* insert code here to draw prompt background */

    /* call support function to do text drawing */
    gx_prompt_text_draw();

    /* draw child widgets */
    gx_widget_children_draw((GX_WIDGET *) prompt);
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_text_get"></a>gx_prompt_text_get


Получение текста запроса (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_text_get(
    GX_PROMPT *prompt, 
    GX_CHAR **return_text);
```

### <a name="description"></a>Описание

Эта служба не рекомендуется. Вместо нее следует использовать gx_prompt_text_get_ext().

Эта служба получает текст мини-приложения запроса.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **return_text**: указатель на место назначения для текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст запроса успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PROMPT my_prompt;
GX_CHAR *my_prompt_text;

/* Get the text of “my_prompt”. */
status = gx_prompt_text_get(&my_prompt, &my_prompt_text);

/* If status is GX_SUCCESS the pointer “my_prompt_text” points to the text displayed by “my_prompt”. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_text_get_ext"></a>gx_prompt_text_get_ext


Получение текста запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_text_get(
    GX_PROMPT *prompt, 
    GX_STRING *return_string);
```

### <a name="description"></a>Описание

Эта служба получает строку мини-приложения запроса.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **return_string**: указатель на место назначения для строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст запроса успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PROMPT my_prompt;
GX_STRING my_prompt_string;

/* Get the text of “my_prompt”. */
status = gx_prompt_text_get_ext(&my_prompt, &my_prompt_string);

/* If status is GX_SUCCESS then my_prompt_string has been initialize to hold a copy of the prompt string. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_id_set
- gx_prompt_text_set

## <a name="gx_prompt_text_id_set"></a>gx_prompt_text_id_set


Задание идентификатора текста запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_text_id_set(
    GX_PROMPT *prompt, 
    GX_RESOURCE_ID string_id);
```

### <a name="description"></a>Описание

Эта служба задает идентификатор строки для мини-приложения текстового запроса.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **string_id**: идентификатор ресурса строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — идентификатор текста запроса успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определена функция освобождения памяти.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the string ID of “my_prompt”. */
status = gx_prompt_text_id_set(&my_prompt, MY_STRING_ID);

/* If status is GX_SUCCESS the text ID for prompt “my_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_get
- gx_prompt_text_set

## <a name="gx_prompt_text_set"></a>gx_prompt_text_set


Задание текста запроса (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_text_set(
    GX_PROMPT *prompt, 
    GX_CHAR *text);
```

### <a name="description"></a>Описание

Эта служба не рекомендуется. Вместо нее следует использовать gx_prompt_text_set_ext().

Эта служба задает текст мини-приложения запроса. Если мини-приложение запроса было создано со стилем GX_STYLE_TEXT_COPY, мини-приложение создает частную копию назначенной текстовой строки. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

GX_PROMPT является производным от GX_WIDGET, поэтому все службы gx_widget API могут использоваться с GX_PROMPT.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **text**: указатель на текст

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст запроса успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определена функция выделения памяти.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the text of “my_prompt” to “my_text”. */
status = gx_prompt_text_set(&my_prompt, “my_text”);

/* If status is GX_SUCCESS the text for “my_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_id_set
- gx_prompt_text_get

## <a name="gx_prompt_text_set_ext"></a>gx_prompt_text_set_ext

Задание текста запроса

### <a name="prototype"></a>Прототип

```C
UINT gx_prompt_text_set_ext(
    GX_PROMPT *prompt,
    GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба задает текст мини-приложения запроса. Если мини-приложение запроса было создано со стилем GX_STYLE_TEXT_COPY, мини-приложение создает частную копию назначенной текстовой строки. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

GX_PROMPT является производным от GX_WIDGET, поэтому все службы gx_widget API могут использоваться с GX_PROMPT.

### <a name="parameters"></a>Параметры

- **prompt**: указатель на блок управления мини-приложением запроса
- **text**: указатель на текст

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст запроса успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определена функция выделения памяти.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING new_string;
new_string.gx_string_ptr = “my_text”;
new_string.gx_string_length = strlen(new_string.gx_string_ptr);

/* Set the text of “my_prompt” to “new_string”. */
status = gx_prompt_text_set(&my_prompt, &new_string);

/* If status is GX_SUCCESS the text for “my_prompt” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_prompt_create
- gx_pixelmap_prompt_draw
- gx_pixelmap_prompt_pixelmap_set
- gx_prompt_create
- gx_prompt_draw
- gx_prompt_event_process
- gx_prompt_font_set
- gx_prompt_text_color_set
- gx_prompt_text_id_set
- gx_prompt_text_get

## <a name="gx_radial_progress_bar_anchor_set"></a>gx_radial_progress_bar_anchor_set


Задание начального угла

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_anchor_set(
    GX_RADIAL_PROGRESS_BAR *progress_bar, 
    GX_VALUE angle);
```

### <a name="description"></a>Описание

Эта служба задает начальный угол для кругового индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **angle**: начальный угол дуги окружности

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — привязка кругового индикатора выполнения успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE start_angle = 90;

/* Set the start angle of “my_progress_bar” to 90 degree. */
status = gx_radial_progress_bar_anchor_set(&my_progress_bar, start_angle);

/* If status is GX_SUCCESS the anchor value of “my_progress_bar” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_background_draw"></a>gx_radial_progress_bar_background_draw


Вывод фона

### <a name="prototype"></a>Прототип

```C
VOID gx_radial_progress_bar_background_draw(GX_RADIAL_PROGRESS_BAR *progress_bar);
```

### <a name="description"></a>Описание

Эта служба рисует фон кругового индикатора выполнения. На эту службу ссылается функция gx_radial_progress_bar_draw, но она доступна для использования приложением в случаях, когда приложение определяет настраиваемую функцию рисования кругового индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom radial progress bar drawing function. */

VOID my_radial_progress_bar_draw(GX_RADIAL_PROGRESS_BAR *radial_progress)
{
    /* Call default radial progress bar background draw. */
    gx_radial_progress_bar_background_draw(radial_progress);

    /* Add your own drawing here. */

    /* Draw child widgets. */
    gx_widget_children_draw((GX_WIDGET *)radial_progress);
}
```
### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_create"></a>gx_radial_progress_bar_create


Создание кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_create(
    GX_RADIAL_PROGRESS_BAR *progress_bar,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RADIAL_PROGRESS_BAR_INFO *info,
    ULONG style
    USHORT id);
```

### <a name="description"></a>Описание

Эта служба создает круговой индикатор выполнения.

Если к индикатору выполнения применяется стиль мини-приложения GX_STYLE_ENABLED, индикатор выполнения будет принимать ввод pen_down, pen_drag и pen_up для изменения текущего значения индикатора выполнения.

Стиль мини-приложения GX_STYLE_PROGRESS_TEXT_DRAW можно использовать для отрисовки значения индикатора выполнения в виде текста в области индикатора выполнения. Если этот стиль используется в сочетании со стилем GX_STYLE_PROGRESS_PERCENT, то значение индикатора выполнения отображается в процентах. В противном случае значение индикатора выполнения отображается как текущее угловое значение.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **name**: имя кругового индикатора выполнения
- **parent**: указатель на родительское мини-приложение
- **info**: указатель на структуру GX_RADIAL_PROGRESS_BAR Определение структуры GX_RADIAL_PROGRESS_BAR см. в **приложении I**.
- **style**: стиль кругового индикатора выполнения
- **id**: определяемый приложением идентификатор индикатора выполнения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — круговой индикатор выполнения успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RAIDAL_PROGRESS_BAR_INFO info;

info.gx_radial_progress_bar_info_xcenter = 200;
info.gx_radial_progress_bar_info_ycenter = 200;
info.gx_radial_progress_bar_info_radius = 100;
info.gx_radial_progress_bar_info_current_angle = 180;
info.gx_radial_progress_bar_info_anchor_val = -180;
info.gx_radial_progress_bar_info_font_id = GX_FONT_ID_SYSTEM;
info.gx_raidal_progress_bar_info_normal_text_color =
                                        GX_COLOR_ID_TEXT;
info.gx_radial_progress_bar_info_selected_text_color =
                                        GX_COLOR_ID_TEXT;
info.gx_radial_progress_bar_info_disabled_text_color =
                                        GX_COLOR_ID_DISABLED_TEXT;
info.gx_radial_progress_bar_info_normal_brush_width = 20;
info.gx_raidal_progress_bar_info_selected_brush_width = 16;
info.gx_radial_progress_bar_info_normal_brush_color =
                                        GX_COLOR_ID_WIDGET_FILL;
info.gx_radial_progress_bar_info_selected_brush_color =
                                        GX_COLOR_ID_SELECTED_FILL;

/* Create a radial progress bar “my_progress_bar”. */
status = gx_radial_progress_bar_create(&my_progress_bar,
                    “my_progress_bar”, parent, &info,
                    GX_STYLE_ENABLED | GX_STYLE_TRANSPARENT |
                    GX_STYLE_PROGRESS_TEXT_DRAW,
                    ID_MY_RADIAL_PROGRESS);

/* If status is GX_SUCCESS the radial progress bar “my_progress_bar” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_draw"></a>gx_radial_progress_bar_draw


Вывод кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
VOID gx_radial_progress_bar_draw(GX_RADIAL_PROGRESS_BAR *progress_bar);
```

### <a name="description"></a>Описание

Эта служба рисует круговой индикатор выполнения. На эту службу ссылается функция gx_radial_progress_bar_create, но она доступна для использования приложением в случаях, когда приложение определяет настраиваемую функцию рисования кругового индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom radial progress bar drawing function. */

VOID my_radial_progress_bar_draw(GX_RADIAL_PROGRESS_BAR *radial_progress)
{
    /* Call default radial progress bar draw. */
    gx_radial_progress_bar_draw(radial_progress);

    /* Add your own drawing here. */

    /* Draw child widgets. */
    gx_widget_children_draw((GX_WIDGET *)radial_progress);
}
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_size_calculate
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_event_process"></a>gx_radial_progress_bar_event_process


Обработка события кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_event_process(
    GX_RADIAL_PROGRESS_BAR *progress_bar,
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие кругового индикатора выполнения. На эту функцию ссылается функция gx_radial_progress_bar_create, но она доступна для использования приложением в случаях, когда приложение определяет настраиваемую функцию обработки события кругового индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие кругового индикатора выполнения успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom event processing function. */

UINT my_event_process (GX_RADIAL_PROGRESS_BAR *radial_progress, GX_EVENT *event_ptr)
{
    switch(event_ptr->gx_event_type)
    {
        case GX_EVENT_SHOW:
        /* Do default handling. */
        status = gx_radial_progress_bar_event_process(
                            radial_progress, event_ptr);
        /* add my own handling here */
        break;
    default:
        status = gx_radial_progress_bar_event_process(
                            radial_progress, event_ptr);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_font_set"></a>gx_radial_progress_bar_font_set


Задание шрифта кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_font_set(
    GX_RADIAL_PROGRESS_BAR *progress_bar,
    GX_RESOURCE_ID font_id);
```

### <a name="description"></a>Описание

Эта служба задает шрифт для мини-приложения кругового индикатора выполнения. Этот параметр не действует, если не задан стиль мини-приложения GX_STYLE_PROGRESS_TEXT_DRAW.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **font_id**: идентификатор ресурса шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт для кругового индикатора выполнения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set font for radial progress bar “my_progress_bar”. */
status = gx_radial_progress_bar_font_set(&my_progress_bar, font);

/* If status is GX_SUCCESS the font of “my_progress_bar” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_info_set"></a>gx_radial_progress_bar_info_set


Задание сведений о круговом индикаторе выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_info_set(
    GX_RADIAL_PROGRESS_BAR *progress_bar,
    GX_RADIAL_PROGRESS_BAR_INFO *info);
```

### <a name="description"></a>Описание

Эта служба сбрасывает параметры сведений, назначенные круговому индикатору выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **info**: указатель на структуру сведений о круговом индикаторе выполнения Определение структуры GX_RADIAL_PROGRESS_BAR_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о круговом индикаторе выполнения успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RAIDAL_PROGRESS_BAR_INFO info;

info.gx_radial_progress_bar_info_xcenter = 200;
info.gx_radial_progress_bar_info_ycenter = 200;
info.gx_radial_progress_bar_info_radius = 100;
info.gx_radial_progress_bar_info_current_angle = 180;
info.gx_radial_progress_bar_info_anchor_val = -180;
info.gx_radial_progress_bar_info_font_id = GX_FONT_ID_SYSTEM;
info.gx_raidal_progress_bar_info_normal_text_color =
                                GX_COLOR_ID_TEXT;
info.gx_radial_progress_bar_info_selected_text_color =
                                GX_COLOR_ID_TEXT;
info.gx_radial_progress_bar_info_disabled_text_color =
                                GX_COLOR_ID_DISABLED_TEXT;
info.gx_radial_progress_bar_info_normal_brush_width = 20;
info.gx_raidal_progress_bar_info_selected_brush_width = 16;
info.gx_radial_progress_bar_info_normal_brush_color =
                                GX_COLOR_ID_WIDGET_FILL;
info.gx_radial_progress_bar_info_selected_brush_color =
                                GX_COLOR_ID_SELECTED_FILL;

/* Set appearance information for radial progress bar “my_progress_bar”. */
status = gx_radial_progress_bar_info_set(&my_progress_bar, &info);

/* If status is GX_SUCCESS the appearance information of “my_progress_bar” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_text_color_set"></a>gx_radial_progress_bar_text_color_set


Задание цвета текста для кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_text_color_set(
    GX_RADIAL_PROGRESS_BAR *progress_bar,
    GX_RESOURCE_ID normal_text_color,
    GX_RESOURCE_ID selected_text_color,
    GX_RESOURCE_ID disabled_text_color);
```

### <a name="description"></a>Описание

Эта служба задает цвет текста для кругового индикатора выполнения. Это значение используется только в том случае, если задан стиль GX_STYLE_PROGRESS_TEXT_DRAW.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **normal_color**: идентификатор ресурса цвета текста в нормальном состоянии В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **selected_color**: идентификатор ресурса цвета текста, когда фокус перемещается на мини-приложение В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **disabled_color**: идентификатор ресурса цвета текста, если не задан стиль GX_STYLE_ENABLED В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста для кругового индикатора выполнения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set text color for radial progress bar “my_progress_bar”. */
status = gx_radial_progress_bar_text_color_set(&my_progress_bar,
                            GX_COLOR_ID_NORMAL_TEXT,
                            GX_COLOR_ID_SELECTED_TEXT,
                            GX_COLOR_ID_DISABLED_TEXT);

/* If status is GX_SUCCESS the text color of “my_progress_bar” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_draw
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_text_draw"></a>gx_radial_progress_bar_text_draw


Вывод текста кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
VOID gx_radial_progress_bar_text_draw(GX_RADIAL_PROGRESS_BAR *progress_bar);
```

### <a name="description"></a>Описание

Эта служба рисует текст указанного кругового индикатора выполнения. Эта функция вызывается внутри как часть gx_radial_progress_bar_draw(), но предоставляется приложению для поддержки в случаях, когда приложение определяет настраиваемую функцию рисования индикатора выполнения.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Draw text for radial progress bar “my_progress_bar”. */
status = gx_radial_progress_bar_text_draw (&my_progress_bar);

/* If status is GX_SUCCESS the text of “my_progress_bar” has been drawn. */

/* Write a custom radial progress bar drawing function. */
VOID my_radial_progress_bar_draw(GX_RADIAL_PROGRESS_BAR *radial_progress)
{
    /* Add your own background draw here. */

    /* Call default radial progress bar text draw. */
    gx_radial_progress_bar_text_draw(radial_progress);

    /* Draw child widgets. */
    gx_widget_children_draw((GX_WIDGET *)radial_progress);
}
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_value_set

## <a name="gx_radial_progress_bar_value_set"></a>gx_radial_progress_bar_value_set


Задание значения кругового индикатора выполнения

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_progress_bar_value_set(
    GX_RADIAL_PROGRESS_BAR *progress_bar, 
    GX_VALUE value);
```

### <a name="description"></a>Описание

Эта служба задает значение кругового индикатора выполнения. Назначаемое значение ограничено диапазоном от –360 до +360, который определяет возможный диапазон угловых значений для текущего расположения индикатора выполнения. Приложение должно масштабировать реальное значение, которое указывается, для назначения углового значения мини-приложению индикатора выполнения.

Индикатор выполнения рисуется так, что текущее значение обозначает угловую разницу между положением привязки и верхней точкой верхней дуги. Отрицательные значения означают отрисовку дуги в направлении по часовой стрелке, начиная от положения привязки. При положительном текущем значении дуга отрисовывается в направлении против часовой стрелки, начиная от положения привязки.

Например, чтобы нарисовать дугу, начиная с верхней части дуги (12 часов на циферблате) и заканчивая справа (3 часа на циферблате), значению привязки присвойте 90 градусов, а текущему значению –90 градусов.

### <a name="parameters"></a>Параметры

- **progress bar**: указатель на блок управления круговым индикатором выполнения
- **value**: новое значение индикатора выполнения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение для кругового индикатора выполнения успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимые указатели.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE new_value = 180;

/* Set value for radial progress bar “my_progress_bar”. */
status = gx_radial_progress_bar_value_set(&my_progress_bar,
                                        new_value);

/* If status is GX_SUCCESS the value of “my_progress_bar” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_radial_progress_bar_anchor_set
- gx_radial_progress_bar_background_draw
- gx_radial_progress_bar_create
- gx_radial_progress_bar_draw
- gx_radial_progress_bar_event_process
- gx_radial_progress_bar_font_set
- gx_radial_progress_bar_info_set
- gx_radial_progress_bar_text_color_set
- gx_radial_progress_bar_text_draw

## <a name="gx_radio_button_create"></a>gx_radio_button_create


Создание переключателя

### <a name="prototype"></a>Прототип

```C
UINT gx_radio_button_create(
    GX_RADIO_BUTTON *button,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RESOURCE_ID text_id, ULONG style,
    USHORT radio_button_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение переключателя. GX_RADIO_BUTTON является производным от GX_TEXT_BUTTON, поэтому все службы gx_text_button также поддерживаются этим типом мини-приложения.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления переключателем
- **name**: логическое имя мини-приложения переключателя
- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса переключателя
- **style**: стиль переключателя Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **radio_button_id**: определяемый приложением идентификатор переключателя
- **size**: размеры переключателя

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — переключатель успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_radio_button”. */
status = gx_radio_button_create(&my_radio_button, “my_radio_button”, &my_parent,
                    MY_RADIO_BUTTON_TEXT_RESOURCE_ID,
                    GX_STYLE_BORDER_RAISED, MY_RADIO_BUTTON_ID, &size);

/* If status is GX_SUCCESS the radio button “my_radio_button” has been created. */
```
### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw
- gx_radio_button_draw

## <a name="gx_radio_button_draw"></a>gx_radio_button_draw


Вывод переключателя

### <a name="prototype"></a>Прототип

```C
VOID gx_radio_button_draw(GX_RADIO_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение переключателя. Эта служба вызывается внутри при обновлении холста GUIX, но также может вызываться переопределенными функциями рисования.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления мини-приложением переключателя

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom radio button drawing function. */

VOID my_radio_button_draw(GX_RADIO_BUTTON *radio_button)
{
    /* Call default radio button draw. */
    gx_radio_button_draw(radio_button);

    /* Add your own drawing here. */

    /* Draw child widgets. */
    gx_widget_children_draw((GX_WIDGET *)radio_button);
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw
- gx_radio_button_create

## <a name="gx_radio_button_pixelmap_set"></a>gx_radio_button_pixelmap_set


Задание карт отображения пикселей для переключателя

### <a name="prototype"></a>Прототип

```C
UINT gx_radio_button_pixelmap_set(
    GX_RADIO_BUTTON *button,
    GX_RESOURCE_ID off_id,
    GX_RESOURCE_ID on_id,
    GX_RESOURCE_ID off_disabled_id,
    GX_RESOURCE_ID on_disabled_id);
```

### <a name="description"></a>Описание

Эта служба назначает карты отображения пикселей для отображения указанным переключателем для каждого состояния переключателя. Идентификаторы ресурсов могут дублироваться.

### <a name="parameters"></a>Параметры

- **off_id**: карта отображения пикселей, используемая для состояния, когда переключатель не установлен
- **on_id**: карта отображения пикселей, используемая для состояния, когда переключатель установлен
- **off_disabled_id**: карта отображения пикселей, используемая для состояния, когда переключатель отключен и не установлен
- **on_disabled_id**: карта отображения пикселей, используемая для состояния, когда переключатель отключен и установлен

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей переключателя успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set pixelmap for “my_radio_button”. */
status = gx_radio_button_pixelmap_set(&my_radio_button,
                                MY_OFF_PIXELMAP,
                                MY_ON_PIXELMAP,
                                MY_OFF_DISABLED_PIXELMAP,
                                MY_ON_DISABLED_PIXELMAP);

/* If status is GX_SUCCESS the pixelmaps for radio button “my_radio_button” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_color_set
- gx_text_button_draw
- gx_radio_button_create

## <a name="gx_radial_slider_anchor_angles_set"></a>gx_radial_slider_anchor_angles_set


Задание списка привязок кругового ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_anchor_angles_set(
    GX_RADIAL_SLIDER *slider,
    GX_VALUE *anchor_angles, 
    USHORT anchor_count);
```

### <a name="description"></a>Описание

Эта служба задает углы привязки для круглого ползунка. Если задан список углов привязки, угол круглого ползунка будет одним из определенных углов привязки.

### <a name="parameters"></a>Параметры

- **slider**: блок управления круглым ползунком
- **anchor_angles**: список углов для задания
- **anchor_count**: количество углов привязки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — углы привязки успешно заданы.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимый список привязок.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE anchor_angles[]={0, 30, 60, 90, 120, 150, 180};
USHORT anchor_count = 7;

/* Set anchor angles for radial slider. */
status = gx_radial_slider_anchor_angles_set(&my_radial_slider,
                                anchor_angles, anchor_count);

/* If status is GX_SUCCESS the anchor angles have been set for “my_radial_slider”. */

/* Set anchor angles for radial slider. */
status = gx_radial_slider_anchor_angles_set(&my_radial_slider,
                                GX_NULL, 0);

/* If status is GX_SUCCESS the anchor angles have been removed from “my_radial_slider”. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_angle_set"></a>gx_radial_slider_angle_set

Задание угла круглого ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_angle_set(
    GX_RADIAL_SLIDER *slider,
    GX_VALUE new_angle);
```

### <a name="description"></a>Описание

Эта служба задает новое значение угла для круглого ползунка.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **new_angle**: новое значение угла, которое необходимо задать

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — угол круглого ползунка успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set “my_radial_slider” angle to 0 degree(3 o’clock position). */
status = gx_radial_slider_angle_set(&my_radial_slider, 0);

/* If status is GX_SUCCESS the value of “my_radial_slider” has been set to 0 degree. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_animation_set"></a>gx_radial_slider_animation_set


Создание сведений об анимации круглого ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_animation_set(
    GX_RADIAL_SLIDER *slider,
    USHORT steps, USHORT delay, 
    USHORT animation_style,
    VOID (*animation_update_callback)(GX_RADIAL_SLIDER *slider));
```

### <a name="description"></a>Описание

Эта служба задает шаги анимации, время задержки и стили анимации для анимации круглого ползунка.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **steps**: общее количество шагов для одной анимации
- **delay**: время задержки для каждого шага анимации
- **animation_style**: функция для реалистичной анимации, включает следующее:
  - GX_ANIMATION_BACK_EASE_IN
  - GX_ANIMATION_BACK_EASE_OUT
  - GX_ANIMATION_BACK_EASE_IN_OUT
  - GX_ANIMATION_BOUNCE_EASE_IN
  - GX_ANIMATION_BOUNCE_EASE_OUT
  - GX_ANIMATION_BOUNCE_EASE_IN_OUT
  - GX_ANIMATION_CIRC_EASE_IN
  - GX_ANIMATION_CIRC_EASE_OUT
  - GX_ANIMATION_CIRC_EASE_IN_OUT
  - GX_ANIMATION_CUBIC_EASE_IN
  - GX_ANIMATION_CUBIC_EASE_OUT
  - GX_ANIMATION_CUBIC_EASE_IN_OUT
  - GX_ANIMATION_ELASTIC_EASE_IN
  - GX_ANIMATION_ELASTIC_EASE_OUT
  - GX_ANIMATION_ELASTIC_EASE_IN_OUT
  - GX_ANIMATION_EXPO_EASE_IN
  - GX_ANIMATION_EXPO_EASE_OUT
  - GX_ANIMATION_EXPO_EASE_IN_OUT
  - GX_ANIMATION_QUAD_EASE_IN
  - GX_ANIMATION_QUAD_EASE_OUT
  - GX_ANIMATION_QUAD_EASE_IN_OUT
  - GX_ANIMATION_QUART_EASE_IN
  - GX_ANIMATION_QUART_EASE_OUT
  - GX_ANIMATION_QUART_EASE_IN_OUT
  - GX_ANIMATION_QUINT_EASE_IN
  - GX_ANIMATION_QUINT_EASE_OUT
  - GX_ANIMATION_QUINT_EASE_IN_OUT
  - GX_ANIMATION_SINE_EASE_IN
  - GX_ANIMATION_SINE_EASE_OUT
  - GX_ANIMATION_SINE_EASE_IN_OUT
- **animation_update_callback**: определяемая пользователем функция обратного вызова, которая будет вызываться после каждого шага анимации

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — анимация круглого ползунка успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
VOID animation_callback(GX_RADIAL_SLIDER *slider)
{
    /* This function will be called after each animation step,
    add custom code here. */
}

/* Set animation info for “my_radial_slider”. */
status = gx_radial_slider_animation_set(&my_radial_slider, 15, 2,
                                GX_ANIMATION_CIRC_EASE_IN_OUT,
                                animation_callback);

/* If status is GX_SUCCESS the radial slider needle will move with specified animation. */

/* Disable animation for “my_radial_slider”. */
status = gx_radial_slider_animation_set(&my_radial_slider, 0, 0,
                                        0, 0);

/* If status is GX_SUCCESS the radial slider needle will move without animation. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_animation_start"></a>gx_radial_slider_animation_start


Задание нового значения круглого ползунка с анимацией

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_animation_start(
    GX_RADIAL_SLIDER *slider,
    GX_VALUE target_angle);
```

### <a name="description"></a>Описание

Эта служба запускает анимацию для перемещения стрелки ползунка из текущей позиции в указанное положение.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **target_angle**: целевое значение угла

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — анимация карты отображения пикселей круглого ползунка успешно запущена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Start an animation to move radial slider needle from
current position to 90 degree position. */
status = gx_radial_slider_animation_start(&my_radial_slider, 90);

/* If status is GX_SUCCESS the radial slider needle animation has been started. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_create"></a>gx_radial_slider_create


Создание круглого ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_create(
    GX_RADIAL_SLIDER *slider,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RADIAL_SLIDER_INFO *info,
    ULONG style,
    USHORT slider_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение круглого ползунка.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **name**: логическое имя мини-приложения круглого ползунка
- **parent**: указатель на родительское мини-приложение
- **info**: определение внешнего вида круглого ползунка. Определение структуры GX_RADIAL_SLIDER_INFO см. в **приложении I**
- **style**: стиль переключателя Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **radio_button_id**: определяемый приложением идентификатор круглого ползунка
- **size**: размеры круглого ползунка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — круглый ползунок успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RADIAL_SLIDER_INFO info;
GX_RECTANGLE size;

/* Distance from left side of widget to rotating center. */
info.gx_radial_slider_info_xcenter = 100;

/* Distance from top size of widget to rotating center. */
info.gx_radial_slider_info_ycenter = 100;

/* Radius of rotating circle. */
info.gx_radial_slider_info_radius = 100;

/* Widget of rotating track. */
info.gx_radial_slider_info_track_width = 40;

/* Current angle value. */
info.gx_radial_slider_info_current_angle = 0;

/* Minimum angle value. */
info.gx_radial_slider_min_anlge = -60;

/* Maximum angle value. */
info.gx_radial_slider_max_angle = 240;

/* Anchor value list. */
info.gx_radial_slider_angle_list = GX_NULL;

/* Anchor value count. */
info.gx_radial_slider_list_count = 0;

/* Resource ID of background pixelmap. */
info.gx_radial_slider_background_pixelmap = GX_PIXELMAP_ID_BKGRD;

/* Resource ID of needle pixelmap. */
info.gx_radial_slider_needle_pixelmap = GX_PIXELMAP_ID_NEEDLE;

/* Define widget size. */
gx_utility_rectangle_define(&size, 0, 0, 200, 200);

/* Create “my_radial_slider”. */
status = gx_radial_slider_create(&my_radial_slider,
                                “my_radial_slider”, &my_parent,
                                &info, GX_STYLE_ENABLED,
                                MY_RADIAL_SLIDER_ID, &size);

/* If status is GX_SUCCESS the radial slider “my_radial_slider” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_draw"></a>gx_radial_slider_draw


Вывод круглого ползунка

### <a name="prototype"></a>Прототип

```C
VOID gx_radial_slider_draw(GX_RADIAL_SLIDER *slider);
```

### <a name="description"></a>Описание

Эта служба рисует круглый ползунок. Эта служба вызывается внутри при обновлении холста GUIX, но также может вызываться переопределенными функциями рисования.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom radio button drawing function. */

VOID my_radial_slider_draw(GX_RADIAL_SLIDER *radial_slider)
{
    /* Call default radial slider draw. */
    gx_radio_slider_draw(radial_slider);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_event_process"></a>gx_radial_slider_event_process


Обработка события круглого ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_event_process(
    GX_RADIAL_SLIDER *slider,
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие круглого ползунка. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий круглого ползунка.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие круглого ползунка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom event processing function. */

UINT my_event_process(GX_RADIAL_SLIDER *slider,
                    GX_EVENT *event_ptr)
{
    switch(event_ptr->gx_event_type)
    {
    case GX_EVENT_SHOW:
        /* Do default handling. */
        status = gx_radial_slider_event_process(slider, event_ptr);

        /* add my own handling here */
        break;
    default:
        status = gx_radial_slider_event_process(slider, event_ptr);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_info_get
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_info_get"></a>gx_radial_slider_info_get


Получение сведений о круглом ползунке

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_info_get(
    GX_RADIAL_SLIDER *slider,
    GX_RADIAL_SLIDER_INFO **info);
```

### <a name="description"></a>Описание

Эта служба получает указатель на сведения о круглом ползунке.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **info**: полученные сведения об указателе на сведения о круглом ползунке

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о круглом ползунке успешно получены.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RADIAL_SLIDER_INFO *info;

/* Retrive radial slider information structure. */
status = gx_radial_slider_info_get(&my_radial_slider,
                                    “my_radial_slider”, &info);

/* If status is GX_SUCCESS the radial slider information pointer of “my_radial_slider” has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_set
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_info_set"></a>gx_radial_slider_info_set


Задание сведений о круглом ползунке

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_info_set(
    GX_RADIAL_SLIDER *slider,
    GX_RADIAL_SLIDER_INFO *info);
```

### <a name="description"></a>Описание

Эта служба задает сведения о круглом ползунке.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **info**: сведения о круглом ползунке для задания

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о круглом ползунке успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RADIAL_SLIDER_INFO info;

/* Distance from left side of widget to rotating center. */
info.gx_radial_slider_info_xcenter = 100;

/* Distance from top size of widget to rotating center. */
info.gx_radial_slider_info_ycenter = 100;

/* Radius of rotating circle. */
info.gx_radial_slider_info_radius = 100;

/* Widget of rotating track. */
info.gx_radial_slider_info_track_width = 40;

/* Current angle value. */
info.gx_radial_slider_info_current_angle = 0;

/* Minimum angle value. */
info.gx_radial_slider_min_anlge = -60;

/* Maximum angle value. */
info.gx_radial_slider_max_angle = 240;

/* Anchor value list. */
info.gx_radial_slider_angle_list = GX_NULL;

/* Anchor value count. */
info.gx_radial_slider_list_count = 0;

/* Resource ID of background pixelmap. */
info.gx_radial_slider_background_pixelmap = GX_PIXELMAP_ID_BKGRD;

/* Resource ID of needle pixelmap. */
info.gx_radial_slider_needle_pixelmap = GX_PIXELMAP_ID_NEEDLE;

/* Reset radial slider info for “my_radial_slider”. */
status = gx_radial_slider_info_set(&my_radial_slider, &info);

/* If status is GX_SUCCESS the radial slider info of “my_radial_slider” has been reset. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_pixelmap_set

## <a name="gx_radial_slider_pixelmap_set"></a>gx_radial_slider_pixelmap_set


Задание карт отображения пикселей круглого ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_radial_slider_pixelmap_set(
    GX_RADIAL_SLIDER *slider,
    GX_RESOURCE_ID background_pixemap,
    GX_REOUSRCE_ID needle_pixelmap);
```

### <a name="description"></a>Описание

Эта служба задает карты отображения пикселей для фона и стрелки круглого ползунка.

### <a name="parameters"></a>Параметры

- **slider**: указатель на блок управления круглого ползунка
- **background_pixelmap**: идентификатор ресурса карты отображения пикселей фона
- **needle_pixelmap**: идентификатор ресурса карты отображения пикселей стрелки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей круглого ползунка успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create “my_radio_button”. */
status = gx_radial_slider_pixelmap_set(&my_radial_slider, GX_PIXELMAP_ID_BG, GX_PIXELMAP_ID_NEEDLE);

/* If status is GX_SUCCESS the background and needle pixelmap of “my_radial_slider” has been reset. */
```

### <a name="see-also"></a>См. также:

- gx_radial_slider_anchor_angles_set
- gx_radial_slider_angle_set
- gx_radial_slider_animation_set
- gx_radial_slider_animation_start
- gx_radial_slider_create
- gx_radial_slider_draw
- gx_radial_slider_event_process
- gx_radial_slider_info_get
- gx_radial_slider_info_set

## <a name="gx_rich_text_view_create"></a>gx_rich_text_view_create

Создание представления форматированного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_rich_text_view_create(
    GX_RICH_TEXT_VIEW *text_view,
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_RESOURCE_ID text_id,
    GX_RICH_TEXT_FONTS *fonts,
    USHORT id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает представление форматированного текста в соответствии с заданными параметрами.


**Теги форматирования, которые используются для вывода специальных типов текста**

- **\<b>\</b>**  — задание шрифта текста с помощью заданного пользователем идентификатора полужирного шрифта
- **\<i>\</i>**  — задание шрифта текста с помощью заданного пользователем идентификатора курсива
- **\<u>\</u>**  — включение подчеркивания текста
- **\<f GX_FONT_ID>\</f>**  — задание шрифта текста с помощью заданного идентификатора шрифта
- **\<c GX_COLOR_ID>\</c>**  — задание шрифта текста с помощью заданного идентификатора цвета
- **\<hc GX_COLOR_ID>\</hc>**  — задание цвета выделения текста с помощью заданного идентификатора цвета
- **\<align left/right/center>\</align>**  — задание выравнивания текста

**Примеры использования тегов форматирования**

- \<b>Полужирный шрифт<\b>
- \<i>Курсив<\i>
- \<u>Подчеркнутый<\u>
- \<f 0>Это идентификатор шрифта текста, для которого задано значение 0<\f>
- \<c 1>Это идентификатор цвета текста, для которого задано значение 1<\c>
- \<hc 2>Это идентификатор цвета выделения текста, для которого задано значение 2<\hc>
- \<align left>Это текст, выровненный по левому краю<\align>

### <a name="parameters"></a>Параметры

- **text_view**: указатель на блок управления представлением форматированного текста
- **name**: имя представления форматированного текста
- **parent**: указатель на родительское мини-приложение
- **text_id**: идентификатор ресурса текстовой строки
- **fonts**: указатель на сведения о шрифте для представления форматированного текста Определение структуры GX_RICH_TEXT_FONTS см. в **приложении I**.
- **style**: стиль мини-приложения Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **id**: определяемый приложением идентификатор представления форматированного текста
- **size**: размер представления форматированного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — представление форматированного текста успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

```C
GX_RICH_TEXT_VIEW rich_view;
GX_RCIH_TEXT_FONTS fonts;
GX_RECTANGLE size;

/* Defined widget size. */
gx_utility_rectangle_define(&size, 100, 100, 300, 150);

/* Define font information. */
fonts.gx_rich_text_fonts_normal_id = GX_FONT_ID_SYSTEM;
fonts.gx_rich_text_fonts_bold_id = GX_FONT_ID_SYSTEM;
fonts.gx_rich_text_fonts_italic_id = GX_FONT_ID_SYSTEM;
fonts.gx_rich_text_fonts_bold_italic_id = GX_FONT_ID_SYSTEM;

/* Create rich text view widget. */
status = gx_rich_text_view_create(&rich_view, “my_rich_view”,
                                  parent, GX_STRING_ID_TEST_STRING,
                                  &fonts,
                                  GX_STYLE_ENABLED,
                                  MY_RICH_VIEW_ID, &size);

/* If status is GX_SUCCESS the rich text view was successfully created. */

```

Демонстрационное приложение demo_guix_widget_types в составе GUIX Studio содержит полный пример использования мини-приложения представления форматированного текста.

### <a name="see-also"></a>См. также:

- gx_rich_text_view_draw
- gx_rich_text_view_fonts_set
- gx_rich_text_view_text_draw

## <a name="gx_rich_text_view_draw"></a>gx_rich_text_view_draw


Вывод представления форматированного текста

### <a name="prototype"></a>Прототип

```C
VOID gx_rich_text_view_draw(GX_RICH_TEXT_VIEW *text_view);
```

### <a name="description"></a>Описание

Эта служба рисует заданное мини-приложение представления форматированного текста. Эта служба обычно вызывается GUIX внутри как часть операции обновления холста, но она также предоставляется приложению, которому может потребоваться предоставить настраиваемую функцию рисования и вызвать представление форматированного текста по умолчанию в качестве настраиваемой базы для рисования.

### <a name="parameters"></a>Параметры

- **text_view**: указатель на блок управления мини-приложением представления форматированного текста

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom rich text view drawing function. */

VOID my_rich_text_view_draw(GX_RICH_TEXT_VIEW *text_view)
{
    /* Call default rich text view draw. */
    gx_rich_text_view_draw(text_view);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_rich_text_view_create
- gx_rich_text_view_fonts_set
- gx_rich_text_view_text_draw

## <a name="gx_rich_text_view_fonts_set"></a>gx_rich_text_view_fonts_set

Задание шрифтов для представления форматированного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_rich_text_view_fonts_set(GX_RICH_TEXT_VIEW *text_view, GX_RICH_TEXT_FONTS *fonts);
```

### <a name="description"></a>Описание

Эта служба задает шрифты для мини-приложения представления форматированного текста.

### <a name="parameters"></a>Параметры

- **text_view**: указатель на блок управления мини-приложением представления форматированного текста
- **fonts**: указатель на сведения о шрифте для представления форматированного текста Определение структуры GX_RICH_TEXT_FONTS см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифты представления форматированного текста успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RICH_TEXT_FONTS fonts;

/* Replace the font ids as needed. */
fonts.gx_rich_text_fonts_normal_id = GX_FONT_ID_SYSTEM;
fonts.gx_rich_text_fonts_bold_id = GX_FONT_ID_SYSTEM;
fonts.gx_rich_text_fonts_italic_id = GX_FONT_ID_SYSTEM;
fonts.gx_rich_text_fonts_bold_italic_id = GX_FONT_ID_SYSTEM;

/* Set the font of “my_rich_view”. */
status = gx_rich_text_view_fonts_set(&my_rich_view, &fonts);

/* If status is GX_SUCCESS the font for rich text view “my_rich_view” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_rich_text_view_create
- gx_rich_text_view_draw
- gx_rich_text_view_text_draw

## <a name="gx_rich_text_view_text_draw"></a>gx_rich_text_view_text_draw


Вывод текста представления форматированного текста

### <a name="prototype"></a>Прототип

```C
VOID gx_rich_text_view_text_draw(GX_RICH_TEXT_VIEW *text_view);
```

### <a name="description"></a>Описание

Эта функция поддержки рисует текстовую часть представления форматированного текста. Эта функция вызывается внутри функцией gx_rich_text_view_draw() и предоставляется как отдельный API для удобства работы приложений, которые определяют настраиваемую функцию рисования представлений форматированного текста. Приложения, которым требуется настроить фоновый рисунок представления форматированного текста, могут предоставить свою настраиваемую функцию рисования и вызвать службу gx_rich_text_view_text_draw в составе настраиваемого рисования, чтобы нарисовать представление форматированного текста поверх фона.

### <a name="parameters"></a>Параметры

- **text_view**: указатель на блок управления мини-приложением представления форматированного текста

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom rich text view drawing function. */

VOID my_rich_text_view_draw(GX_RICH_TEXT_VIEW *text_view)
{
    /* Insert code here to draw rich text view background. */

    /* Call support function to do text drawing. */
    gx_rich_text_view_text_draw();

    /* Draw child widgets. */
    gx_widget_children_draw((GX_WIDGET *) rich_text_view);
}
```

### <a name="see-also"></a>См. также:

- gx_rich_text_view_create
- gx_rich_text_view_draw
- gx_rich_text_view_fonts_set

## <a name="gx_screen_stack_create"></a>gx_screen_stack_create


Инициализация стека экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_screen_stack_create(
    GX_SCREEN_STACK_CONTROL *control,
    GX_WIDGET **memory_buffer, 
    INT buffer_size);
```

### <a name="description"></a>Описание

Эта служба инициализирует стек экранов. Приложение должно определить блок памяти и размер буфера, используемые для реализации функции стека экранов.

> [!NOTE]
> *Этот API является устаревшим. Вместо него используется gx_system_screen_stack_create(). Эта версия предоставляется только для обеспечения обратной совместимости с предыдущими выпусками библиотеки.*

### <a name="parameters"></a>Параметры

- **control**: блок управления стеком экранов
- **memory_buffer**: указатель на буфер памяти, используемый в качестве стека экранов
- **buffer_size**: размер памяти в байтах

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стек экранов успешно создан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер буфера.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
#define SCREEN_STACK_SIZE 10

GX_SCREEN_STACK_CONTROL my_stack_control;
GX_WIDGET *screen_stack[SCREEN_STACK_SIZE];

/* Initialize “my_stack_control”. */
status = gx_screen_stack_create(&my_stack_control, screen_stack,
                        SCREEN_STACK_SIZE * sizeof(GX_WIDGET *));

/* If status is GX_SUCCESS the screen control block “my_stack control” has been initialized. */
```

### <a name="see-also"></a>См. также:

- gx_screen_stack_push
- gx_screen_stack_pop
- gx_screen_stack_reset

## <a name="gx_screen_stack_pop"></a>gx_screen_stack_pop


Удаление самой верхней записи из стека экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_screen_stack_pop(GX_SCREEN_STACK_CONTROL *control);
```

### <a name="description"></a>Описание

Эта служба удаляет самую верхнюю запись из стека экранов и прикрепляет извлеченный экран к его предыдущему родительскому элементу. Этот API также отсоединяет все существующие дочерние элементы от родительского элемента.

> [!NOTE]
> *Этот API является устаревшим. Вместо него используется gx_system_screen_stack_pop(). Эта версия предоставляется только для обеспечения обратной совместимости с предыдущими выпусками библиотеки.*

### <a name="parameters"></a>Параметры

- **control**: блок управления стеком экранов

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стек экранов успешно отображен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Remove the topmost entry from the screen stack. */
status = gx_screen_stack_pop(&my_stack_control);

/* If status is GX_SUCCESS the topmost entry has been removed from the screen stack, 
    and the popped screen has been attached to its parent. */
```

### <a name="see-also"></a>См. также:

- gx_screen_stack_create
- gx_screen_stack_push
- gx_screen_stack_reset

## <a name="gx_screen_stack_push"></a>gx_screen_stack_push


Отправка экрана и его родительских элементов в стек

### <a name="prototype"></a>Прототип

```C
UINT gx_screen_stack_push(
    GX_SCREEN_STACK_CONTROL *control,
    GX_WIDGET *screen,
    GX_WIDGET *new_screen);
```

### <a name="description"></a>Описание

Эта служба отсоединяет экран от его родительского элемента и помещает указатель экрана и указатель родительского элемента в стек экранов. После этого родительскому элементу присоединяется новый указатель экрана.


> [!NOTE]
> *Этот API является устаревшим. Вместо него используется gx_system_screen_stack_pop(). Эта версия предоставляется только для обеспечения обратной совместимости с предыдущими выпусками библиотеки.*

### <a name="parameters"></a>Параметры

- **control**: блок управления стеком экранов
- **screen**: указатель экрана для отправки
- **new_screen**: указатель на новый экран

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно отправлено в стек экранов.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Push “screen” and its parent to screen stack, Dettach “screen” from its parent, and attach “new screen” to the parent. */
status = gx_screen_stack_push(&my_stack_control,
                                screen, new_screen);

/* If status is GX_SUCCESS the widget “screen” and its parent have been pushed to screen stack, “screen” has been detached from its parent, “new_screen” has been attached to the parent. */
```

### <a name="see-also"></a>См. также:

- gx_screen_stack_create
- gx_screen_stack_push
- gx_screen_stack_reset

## <a name="gx_screen_stack_reset"></a>gx_screen_stack_reset


Удаляет все записи из стека экранов.

### <a name="prototype"></a>Прототип

```C
UINT gx_screen_stack_reset(GX_SCREEN_STACK_CONTROL *control);
```

### <a name="description"></a>Описание

Эта служба удаляет все записи из стека экранов.

> [!NOTE]
> *Этот API является устаревшим. Вместо него используется gx_system_screen_stack_pop(). Эта версия предоставляется только для обеспечения обратной совместимости с предыдущими выпусками библиотеки.*

### <a name="parameters"></a>Параметры

- **control**: блок управления стеком экранов

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — бегунок прокрутки успешно создан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Remove all enteries from the screen stack. */
status = gx_screen_stack_reset(&my_stack_control);

/* If status is GX_SUCCESS all entries of screen stack has been removed. */
```

### <a name="see-also"></a>См. также:

- gx_screen_stack_create
- gx_screen_stack_push
- gx_screen_stack_pop

## <a name="gx_scroll_thumb_create"></a>gx_scroll_thumb_create


Создание бегунка прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_thumb_create(
    GX_SCROLL_THUMB *scroll_thumb,
    GX_SCROLLBAR *parent, ULONG style);
```

### <a name="description"></a>Описание

Эта служба создает бегунок для полосы прокрутки. Эта служба обычно вызывается внутри при создании GX_SCROLLBAR, но теперь она общедоступная, чтобы разрешить настраиваемые реализации полосы прокрутки.

### <a name="parameters"></a>Параметры

- **scroll_thumb**: блок управления мини-приложением бегунка прокрутки
- **parent**: указатель на родительскую полосу прокрутки
- **style**: стиль мини-приложения полосы прокрутки Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — бегунок прокрутки успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SCROLL_THUMB my_scroll_thumb;

/* Create scroll thumb “my_scroll_thumb”. */
status = gx_scroll_thumb_create(&my_scroll_thumb, &my_scrollbar,
                                GX_STYLE_NONE);

/* If status is GX_SUCCESS the scroll thumb “my_scroll_thumb” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_scroll_thumb_draw
- gx_scroll_thumb_event_process

## <a name="gx_scroll_thumb_draw"></a>gx_scroll_thumb_draw


Вывод бегунка прокрутки

### <a name="prototype"></a>Прототип

```C
VOID gx_scroll_thumb_draw(GX_SCROLL_THUMB *scroll_thumb);
```

### <a name="description"></a>Описание

Эта служба рисует бегунок для полосы прокрутки. Эта служба вызывается внутри при обновлении холста GUIX, но также может вызываться переопределенными функциями рисования.

### <a name="parameters"></a>Параметры

- **scroll_thumb**: блок управления мини-приложением бегунка прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom scroll thumb drawing function. */

VOID my_scroll_thumb_draw(GX_SCROLL_THUMB *thumb)
{
    /* Call default scroll thumb draw. */
    gx_scroll_thumb_draw(thumb);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_scroll_thumb_create
- gx_scroll_thumb_event_process

## <a name="gx_scroll_thumb_event_process"></a>gx_scroll_thumb_event_process


Обработка события бегунка прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_thumb_event_process(
    GX_SCROLL_THUMB *scroll_thumb,
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает события, отправляемые бегунку на полосе прокрутки. Эта служба обычно используется GUIX внутри, но теперь она общедоступна для помощи в реализации настраиваемого поведения полосы прокрутки.

### <a name="parameters"></a>Параметры

- **scroll_thumb**: блок управления мини-приложением бегунка прокрутки
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие бегунка прокрутки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom event processing function. */

UINT my_event_process (GX_SCROLL_THUMB *thumb, GX_EVENT *event_ptr)
{
    switch(event_ptr->gx_event_type)
    {
    case GX_EVENT_SHOW:
        /* Do default handling. */
        status = gx_scroll_thumb_event_process(thumb, event_ptr);

        /* add my own handling here */
        break;
    default:
        status = gx_scroll_thumb_event_process(thumb, event_ptr);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_scroll_thumb_create
- gx_scroll_thumb_draw

## <a name="gx_scroll_wheel_create"></a>gx_scroll_wheel_create


Создание базового мини-приложения колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_create( 
    GX_SCROLL_WHEEL *wheel, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    INT total_rows, 
    ULONG style,
    USHORT id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение базового колеса прокрутки.

Базовое колесо прокрутки — это базовое мини-приложение для всех типов колес прокрутки, включая **gx_generic_scroll_wheel** и мини-приложение **gx_text_scroll_wheel**, являющееся базой для **gx_numeric_scroll_wheel** и **gx_string_ scroll_wheel**. Мини-приложение колесика прокрутки обеспечивает обработку событий, анимацию прокрутки и вычисление выбранных строк для всех типов мини-приложений колесика прокрутки.

Приложения обычно не создают экземпляр универсального мини-приложения колесика прокрутки, поскольку этот тип мини-приложений не предоставляет функцию рисования. Однако доступ к этому API предоставляется для помощи приложениям, которым необходимо создать настраиваемый тип мини-приложения колесика прокрутки.

GX_SCROLL_WHEEL основан на GX_WINDOW, поэтому все API GX_WINDOW могут использоваться с GX_SCROLL_WHEEL и мини-приложениями, производными от GX_SCROLL_WHEEL.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **name**: имя мини-приложения, заданное приложением
- **parent**: родительское мини-приложение (или GX_NULL)
- **total_rows**: общее количество доступных строк
- **style**: флаги стиля мини-приложения
- **id**: присвоенный приложением идентификатор мини-приложения
- **size**: прямоугольник, определяющий начальный размер мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — колесико прокрутки успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение создано.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Call generic create function during custom widget createl. */

UINT custom_scroll_wheel_create(CUSTOM_SCROLL_WHEEL *wheel,
                    GX_CONST GX_CHAR *name,
                    GX_WIDGET *parent,
                    INT total_rows, ULONG style,
                    USHORT id,
                    GX_CONST GX_RECTANGLE *size)
{
    /* create base widget as part of custom create */
    status = gx_scroll_wheel_create(wheel, name, parent,
                            total_rows, style, id, size);

    /* If status is GX_SUCCESS the base scroll wheel has been created. */
}
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_event_process"></a>gx_scroll_wheel_event_process


Функция обработки событий для универсального мини-приложения колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_event_process(
    GX_SCROLL_WHEEL *wheel,
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба предоставляет базовую обработку событий ввода для всех типов мини-приложений колесика прокрутки.

Эта функция предоставляется программному обеспечению приложения для помощи с приложениями, которым требуется создать настраиваемый тип мини-приложения колесика прокрутки. Приложения часто предоставляют собственную функцию обработки событий, но вызывают универсальную обработку событий для мини-приложений колесика прокрутки, которые им не нужно настраивать.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **event**: указатель GX_EVENT

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие колесика прокрутки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Call generic scroll wheel event processing 
    as part of custom event processing function. */

UINT custom_scroll_wheel_event_process(CUSTOM_SCROLL_WHEEL *wheel,
                    GX_EVENT *event)
{
    switch(event->gx_event_type)
    {
    case xyz:
        /* insert custom event handling here */
        break;

    default:
        /* pass all other events to the generic scroll wheel
            event processing */
        gx_scroll_wheel_event_process(wheel, event);
        break;
    }
}
```


### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_gradient_alpha_set"></a>gx_scroll_wheel_gradient_alpha_set


Назначение альфа-значений градиента для необязательного наложения градиента

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_gradient_alpha_set(
    GX_SCROLL_WHEEL *wheel,
    GX_UBYTE start_alpha,
    GX_UBYTE end_alpha);
```

### <a name="description"></a>Описание

Эта служба определяет начальные и конечные альфа-значения для необязательного наложения градиента мини-приложения колесика прокрутки.

Все мини-приложения колесика прокрутки поддерживают эффект исчезания для строк колесика прокрутки, расположенных ближе к верхнему и нижнему краям мини-приложения колесика прокрутки. Этот эффект исчезания достигается путем рисования градиентной карты отображения пикселей поверх строк колесика прокрутки, что делает строки невидимыми по мере отрисовки строк в верхней и нижней частях мини-приложения колесика прокрутки.

Эта служба API позволяет приложению изменять интенсивность эффекта исчезания или полностью отключать этот эффект, устанавливая начальные и конечные альфа-значения равными 0.

Градиентная карта отображения пикселей создается во время выполнения, когда колесико прокрутки изначально становится видимым. Для этого требуется, чтобы служба выделения памяти среды выполнения была определена с помощью _gx_system_memory_allocator_set(). Если функция выделения памяти не определена, то изображение градиента не будет создано и эффект исчезания будет недоступен.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **start_alpha**: начальное альфа-значение наложения градиента
- **end_alpha**: конечное альфа-значение наложения градиента

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — альфа-значения градиента колесика прокрутки успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_scroll_wheel_gradient_alpha_set(&wheel, 240, 0);
/* if status == GX_SUCCESS the wheel gradient alpha values were
Successfully assigned. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_row_height_set"></a>gx_scroll_wheel_row_height_set


Назначение высоты строки для каждой строки колесика

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_row_height_set(
    GX_SCROLL_WHEEL *wheel,
    GX_VALUE row_height);
```

### <a name="description"></a>Описание

Эта служба назначает высоту для каждой строки колесика прокрутки. Обратите внимание, что если колесико прокрутки имеет стиль GX_STYLE_TEXT_SCROLL_WHEEL_ROUND, то высота строки проходит через преобразование, которое эффективно сокращает высоту строки по мере приближения строки к верхней или нижней границе колесика.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **row_height**: значение высоты строки (в пикселях)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — высота строк колесика прокрутки успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_scroll_wheel_row_height_set(&wheel, 40);

/* if status == GX_SUCCESS the wheel row height has been set to 40
pixels. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_gradient_alpha_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_selected_background_set"></a>gx_scroll_wheel_selected_background_set


Назначение фонового изображения для выбранной строки колесика

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_selected_background_set(
    GX_SCROLL_WHEEL*wheel, 
    GX_RESOURCE_ID image_id);
```

### <a name="description"></a>Описание

Эта служба назначает необязательный идентификатор карты отображения пикселей, которая рисуется за выбранной строкой колесика прокрутки. Это можно использовать для выделения выбранной строки, чтобы пользователь мог легко определить, какая строка колесика прокрутки выбрана.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **image_id**: идентификатор карты отображения пикселей, используемой в качестве фонового изображения для выбранной строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — фон колесика прокрутки успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_scroll_wheel_selected_background_set(&wheel,
GX_PIXELMAP_ID_SELECTED_ROW);

/* if status == GX_SUCCESS the background image has been
assigned. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_selected_get"></a>gx_scroll_wheel_selected_get


Получение выбранной в данный момент строки колесика

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_selected_get(
    GX_SCROLL_WHEEL *wheel,
    INT *row);
```

### <a name="description"></a>Описание

Эта служба запрашивает колесико прокрутки, чтобы получить выбранную в данный момент строку. Вызывающий объект должен передать расположение, чтобы вернуть индекс выбранной строки в качестве второго параметра этой функции.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **row**: расположение, в котором будет возвращено значение выбранной строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — выбранная строка колесика успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x19) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
INT row;
status = gx_scroll_wheel_selected_get(&wheel, &row);

/* if status == GX_SUCCESS the selected row has been returned in
the row variable. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_selected_set"></a>gx_scroll_wheel_selected_set


Назначение выбранной строки колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_selected_set(
    GX_SCROLL_WHEEL *wheel,
    INT row);
```

### <a name="description"></a>Описание

Эта служба назначает текущую выбранную строку колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **row**: строка колесика прокрутки для выбора

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — выбранная строка колесика прокрутки успешно выбрана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_scroll_wheel_selected_set(&wheel, 20);

/* if status == GX_SUCCESS the scroll wheel has been set to select
row 20 */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_speed_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_speed_set"></a>gx_scroll_wheel_speed_set


Назначение скорости прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_speed_set(
    GX_SCROLL_WHEEL *wheel,
    GX_FIXED_VAL start_speed_rate,
    GX_FIXED_VAL end_speed_rate,
    GX_VALUE max_steps,
    GX_VALUE delay);
```

### <a name="description"></a>Описание

Эта служба назначает скорость прокрутки для мини-приложения колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **start_speed_rate**: соотношение начальной скорости прокрутки и скорости перемещения колесика
- **end_speed_rate**: соотношение конечной скорости прокрутки и скорости перемещения колесика
- **max_steps**: максимальное число шагов для прокрутки
- **delay**: время задержки каждого шага

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — скорость колесика успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_scroll_wheel_speed_set(&wheel, GX_FIXED_VAL_MAKE(2),
                                GX_FIXED_VAL_MAKE(1) / 2, 10, 2);

/* if status == GX_SUCCESS the scroll wheel speed has been
successfully set. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scroll_wheel_total_rows_set"></a>gx_scroll_wheel_total_rows_set


Назначение общего числа доступных строк колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scroll_wheel_total_rows_set(
    GX_SCROLL_WHEEL *wheel,
    INT total_rows);
```

### <a name="description"></a>Описание

Эта служба назначает число строк, доступных в указанном колесике прокрутки. Мини-приложение колесика прокрутки обычно получает содержимое строки из приложения в виде массива строк или данных строк, предоставляемых пользователем. Этот API информирует колесико прокрутки об общем числе строк, которые должны быть представлены пользователю.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления универсального колесика прокрутки
- **total_rows**: общее число строк колесика, которое будет представлено пользователю

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — общее количество строк колесика прокрутки успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_scroll_wheel_total_rows_set(&wheel, 100);

/* if status == GX_SUCCESS the scroll wheel has been changed to
display 100 total rows */
```
### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_speed_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_text_get

## <a name="gx_scrollbar_draw"></a>gx_scrollbar_draw


Вывод полосы прокрутки

### <a name="prototype"></a>Прототип

```C
VOID gx_scrollbar_draw(GX_SCROLLBAR *scrollbar);
```

### <a name="description"></a>Описание

Эта служба рисует полосу прокрутки. Как для вертикальных, так и для горизонтальных мини-приложений полосы прокрутки используется общая функция рисования. Эта служба вызывается внутри при обновлении холста GUIX, но также может вызываться переопределенными функциями рисования.

### <a name="parameters"></a>Параметры

- **scrollbar**: мини-приложение полосы прокрутки для рисования

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom scrollbar drawing function. */

VOID my_scrollbar_draw(GX_SCROLLBAR *scrollbar)
{
    /* Call default scrollbar draw. */
    gx_scrollbar_draw(thumb);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_horizontal_scrollbar_create
- gx_scrollbar_event_process
- gx_scrollbar_limit_check
- gx_scrollbar_reset
- gx_vertical_scrollbar_create

## <a name="gx_scrollbar_event_process"></a>gx_scrollbar_event_process


Обработка события полосы прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scrollbar_event_process(
    GX_SCROLLBAR *scrollbar,
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие полосы прокрутки. Как для вертикальных, так и для горизонтальных мини-приложений полосы прокрутки используется общая функция обработки событий.

### <a name="parameters"></a>Параметры

- **scrollbar**: блок управления мини-приложением полосы прокрутки
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие полосы прокрутки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
Call generic scrollbar event processing as part of custom event processing function. */

UINT custom_scrollbar_event_process(GX_SCROLLBAR *scrollbar,
                                    GX_EVENT *event)
{
    switch(event->gx_event_type)
    {
    case xyz:
        /* insert custom event handling here */
        break;
    default:
        /* pass all other events to the generic scrollbar
            event processing */
        gx_scrollbar_event_process(scrollbar, event);
        break;
    }
}
```

### <a name="see-also"></a>См. также:

- gx_horizontal_scrollbar_create
- gx_scrollbar_draw
- gx_scrollbar_limit_check
- gx_scrollbar_reset
- gx_vertical_scrollbar_create

## <a name="gx_scrollbar_limit_check"></a>gx_scrollbar_limit_check


Проверка предела полосы прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scrollbar_limit_check(GX_SCROLLBAR *scrollbar);
```

### <a name="description"></a>Описание

Эта служба проверяет ограничение для полосы прокрутки и предотвращает перемещение бегунка полосы прокрутки за пределы элемента.

### <a name="parameters"></a>Параметры

- **scrollbar**: блок управления мини-приложением полосы прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — предел полосы прокрутки успешно проверен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Check scrollbar limit of “my_scrollbar”. */
status = gx_scrollbar_limit_check(&my_scrollbar);

/* If status is GX_SUCCESS the limit of scrollbar “my_scrollbar” has been checked. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_scrollbar_create
- gx_scrollbar_draw
- gx_scrollbar_event_process
- gx_scrollbar_reset
- gx_vertical_scrollbar_create

## <a name="gx_scrollbar_reset"></a>gx_scrollbar_reset


Сброс полосы прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scrollbar_reset(
    GX_SCROLLBAR *scrollbar,
    GX_SCROLL_INFO *info);
```

### <a name="description"></a>Описание

Эта служба сбрасывает полосу прокрутки.

### <a name="parameters"></a>Параметры

- **scrollbar**: блок управления мини-приложением полосы прокрутки
- **info**: указатель на структуру GX_SCROLL_INFO, определяющую ограничения полосы прокрутки, текущее значение и шаг или приращение Определение структуры GX_SCROLL_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — полоса прокрутки успешно сброшена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимые сведения о прокрутке.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Reset scrollbar “my_scrollbar”. */

GX_SCROLL_INFO my_info;

my_info.gx_scroll_value = 0;
my_info.gx_scroll_minimum = 0;
my_info.gx_scroll_maximum = 100;
my_info.gx_scroll_visible = 10;
my_info.gx_scroll_increment = 1;

status = gx_scrollbar_reset(&my_scrollbar, &my_info);

/* If status is GX_SUCCESS the scrollbar “my_scrollbar” has been reset. */
```

### <a name="see-also"></a>См. также:

- gx_horizontal_scrollbar_create
- gx_scrollbar_draw
- gx_scrollbar_event_process
- gx_scrollbar_limit_check
- gx_vertical_scrollbar_create

## <a name="gx_scrollbar_value_set"></a>gx_scrollbar_value_set


Назначение значения полосы прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_scrollbar_value_set(
    GX_SCROLLBAR *scrollbar,
    INT value);
```

### <a name="description"></a>Описание

Эта служба назначает текущее значение полосы прокрутки. Для родительского окна будет сгенерировано событие GX_EVENT_VERTICAL_SCROLL или GX_EVENT_HORIZONTAL_SCROLL.

### <a name="parameters"></a>Параметры

- **scrollbar**: блок управления мини-приложением полосы прокрутки
- **value**: новое значение полосы прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие полосы прокрутки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение прокрутки.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Assign new value for scrollbar “my_scrollbar”. */

status = gx_scrollbar_value_set(&my_scrollbar, 0);

/* If status is GX_SUCCESS the current position of scrollbar “my_scrollbar” has been set. */

```

### <a name="see-also"></a>См. также:

- gx_horizontal_scrollbar_create
- gx_scrollbar_draw
- gx_scrollbar_limit_check
- gx_scrollbar_reset
- gx_vertical_scrollbar_create

## <a name="gx_single_line_text_input_backspace"></a>gx_single_line_text_input_backspace


Обработка символа BACKSPACE в мини-приложении ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_backspace(GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба удаляет символ перед позицией курсора ввода текста. Эта служба вызывается внутри при получении события нажатия клавиши BACKSPACE, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ввод однострочного текста успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x23) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete a character before the cursor of “my_text_input”. */
status = gx_single_line_text_input_backspace(&my_text_input);

/* If status is GX_SUCCESS the character before the cursor has been deleted. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_input_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_buffer_clear"></a>gx_single_line_text_input_buffer_clear


Удаление всех символов из буфера ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_buffer_clear(
    GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба удаляет все символы из буфера ввода текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — буфер ввода однострочного текста успешно очищен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* clear input buffer of “my_text_input”. */
status = gx_single_line_text_input_clear(&my_text_input);

/* If status is GX_SUCCESS the text input widget has emptied its input buffer. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_buffer_backspace
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_input_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_buffer_get"></a>gx_single_line_text_input_buffer_get


Извлечение сведений о буфере мини-приложения ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_buffer_get(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_CHAR **buffer_address, 
    UINT *content_size, 
    UINT *buffer_size);
```

### <a name="description"></a>Описание

Эта служба извлекает сведения о буфере мини-приложения ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **buffer_address**: адрес буфера ввода
- **content_size**: число байтов входных данных
- **buffer_size**: размер входных данных

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о буфере успешно получены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *buffer_address;
UINT buffer_size;
UINT content_size;

/* Retrieve buffer information of “my_text_input” widget. */
status = gx_single_line_text_input_buffer_get(&my_text_input,
                    &buffer_address, &string_size, &buffer_size);

/* If status is GX_SUCCESS the value of buffer_address, string_size and buffer_size has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_character_delete"></a>gx_single_line_text_input_character_delete


Удаление символа в текущей позиции курсора

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_character_delete(
    GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба удаляет символ после позиции курсора ввода текста. Эта служба вызывается внутри при получении события нажатия клавиши DELETE, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — символ после курсора успешно удален.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete the character after the cursor of “my_text_input”. */
status = gx_single_line_text_input_character_delete(&my_text_input);

/* If status is GX_SUCCESS the character after the cursor has been deleted. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_input_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_multi_line_text_input_create
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_character_insert"></a>gx_single_line_text_input_character_insert


Вставка строки символов в текущую позицию курсора

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_character_insert(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_UBYTE *insert_str,
    UINT insert_size);
```

### <a name="description"></a>Описание

Эта служба вставляет символьную строку в буфер строки ввода текста в текущей позиции курсора.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **insert_str**: вставляемая символьная строка
- **insert_size**: число байтов для вставки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — символ успешно вставлен
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Insert characters at current cursor position. */

GX_CHAR insert_text[10] = “insert”;
status = gx_single_line_text_input_character_insert(&my_text_input,
                                            insert_text,
                                            GX_STRLEN(insert_text));

/* If status is GX_SUCCESS the text input widget has successfully insert the string. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_input_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_create"></a>gx_single_line_text_input_create


Создание мини-приложения ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_create(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_CONST GX_CHAR *name, GX_WIDGET *parent,
    GX_CHAR *input_buffer, UINT buffer_size,
    UINT style, USHORT text_input_id, GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение ввода текста. Вызывающий объект должен предоставить хранилище для строки ввода и указать максимальную длину строки.

GX_SINGLE_LINE_TEXT_INPUT является производным от GX_PROMPT и поэтому все службы gx_prompt могут использоваться с мини-приложениями GX_SINGLE_LINE_TEXT_INPUT.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **name**: логическое имя мини-приложения (необязательно)
- **parent**: родительское мини-приложение (необязательно)
- **input_buffer**: область хранения для строки ввода
- **buffer_size**: размер области хранения строки ввода (в байтах)
- **style**: флаги стиля ввода текста
- **text_input_id**: идентификатор мини-приложения ввода (необязательно)
- **size**: прямоугольник, определяющий начальный размер мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ввод однострочного текста успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SINGLE_LINE_TEXT_INPUT my_text_input;
static GX_CHAR           text_input_buffer[100];
GX_RECTANGLE             size;

/* Define widget size. */
gx_utility_rectangle_define(&size, 10, 10, 110, 40);

/* Create single-line text input widget “my_text_input”. */
status = gx_single_line_text_input_create(&my_text_input,
                        “text_input”, GX_NULL, text_input_buffer, 100,
                        GX_STYLE_ENABLED, 0, &size);

/* If status is GX_SUCCESS, the text input widget has been created. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_draw"></a>gx_single_line_text_input_draw


Вывод мини-приложения ввода текста

### <a name="prototype"></a>Прототип

```C
VOID gx_single_line_text_input_draw(GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение ввода текста. Эта служба обычно вызывается внутри во время обновления холста, но также может вызываться из настраиваемых функций рисования ввода текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom single line text input draw function. */

VOID my_sl_text_input_draw(GX_SINGLE_LINE_TEXT_INPUT *input)
{
    /* Call default single line text input draw. */
    gx_single_line_text_input_draw(input);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_draw_position_get"></a>gx_single_line_text_input_draw_position_get


Получение начальной точки вывода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_draw_position_get(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_VALUE *xpos, GX_VALUE *ypos);
```

### <a name="description"></a>Описание

Эта служба получает начальную точку рисования ввода текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **xpos**: полученное начальное положение рисования в виде координаты по оси X
- **ypos**: полученное начальное положение рисования в виде координаты по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор ввода текста успешно перемещен в конец строки.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom single line text input draw function. */

VOID my_sl_text_input_draw(GX_SINGLE_LINE_TEXT_INPUT *input)
{
GX_VALUE xpos;
GX_VALUE ypos;

    /* Draw background. */
    gx_widget_border_draw(input, border_color, upper_fill_color,
                          lower_fill_color, GX_TRUE);

    /* Retrieve text draw start position. */
    gx_single_line_text_input_draw_position_get(input, xpos,
                                                ypos);

    /* Add your text drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_end"></a>gx_single_line_text_input_end


Перемещение курсора ввода текста в конец строки

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_end(GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба размещает курсор мини-приложения ввода текста в конце строки ввода. Эта служба вызывается внутри при получении события нажатия клавиши END, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор ввода текста успешно перемещен в конец строки.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move input cursor to end. */
status = gx_single_line_text_input_end(&my_text_input);

/* If status is GX_SUCCESS, text text input cursor has been moved to end. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_event_process"></a>gx_single_line_text_input_event_process


Функция обработки событий мини-приложения ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_event_process(
    GX_SINGLE_LINE_TEXT_INPUT *text_input, 
    GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие ввода однострочного текста. На эту функцию ссылается функция gx_single_line_text_input_create, но она доступна для использования приложением в случаях, когда приложение определяет настраиваемую функцию обработки события ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **event_ptr**: указатель на структуру GX_EVENT

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие ввода текста успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Call generic single line text input event processing as part of custom event processing function. */

UINT custom_sl_text_input_event_process(
                                GX_SINGLE_LINE_TEXT_INPUT *input,
                                GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;
    default:
        /* Pass all other events to the default single line
            text input event processing */
        status =
        gx_single_line_text_input_event_process(input, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_fill_color_set"></a>gx_single_line_text_input_fill_color_set


Задание цвета фона для ввода однострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_fill_color_set(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_RESOURCE_ID normal_fill_color_id,
    GX_RESOURCE_ID selected_fill_color_id,
    GX_RESOURCE_ID disabled_fill_color_id,
    GX_RESOURCE_ID readonly_fill_color_id);
```

### <a name="description"></a>Описание

Эта служба задает цвет заливки для ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом однострочного текста
- **normal_fill_color_id**: идентификатор ресурса цвета заливки мини-приложения в нормальном состоянии В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **selected_fill_color_id**: идентификатор ресурса цвета заливки мини-приложения, когда фокус перемещается на мини-приложение В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **disabled_fill_color_id**: идентификатор ресурса цвета заливки мини-приложения, если не задан стиль GX_STYLE_ENABLED В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **readonly_fill_color_id**: идентификатор ресурса цвета заливки мини-приложения, если заданы стили GX_STYLE_ENABLED и GX_STYLE_TEXT_INPUT_READYONLY В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет заливки ввода однострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set fill colors for single line text input “my_text_input”. */
status = gx_single_line_text_input_fill_color_set(&my_text_input,
                    GX_COLOR_ID_NORMAL_FILL,
                    GX_COLOR_ID_SELECTED_FILL,
                    GX_COLOR_ID_DISABLED_FILL,
                    GX_COLOR_ID_READONLY_FILL);

/* If status is GX_SUCCESS, the fill color of “my_text_input” was
set. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_home"></a>gx_single_line_text_input_home


Перемещение курсора ввода текста в начальное положение

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_home(GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает позицию курсора текстового ввода в начало строки ввода. Эта служба вызывается внутри при получении события нажатия клавиши HOME, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен в начальное положение.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move cursor to the start of the input text. */
status = gx_single_line_text_input_home(&my_text_input);

/* If status is GX_SUCCESS the cursor has been moved to the home position */
```

### <a name="see-also"></a>См. также:
- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_left_arrow"></a>gx_single_line_text_input_left_arrow


Перемещение курсора ввода на один символ влево

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_left_arrow(GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает курсор ввода текста на один символ влево. Эта служба вызывается внутри при получении события нажатия клавиши СТРЕЛКА ВЛЕВО, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен влево.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move the cursor one character to the left. */
status = gx_single_line_text_input_left_arrow(&my_text_input);

/* If status is GX_SUCCESS the text input cursor has been moved one character to the left. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_position_get"></a>gx_single_line_text_input_position_get

Перемещение курсора на позицию пикселя

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_position_get(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    INT pixel_position);
```

### <a name="description"></a>Описание

Эта служба размещает курсор ввода текста на основе запрошенной позиции пикселя. Индекс курсора ввода текста будет рассчитываться на основе значения позиции пикселя на оси X. Эта служба вызывается внутри при получении события перемещения пера вниз, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **pixel_position**: значение координаты положения пикселя по оси X

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно помещен в требуемое положение.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set cursor to requested position. */
status = gx_single_line_text_input_position_get(&my_text_input,
                                                100);

/* If status is GX_SUCCESS the text input widget cursor has been positioned */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_right_arrow"></a>gx_single_line_text_input_right_arrow


Перемещение курсора ввода на один символ вправо

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_right_arrow(
    GX_SINGLE_LINE_TEXT_INPUT *text_input);
```

### <a name="description"></a>Описание

Эта служба перемещает курсор ввода текста на один символ вправо. Эта служба вызывается внутри при получении события нажатия клавиши СТРЕЛКА ВПРАВО, но также может вызываться приложением.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — курсор успешно перемещен вправо.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move cursor one character to the right. */
status = gx_single_line_text_input_right_arrow(&my_text_input);

/* If status is GX_SUCCESS the text input cursor has been moved one character to the right. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_position_get
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_style_add"></a>gx_single_line_text_input_style_add


Добавление стилей

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_style_add(
    GX_SINGLE_LINE_TEXT_INPUT *text_input, 
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба добавляет указанные стили в мини-приложение ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **style**: новый стиль для добавления Предварительно заданные общие стили для всех мини-приложений см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль успешно добавлен в мини-приложение.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Add a new style to “my_text_input”. */
status = gx_single_line_text_input_style_add(&my_text_input,
GX_STYLE_CURSOR_AWAYS);

/* If status is GX_SUCCESS the GX_STYLE_CUSROSR_SHOR have been successfully added. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gax_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_style_remove"></a>gx_single_line_text_input_style_remove

Удаление стилей

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_style_remove(
    GX_SINGLE_LINE_TEXT_INPUT *text_input, 
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба удаляет указанные стили из мини-приложения ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **style**: удаляемые стили Предварительно заданные общие стили для всех мини-приложений см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стили успешно удалены из мини-приложения.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Remove cursor blink style from “my_text_input”. */
status = gx_single_line_text_input_style_remove(&my_text_input,
GX_STYLE_CURSOR_BLINK);

/* If status is GX_SUCCESS the GX_STYLE_CURSOR_BLINK style has been successfully removed. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_home
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_style_set"></a>gx_single_line_text_input_style_set


Задание стилей ввода текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_style_set(
    GX_SINGLE_LINE_TEXT_INPUT *text_input, 
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба задает указанные стили для мини-приложения ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления мини-приложением ввода однострочного текста
- **style**: флаги стилей для назначения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль ввода текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set style for “my_text_input”. */
status = gx_single_line_text_input_style_set(&my_text_input,
                    GX_STYLE_ENABLED | GX_STYLE_CURSOR_BLINK);

/* If status is GX_SUCCESS the text input style has been successfully set to the specified styles. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_signle_line_text_input_event_process
- gx_single_line_text_input_home
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_text_color_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_text_color_set"></a>gx_single_line_text_input_text_color_set


Задание цвета текста для ввода однострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_text_color_set(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_RESOURCE_ID normal_text_color_id,
    GX_RESOURCE_ID selected_text_color_id,
    GX_RESOURCE_ID disabled_text_color_id,
    GX_RESOURCE_ID readonly_text_color_id);
```

### <a name="description"></a>Описание

Эта служба задает цвет текста для ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом однострочного текста
- **normal_text_color_id**: идентификатор ресурса цвета текста в нормальном состоянии В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **selected_text_color_id**: идентификатор ресурса цвета текста, когда фокус перемещается на мини-приложение В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **disabled_text_color_id**: идентификатор ресурса цвета текста, если не задан стиль GX_STYLE_ENABLED В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **readonly_text_color_id**: идентификатор ресурса цвета текста, если заданы стили GX_STYLE_ENABLED и GX_STYLE_TEXT_INPUT_READONLY В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста для ввода однострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set text colors for single line text input “my_text_input”. */
status = gx_single_line_text_input_text_color_set(&my_text_input,
                                        GX_COLOR_ID_NORMAL_TEXT,
                                        GX_COLOR_ID_SELECTED_TEXT,
                                        GX_COLOR_ID_DISABLED_TEXT,
                                        GX_COLOR_ID_READONLY_TEXT);

/* If status is GX_SUCCESS, the text color “my_text_input” was set. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_select
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_text_select"></a>gx_single_line_text_input_text_select

Выделение текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_text_color_set(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    UINT start_index, UINT end_index);
```

### <a name="description"></a>Описание

Эта служба выбирает введенный текст с указанной начальной и конечной метками и выделяет выбранный текст, используя выбранные цвета заливки и текст.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом однострочного текста
- **start_index**: индекс первого выбранного символа
- **end_index**: индекс последнего выбранного символа

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — однострочный текст успешно выбран.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение индекса.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Select text between index [0, 9]. */
status = gx_single_line_text_input_text_select(&my_text_input,
                                                0,9);

/* If status is GX_SUCCESS, the text between index [0, 9] “my_text_input” was selected. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_set

## <a name="gx_single_line_text_input_text_set"></a>gx_single_line_text_input_text_set


Задание текста для ввода однострочного текста (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_text_set(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_CONST GX_CHAR *text);
```

### <a name="description"></a>Описание

Эта служба не рекомендуется. Вместо нее следует использовать gx_single_line_text_input_text_set_ext().

Эта служба задает текст для ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом однострочного текста
- **text**: текстовая строка, заканчивающаяся NULL

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста для ввода однострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CONST GX_CHAR new_text = “Set Single Line Text Input Text”;

/* Set text for single line text input “my_text_input”. */
status = gx_single_line_text_input_text_set(&my_text_input,
                                        new_text);

/* If status is GX_SUCCESS, the text of “my_text_input” was set. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_text_set_ext

## <a name="gx_single_line_text_input_text_set_ext"></a>gx_single_line_text_input_text_set_ext


Задание текста для ввода однострочного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_single_line_text_input_text_set(
    GX_SINGLE_LINE_TEXT_INPUT *text_input,
    GX_CONST GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба задает текст для ввода однострочного текста.

### <a name="parameters"></a>Параметры

- **text_input**: блок управления вводом однострочного текста
- **string**: переменная GX_STRING

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста для ввода однострочного текста успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING new_string;
new_string.gx_string_ptr = “Set Single Line Text Input Text”;
new_string.gx_string_length = strlen(new_string.gx_string_ptr);

/* Set text for single line text input “my_text_input”. */
status = gx_single_line_text_input_text_set_ext(&my_text_input,
                                        &new_string);

/* If status is GX_SUCCESS, the string has been assigned to my_text_input. */
```

### <a name="see-also"></a>См. также:

- gx_single_line_text_input_backspace
- gx_single_line_text_input_buffer_clear
- gx_single_line_text_input_buffer_get
- gx_single_line_text_input_character_delete
- gx_single_line_text_input_character_insert
- gx_single_line_text_input_create
- gx_single_line_text_input_draw
- gx_single_line_text_draw_position_get
- gx_single_line_text_input_end
- gx_single_line_text_input_event_process
- gx_single_line_text_input_fill_color_set
- gx_single_line_text_input_home
- gx_single_line_text_input_left_arrow
- gx_single_line_text_input_position_get
- gx_single_line_text_input_right_arrow
- gx_single_line_text_input_style_add
- gx_single_line_text_input_style_remove
- gx_single_line_text_input_style_set
- gx_single_line_text_input_text_set_ext


## <a name="gx_slider_create"></a>gx_slider_create

Создание ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_create(
    GX_SLIDER *slider, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent, INT tick_count,
    GX_SLIDER_INFO *slider_info,
    ULONG style, USHORT slider_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение ползунка.

GX_SLIDER является производным от GX_WIDGET, поэтому все службы API gx_widget могут использоваться с мини-приложениями типа GX_SLIDER.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **name**: имя ползунка
- **parent**: указатель на родительское мини-приложение
- **tick_count**: количество делений ползунка
- **slider_info**: указатель на сведения о ползунке, который представляет собой структуру, используемую для передачи предельного значения ползунка, размера и положения стрелки ползунка, а также других параметров ползунка Определение структуры GX_SLIDER_INFO см. в **приложении I**.
- **style**: стиль ползунка Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **slider_id**: определяемый приложением идентификатор ползунка
- **size**: размеры ползунка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ползунок успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create slider “my_slider”. */

GX_SLIDER my_slider;
GX_SLIDER_INFO info;

info.gx_slider_info_min_val = 0;
info.gx_slider_info_max_val = 100;
info.gx_slider_info_current_val = 50;
info.gx_slider_info_increment = 1;
info.gx_slider_info_min_travel = 20;
info.gx_slider_info_max_travel = 20;
info.gx_slider_info_needle_width = 10;
info.gx_slider_info_needle_height = 10;
info.gx_slider_info_needle_inset = 5;
info.gx_slider_info_needle_hotspot_offset = 5;

status = gx_slider_create(&my_slider, “my_slider”,
    &my_parent, 10, info, GX_STYLE_ENABLED, ID_MY_SLIDER, &size);

/* If status is GX_SUCCESS the slider “my_slider” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_draw"></a>gx_slider_draw

Вывод ползунка

### <a name="prototype"></a>Прототип

```C
VOID gx_slider_draw(GX_SLIDER *slider);
```

### <a name="description"></a>Описание

Эта служба рисует ползунок. Эта служба используется внутри функцией gx_slider_create, но она также предоставляется для использования приложением в этих экземплярах, когда определена настраиваемая функция рисования ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom slider draw function. */
VOID my_slider_draw(GX_SLIDER *slider)
{
    /* Call default slider draw. */
    gx_slider_draw(slider);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_event_process"></a>gx_slider_event_process

Обработка события ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_event_process(
    GX_SLIDER *slider, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие ползунка. На эту функцию ссылается функция gx_slider_create, но она доступна для использования приложением в случаях, когда приложение определяет настраиваемую функцию обработки события ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие ползунка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic slider event processing as part of custom event processing function. */

UINT custom_slider_event_process(GX_SLIDER *slider,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;
    switch(event->gx_event_type)
    {
        case xyz:
            /* Insert custom event handling here */
            break;
        default:
            /* Pass all other events to the default slider event processing */
            status = gx_slider_event_process(slider, event);
            break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_info_set"></a>gx_slider_info_set

Задание блока сведений о ползунке

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_info_set(
    GX_SLIDER *slider, 
    GX_SLIDER_INFO *info);
```

### <a name="description"></a>Описание

Эта служба назначает указанному ползунку определенные сведения о ползунке, такие как минимальное, максимальное и текущее значения указанного ползунка. Ползунок обновит положение стрелки и будет перерисовываться на основе новых данных ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **info**: указатель на информационную структуру ползунка Определение структуры GX_SLIDER_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о ползунке успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SLIDER_INFO my_slider_info;
my_slider_info.gx_slider_info_min_val = 0;
my_slider_info.gx_slider_info_max_val = 100;
my_slider_info.gx_slider_info_current_val = 50;
my_slider_info.gx_slider_info_increment = 1;
my_slider_info.gx_slider_info_min_travel = 20;
my_slider_info.gx_slider_info_max_travel = 20;
my_slider_info.gx_slider_info_needle_width = 10;
my_slider_info.gx_slider_info_needle_height = 10;
my_slider_info.gx_slider_info_needle_inset = 5;

my_slider_info.gx_slider_info_needle_hotspot_offset = 5;

/* Set slider information for slider “my_slider”. */
status = gx_slider_info_set (&my_slider, &my_slider_info);

/* If status is GX_SUCCESS the “my_slider” is configured with my_slider_info. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_needle_draw"></a>gx_slider_needle_draw

Вывод стрелки ползунка

### <a name="prototype"></a>Прототип

```C
VOID gx_slider_needle_draw(GX_SLIDER *slider);
```

### <a name="description"></a>Описание

Эта служба рисует стрелку ползунка. Эта служба вызывается автоматически функцией gx_slider_draw, но может также вызываться приложением как часть настраиваемой функции рисования ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom slider draw function. */
VOID my_slider_draw(GX_SLIDER *slider)
{
    /* Add your own background draw here. */

    /* Call default tickmarks draw. */
    gx_slider_tickmarks_draw(slider);

    /* Call default slider needle draw. */
    gx_slider_needle_draw(slider);
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_needle_position_get"></a>gx_slider_needle_position_get

Получение стрелки ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_needle_position_get(
    GX_SLIDER *slider,
    GX_SLIDER_INFO *slider_info,
    GX_RECTANGLE *return_position);
```

### <a name="description"></a>Описание

Эта служба вычисляет положение стрелки ползунка на основе текущего значения ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **slider_info**: указатель на структуру сведений о ползунке, определяющую пределы ползунка, размер стрелки и смещение, а также другие параметры ползунка Определение структуры GX_SLIDER_INFO см. в **приложении I**.
- **return_position**: указатель на назначение для позиции стрелки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — положение стрелки ползунка успешно получено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимые сведения о ползунке.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RECTANGLE needle_position;

/* Get the needle position for slider “my_slider”. */
status = gx_slider_needle_posistion_get(&my_slider, &slider_info,
    &needle_position);

/* If status is GX_SUCCESS the needle position for slider “my_slider” has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_tickmarks_draw"></a>gx_slider_tickmarks_draw

Вывод делений ползунка

### <a name="prototype"></a>Прототип

```C
VOID gx_slider_tickmarks_draw(GX_SLIDER *slider);
```

### <a name="description"></a>Описание

Эта служба рисует деления ползунка. Эта функция вызывается внутри функцией gx_slider_draw, но предоставляется для использования приложениями, которые могут реализовать настраиваемую функцию рисования ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom slider draw function. */
VOID my_slider_draw(GX_SLIDER *slider)
{
    /* Add your own background draw here. */

    /* Call default tickmarks draw. */
    gx_slider_tickmarks_draw(slider);

    /* Call default slider needle draw. */
    gx_slider_needle_draw(slider);
}
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_travel_get
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_travel_get"></a>gx_slider_travel_get

Получение перемещения ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_travel_get(
    GX_SLIDER *widget,
    GX_SLIDER_INFO *info,
    INT *return_min_travel, 
    INT *return_max_travel);
```

### <a name="description"></a>Описание

Эта служба получает перемещение ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **info**: указатель на структуру сведений о ползунке Определение структуры GX_SLIDER_INFO см. в **приложении I**.
- **return_min_travel**: указатель на место назначения для минимального значения перемещения</td>
- **return_max_travell**: указатель на место назначения для максимального значения перемещения</td>

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — перемещение ползунка успешно получено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимые сведения о ползунке.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get travel information for for slider “my_slider”. */
status = gx_slider_travel_get(&my_slider, &info,
    &my_min_travel, &my_max_travel);

/* If status is GX_SUCCESS the travel max/min values for slider “my_slider” have been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_value_calculate
- gx_slider_value_set

## <a name="gx_slider_value_calculate"></a>gx_slider_value_calculate

Вычисление значения ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_value_calculate(
    GX_SLIDER *slider, 
    GX_SLIDER_INFO *info,
    INT new_position);
```

### <a name="description"></a>Описание

Эта служба вычисляет значение ползунка на основе положения стрелки ползунка. Эта функция вызывается GUIX внутри, когда пользователь перемещает стрелку ползунка, но также может вызываться приложением при реализации настраиваемого мини-приложения ползунка.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **info**: указатель на сведения о ползунке Определение структуры GX_LISDER_INFO см. в **приложении I**.
- **new_position**: новое положение ползунка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение ползунка успешно вычислено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимые сведения о ползунке.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Calculate new value for slider “my_slider”. */
status = gx_slider_value_calculate(&my_slider,
    &my_slider.gx_slider_info, new_slider_position);

/* If status is GX_SUCCESS the slider value of “my_slider” has been calculated. */

```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_set

## <a name="gx_slider_value_set"></a>gx_slider_value_set

Задание значения ползунка

### <a name="prototype"></a>Прототип

```C
UINT gx_slider_value_set(
    GX_SLIDER *slider,
    GX_SLIDER_INFO *info, 
    INT new_value);
```

### <a name="description"></a>Описание

Эта служба задает значение ползунка. Этот API может быть вызван приложением для перемещения стрелки ползунка под управлением программы, минуя необходимость ввода данных путем перетаскивания стрелки ползунка пользователем.

### <a name="parameters"></a>Параметры

- **slider**: блок управления мини-приложением ползунка
- **info**: указатель на структуру сведений о ползунке Определение структуры GX_SLIDER_INFO см. в **приложении I**.
- **new_value**: новое значение ползунка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — значение ползунка успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set new value for slider “my_slider”. */
status = gx_slider_value_set(&my_slider,
    &my_slider.gx_slider_info, new_value);

/* If status is GX_SUCCESS the new value has been set for slider “my_slider”. */
```

### <a name="see-also"></a>См. также:

- gx_pixelmap_slider_create
- gx_pixelmap_slider_draw
- gx_pixelmap_slider_event_process
- gx_pixelmap_slider_pixelmap_set
- gx_slider_create
- gx_slider_draw
- gx_slider_event_process
- gx_slider_needle_draw
- gx_slider_needle_position_get
- gx_slider_needle_position_get
- gx_slider_tickmarks_draw
- gx_slider_travel_get
- gx_slider_value_calculate

##  <a name="gx_sprite_create"></a>gx_sprite_create

Создание мини-приложения спрайта

### <a name="prototype"></a>Прототип

```C
UINT gx_sprite_create(
    GX_SPRITE *sprite, GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    GX_SPRITE_FRAME *frame_list,
    USHORT frame_count,
    ULONG style, USHORT sprite_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение GX_SPRITE. Спрайт используется для отображения последовательности карт отображения пикселей в виде анимации или может использоваться как мини-приложение отображения карты отображения пикселей с несколькими состояниями.

GX_SPRITE является производным от GX_WIDGET и поддерживает все службы API gx_widget.

Мини-приложению GX_SPRITE требуется массив структур GX_SPRITE_FRAME, чтобы определить анимацию спрайта. Определение структуры GX_PRITE_FRAME см. в **приложении I**.

### <a name="parameters"></a>Параметры

- **sprite**: блок управления мини-приложением спрайта
- **name**: имя спрайта (необязательно)
- **parent**: указатель на родительское мини-приложение
- **frame_list**: массив структур GX_SPRITE_FRAME
- **frame_count** указывает количество записей в массиве списка кадров
- **style**: стиль спрайта Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **sprite_id**: определяемый приложением идентификатор спрайта
- **size**: размеры спрайта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — спрайт успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create sprite “my_sprite”. */

status = gx_sprite_create(&my_sprite, “my_sprite”,
    &my_parent,
    sprite_frame_list, frame_count,
    GX_STYLE_SPRITE_AUTO|GX_STYLE_SPRITE_LOOP,
    ID_MY_SPRITE, &size);

/* If status is GX_SUCCESS the sprite “my_sprite” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_sprite_start
- gx_sprite_stop
- gx_sprite_current_frame_set
- gx_sprite_frame_list_set

## <a name="gx_sprite_current_frame_set"></a>gx_sprite_current_frame_set

Назначение кадра спрайта

### <a name="prototype"></a>Прототип

```C
UINT gx_sprite_current_frame_set(
    GX_SPRITE *sprite, 
    USHORT frame);
```

### <a name="description"></a>Описание

Эта служба назначает текущий кадр спрайта. Если мини-приложение GX_SPRITE не запускается автоматически, оно может использоваться как управляемое программой состояние освещения, отображая карту отображения пикселей, заданную командой.

### <a name="parameters"></a>Параметры

- **sprite**: блок управления мини-приложением спрайта
- **frame**: рамка спрайта для вывода

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Assign frame number 3 as the current sprite frame */
status = gx_sprite_current_frame_set(&my_sprite, 3);

/* If status is GX_SUCCESS the sprite “my_sprite” will display frame index 3. */
```

### <a name="see-also"></a>См. также:

- gx_sprite_start
- gx_sprite_stop
- gx_sprite_create
- gx_sprite_frame_list_set

## <a name="gx_sprite_frame_list_set"></a>gx_sprite_frame_list_set

Назначение или изменение списка кадров спрайта

### <a name="prototype"></a>Прототип

```C
UINT gx_sprite_frame_list_set(
    GX_SPRITE *sprite,
    GX_SPRITE_FRAME *frame_list,
    USHORT frame_count);
```

### <a name="description"></a>Описание

Эта служба может использоваться для назначения или повторного назначения списка кадров, используемого мини-приложением спрайта после создания мини-приложения спрайта. Сведения о содержимом списка кадров спрайта см. в документации по API gx_sprite_create.

### <a name="parameters"></a>Параметры

- **sprite**: блок управления мини-приложением спрайта
- **frame_list**: массив структур GX_SPRITE_FRAME или GX_NULL, если список кадров отсутствует
- **frame_count**: число кадров в массиве списка кадров

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — список кадров спрайта успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Assign framelist_1, which has 10 frames, to my_sprite */
status = gx_sprite_frame_list_set(&my_sprite, framelist_1, 10);

/* If status is GX_SUCCESS the new frame list is now associated with this sprite */
```

### <a name="see-also"></a>См. также:

- gx_sprite_current_frame_set
- gx_sprite_stop
- gx_sprite_create
- gx_sprite_create

## <a name="gx_sprite_start"></a>gx_sprite_start

Запуск последовательности выполнения спрайта

### <a name="prototype"></a>Прототип

```C
UINT gx_sprite_start(
    GX_SPRITE *sprite, 
    USHORT frame);
```

### <a name="description"></a>Описание

Эта служба запускает последовательность автоматического выполнения спрайта. Мини-приложение спрайта будет циклически перебирать кадры спрайта, пока не будет достигнут последний кадр, или будет выполнять это непрерывно, если задан стиль GX_SPRITE_LOOP.

### <a name="parameters"></a>Параметры

- **sprite**: блок управления мини-приложением спрайта
- **frame**: начальный кадр спрайта для показа; обычно кадр 0

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — выполнение спрайта успешно запущено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Start the sprite “my_sprite” */
status = gx_sprite_start(&my_sprite, 0);

/* If status is GX_SUCCESS the sprite “my_sprite” will start running */
```

### <a name="see-also"></a>См. также:

- gx_sprite_current_frame_set
- gx_sprite_stop
- gx_sprite_create
- gx_sprite_frame_list_set

## <a name="gx_sprite_stop"></a>gx_sprite_stop

Остановка последовательности выполнения спрайта

### <a name="prototype"></a>Прототип

```C
UINT gx_sprite_stop(GX_SPRITE *sprite);
```

### <a name="description"></a>Описание

Эта служба останавливает последовательность автоматического выполнения спрайта.

### <a name="parameters"></a>Параметры

- **sprite**: блок управления мини-приложением спрайта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — выполнение спрайта успешно остановлено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Stop the sprite sequence */
status = gx_sprite_stop(&my_sprite);

/* If status is GX_SUCCESS the sprite “my_sprite” is stopped. */
```

### <a name="see-also"></a>См. также:

- gx_sprite_current_frame_set
- gx_sprite_start
- gx_sprite_create
- gx_sprite_frame_list_set

## <a name="gx_string_scroll_wheel_create"></a>gx_string_scroll_wheel_create

Создание колесика прокрутки типа строки

### <a name="prototype"></a>Прототип

```C
UINT gx_string_scroll_wheel_create(
    GX_STRING_SCROLL_WHEEL *wheel,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent, 
    INT total_rows,
    GX_CONST GX_CHAR **string_list, 
    ULONG style,
    USHORT Id, 
    GX_CONST GX_RECTANGLE *size)
```

### <a name="description"></a>Описание

Эта служба создает колесико прокрутки типа строки.

GX_STRING_SCROLL_WHEEL является производным от GX_TEXT_SCROLL_WHEEL, поэтому все функции API gx_text_scroll_wheel могут использоваться с мини-приложениями GX_STRING_SCROLL_WHEEL.

Приложение может передать простой массив строк в функцию создания, которая определяет строки, которые будут отображаться колесиком прокрутки, или же приложение может передать GX_NULL в качестве параметра string_list и вызвать API `gx_string_scroll_wheel_string_id_list_set()` для предоставления массива идентификаторов строк. Во втором случае колесико прокрутки строк будет автоматически переключать отображаемые строки при изменении активного языка приложения.

В качестве альтернативы, если отображаемые строки не определены статически или не известны во время создания колесика прокрутки, приложение может передать GX_NULL в качестве параметра списка строк, а также вызвать функцию API gx_text_scroll_wheel_callback_set(), чтобы определить функцию обратного вызова, которая предоставит строки для отображения в режиме реального времени по мере необходимости.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки
- **name**: имя мини-приложения, определенное приложением
- **parent**: родительский элемент колесика или GX_NULL
- **total_rows**: общее число строк, отображаемых для пользователя
- **string_list**: статически определенный массив строк или GX_NULL
- **style**: флаги требуемого стиля
- **Id**: определенные приложением флаги стиля колесика
- **size**: размер исходного колесика прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — колесико прокрутки строки успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CONST GX_CHAR *days[] = {
    “Sunday”,
    “Monday”,
    “Tuesday”,
    “Wednesday”,
    “Thursday”,
    “Friday”,
    “Saturday”
};

GX_STRING_SCROLL_WHEEL wheel;

/* Create the string scroll wheel. */

status = gx_string_scroll_wheel_create(&wheel, “Day Wheel”,
    root, 7, days,
    GX_STYLE_ENABLED|GX_STYLE_TEXT_CENTER|GX_STYLE_TRANSPARENT|
    GX_STYLE_WRAP|GX_STYLE_TEXT_SCROLL_WHEEL_ROUND,
    ID_SCROLL_WHEEL_DAY, &size);

/* If status is GX_SUCCESS the string scroll wheel has been created. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_string_id_list_set
- gx_string_scroll_wheel_string_list_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_string_scroll_wheel_event_process"></a>gx_string_scroll_wheel_event_process


Обработка события колесика прокрутки строки

### <a name="prototype"></a>Прототип

```C
UINT gx_string_scroll_wheel_string_id_list_set(
    GX_STRING_SCROLL_WHEEL *wheel,
    GX_CONST GX_RESOURCE_ID *string_id_list, 
    INT id_count);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного колесика прокрутки строки. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий строкового колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления строкового колесика прокрутки
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие строкового колесика прокрутки успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic string scroll wheel event processing as part of custom event processing function. */

UINT custom_string_scroll_wheel_event_process(GX_STRING_SCROLL_WHEEL *wheel, GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default string scroll wheel event processing */
        status = gx_string_scroll_wheel_event_process(wheel, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_string_id_list_set
- gx_string_scroll_wheel_string_list_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_string_scroll_wheel_string_id_list_set"></a>gx_string_scroll_wheel_string_id_list_set

Назначение массива идентификаторов строк

### <a name="prototype"></a>Прототип

```C
UINT gx_string_scroll_wheel_string_id_list_set(
    GX_STRING_SCROLL_WHEEL *wheel,
    GX_CONST GX_RESOURCE_ID *string_id_list, INT id_count);
```

### <a name="description"></a>Описание

Эта служба назначает массив идентификаторов строк для мини-приложения строкового колесика прокрутки. Этот метод назначения строк строковому колесику прокрутки рекомендуется, если строки определены статически и мини-приложение должно выполняться на нескольких языках. Если требуется использовать этот API, сначала необходимо создать мини-приложение колесика прокрутки со списком строк GX_NULL.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки
- **string_id_list**: массив идентификаторов строк
- **id_count**: размер списка идентификаторов

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — массив идентификаторов строк успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер списка идентификаторов.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CONST RESOURCE_ID wheel_ids[] = {
    GX_STRING_ID_SUNDAY,
    GX_STRING_ID_MONDAY,
    GX_STRING_ID_TUESDAY,
    GX_STRING_ID_WEDNESDAY,
    GX_STRING_ID_THURSDAY,
    GX_STRING_ID_FRIDAY,
    GX_STRING_ID_SATURDAY
};

GX_STRING_SCROLL_WHEEL wheel;

/* Stop the sprite sequence */

status = gx_string_scroll_wheel_string_id_list_set(&wheel, wheel_ids, 7);

/* If status is GX_SUCCESS the ID list has been assigned. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_string_list_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_string_scroll_wheel_string_list_set"></a>gx_string_scroll_wheel_string_list_set

Назначение массива строк

### <a name="prototype"></a>Прототип

```C
UINT gx_string_scroll_wheel_string_list_set(
    GX_STRING_SCROLL_WHEEL *wheel,
    GX_CONST GX_CHAR *string_list, 
    INT string_count);
```

### <a name="description"></a>Описание

Эта служба назначает массив строк для мини-приложения строкового колесика прокрутки. Ее можно использовать для изменения строк, отображаемых после первоначального создания мини-приложения.

Обратите внимание, что string_scroll_wheel не поддерживает GX_STYLE_TEXT_COPY, поэтому массив строк, передаваемых в эту функцию, должен быть статически определен приложением.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки
- **string_list**: массив указателей строк
- **string_count**: размер массива строк

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строки для колесика прокрутки успешно заданы.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимый размер списка строк.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CONST GX_CHAR *days[] = {
    “Sunday”,
    “Monday”,
    “Tuesday”,
    “Wednesday”,
    “Thursday”,
    “Friday”,
    “Saturday”
};

GX_STRING_SCROLL_WHEEL wheel;

/* Set the array of strings to the scroll wheel. */

status = gx_string_scroll_wheel_string_list_set(&wheel, days, 7);

/* If status is GX_SUCCESS the string array has been assigned. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_string_scroll_wheel_create
- gx_string_scroll_wheel_event_process
- gx_string_scroll_wheel_string_list_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_studio_widget_create"></a>gx_studio_widget_create

Создание мини-приложения, определенного в файле спецификаций, который сформирован Studio

### <a name="prototype"></a>Прототип

```C
GX_WIDGET *gx_studio_widget_create(
    GX_BYTE *control,
    GX_CONST GX_STUDIO_WIDGET *definition, 
    GX_WIDGET *parent);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение и его дочерние элементы с помощью спецификации мини-приложения, определенной в файле спецификаций, который сформирован в GUIX Studio. Эта функция позволяет избежать поиска аналогичной функции по имени `gx_studio_named_widget_create()`.

Структура GX_STUDIO_WIDGET определяется в файле заголовка спецификаций приложения, созданном GUIX Studio.

Для статически выделяемых мини-приложений блок управления мини-приложением определяется в созданном файле specifications.c, а имя мини-приложения определяется в GUIX Studio. Для динамически выделяемых мини-приложений приложение должно передавать GX_NULL в качестве адреса блока управления мини-приложением, и функция будет пытаться динамически выделить блок управления мини-приложением с помощью функции `gx_system_memory_allocate()`, которая также определяется и предоставляется приложением.

Чтобы приложение непосредственно ссылалось на определение мини-приложения GUIX Studio в сформированном файле спецификаций, необходимо использовать соглашение об именовании, применяемое генератором кода GUI Studio. Имя структуры GX_STUDIO_WIDGET, сгенерированной в файле specifications.c, всегда имеет следующий формат: <имя мини-приложения>_define, — где поле <имя мини-приложения> может повторяться несколько раз, если мини-приложение является дочерним по отношению к дочернему мини-приложению.

### <a name="parameters"></a>Параметры

- **control**: указатель на блок элемента управления мини-приложением или GX_NULL в случае динамического выделения
- **definition**: структура определения мини-приложения, сгенерированная Studio
- **parent**: указатель на родительский элемент мини-приложения, если таковой имеется

### <a name="return-values"></a>Возвращаемые значения

Указатель на блок управления созданным мини-приложением или GX_NULL, если создание завершилось сбоем.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create the widget “playlist_screen”, which is statically allocated. */

widget = gx_studio_widget_create(&playlist_screen,
    &playlist_screen_define,
    root_window);

/* If widget != GX_NULL the widget was created. */

/* create the widget “songs_screen”, which is dynamically allocated */

widget = gx_studio_widget_create(GX_NULL,
    &songs_screen_define, root_window);
```

### <a name="see-also"></a>См. также:

- gx_studio_named_widget_create

## <a name="gx_studio_named_widget_create"></a>gx_studio_named_widget_create

Создание мини-приложения, определенного в файле спецификаций, который сформирован Studio

### <a name="prototype"></a>Прототип

```C
UINT *gx_studio_named_widget_create(
    char *name, 
    GX_WIDGET *parent,
    GX_WIDGET **new_widget);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение и его дочерние элементы с помощью спецификации мини-приложения, определенной в файле спецификаций, который сформирован в GUIX Studio.

Эта функция API используется для создания экранов верхнего уровня с использованием имени экрана, указанного в приложении GUIX Studio, в качестве идентификатора определения мини-приложения.

### <a name="parameters"></a>Параметры

- **name**: имя экрана, определенное в приложении GUIX Studio
- **parent**: указатель на родительский элемент мини-приложения, если таковой имеется
- **new_widget**: расположение для возврата указателя созданного мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_FAILURE** (0x11) — не удалось найти мини-приложение с таким именем.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create the widget named “child_popup”,
   which is a child of the top-level screen “main_screen”. */

GX_WIDGET *menu_screen;

status = gx_studio_named_widget_create(“main_menu”,
    &root_window, &menu_screen);

/* If status == GX_SUCCESS the screen was created
   and linked to the root window. */
```

### <a name="see-also"></a>См. также:

- gx_studio_widget_create

## <a name="gx_studio_display_configure"></a>gx_studio_display_configure

Настройка дисплея, определенного в проекте GUIX Studio

### <a name="prototype"></a>Прототип

```C
UINT *gx_studio_display_configure(
    USHORT display,
    UINT (*driver)(GX_DISPLAY *),
    USHORT language, 
    USHORT theme,
    GX_WINDOW_ROOT **return_root);
```

### <a name="description"></a>Описание

Эта служба инициализирует GX_DISPLAY, чтобы она была готова к использованию. Эта функция объединяет функции для инициализации блока управления GX_DISPLAY, создания холста по размерам дисплея и создания корневого окна для холста. Эта функция также устанавливает язык и тему ресурса, запрошенные после инициализации экрана.

Эта функция объединяет усилия по программированию, которые чаще всего необходимы для подготовки дисплея к использованию. Функция вызывает функции gx_display_create(), gx_display_color_table_set, gx_display_font_table_set, gx_display_pixelmap_table_set, gx_system_language_table_set, gx_system_active_language_set, gx_system_scroll_appearance_set, gx_canvas_create и gx_window_root_create, все или часть из которых потребовались бы приложению.

### <a name="parameters"></a>Параметры

- **display**: индекс в таблице дисплеев, соответствующий определениям дисплея в файле проекта Studio
- **driver**: указатель на функцию для инициализации драйвера дисплея Эта функция вызывается для инициализации косвенных указателей функций блока управления GX_DISPLAY, а также для выполнения необходимой настройки оборудования.
- **language**: исходный индекс таблицы языков
- **language**: исходный индекс темы
- **root**: указатель на переменную, в которой возвращается адрес корневого окна, или GX_NULL.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно.
- **GX_FAILURE** (0x11) — не удалось инициализировать дисплей.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create the widget named “child_popup”, which is a child of the
   top-level screen “main_screen”. */

GX_WIDGET *menu_screen;

status = gx_studio_display_configure(MAIN_DISPLAY,
    my_driver_setup, LANGUAGE_ENGLISH, DEFAULT_THEME, GX_NULL);

/* If status == GX_SUCCESS the display was initialized, a canvas was
created for the display, a root window was created for the canvas, and
the requested language and theme have been installed.
*/
```

### <a name="see-also"></a>См. также:

- gx_display_create
- gx_display_color_table_set
- gx_display_font_table_set
- gx_display_pixelmap_table_set
- gx_system_language_table_set
- gx_system_active_language_set
- gx_system_scroll_appearance_set
- gx_canvas_create
- gx_window_root_create

## <a name="gx_system_active_language_set"></a>gx_system_active_language_set

Задание активного языка

### <a name="prototype"></a>Прототип

```C
UINT gx_system_active_language_set(GX_UBYTE language);
```

### <a name="description"></a>Описание

Эта служба задает текущий язык. Индекс языка должен быть меньше, чем число столбцов в таблице строк приложения. Эта функция устарела, и вместо нее теперь используется gx_display_active_language_set. Все новые приложения должны использовать gx_display_active_langauge_set.

### <a name="parameters"></a>Параметры

- **language**: индекс языка, определенный в файле заголовка ресурса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — активный язык успешно задан.
- **GX_CALLER_ERROR**** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR**** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE**** (0x22) — недопустимый индекс языка.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set active language and mark widget canvas as dirty. */
status = gx_system_active_language_set(ID_LANGUAGE_ENGLISH);

/* If status is GX_SUCCESS the active language has been assigned. */
```

### <a name="see-also"></a>См. также:

- gx_display_language_table_set
- gx_display_active_langauge_set
- gx_display_string_get

## <a name="gx_system_animation_get"></a>gx_system_animation_get

Получение блока управления анимацией из системного пула

### <a name="prototype"></a>Прототип

```C
UINT gx_system_animation_get(GX_ANIMATION **animation);
```

### <a name="description"></a>Описание

Эта служба может использоваться для получения блока управления анимацией из пула таких блоков управления, поддерживаемых компонентом gx_system. Пул блоков управления анимацией и связанные службы API предоставляются только в том случае, если константа GX_ANIMATION_POOL_SIZE определена со значением 0. Значение по умолчанию для этого параметра составляет 6, то есть пул блоков управления анимацией содержит блок управления GX_ANIMATION для размера.

Блок управления анимацией, выделенный с помощью этого API, автоматически возвращается в свободный пул, если анимация выполняется до завершения. Если анимация остановлена с помощью gx_animation_stop или не запускается из-за возвращенной ошибки, блок управления анимацией должен возвращаться приложением в свободный пул с помощью gx_system_animation_free.

### <a name="parameters"></a>Параметры

- **animation**: адрес указателя для получения указателя GX_ANIMATION указателя

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — блок управления анимацией успешно получен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель анимации.
- **GX_OUT_OF_ANIMATIONS** (0x31) — системный пул анимации исчерпан.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION *animation;

UINT status = gx_system_animation_get(&animation);

if (status == GX_SUCCESS)
{
    gx_animation_start(animation, animation_info);
}
```

### <a name="see-also"></a>См. также:

- gx_animation_create
- gx_animation_start
- gx_animation_stop
- gx_system_animation_free

## <a name="gx_system_animation_free"></a>gx_system_animation_free

Возврат блока управления анимацией в системный пул

### <a name="prototype"></a>Прототип

```C
UINT gx_system_animation_free(GX_ANIMATION *animation);
```

### <a name="description"></a>Описание

Эта служба может использоваться для возврата блока управления анимацией в системный пул. Пул блоков управления анимацией и связанные службы API предоставляются только в том случае, если константа GX_ANIMATION_POOL_SIZE определена со значением 0. Значение по умолчанию для этого параметра составляет 6, то есть пул блоков управления анимацией содержит блок управления GX_ANIMATION для размера.

Блок управления анимацией, выделенный с помощью gx_system_animation_get(), автоматически возвращается в свободный пул, если анимация выполняется до завершения. Попытка вернуть блок управления анимацией в свободный пул, который уже был возвращен в свободный пул, не будет иметь никакого эффекта.

Если анимация остановлена с помощью gx_animation_stop или не запускается из-за возвращенной ошибки, блок управления анимацией, полученный с помощью gx_system_animation_get(), должен возвращаться приложением в свободный пул с помощью gx_system_animation_free().

Анимация должна находиться в состоянии IDLE, прежде чем ее можно будет вернуть в свободный пул. Анимация находится в состоянии IDLE, когда она не была запущена, когда она остановлена или когда она выполняется до завершения.

### <a name="parameters"></a>Параметры

- **animation**: указатель на блок управления GX_ANIMATION

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — блок управления анимацией успешно освобожден.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель анимации.
- **GX_INVALID_ANIMATION** (0x32) — анимация не находится в режиме IDLE или не была выделена из системного пула.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ANIMATION *animation;

UINT status = gx_system_animation_get(&animation);

if (status == GX_SUCCESS)
{
    status = gx_animation_start(animation, animation_info);

    if (status != GX_SUCCESS)
    {
        /* animation did not start, return it to the free pool */
        gx_system_animation_free(animation);
    }
}
```

### <a name="see-also"></a>См. также:

- gx_animation_create
- gx_animation_start
- gx_animation_stop
- gx_system_animation_get

## <a name="gx_system_bidi_text_disable"></a>gx_system_bidi_text_disable

Отключение динамической поддержки двунаправленного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_system_bidi_text_disable(VOID);
```

### <a name="description"></a>Описание

Эта служба отключает динамическую поддержку двунаправленного текста. Для этой службы требуется, чтобы при построении библиотеки GUIX был определен GX_DYNAMIC_BIDI_TEXT_SUPPORT, и она необходима только в том случае, если во время выполнения требуется изменить порядок строковых данных с двунаправленным текстом. Большинство приложений использует GUIX Studio для правильного переупорядочения текстовых строк с двунаправленным текстом.

### <a name="parameters"></a>Параметры

**Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — поддержка двунаправленного текста успешно отключена.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* GX_DYNAMIC_BIDI_TEXT_SUPPORT is defined. */

/* Diable bidi text support. */
status = gx_system_bidi_text_disable();

/* If status is GX_SUCCESS, bidi text support was disabled. */
```

### <a name="see-also"></a>См. также:

- gx_system_bidi_text_enable

## <a name="gx_system_bidi_text_enable"></a>gx_system_bidi_text_enable

Включение динамической поддержки двунаправленного текста

### <a name="prototype"></a>Прототип

```C
UINT gx_system_bidi_text_enable(VOID);
```

### <a name="description"></a>Описание

Эта служба обеспечивает динамическую поддержку двунаправленного текста. Для этой службы требуется, чтобы при построении библиотеки GUIX был определен GX_DYNAMIC_BIDI_TEXT_SUPPORT, и она необходима только в том случае, если во время выполнения требуется изменить порядок строковых данных с двунаправленным текстом. Большинство приложений использует GUIX Studio для правильного переупорядочения текстовых строк с двунаправленным текстом.

### <a name="parameters"></a>Параметры

**Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — поддержка двунаправленного текста успешно включена.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* GX_DYNAMIC_BIDI_TEXT_SUPPORT is defined. */

/* Enable bidi text support. */
status = gx_system_bidi_text_enable();

/* If status is GX_SUCCESS, bidi text support was enabled. */
```

### <a name="see-also"></a>См. также:

- gx_system_bidi_text_disable

## <a name="gx_system_canvas_refresh"></a>gx_system_canvas_refresh

Обновление всех "грязных" холстов

### <a name="prototype"></a>Прототип

```C
UINT gx_system_canvas_refresh(VOID);
```

### <a name="description"></a>Описание

Эта служба выполняет принудительную немедленную перерисовку всех "грязных" мини-приложений и холстов. Эта служба обычно вызывается компонентом системы GUIX, но может быть вызвана приложением для принудительной немедленной системной операции перерисовки.

### <a name="parameters"></a>Параметры

**Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — блок анимации успешно освобожден.
- **GX_INVALID_CANVAS** (0x20) — нет созданных холстов.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Force immediate redraw operation. */
status = gx_system_canvas_refresh();

/* If status is GX_SUCCESS, canvas has been redraw. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_dirty_mark"></a>gx_system_dirty_mark

Пометка области как грязной

### <a name="prototype"></a>Прототип

```C
UINT gx_system_dirty_mark(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба помечает область этого мини-приложения как грязную. Это позволяет эффективно поставить мини-приложение в очередь на перерисовку после завершения обработки системных событий.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — область успешно отмечена как "грязная".
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Mark widget “my_widget” as dirty. */
status = gx_system_dirty_mark(&my_widget);

/* If status is GX_SUCCESS the area associated
   with “my_widget” has been marked as dirty. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_dirty_partial_add"></a>gx_system_dirty_partial_add

Пометка частичной области как грязной

### <a name="prototype"></a>Прототип

```C
UINT gx_system_dirty_partial_add(
    GX_WIDGET *widget,
    GX_RECTANGLE *dirty_area);
```

### <a name="description"></a>Описание

Эта служба помечает частичную область этого мини-приложения как грязную. Это помещает мини-приложение в очередь для перерисовки с помощью операции обновления холста GUIX по завершении обработки системных событий.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением
- **dirty_area**: "грязная" область мини-приложения для пометки как грязной

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — частичная область успешно отмечена как грязная.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер "грязной" области.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Mark widget “my_widget” partial area as dirty. */

status = gx_system_dirty_partial_add(&my_widget,
    &partial_area);

/* If status is GX_SUCCESS the partial area “partial_area”
associated with “my_widget” has been marked as dirty. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_draw_context_get"></a>gx_system_draw_context_get

Получение контекста рисования

### <a name="prototype"></a>Прототип

```C
UINT gx_system_draw_context_get(GX_DRAW_CONTEXT **current_context);
```

### <a name="description"></a>Описание

Эта служба возвращает указатель на текущий контекст рисования.

### <a name="parameters"></a>Параметры

- **current_context**: указатель на место назначения для текущего указателя контекста рисования

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текущий контекст рисования успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_DRAW_CONTEXT *current_context;

/* Get current drawing context. */

status = gx_system_draw_context_get(&current_context);

/* If status is GX_SUCCESS the current drawing context
   is contained in “current_context”. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_event_fold"></a>gx_system_event_fold

Событие отправки

### <a name="prototype"></a>Прототип

```C
UINT gx_system_event_fold(GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба ищет в очереди событий GUIX событие того же типа. Если событие того же типа существует, полезные данные события обновляются в соответствии с новым событием. Если соответствующее событие не найдено, вызывается функция gx_system_event_send для добавления нового события в конец очереди событий.

Эта функция обычно используется драйверами быстрого сенсорного ввода для предотвращения заполнения очереди событий несколькими событиями PEN_DRAG. Эта функция также может вызываться приложением для предотвращения добавления нескольких событий одного и того же типа в очередь событий GUIX.

### <a name="parameters"></a>Параметры

- **event**: указатель на событие

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие успешно отправлено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_EVENT my_event;

memset(&my_event, 0, sizeof(GX_EVENT));

my_event.gx_event_type = GX_EVENT_PEN_DOWN;

my_event.gx_event_payload.gx_event_pointdata.gx_point_x = 100;
my_event.gx_event_payload.gx_event_pointdata.gx_point_y = 200;

/* Send “my_event” for processing. */
status = gx_system_event_fold(&my_event);

/* If status is GX_SUCCESS the event has been sent for processing. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_event_send"></a>gx_system_event_send

Событие отправки

### <a name="prototype"></a>Прототип

```C
UINT gx_system_event_send(GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба отправляет указанное событие в очередь системных событий GUIX. Новое событие помещается в конец очереди.

### <a name="parameters"></a>Параметры

- **event**: указатель на событие

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие успешно отправлено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Send “new_event” for processing. */

GX_EVENT new_event;

new_event.gx_event_target = widget ->gx_widget_parent;
new_event.gx_event_type = MY_EVENT_TYPE;

/* Set optional param. */
new_event.gx_event_payload.xxxx = yyyy
new_event.gx_event_sender = widget->gx_widget_id;

/* Push the event to event pool. */
status = gx_system_event_send(&new_event);

/* If status is GX_SUCCESS the event has been sent for processing. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_focus_claim"></a>gx_system_focus_claim

Запрос фокуса

### <a name="prototype"></a>Прототип

```C
UINT gx_system_focus_claim(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба запрашивает перемещение фокуса на указанное мини-приложение. Если мини-приложение ранее не получало фокус, оно получит событие GX_EVENT_FOCUS_GAINED.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением для перемещения фокуса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — фокус успешно запрошен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_NO_CHANGE** (0x08) — мини-приложение уже находится в фокусе.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Claim focus for widget “my_widget”. */
status = gx_system_claim_focus(&my_widget);

/* If status is GX_SUCCESS the focus has been claimed for
   “my_widget”. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_initialize"></a>gx_system_initialize

Инициализация GUIX

### <a name="prototype"></a>Прототип

```C
UINT gx_system_initialize(VOID);
```

### <a name="description"></a>Описание

Эта служба инициализирует GUIX. Эта служба должна вызываться перед любой другой службой API GUIX, и ее следует вызывать только один раз при запуске системы.

### <a name="parameters"></a>Параметры

- **Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — система успешно инициализирована.
- **GX_SYSTEM_ERROR** (0xFE) — недопустимый размер блока управления GX_EVENT, или не удалось создать очередь событий/взаимное исключение/поток.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Initialize GUIX. */
status = gx_system_initialize();

/* If status is GX_SUCCESS, GUIX has been initialized. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_language_table_get"></a>gx_system_language_table_get

Получение активной таблицы языков

### <a name="prototype"></a>Прототип

```C
UINT gx_system_language_table_get( 
    GX_CHAR ****language_table,
    GX_UBYTE *languages_count, 
    UINT *string_count);
```

### <a name="description"></a>Описание

Эта служба получает активную таблицу языков. Эта функция устарела, и вместо нее теперь используется gx_display_language_table_get. Все новые приложения должны использовать gx_display_language_table_get.

### <a name="parameters"></a>Параметры

- **language_table**: адрес указателя для возврата таблицы языков
- **languages_count**: адрес переменной для возврата столбцов таблицы
- **string_count**: адрес указателя для возврата строк таблицы

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — активная таблица языков успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR ***language_table;

GX_UBYTE language_count;

UINT string_count;

/* Retrieve the language table */

status = gx_system_language_table_get(&language_table,
    &language_count, &string_count);
```

### <a name="see-also"></a>См. также:

- gx_display_language_table_get
- gx_display_language_table_set

## <a name="gx_system_language_table_set"></a>gx_system_language_table_set

Назначение активной таблицы языков

### <a name="prototype"></a>Прототип

```C
UINT gx_system_language_table_set(
    GX_CHAR ***language_table,
    GX_UBYTE languages_count, 
    UINT string_count);
```

### <a name="description"></a>Описание

Эта служба устанавливает активную таблицу языков. Эта функция устарела, и вместо нее теперь используется gx_display_language_table_set. Все новые приложения должны использовать gx_display_language_table_set.

### <a name="parameters"></a>Параметры

- **language_table**: указатель на таблицу языков
- **languages_count**: число языков в таблице
- **string_count**: число строк в таблице

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица языков успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Retrieve the language table */

status = gx_system_language_table_set(language_table,
    language_count, string_count);
```

### <a name="see-also"></a>См. также:

- gx_display_language_table_set
- gx_display_language_table_get
- gx_display_active_language_set

## <a name="gx_system_memory_allocator_set"></a>gx_system_memory_allocator_set

Назначение функций для выделения памяти и отмены выделения

### <a name="prototype"></a>Прототип

```C
UINT gx_system_memory_allocator_set(VOID *(allocate)(ULONG size),
    VOID(*release)(VOID *));
```

### <a name="description"></a>Описание

Эта служба назначает функцию обратного вызова, предоставляемую приложением, для динамического выделения памяти и отмены такого выделения.

Если приложению не требуется служба GUIX, использующая динамическое выделение памяти, эту службу вызывать не нужно.

При использовании эту службу следует вызывать после gx_system_initialize(), которая очищает указатели службы GUIX, и до вызова любой службы GUIX, для которой требуется динамическое выделение памяти.

К службам GUIX, для которых требуется служба выделения памяти и отмены выделения памяти, относятся следующие:

- загрузка двоичных ресурсов из внешнего хранилища в среду выполнения GUIX;
- декодер изображений JPEG для среды выполнения программного обеспечения;
- декодер изображений PNG для среды выполнения программного обеспечения;
- использование текстовых мини-приложений с GX_STYLE_TEXT_COPY;
- служебные функции изменения размера и поворота пиксельной карты для среды выполнения;
- выделение блока управления экраном и мини-приложением для среды выполнения.

### <a name="parameters"></a>Параметры

- **allocator**: функция выделения памяти
- **release**: функция освобождения памяти

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — функция выделения памяти успешно назначена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

В следующем примере используется пул байтов ThreadX для реализации потокобезопасной службы динамического выделения памяти и отмены выделения памяти.

```C
TX_BYTE_POOL memory_pool;

#define SCRATCHPAD_SIZE (1024 * 4)

ULONG scratchpad[SCRATCHPAD_SIZE];

/* define memory allocation service */

VOID *memory_allocate(ULONG size)
{
    VOID *memptr;

    if (tx_byte_allocate(&memory_pool, &memptr, size, TX_NO_WAIT) == TX_SUCCESS)
    {
        return memptr;
    }
    return NULL;
}

/* define memory de-allocation service */
void memory_free(VOID *mem)
{
    tx_byte_release(mem);
}

/* create byte pool and install our dynamic memory services with GUIX
*/

VOID tx_application_define(void *first_unused_memory)
{
    /* create byte pool for GUIX to use */
    tx_byte_pool_create(&memory_pool, "scratchpad", scratchpad,
        SCRATCHPAD_SIZE * sizeof(ULONG));

    guix_setup();

    /* install our memory allocator and de-allocator */
    gx_system_memory_allocator_set(memory_allocate, memory_free);
}
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_pen_configure"></a>gx_system_pen_configure

Задание конфигурации пера

### <a name="prototype"></a>Прототип

```C
UINT gx_system_pen_configure(GX_PEN_CONFIGURATION *pen_configuration);
```

### <a name="description"></a>Описание

Эта служба задает конфигурацию пера для управления скоростью пера и параметрами расстояния, используемыми для активации создания типов событий GX_EVENT_FLICK.

Элемент gx_pen_configuration_min_drag_dist службы GX_PEN_CONFIGURATION является типом данных с фиксированной точкой, и для преобразования из типа INT в тип GX_FIXED_VAL необходимо использовать GX_FIXED_VAL_MAKE(значение). Например, если требуется задать минимальное расстояние перетаскивания в 0,5 пикселя на такт, необходимо установить для параметра gx_pen_configuration_min_drag_dist значение `GX_FIXED_VAL_MAKE(1) / 2`.

В GUIX версии 5.4.0 и более ранних версий элемент gx_pen_configuration_min_drag_dist интерфейса API GX_PEN_CONFIGURATION имел тип (INT << 8), а не GX_FIXED_VAL. Если этот API используется в проекте с библиотекой GUIX версии 5.4.0, то при создании библиотеки GUIX необходимо изменить параметр min_drag_dist или #define GUIX_5_4_0_COMPATIBILITY.

### <a name="parameters"></a>Параметры

- **pen_configuration**: указатель на структуру конфигурации пера Определение структуры GX_PEN_CONFIGURATION см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — конфигурация пера успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Например, .

```C
/* Define pen configuration, set minimum drag distance to 0.5 pixel
per tick, maximum pen speed to 10 ticks. That means GUIX will trigger
a vertical or horizontal flick event if the drag time is smaller than
10 ticks and the drag distance is bigger than 0.5 * drag_ticks. */

GX_PEN_CONFIGURATION pen_configuration;

#if defined(GUIX_5_4_0_COMPATIBILITY)
Pen_configuration.gx_pen_configuration_min_drag_dist = (1 << 8)/ 2;
#else
pen_configuration.gx_pen_configuration_min_drag_dist = GX_FIXED_VAL_MAKE(1) / 2;
#endif

pen_configuration.gx_pen_configuration_max_pen_speed_ticks = 10;

/* Set the pen configuration. */
status = gx_system_pen_configure(&pen_configuration);

/* If status is GX_SUCCESS the touch configure has been set. */
```

## <a name="gx_system_screen_stack_create"></a>gx_system_screen_stack_create

Создание и инициализация системного стека экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_system_screen_stack_create(
    GX_WIDGET **memory, 
    INT size);
```

### <a name="description"></a>Описание

Эта служба определяет пул памяти, используемый для системного стека экранов. Системный стек экранов — это дополнительный компонент, который может использоваться приложением для управления отображением потока экранов приложения.

Если приложение планирует использовать службы стека экранов, сначала необходимо вызвать функцию gx_system_screen_stack_create, чтобы настроить область памяти стека экранов.

### <a name="parameters"></a>Параметры

- **memory**: указатель на зарезервированный блок памяти
- **size**: размер зарезервированного блока памяти (в байтах)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
#define SCREEN_STACK_DEPTH 8
GX_WIDGET *screen_stack[SCREEN_STACK_DEPTH * 2];

UINT status;

/* Get the scrollbar appearance. */

status = gx_system_screen_stack_create(screen_stack,
    sizeof(GX_WIDGET *) * SCREEN_STACK_DEPTH * 2);

/* If status is GX_SUCCESS the system screen stack is initialized
and ready for use. */
```

### <a name="see-also"></a>См. также:

- gx_system_screen_stack_get
- gx_system_screen_stack_pop
- gx_system_screen_stack_push
- gx_system_screen_stack_reset

## <a name="gx_system_screen_stack_get"></a>gx_system_screen_stack_get

Вывод самых верхних указателей в стеке экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_system_screen_stack_get(
    GX_WIDGET **popped_parent,
    GX_WIDGET **popped_screen);
```

### <a name="description"></a>Описание

Эта функция выводит самые верхние указатели стека экранов и возвращает эти указатели вызывающему объекту. Эта функция отличается от gx_system_screen_stack_pop() тем, что извлеченный экран не будет автоматически повторно присоединен к предыдущему родительскому элементу. Вместо этого указатели выводятся из стека и возвращаются вызывающему объекту, что позволяет вызывающему объекту присоединить или отбросить возвращенный экран по желанию.

### <a name="parameters"></a>Параметры

- **popped_parent**: расположение для хранения указателя родительского мини-приложения
- **popped_screen**: расположение для хранения выведенного указателя экрана

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатели стека экранов успешно получены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_FAILURE** (0x10) — недопустимый или пустой стек экранов.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status;

GX_WIDGET *parent_screen;

GX_WIDGET *popped_screen;

/* Pop a screen stack entry. */
status = gx_system_screen_stack_get(&parent_screen, &popped_screen);

/* If status is GX_SUCCESS, parent_screen and
popped_screen hold the topmost screen stack pointers. */
```

### <a name="see-also"></a>См. также:

- gx_system_screen_stack_create
- gx_system_screen_stack_pop
- gx_system_screen_stack_push
- gx_system_screen_stack_reset

## <a name="gx_system_screen_stack_pop"></a>gx_system_screen_stack_pop

Вывод самой верхней записи из системного стека экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_system_screen_stack_pop();
```

### <a name="description"></a>Описание

Эта функция выводит самую верхнюю запись стека экранов и автоматически присоединяет выведенный экран к выведенному родительскому мини-приложению.

### <a name="parameters"></a>Параметры

**Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатели стека экранов успешно получены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_FAILURE** (0x10) — недопустимый или пустой стек экранов.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status;

/* Pop a screen stack entry. */
status = gx_system_screen_stack_pop();

/* If status is GX_SUCCESS, the topmost screen stack entry has been
popped from the stack and re-attached to the previous parent. */
```

### <a name="see-also"></a>См. также:

- gx_system_screen_stack_get
- gx_system_screen_stack_create
- gx_system_screen_stack_push
- gx_system_screen_stack_reset

## <a name="gx_system_screen_stack_push"></a>gx_system_screen_stack_push

Отправка мини-приложения и родительского указателя в стек экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_system_screen_stack_push(GX_WIDGET *screen)
```

### <a name="description"></a>Описание

Эта служба помещает указатель на указанное мини-приложение, которое обычно находится на экране верхнего уровня, в стек экранов. Если мини-приложение имеет родительский элемент, оно отсоединяется от родительского элемента. Указатель родительского мини-приложения также помещается в стек экранов. Родительское мини-приложение может иметь значение NULL, то есть экран, который не виден или присоединен к какому-либо родительскому элементу, может быть помещен в стек экранов. Если в стек экранов помещается мини-приложение без родительского элемента, то для получения указателя на экран необходимо использовать API screen_stack_get(), а не API screen_stack_pop(), который пытается повторно присоединить выведенное мини-приложение к его предыдущему родительскому элементу.

### <a name="parameters"></a>Параметры

- **screen**: указатель на мини-приложение, которое будет отправлено в стек экранов

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — внешний вид полосы прокрутки успешно получен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get the scrollbar appearance. */
status = gx_system_screen_stack_push(window);

/* If status is GX_SUCCESS, the widget pointed to by “window” has
been pushed to the screen stack, along with the widget’s parent
ponter. */
```

### <a name="see-also"></a>См. также:

- gx_system_screen_stack_get
- gx_system_screen_stack_pop
- gx_system_screen_stack_create
- gx_system_screen_stack_reset

## <a name="gx_system_screen_stack_reset"></a>gx_system_screen_stack_reset

Сброс системного стека экранов

### <a name="prototype"></a>Прототип

```C
UINT gx_system_screen_stack_reset();
```

### <a name="description"></a>Описание

Эта функция удаляет все записи из системного стека экранов. Если экраны, извлеченные из стека, имеют блоки управления, динамически выделяемые GUIX Studio, память для этих блоков управления освобождается.

### <a name="parameters"></a>Параметры

**Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — внешний вид полосы прокрутки успешно получен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get the scrollbar appearance. */
status = gx_system_screen_stack_reset();

/* If status is GX_SUCCESS the system screen stack has been cleared
of entries. */
```

### <a name="see-also"></a>См. также:

- gx_system_screen_stack_get
- gx_system_screen_stack_pop
- gx_system_screen_stack_push
- gx_system_screen_stack_create

## <a name="gx_system_scroll_appearance_get"></a>gx_system_scroll_appearance_get

Получение внешнего вида прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_system_scroll_appearance_get(
    ULONG style,
    GX_SCROLLBAR_APPEARANCE *return_appearance);
```

### <a name="description"></a>Описание

Эта служба получает внешний вид полосы прокрутки.

### <a name="parameters"></a>Параметры

- **style**: стиль полосы прокрутки, `GX_SCROLLBAR_HORIZONTAL` или `GX_SCROLLBAR_VERTICAL`
- **return_appearance**: указатель на назначение для внешнего вида Определение структуры GX_SCROLLBAR_APPERANCE см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — внешний вид полосы прокрутки успешно получен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SCROLLBAR_APPEARANCE my_appearance;

/* Get the scrollbar appearance. */

status = gx_system_scroll_appearance_get(style, &my_appearance);

/* If status is GX_SUCCESS “my_appearance” now contains the scroll
appearance. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_set
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_scroll_appearance_set"></a>gx_system_scroll_appearance_set

Задание внешнего вида прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_system_scroll_appearance_set(
    ULONG style,
    GX_SCROLLBAR_APPEARANCE *appearance);
```

### <a name="description"></a>Описание

Эта служба задает внешний вид прокрутки по умолчанию. При создании прокрутки используется эта структура оформления, если только приложение не предоставляет настраиваемую версию.

### <a name="parameters"></a>Параметры

- **style**: стиль прокрутки, `GX_SCROLLBAR_HORIZONTAL` или `GX_SCROLLBAR_VERTICAL`
- **appearance**: указатель на структуру внешнего вида, инициализированную различными атрибутами оформления полосы прокрутки. Определение структуры GX_SCROLLBAR_APPEARANCE см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — внешний вид прокрутки успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SCROLLBAR_APPEARANCE my_appearance;

memset(&my_appearance, 0, sizeof(GX_SCROLLBAR_APPEARANCE));

my_appearance.gx_scroll_width = 20;
my_appearance.gx_scroll_thumb_width = 18;

my_appearance.gx_scroll_thumb_color = GX_COLOR_ID_SCROLL_BUTTON;
my_appearance.gx_scroll_thumb_border_color = GX_COLOR_ID_SCROLL_BUTTON;
my_appearance.gx_scroll_button_color = GX_COLOR_ID_SCROLL_BUTTON;
my_appearance.gx_scroll_thumb_travel_min = 20;
my_appearance.gx_scroll_thumb_travel_max = 20;
my_appearance.gx_scroll_thumb_border_style = GX_STYLE_BORDER_THIN;

/* Set the scroll appearance. */

status = gx_system_scroll_appearance_set(GX_SCROLLBAR_VERTICAL, &my_appearance);

/* If status is GX_SUCCESS the scroll appearance has been set. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_start"></a>gx_system_start

Запуск GUIX

### <a name="prototype"></a>Прототип

```C
UINT gx_system_start(VOID);
```

### <a name="description"></a>Описание

Эта служба запускает обработку GUIX. В обычных условиях эта функция никогда не возвращает значение, а начинает обработку очереди событий GUIX. Если очередь событий GUIX пуста, эта служба приостанавливает вызывающий поток до тех пор, пока новые события не поступят в очередь событий GUIX.

### <a name="parameters"></a>Параметры

- **Нет**

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — система успешно запущена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Start GUIX. */
status = gx_system_start();

/* If status is GX_SUCCESS . GUIX has been started. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_string_get"></a>gx_system_string_get

Получение строки

### <a name="prototype"></a>Прототип

```C
UINT gx_system_string_get(
    GX_RESOURCE_ID string_id,
    GX_CHAR **return_string);
```

### <a name="description"></a>Описание

Эта служба получает строку для указанного идентификатора ресурса, используя первый определенный дисплей и текущий активный язык. Эта функция устарела, и вместо нее теперь используется gx_display_string_get. Все новые приложения должны использовать gx_display_string_get.

### <a name="parameters"></a>Параметры

- **string_id**: идентификатор ресурса строки
- **return_string**: указатель на указатель назначения строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get the string associated with MY_STRING_ID. */

status = gx_system_string_get(MY_STRING_RESOURCE_ID, &my_string);

/* If status is GX_SUCCESS the string is contained in “my_string”.
*/
```

### <a name="see-also"></a>См. также:

- gx_display_string_get
- gx_display_string_table_get
- gx_display_language_table_set

## <a name="gx_system_string_table_get"></a>gx_system_string_table_get

Извлекает таблицу строк

### <a name="prototype"></a>Прототип

```C
UINT gx_system_string_table_get(
    GX_UBYTE language,
    GX_CHAR ***string_table,
    UINT *get_size);
```

### <a name="description"></a>Описание

Эта служба извлекает таблицу строк для запрошенного языка с первого дисплея. Эта функция устарела, и вместо нее теперь используется gx_display_string_table_get. Все новые приложения должны использовать gx_display_string_table_get.

### <a name="parameters"></a>Параметры

- **language**: индекс языка
- **string_table**: указатель на пространство хранилища указателя таблицы строк (или NULL, если вызывающему объекту не нужно получать указатель на таблицу строк)
- **get_size**: указатель на хранилище для количества строк в таблице строк (или NULL, если вызывающему объекту не нужно получать количество строк в таблице строк)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таблица строк успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get the string table. */

CHAR **my_string_table; UINT table_size;

status = gx_system_string_table_get(LANGUAGE_ID_ENGLISH,
    &my_string_table, &table_size);

/* If status is GX_SUCCESS . the pointer to the string table has
been obtained. */
```

### <a name="see-also"></a>См. также:

- gx_display_string_table_get
- gx_display_string_get
- gx_display_active_language_set
- gx_display_language_table_set

## <a name="gx_system_string_width_get"></a>gx_system_string_width_get

Получение ширины строки (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_system_string_width_get(
    GX_FONT *font, GX_CHAR *string,
    INT string_length,
    GX_VALUE *return_width);
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_system_string_width_get_ext().

Эта служба вычисляет ширину отображаемой строки в пикселях, используя указанный шрифт. Если параметр string_length имеет значение >= 0, в вычисление включаются только счетчики запросов символов. Если параметр string_length имеет значение -1, в вычислении используется вся строка вплоть до NULL в конце.

### <a name="parameters"></a>Параметры

- **font**: указатель на шрифт строки
- **string**: указатель на строку
- **string_length**: длина строки
- **return_width**: целевая ширина строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина строки успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_FONT** (0x16) — недопустимый шрифт.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get the string width of “my_string”. */

status = gx_system_string_width_get(&my_font, &my_string,
    strlen(my_string), &my_width);

/* If status is GX_SUCCESS . “my_width” contains the string width.
*/
```

### <a name="see-also"></a>См. также:

- gx_system_string_width_get_ext

## <a name="gx_system_string_width_get_ext"></a>gx_system_string_width_get_ext

Получение ширины строки

### <a name="prototype"></a>Прототип

```C
UINT gx_system_string_width_get_ext(
    GX_FONT *font,
    GX_STRING *string,
    GX_VALUE *return_width);
```

### <a name="description"></a>Описание

Эта служба вычисляет ширину отображаемой строки в пикселях, используя указанный шрифт.

### <a name="parameters"></a>Параметры

- **font**: указатель на шрифт строки
- **string**: указатель на строку
- **return_width**: целевая ширина строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина строки успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_FONT** (0x16) — недопустимый шрифт.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING my_string; my_string.gx_string_ptr = “Monday”;

my_string.gx_string_length = strlen(my_string.gx_string_ptr);

/* Get the string width of “my_string”. */

status = gx_system_string_width_get_ext(&my_font,
    &my_string, &my_width);

/* If status is GX_SUCCESS . “my_width” contains the string width.
*/
```

### <a name="see-also"></a>См. также:

- gx_system_event_fold
- gx_system_focus_claim
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_timer_start"></a>gx_system_timer_start

Запуск таймера

### <a name="prototype"></a>Прототип

```C
UINT gx_system_timer_start(
    GX_WIDGET *owner, 
    UINT timer_id,
    UINT initial_ticks,
    UINT reschedule_ticks);
```

### <a name="description"></a>Описание

Эта служба запускает таймер для указанного мини-приложения. Константа GX_MAX_ACTIVE_TIMERS определяет максимальное число поддерживаемых активных таймеров. Значение по умолчанию — 32.

### <a name="parameters"></a>Параметры

- **owner**: указатель на блок управления мини-приложением
- **timer_id**: идентификатор таймера
- **initial_ticks**: начальные такты истечения времени
- **reschedule_ticks**: периодические такты истечения времени

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таймер успешно запущен.
- **GX_OUT_OF_TIMERS** (0x04) — больше нет таймеров.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимые значения таймера.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Start a periodic timer for the widget “my_widget”. */
status = gx_system_timer_start(&my_widget, MY_TIMER_ID, 10, 20);

/* If status is GX_SUCCESS . the timer for “my_widget” has been
started. */
```

### <a name="see-also"></a>См. также:

- gx_system_event_fold
- gx_system_focus_claim
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_timer_stop"></a>gx_system_timer_stop

Остановка таймера

### <a name="prototype"></a>Прототип

```C
UINT gx_system_timer_stop(
    GW_WIDGET *owner, 
    UINT timer_id);
```

### <a name="description"></a>Описание

Эта служба останавливает таймер с указанным timer_id, связанным с вызывающим мини-приложением. Чтобы отключить все таймеры, связанные с определенным мини-приложением, приложение может передать значение timer_id 0.

### <a name="parameters"></a>Параметры

- **owner**: указатель на блок управления мини-приложением
- **timer_id**: идентификатор таймера; 0 означает все таймеры

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — таймер успешно остановлен.
- **GX_NOT_FOUND** (0x09) — идентификатор таймера не найден.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Stop the periodic timer for the widget “my_widget”. */
status = gx_system_timer_stop(&my_widget, MY_TIMER_ID);

/* If status is GX_SUCCESS . the timer for “my_widget” has been
stopped. */
```

### <a name="see-also"></a>См. также:

- gx_system_event_fold
- gx_system_focus_claim
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_pen_configure
- gx_system_version_string_get
- gx_system_widget_find

## <a name="gx_system_version_string_get"></a>gx_system_version_string_get

Получение строки версии библиотеки GUIX (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_system_version_string_get(GX_CHAR **version);
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_system_version_string_get_ext().

Эта служба получает строку версии библиотеки GUIX.

### <a name="parameters"></a>Параметры

- **version**: указатель на возвращаемое значение строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка версии успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *version;

/* get the library version string. */

status = gx_system_verrsion_string_get(&version);
```

### <a name="see-also"></a>См. также:

- gx_system_version_string_get_ext()

## <a name="gx_system_version_string_get_ext"></a>gx_system_version_string_get_ext

Получение строки версии библиотеки GUIX

### <a name="prototype"></a>Прототип

```C
UINT gx_system_version_string_get(GX_STRING *version);
```

### <a name="description"></a>Описание

Эта служба получает строку версии библиотеки GUIX.

### <a name="parameters"></a>Параметры

- **version**: указатель на возвращаемое значение строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — строка версии успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING version;

/* get the library version string. */

status = gx_system_version_string_get_ext(&version);
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_widget_find

## <a name="gx_system_widget_find"></a>gx_system_widget_find

Поиск мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_system_widget_find(
    USHORT widget_id,
    INT search_level, 
    GX_WIDGET **return_search_result);
```

### <a name="description"></a>Описание

Эта служба выполняет поиск указанного идентификатора мини-приложения. В отличие от gx_widget_find() эта функция ищет потомки всех корневых окон, определенных в системе, то есть выполняет исчерпывающий поиск всех видимых мини-приложений. Если вам известен родительский элемент искомого мини-приложения, используйте gx_widget_find().

### <a name="parameters"></a>Параметры

- **widget_id**: идентификатор мини-приложения для поиска
- **search_level**: определяет рекурсивный уровень вложенности, в котором выполняется поиск дочерних мини-приложений Если это значение равно 0, выполняется поиск только непосредственных дочерних элементов каждого корневого окна. Если это значение равно GX_SEARCH_DEPTH_INFINITE, функция вкладывается во все дочерние элементы для поиска запрошенного идентификатора мини-приложения. Для любого другого значения > 0 уровень поиска определяется глубиной вложения, которую эта функция будет использовать при поиске запрошенного идентификатора мини-приложения.
- **return_search_result**: указатель на место назначения, где найдено мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — поиск мини-приложения успешно выполнен.
- **GX_NOT_FOUND** (0x09) — не удалось найти идентификатор мини-приложения.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Search recursively from the top level widget for the widget with
ID of MY_WIDGET_ID. */

status = gx_system_widget_find(MY_WIDGET_ID,
    GX_SEARCH_DEPTH_INFINITE,
    &my_widget);

/* If status is GX_SUCCESS . the search was successful and
“my_widget” contains the pointer to the widget. */
```

### <a name="see-also"></a>См. также:

- gx_system_active_language_set
- gx_system_canvas_refresh
- gx_system_dirty_mark
- gx_system_dirty_partial_add
- gx_system_draw_context_get
- gx_system_event_fold
- gx_system_event_send
- gx_system_focus_claim
- gx_system_initialize
- gx_system_initialize
- gx_system_language_table_get
- gx_system_language_table_set
- gx_system_memory_allocator_set
- gx_system_scroll_appearance_get
- gx_system_scroll_appearance_get
- gx_system_start
- gx_system_string_get
- gx_system_string_table_get
- gx_system_string_width_get
- gx_system_timer_start
- gx_system_timer_stop
- gx_system_pen_configure
- gx_system_version_string_get

## <a name="gx_text_button_create"></a>gx_text_button_create

Создание кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_create(
    GX_TEXT_BUTTON *text_button,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent,
    GX_RESOURCE_ID text_id, 
    ULONG style,
    USHORT text_button_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает мини-приложение кнопки с текстом.

GX_TEXT_BUTTON является производным от GX_BUTTON и поддерживает все службы API gx_button.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **name**: логическое имя кнопки с текстом
- **parent**: указатель на родительское мини-приложение кнопки
- **text_id**: идентификатор ресурса текста
- **style**: стиль кнопки текста Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **text_button_id**: определяемый приложением идентификатор кнопки с текстом
- **size**: размер кнопки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — кнопка с текстом успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.


### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_BUTTON my_text_button;

GX_RECTANGLE size;

/* Define widget size. */

gx_utility_rectangle_define(&size, 0, 0, 100, 100);

/* Create text button “my_text_button”. */

status = gx_text_button_create(&my_text_button, "my text button",
    &my_parent_window, MY_TEXT_RESOURCE_ID,
    GX_STYLE_BUTTON_TOGGLE, MY_TEXT_BUTTON_ID, &size);

/* If status is GX_SUCCESS, the text button “my_text_button” was
created. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_color_set
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_font_set
- gx_text_button_text_get
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_draw"></a>gx_text_button_draw

Вывод кнопки с текстом

### <a name="prototype"></a>Прототип

```C
VOID gx_text_button_draw(GX_TEXT_BUTTON *button);
```

### <a name="description"></a>Описание

Эта служба рисует кнопку с текстом. Эта служба обычно вызывается внутри во время обновления холста, но также может вызываться из настраиваемых функций рисования кнопки с текстом.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой с текстом

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom text button draw function. */

VOID my_text_button_draw(GX_TEXT_BUTTON *text_button)
{
    /* Call default text button draw. */
    gx_text_button_draw(text_button);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_event_process
- gx_text_button_color_set
- gx_text_button_font_set
- gx_text_button_text_get
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_event_process"></a>gx_text_button_event_process


Обработка события кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_event_process(GX_TEXT_BUTTON *text_button, GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанной кнопки с текстом. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий кнопки с текстом.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие кнопки с текстом успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic text button event processing as part of custom event processing function. */

UINT custom_text_button_event_process(GX_TEXT_BUTTON *text_button, GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default text button event processing */
        status = gx_text_button_event_process(wheel, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_color_set
- gx_text_button_font_set
- gx_text_button_text_get
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_font_set"></a>gx_text_button_font_set

Задание шрифта для кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_font_set(
    GX_TEXT_BUTTON *button,
    GX_RESOURCE_ID font_id);
```

### <a name="description"></a>Описание

Эта служба назначает шрифт для указанной кнопки.

### <a name="parameters"></a>Параметры

- **button**: указатель на блок управления кнопкой с текстом
- **font_id**: идентификатор ресурса для шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the text button with the font ID MY_FONT. */
status = gx_text_button_font_set(&my_text_button, MY_FONT);

/* If status is GX_SUCCESS, the font of the text button
“my_text_button” was set to MY_FONT. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create
- gx_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_color_set
- gx_text_button_text_get
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_text_color_set"></a>gx_text_button_text_color_set

Задание цвета для кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_text_color_set(
    GX_TEXT_BUTTON *text_button,
    GX_RESOURCE_ID normal_text_color_id,
    GX_RESOURCE_ID selected_text_color_id,
    GX_RESOURCE_ID disabled_text_color_id);
```

### <a name="description"></a>Описание

Эта служба задает цвет для кнопки с текстом.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **normal_text_color_id**: идентификатор ресурса обычного текста В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **selected_text_color_id**: идентификатор ресурса выбранного текста В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **disabled_text_color_id**: идентификатор ресурса цвета для отключенного текста В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет для кнопки с текстом успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the color of the text button “my_text_button”. */
status = gx_text_button_text_color_set(&my_text_button,
    GX_COLOR_ID_NORMAL_TEXT,
    GX_COLOR_ID_SELECTED_TEXT,
    GX_COLOR_ID_DISABLED_TEXT);

/* If status is GX_SUCCESS, the text color of “my_text_button” was
set. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create, x_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_font_set
- gx_text_button_text_get
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_text_draw"></a>gx_text_button_text_draw

Функция поддержки для вывода текста кнопки

### <a name="prototype"></a>Прототип

```C
VOID gx_text_button_text_draw(GX_TEXT_BUTTON *text_button);
```

### <a name="description"></a>Описание

Эта функция поддержки рисует текстовую часть кнопки с текстом. Эта функция вызывается внутри функцией gx_text_button_draw() и предоставляется как отдельный API для удобства работы приложений, которые определяют настраиваемую функцию рисования кнопок с текстом. Приложения, которым требуется настроить фоновый рисунок кнопки, могут предоставить свою настраиваемую функцию рисования и вызвать службу gx_text_button_text_draw в составе настраиваемого рисования, чтобы нарисовать текст на кнопке поверх фона.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Define a custom drawing function */

VOID my_button_draw(GX_TEXT_BUTTON *button)
{
    /* Insert code here to draw button background */

    /* Call support function to do text drawing */
    gx_text_button_text_draw(button);

    /* Draw child widgets */
    gx_widget_children_draw((GX_WIDGET *) button);
}
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create, x_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_font_set
- gx_text_button_text_color_set
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_text_get"></a>gx_text_button_text_get

Получение текста из кнопки с текстом (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_text_get(
    GX_TEXT_BUTTON *text_button,
    GX_CHAR **return_text);
```

### <a name="description"></a>Описание

Эта служба не рекомендуется. Вместо нее следует использовать gx_text_button_text_get_ext().

Эта служба получает указанную строку из кнопки с текстом.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **return_text**: указатель на строку, полученную из кнопки с текстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно получен из кнопки.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CHAR *string;

/* Get the string from the text button “my_text_button”. */
status = gx_text_button_text_get(&my_text_button, &string);

/* If status is GX_SUCCESS, the string pointer from
“my_text_button” is retrieved and stored in string. */
```

### <a name="see-also"></a>См. также:

- gx_text_button_text_get_ext

## <a name="gx_text_button_text_get_ext"></a>gx_text_button_text_get_ext

Получение текста из кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_text_get_ext(
    GX_TEXT_BUTTON *text_button,
    GX_STRING *return_string);
```

### <a name="description"></a>Описание

Эта служба получает указанную строку из кнопки с текстом.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **return_string**: указатель на строку, полученную из кнопки с текстом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно получен из кнопки.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING string;

/* Get the string from the text button “my_text_button”. */
status = gx_text_button_text_get_ext(&my_text_button, &string);

/* If status is GX_SUCCESS, the string pointer and length from
“my_text_button” is retrieved and stored in string. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create, x_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_font_set
- gx_text_button_text_color_set
- gx_text_button_text_set
- gx_text_button_text_id_set

## <a name="gx_text_button_text_id_set"></a>gx_text_button_text_id_set

Задание идентификатора ресурса текста для кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_text_id_set(
    GX_TEXT_BUTTON *text_button,
    RESOURCE_ID string_id);
```

### <a name="description"></a>Описание

Эта служба присваивает указанный идентификатор ресурса строки кнопке с текстом.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **string_id**: идентификатор ресурса строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — идентификатор ресурса строки успешно присвоен кнопке с текстом.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the string ID ”MY_STRING_ID” to the text button
“my_text_button”. */

status = gx_text_button_text_id_set(&my_text_button,
    MY_STRING_ID);

/* If status is GX_SUCCESS, the string ID MY_STRING_ID was set to
“my_text_button”. */
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create, x_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_font_set
- gx_text_button_text_color_set
- gx_text_button_text_get

## <a name="gx_text_button_text_set"></a>gx_text_button_text_set

Назначение текста для кнопки с текстом (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_text_set(
    GX_TEXT_BUTTON *text_button,
    GX_CHAR *text);
```

### <a name="description"></a>Описание

Эта служба не рекомендуется. Вместо нее следует использовать gx_text_button_text_set_ext().

Эта служба назначает указанную строку для кнопки с текстом. Если мини-приложение text_button было создано со стилем GX_STYLE_TEXT_COPY, мини-приложение создает частную копию назначенной текстовой строки. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **text**: указатель на строку, завершающуюся NULL

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно присвоен кнопке.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the string “my string” to the text button “my_text_button”.
*/

status = gx_text_button_text_set(&my_text_button, “my string”);

/* If status is GX_SUCCESS, the string “my_text_button” was set.
*/
```

### <a name="see-also"></a>См. также:

- gx_text_button_event_process
- gx_text_button_text_set_ext
- gx_text_button_text_id_set

## <a name="gx_text_button_text_set_ext"></a>gx_text_button_text_set_ext

Назначение текста для кнопки с текстом

### <a name="prototype"></a>Прототип

```C
UINT gx_text_button_text_set_ext(
    GX_TEXT_BUTTON *text_button,
    GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба назначает указанную строку для кнопки с текстом. Если мини-приложение text_button было создано со стилем GX_STYLE_TEXT_COPY, мини-приложение создает частную копию назначенной текстовой строки. Если GX_STYLE_TEXT_COPY неактивен, мини-приложение не создает частную копию строки, поэтому она должна быть назначена статически или глобально, то есть она может не быть автоматической или временной переменной.

### <a name="parameters"></a>Параметры

- **text_button**: указатель на блок управления кнопкой с текстом
- **string**: указатель на переменную GX_STRING

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текст успешно присвоен кнопке.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING new_string; new_string.gx_string_ptr = “Monday”;

new_string.gx_string_length = strlen(new_string.gx_string_ptr);

/* Assign the string “new_string” to the text button
“my_text_button”. */

status = gx_text_button_text_set_ext(&my_text_button, &new_string);

/* If status is GX_SUCCESS, the string “my_text_button” was set.
*/
```

### <a name="see-also"></a>См. также:

- gx_button_background_draw
- gx_button_create
- gx_button_deselect
- gx_button_draw
- gx_button_event_process
- gx_button_select
- gx_icon_button_create, x_pixelmap_button_create
- gx_pixelmap_button_draw
- gx_text_button_create
- gx_text_button_draw
- gx_text_button_event_process
- gx_text_button_font_set
- gx_text_button_text_color_set
- gx_text_button_text_get
- gx_text_button_text_id_set

## <a name="gx_text_input_cursor_blink_interval_set"></a>gx_text_input_cursor_blink_interval_set

Задание интервала мерцания курсора

### <a name="prototype"></a>Прототип

```C
UINT gx_text_input_cursor_blink_interval_set(
    GX_TEXT_INPUT_CURSOR *cursor_input,
    GX_UBYTE blink_interval);
```

### <a name="description"></a>Описание

Эта служба задает значение интервала мерцания курсора.

### <a name="parameters"></a>Параметры

- **cursor_input**: блок управления курсором
- **blink_interval**: значение, которое необходимо задать

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — интервал мерцания курсора успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение интервала мерцания.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_INPUT_CURSOR *input_cursor;

/* Pointer the input cursor to the cursor instance of single/multi
line text input widget. */

input_cursor = &sl_input.gx_single_line_text_input_cursor_instance;

/* Set the blink interval value of “input_cursor” to 2. */
status = gx_text_input_cursor_blink_interval_set(input_cursor, 2);

/* If status is GX_SUCCESS, the blink interval value of
“input_cursor” has been successfully set to 2. */
```

### <a name="see-also"></a>См. также:

- gx_text_input_cursor_height_set
- gx_text_input_cursor_width_set

## <a name="gx_text_input_cursor_height_set"></a>gx_text_input_cursor_height_set

Задание высоты курсора

### <a name="prototype"></a>Прототип

```C
UINT gx_text_input_cursor_height_set(
    GX_TEXT_INPUT_CURSOR *cursor_input,
    GX_UBYTE height);
```

### <a name="description"></a>Описание

Эта служба задает высоту курсора.

### <a name="parameters"></a>Параметры

- **cursor_input**: блок управления курсором
- **height**: значение, которое необходимо задать

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — высота курсора успешно задана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение высоты.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_INPUT_CURSOR *input_cursor;

/* Pointer the input cursor to the cursor instance of single/multi
line text input widget. */

input_cursor = &sl_input.gx_single_line_text_input_cursor_instance;

/* Set height value of “input_cursor”. */
status = gx_text_input_cursor_height_set(&input_cursor, 15);

/* If status is GX_SUCCESS, the height value of “input_curosr” has
been successfully set to 15. */
```

### <a name="see-also"></a>См. также:

- gx_text_input_cursor_blink_interval_set
- gx_text_input_cursor_width_set

## <a name="gx_text_input_cursor_width_set"></a>gx_text_input_cursor_width_set

Задание ширины курсора

### <a name="prototype"></a>Прототип

```C
UINT gx_text_input_cursor_blink_width_set(
    GX_TEXT_INPUT_CURSOR *cursor_input,
    GX_UBYTE *width);
```

### <a name="description"></a>Описание

Эта служба задает ширину курсора.

### <a name="parameters"></a>Параметры

- **cursor_input**: блок управления курсором
- **width**: значение, которое должно быть задано

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина курсора успешно задана.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение ширины.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_INPUT_CURSOR *input_cursor;

/* Pointer the input cursor to the cursor instance of single/multi
line text input widget. */

input_cursor = &sl_input.gx_single_line_text_input_cursor_instance;

/* Set width of “input_curosr” to 2. */

status = gx_text_input_cursor_blink_width_set(&input_cursor, 2);

/* If status is GX_SUCCESS, the width of “input_cursor” has been
successfully set to 2. */
```

### <a name="see-also"></a>См. также:

- gx_text_input_cursor_blink_interval_set
- gx_text_input_cursor_height_set

## <a name="gx_text_scroll_wheel_callback_set"></a>gx_text_scroll_wheel_callback_set

Назначение функции обратного вызова колесика прокрутки текстового типа (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_text_scroll_wheel_callback_set(
    GX_TEXT_SCROLL_WHEEL *wheel,
    GX_CONST GX_CHAR *(*callback)(GX_TEXT_SCROLL_WHEEL *, int));
```

### <a name="description"></a>Описание

Эта служба является устаревшей. Вместо нее используется gx_text_scroll_wheel_callback_set_ext().

Эта служба назначает функцию обратного вызова, которую будет вызывать колесико прокрутки текстового типа, чтобы определить текстовую строку, отображаемую в каждой строке колесика прокрутки.

Для GX_NUMERIC_SCROLL_WHEEL и GX_STRING_SCROLL_WHEEL предоставляются функции обратного вызова по умолчанию, и приложению не нужно вносить никаких изменений для использования этих реализаций по умолчанию.

Этот API позволяет приложению настраивать форматирование или другие параметры строки, отображаемые в каждой строке мини-приложения колесика прокрутки.

Функция обратного вызова получит в качестве входных данных указатель на блок управления колесиком прокрутки и отображаемый номер строки. Функция должна возвращать указатель на текстовую строку.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки
- **callback**: указатель на функцию обратного вызова

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — обратный вызов успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_SCROLL_WHEEL wheel;

GX_CHAR string_buffer[20];

GX_CHAR *my_wheel_callback(GX_TEXT_SCROLL_WHEEL *wheel, int row)
{
    /* Just for an example, return row number as string for rows
    >= 0, and return text “Invalid” otherwise */
    if (row >= 0)
    {
        gx_utility_ltoa(row, string_buffer, 20);
    }
    else
    {
        return(“Invalid”);
    }
}

gx_text_scroll_wheel_create(&wheel, “my wheel”, root, 10,
    GX_STYLE_ENABLED|GX_STYLE_TEXT_CENTER|GX_STYLE_TRANSPARENT|
    GX_STYLE_WRAP|ID_MY_WHEEL, &size);

status = gx_text_scroll_wheel_callback_set(&wheel,
    my_wheel_callback);

/* If status is GX_SUCCESS, the scroll whell callback function has
been set. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_event_process
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_text_scroll_wheel_callback_set_ext"></a>gx_text_scroll_wheel_callback_set_ext

Назначение функции обратного вызова колесика прокрутки текстового типа

### <a name="prototype"></a>Прототип

```C
UINT gx_text_scroll_wheel_callback_set_ext(
    GX_TEXT_SCROLL_WHEEL *wheel,
    UINT *(*callback)(GX_TEXT_SCROLL_WHEEL *, int, GX_STRING *));
```

### <a name="description"></a>Описание

Эта служба назначает функцию обратного вызова, которую будет вызывать колесико прокрутки текстового типа, чтобы определить текстовую строку, отображаемую в каждой строке колесика прокрутки.

Для GX_NUMERIC_SCROLL_WHEEL и GX_STRING_SCROLL_WHEEL предоставляются функции обратного вызова по умолчанию, и приложению не нужно вносить никаких изменений для использования этих реализаций по умолчанию.

Этот API позволяет приложению настраивать форматирование или другие параметры строки, отображаемые в каждой строке мини-приложения колесика прокрутки.

Функция обратного вызова получит в качестве входных данных указатель на блок управления колесиком прокрутки и отображаемый номер строки. Функция должна возвращать указатель на текстовую строку.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки
- **callback**: указатель на функцию обратного вызова

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — обратный вызов успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_SCROLL_WHEEL wheel;

GX_CHAR string_buffer[20];

UINT *my_wheel_callback(GX_TEXT_SCROLL_WHEEL *wheel, int row,
    GX_STRING *return_string)
{
    /* Just for an example, return row number as string for rows
    >= 0, and return text “Invalid” otherwise */
    if (row >= 0)
    {
        gx_utility_ltoa(row, string_buffer, 20);
        return_string->gx_string_ptr = string_buffer;
        return_string->gx_string_length = strlen(string_buffer);
    }
    else
    {
        return_string->gx_string_ptr = “Invalid”;
        return_string->gx_string_length = strlen(“Invalid”);
    }

    return GX_SUCCESS;
}

gx_text_scroll_wheel_create(&wheel, “my wheel”, root, 10,
    GX_STYLE_ENABLED|GX_STYLE_TEXT_CENTER|GX_STYLE_TRANSPARENT|
    GX_STYLE_WRAP|ID_MY_WHEEL, &size);

status = gx_text_scroll_wheel_callback_set_ext(&wheel,
    my_wheel_callback);

/* If status is GX_SUCCESS, the scroll whell callback function has
been set. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_event_process
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_text_scroll_wheel_create"></a>gx_text_scroll_wheel_create

Создание колесика прокрутки текста

### <a name="prototype"></a>Прототип

```C
UINT gx_text_scroll_wheel_create(
    GX_TEXT_SCROLL_WHEEL *wheel,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent, 
    INT total_rows,
    ULONG style, 
    USHORT Id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает колесико прокрутки текста. Колесико прокрутки текста — это базовое мини-приложение для мини-приложений типов GX_STRING_SCROLL_WHEEL и GX_NUMERIC_SCROLL_WHEEL. Эта функция вызывается внутренним образом gx_string_scroll_wheel_create и gx_numeric_scroll_wheel_create и предоставляется как отдельный интерфейс API для удобства работы с приложениями, определяющими настраиваемое мини-приложение колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки текста
- **name**: имя мини-приложения, определенное приложением
- **parent**: родительский элемент колесика или GX_NULL
- **total_rows**: общее число строк, отображаемых для пользователя
- **style**: флаги требуемого стиля
- **Id**: определенные приложением флаги стиля колесика
- **size**: размер исходного колесика прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — колесико прокрутки текста успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.


### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define a custom scroll wheel widget. */
typedef MY_SCROLL_WHEEL_STRUCT
{
    GX_TEXT_SCROLL_WHEEL text_scroll_wheel;

    /* Add custom members here. */

} MY_SCROLL_WHEEL;

MY_SCROLL_WHEEL my_scroll_wheel;

UINT my_scroll_wheel_create(MY_SCROLL_WHEEL *wheel,
    GX_CONST GX_CHAR *name, GX_WIDGET *parent,
    INT total_rows, ULONG style, USHORT Id,
    GX_CONST GX_RECTANGLE *size)
{
    /* Call base creation. */
    status = gx_text_scroll_wheel_create(
        &wheel.text_scroll_wheel,
        ”my_text_scroll_wheel”, GX_NULL, 7,
        GX_STYLE_ENABLED, ID_MY_SCROLL_WHEEL, &size);

    if (status == GX_SUCCESS)
    {
        /* Add custom initialization here. */
        if(parent)
        {
            gx_widget_link(parent, (GX_WIDGET *)wheel);
        }
    }
}
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_string_scroll_wheel_string_id_list_set
- gx_string_scroll_wheel_string_list_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_event_process
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_text_scroll_wheel_draw"></a>gx_text_scroll_wheel_draw

Вывод колесика прокрутки текста

### <a name="prototype"></a>Прототип

```C
VOID gx_text_scroll_wheel_draw(GX_TEXT_SCROLL_WHEEL *wheel);
```

### <a name="description"></a>Описание

Это функция рисования по умолчанию для всех типов колесиков на основе GX_TEXT_SCROLL_WHEEL. Эта функция может быть переопределена приложениями, которым требуется настроить внешний вид колесика прокрутки текста.

GX_STRING_SCROLL_WHEEL и GX_NUMERIC_SCROLL_WHEEL основаны на GX_TEXT_SCROLL_WHEEL или являются производными от них.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom wheel draw function. */
UINT my_wheel_draw(GX_TEXT_SCROLL_WHEEL *wheel)
{
    /* Perform default drawing */
    gx_text_scroll_wheel_draw(wheel);

    /* Add custom drawing here */
}
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_event_process
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_text_scroll_wheel_event_process"></a>gx_text_scroll_wheel_event_process


Обработка события колесика прокрутки текста

### <a name="prototype"></a>Прототип

```C
UINT gx_text_scroll_wheel_event_process(GX_TEXT_SCROLL_WHEEL *wheel, GX_EVENT *event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного колесика прокрутки текста. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий колесика прокрутки текста.

### <a name="parameters"></a>Параметры

- **wheel**: указатель на блок управления колесиком прокрутки текста
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие колесика прокрутки текста успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic text scroll wheel event processing as part of custom event processing function. */

UINT custom_text_scroll_wheel_event_process(GX_TEXT_SCROLL_WHEEL *wheel, GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;

    default:
        /* Pass all other events to the default text scroll wheel event processing */
        status = gx_text_scroll_wheel_event_process(wheel, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_font_set
- gx_text_scroll_wheel_text_color_set

## <a name="gx_text_scroll_wheel_font_set"></a>gx_text_scroll_wheel_font_set

Назначение шрифтов, используемых для отрисовки строк колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_text_scroll_font_set(
    GX_TEXT_SCROLL_WHEEL *wheel,
    GX_RESOURCE_ID normal_font, 
    GX_RESOURCE_ID selected_font);
```

### <a name="description"></a>Описание

Назначение шрифтов для отрисовки текста мини-приложения на основе колесика прокрутки.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт колесика успешно назначен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_NUMERIC_SCROLL_WHEEL wheel;

status = gx_text_scroll_wheel_font_set(&wheel,
    GX_FONT_ID_WHEEL_NORMAL,
    GX_FONT_ID_WHEEL_SELECTED);

/* If status is GX_SUCCESS, the scroll wheel fonts have been assigned. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_event_process
- gx_text_scroll_wheel_text_color_set

## <a name="gx_text_scroll_wheel_text_color_set"></a>gx_text_scroll_wheel_text_color_set

Назначение цветов, используемых для отрисовки строк колесика прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_text_scroll_wheel_text_color_set(
    GX_TEXT_SCROLL_WHEEL *wheel,
    GX_RESOURCE_ID normal_text_color,
    GX_RESOURCE_ID selected_text_color,
    GX_RESOURCe_ID disabled_text_color);
```

### <a name="description"></a>Описание

Эта функция назначает цвета текста, используемые для рисования строк колесика прокрутки текста.

### <a name="parameters"></a>Параметры

- **wheel**: адрес блока управления колесиком прокрутки строки
- **normal_text_color**: цвет, используемый для рисования невыбранных строк
- **selected_text_color**: цвет, используемый для рисования выбранной строки
- **disabled_text_color**: цвет, используемый для рисования текста для отключенного мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет текста колесика прокрутки успешно назначен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING_SCROLL_WHEEL wheel;

UINT status = gx_text_scroll_wheel_text_color_set(&wheel,
    GX_COLOR_ID_NORMAL_TEXT,
    GX_COLOR_ID_SELECTED_TEXT,
    GX_COLOR_ID_DISABLED_TEXT);

/* If status is GX_SUCCESS, the colors used to draw the wheel text have been assigned. */
```

### <a name="see-also"></a>См. также:

- gx_numeric_scroll_wheel_create
- gx_numeric_scroll_wheel_range_set
- gx_scroll_wheel_create
- gx_scroll_wheel_event_process
- gx_scroll_wheel_gradient_alpha_set
- gx_scroll_wheel_row_height_set
- gx_scroll_wheel_selected_background_set
- gx_scroll_wheel_selected_get
- gx_scroll_wheel_selected_set
- gx_scroll_wheel_total_rows_set
- gx_text_scroll_wheel_callback_set
- gx_text_scroll_wheel_create
- gx_text_scroll_wheel_draw
- gx_text_scroll_wheel_event_process
- gx_text_scroll_wheel_font_set

## <a name="gx_tree_view_create"></a>gx_tree_view_create

Создание представления в виде дерева

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_create(
    GX_TREE_VIEW *tree,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent,
    ULONG style, 
    USHORT tree_view_id,
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает представление в виде дерева, как указано, и связывает представление в виде дерева с указанным родительским мини-приложением. Она принимает все типы мини-приложений в качестве дочерних элементов меню. В качестве дочернего элемента меню рекомендуется использовать мини-приложение типа GX_MENU.

GX_TREE_VIEW является производным от GX_WINDOW и поддерживает все службы API gx_window.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **name**: имя представления в виде дерева
- **parent**: указатель на родительское мини-приложение
- **style**: стиль мини-приложения Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **menu_id**: определяемый приложением идентификатор представления в виде дерева
- **size**: размер представления в виде дерева

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — представление в виде дерева успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
status = gx_tree_view_create(&my_tree_view,
    “my_tree_view”, parent,
    GX_STYLE_ENABLED, MY_TREE_VIEW_ID,
    &size);

/* If status is GX_SUCCESS the tree view was successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_draw
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_draw"></a>gx_tree_view_draw

Вывод представления в виде дерева

### <a name="prototype"></a>Прототип

```C
VOID gx_tree_view_draw(GX_TREE_VIEW *tree);
```

### <a name="description"></a>Описание

Эта служба рисует указанное представление в виде дерева. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования для мини-приложений настраиваемых представлений в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom tree view draw function. */
UINT my_tree_view_draw(GX_TREE_VIEW *tree_view)
{
    /* Perform default drawing */
    gx_tree_view_draw(tree_view);

    /* Add custom drawing here */
}
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_event_process"></a>gx_tree_view_event_process

Обработка события представления в виде дерева

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_event_process(
    GX_TREE_VIEW *tree, 
    GX_EVENT event_ptr);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для указанного представления в виде дерева. Эта служба должна вызываться как обработчик событий по умолчанию любыми настраиваемыми функциями обработки событий представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **event_ptr**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие представления в виде дерева успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic tree view event processing as part of custom event
    processing function. */

UINT custom_tree_view_event_process(GX_TREE_VIEW *tree_view,
    GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
        case xyz:
            /* Insert custom event handling here */
            break;
        default:
            /* Pass all other events to the default tree view
                event processing */
            status = gx_tree_view_event_process(tree_view, event);
            break;
    }
}
return status;
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_indentation_set"></a>gx_tree_view_indentation_set

Задание отступа для представления в виде дерева

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_indentation_set(
    GX_TREE_VIEW *tree,
    GX_VALUE indentation);
```

### <a name="description"></a>Описание

Эта служба задает отступ для представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **indentation**: отступ для задания

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — отступ для представления в виде дерева успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set tree view “my_tree” indentation to 10. */
status = gx_tree_view_indentation_set(&my_tree, 10);

/* If status is GX_SUCCESS the indentation of tree view “my_tree”
has been set to 10. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_event_process
- tree_view_position
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixemlap_set
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_position"></a>gx_tree_view_position

Размещение элементов представления в виде дерева

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_position(GX_TREE_VIEW *tree);
```

### <a name="description"></a>Описание

Эта служба размещает элементы представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — элементы представления в виде дерева успешно размещены.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Position tree view “my_tree” items. */
status = gx_tree_view_position(&my_tree);

/* If status is GX_SUCCESS the items of tree view “my_tree” has
been positioned. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_root_line_color_set"></a>gx_tree_view_root_line_color_set

Задание цвета корневой линии представления в виде дерева

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_root_line_color_set(
    GX_TREE_VIEW *tree,
    GX_RESOURCE_ID color_id);
```

### <a name="description"></a>Описание

Эта служба назначает цвет корневой линии для представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **color_id**: идентификатор ресурса для цвета корневой линии

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет корневой линии успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set root line color for tree view “my_tree”. */

status = gx_tree_view_root_line_color_set(&my_tree,
    MY_ROOT LINE_COLOR_ID);

/* If status is GX_SUCCESS the root line color of the tree view
“my_tree” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_root_pixelmap_set"></a>gx_tree_view_root_pixelmap_set

Задание карты отображения пикселей корня представления в виде дерева

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_root_pixelmap_set(
    GX_TREE_VIEW *tree,
    GX_RESOURCE_ID expand_map_id,
    GX_RESOURCE_ID collapse_map_id);
```

### <a name="description"></a>Описание

Эта служба назначает карту отображения пикселей развертывания и свертывания для представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **expand_map_id**: идентификатор ресурса карты отображения пикселей развертывания
- **collapse_map_id**: идентификатор ресурса карты отображения пикселей свертывания

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей корня представления в виде дерева успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set root pixelmaps for tree view “my_tree”. */

status = gx_tree_view_root_pixelmap_set(&my_tree,
    MY_EXPAND_MAP_ID,
    MY_COLLAPSE_MAP_ID);

/* If status is GX_SUCCESS the root pixelmaps of tree view
“my_tree” has been set. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_selected_get
- gx_tree_view_selected_set

## <a name="gx_tree_view_selected_get"></a>gx_tree_view_selected_get

Получение выбранного элемента

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_selected_get(
    GX_TREE_VIEW *tree,
    GX_WIDGET **selected);
```

### <a name="description"></a>Описание

Эта служба извлекает текущий выбранный элемент представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **selected**: указатель на выбранный указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — выбранный элемент успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Retrieve selected item of tree view “my_tree”. */

GX_WIDGET *selected;

status = gx_tree_view_selected_get(&my_tree, &selected);

/* If status is GX_SUCCESS the selected item of tree view “my_tree”
has been retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_set

## <a name="gx_tree_view_selected_set"></a>gx_tree_view_selected_set

Задание выбранного элемента

### <a name="prototype"></a>Прототип

```C
UINT gx_tree_view_selected_set(
    GX_TREE_VIEW *tree,
    GX_WIDGET *selected);
```

### <a name="description"></a>Описание

Эта служба задает выбранный элемент для представления в виде дерева.

### <a name="parameters"></a>Параметры

- **tree**: указатель на блок управления представлением в виде дерева
- **selected**: указатель на новый выбранный элемент

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — меню успешно нарисовано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set selected item of tree view “my_tree” to “tree_view_item’. */
status = gx_tree_view_selected_set(&my_tree, &tree_view_item);

/* If status is GX_SUCCESS selected item of tree view “my_menu” has
been set to “tree_view_item”. */
```

### <a name="see-also"></a>См. также:

- gx_menu_draw
- gx_menu_insert
- gx_menu_remove
- gx_menu_text_draw
- gx_menu_text_offset_set
- gx_tree_view_create
- gx_tree_view_draw
- gx_tree_view_event_process
- gx_tree_view_indentation_set
- gx_tree_view_position
- gx_tree_view_root_line_color_set
- gx_tree_view_root_pixelmap_set
- gx_tree_view_selected_get

## <a name="gx_utility_canvas_to_bmp"></a>gx_utility_canvas_to_bmp

Преобразование памяти холста в растровое изображение

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_canvas_to_bmp(
    GX_CANVAS *canvas, 
    GX_RECTANGLE *rect,
    UINT (*write_data)(GX_UBYTE *byte_data, UINT data_count));
```

### <a name="description"></a>Описание

Эта служба преобразует память холста в файл растрового изображения.

### <a name="parameters"></a>Параметры

- **canvas**: указатель на блок управления холстом
- **rect**: прямоугольник для преобразования
- **write_data**: указатель функции обратного вызова для записи данных

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — целочисленное значение успешно преобразовано в строку.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на возвращаемый буфер.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер возвращаемого буфера.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
FILE *fp = GX_NULL;

/* define call back function of how to write the data read from
canvas memory. */

UINT write_data_callback(GX_UBYTE *byte_data, UINT data_count)
{
    if (fp)
    {
        fwrite(byte_data, 1, data_count, fp);
    }
    return GX_SUCCESS;
}

VOID scroll_wheel_screen_draw(GX_WINDOW *window)
{
    UINT status;

    GX_RECTANGLE size = {31,31,610,450};
    gx_window_draw(window);
    if (screenshot)
    {
        fp = fopen("../screenshot.bmp", "wb");
        /* Convert canvas memory to bitmap format.
           Status GX_SUCCESS means operation succeed. */
        status = gx_utility_canvas_to_bmp(root->gx_window_root_canvas,
            &size, write_data_callback);

        fclose(fp);
    }
}
```

### <a name="see-also"></a>См. также:

- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_circle_point_get"></a>gx_utility_circle_point_get

Вычисление точки на окружности

### <a name="prototype"></a>Прототип

```C
INT gx_utility_circle_point_get(
    INT xCenter, 
    INT yCenter,
    UINT radius, 
    INT angle, 
    GX_POINT *point);
```

### <a name="description"></a>Описание

Эта служба вычисляет точку на окружности с учетом центра окружности, радиуса и угла.

### <a name="parameters"></a>Параметры

- **xCenter**: координата центра окружности по оси X
- **yCenter**: координата центра окружности по оси Y
- **radius**: радиус окружности
- **angle**: угол для вычисления точки периметра окружности (в градусах)
- **point**: адрес переменной GX_POINT, в которой будут храниться вычисленные координаты по осям X, Y
- **end_alpha**: конечное альфа-значение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — градиент создан.
- **GX_PTR_ERROR** (0x07) — недопустимый адрес возвращаемой точки.
- **GX_INVALID_VALUE** (0x22) — недопустимый параметр радиуса.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_POINT point;
    UINT status;

        status = gx_utility_circle_point_get(100, 100,
20, 45,
&point);

/* If status is GX_SUCCESS the value of point is the x,y coordinate of a point on the circle perimeter at an angle of 45 degrees, where the circle is centered at (100, 100), has a radius of 20 pixels */

```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_asin
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_gradient_create"></a>gx_utility_gradient_create

Создание градиентной карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
INT gx_utility_gradient_create(
    GX_GRADIENT *gradient,
    GX_VALUE width, 
    GX_VALUE height,
    UCHAR type, 
    GX_UBYTE start_alpha, 
    GX_UBYTE end_alpha);
```

### <a name="description"></a>Описание

Эта служба создает градиентную карту отображения пикселей во время выполнения. Градиентное изображение можно использовать для создания эффектов исчезновения и других визуальных изменений.

Ширина и высота запрошенного градиента должна быть не меньше 2 x 2 пикселя.

GUIX хранит список созданных градиентов, и эта функция сначала выполняет поиск по списку градиентов, чтобы найти соответствующую карту отображения пикселей градиента, прежде чем создать новую карту отображения пикселей. Иными словами, одна и та же карта отображения пикселей градиента требуется несколько раз, фактически создается только одна карта отображения пикселей, и каждый градиент, которому требуется эта карта отображения пикселей, совместно использует созданную карту отображения пикселей.

Для этого API необходимо определить функцию gx_system_memory_allocator, чтобы разрешить выделение памяти во время выполнения.

К флагам типа градиента относятся GX_GRADIENT_TYPE_ALPHA и GX_GRADIENT_TYPE_MIRROR. В настоящее время поддерживаются только градиенты типа GX_GRADIENT_TYPE_ALPHA (т. е. этот флаг типа должен быть задан). Флаг GX_GRADIENT_TYPE_MORROR является необязательным, и, если задан параметр SET, логика создания градиента создает градиент, который изменяется с start_alpha на end_alpha и обратно в start_alpha. В противном случае создается линейный градиент.

### <a name="parameters"></a>Параметры

- **gradient**: указатель на структуру блока управления градиентом
- **width**: запрошенная ширина карты отображения пикселей
- **height**: запрошенная высота карты отображения пикселей
- **type**: запрошенный тип градиента
- **start_alpha**: начальное альфа-значение
- **end_alpha**: конечное альфа-значение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — градиент создан.
- **GX_INVALID_SIZE** (0x19) — градиент менее 2 x 2 пикселя.
- **GX_NOT_SUPPORTED** (0x28) — градиент не является градиентом типа GX_GRADIENT_TYPE_ALPHA.
- **GX_FAILURE** (0x10) — распределитель памяти не определен, или не удалось выделить память.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на градиент.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение ширины или высоты.
- **GX_INVALID_TYPE** (0x1B) — недопустимый тип градиента.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_GRADIENT gradient;

UINT status;

status = gx_utiity_gradient_create(&gradient, 3, 40,
    GX_GRADIENT_TYPE_ALPHA, 240, 0);

/* If status == GX_SUCCESS the gradient pixelmap has been created */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_asin
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_gradient_delete"></a>gx_utility_gradient_delete

Удаление созданного ранее градиента

### <a name="prototype"></a>Прототип

```C
INT gx_utility_gradient_delete(GX_GRADIENT *gradient);
```

### <a name="description"></a>Описание

Эта служба удаляет созданный ранее градиент. Если карта отображения пикселей, связанная с этим градиентом, не используется другими градиентами, данные карты отображения пикселей также будут удалены.

### <a name="parameters"></a>Параметры

- **gradient**: указатель на блок управления градиентом

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — градиент удален.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на градиент.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_GRADIENT gradient;

UINT status;

/* Delete previously created gradient. */
status = gx_utility_gradient_delete(&gradient);

/* If status == GX_SUCCESS, the gradient has been deleted. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_asin
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_ltoa"></a>gx_utility_ltoa

Преобразование длинного целого числа в ASCII

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_itoa(
    LONG value, 
    GX_CHAR *return_buffer,
    UINT seturn_buffer_size);
```

### <a name="description"></a>Описание

Эта служба преобразует длинное целочисленное значение в строку ASCII.

### <a name="parameters"></a>Параметры

- **value**: длинное целочисленное значение для преобразования
- **return_buffer**: буфер назначения для строки ASCII
- **return_buffer_size**: размер буфера назначения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — целочисленное значение успешно преобразовано в строку.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на возвращаемый буфер.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер возвращаемого буфера.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
INT my_value = 200;

GX_CHAR string_buffer[10];

UINT status;

/* Convert “my_value” into an ASCII string. */
status = gx_utility_ltoa(my_value, string_buffer, 10);

/* If status is GX_SUCCESS, “string_buffer” contains the ASCII
representation of “my_value”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_math_acos"></a>gx_utility_math_acos

Вычисление арккосинуса

### <a name="prototype"></a>Прототип

```C
INT gx_utility_math_acos(GX_FIXED_VAL x);
```

### <a name="description"></a>Описание

Эта служба вычисляет значение угла арккосинуса x.

Входное значение является типом данных с фиксированной точкой; вызовите GX_FIXED_VAL_MAKE для преобразования из INT в GX_FIXED_VAL. Например, если необходимо вычислить арккосинус 0,5, укажите входные данные GX_FIXED_VAL_MAKE(1) / 2.

В GUIX версии 5.4.0 и более ранней типом входных значений этой функции является INT, а значение ограничено диапазоном [–256, 256]. Приложение должно масштабировать значение из диапазона [–1, 1] в диапазон [–256, 256] перед вызовом этой службы. Если имеется проект с версией GUIX 5.4.0 и более поздней, который ссылается на этот API, и вы хотите обновить проект с помощью последней библиотеки guix, Имеются две возможности.

1. Исправить входное значение этого вызова API, чтобы использовать значение типа данных GX_FIXED_VAL.
1. Определить GUIX_5_4_0_COMPATIBILITY.

### <a name="parameters"></a>Параметры

- **x**: значение, для которого следует вычислить арккосинус

### <a name="return-values"></a>Возвращаемые значения

- **angle** — значение угла для арккосинуса x.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
/* Compute the angle value of arc cosine of “0.5”. */

#if defined(GUIX_5_4_0_COMPATIBILITY)
    x = 256 / 2;
#else
    x = GX_FIXED_VAL_MAKE(1) / 2;
#endif

angle = gx_utility_math_acos(x);

/* “angle” contains the angle value of arc cosine “x”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_asin
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_math_asin"></a>gx_utility_math_asin

Вычисление арксинуса

### <a name="prototype"></a>Прототип

```C
INT gx_utility_math_asin(GX_FIXED_VAL x);
```

### <a name="description"></a>Описание

Эта служба вычисляет значение угла арксинуса x.

Входное значение является типом данных с фиксированной точкой; вызовите GX_FIXED_VAL_MAKE для преобразования из INT в GX_FIXED_VAL. Например, если необходимо вычислить арксинус 0,5, укажите входные данные GX_FIXED_VAL_MAKE(1) / 2.

В GUIX версии 5.4.0 и более ранней типом входных значений этой функции является INT, а значение ограничено диапазоном [–256, 256]. Приложение должно масштабировать значение из диапазона [–1, 1] в диапазон [–256, 256] перед вызовом этой службы. Если имеется проект с версией GUIX 5.4.0 и более ранней и вы хотите обновить проект с помощью последней библиотеки guix, Имеются две возможности.

1. Исправить входное значение этого вызова API, чтобы использовать значение типа данных GX_FIXED_VAL.
1. Определить GUIX_5_4_0_COMPATIBILITY.

### <a name="parameters"></a>Параметры

- **x**: значение, для которого следует вычислить арксинус

### <a name="return-values"></a>Возвращаемые значения

- **angle** — значение угла для арксинуса x.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
/* Compute the angle value of arc sine of “x”. */

#if defined GUIX_5_4_0_COMPATIBILITY
    x = 256 / 2;
#else
    X = GX_FIXED_VAL_MAKE(1) / 2;
#endif

angle = gx_utility_math_asin(x);

/* “angle” contains the angle value of arc sine “x”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_acos
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_math_cos"></a>gx_utility_math_cos

Вычисление косинуса

### <a name="prototype"></a>Прототип

```C
GX_FIXED_VAL gx_utility_math_cos(GX_FIXED_VAL angle);
```

### <a name="description"></a>Описание

Эта служба вычисляет косинус заданного угла.

Входное значение является типом данных с фиксированной точкой; вызовите GX_FIXED_VAL_MAKE для преобразования из INT в GX_FIXED_VAL. Например, если необходимо вычислить косинус 90, укажите входные данные GX_FIXED_VAL_MAKE(90).

Возвращаемое значение является типом данных с фиксированной точкой; вызовите GX_FIXED_VAL_TO_INT для преобразования из GX_FIXED_VAL в INT.

В версии GUIX 5.4.0 или более ранней значение входного значения и тип возвращаемого значения этой службы — INT, входное значение и возвращаемое значение увеличено на 256. Поэтому приложение должно масштабировать значение угла на 256 перед вызовом этой службы. Если имеется проект с версией GUIX 5.4.0 и более ранней и вы хотите обновить проект с помощью последней библиотеки guix, у вас есть два варианта.

1. Исправить входное значение и обработку возвращаемого значения этого вызова API, чтобы использовать значение типа данных GX_FIXED_VAL.
1. Определить GUIX_5_4_0_COMPATIBILITY.

### <a name="parameters"></a>Параметры

- **angle**: угол, для которого следует вычислить косинус

### <a name="return-values"></a>Возвращаемые значения

- **cosine**: косинус указанного угла

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
/* Compute cosine of 90 degree. */

INT angle = 90;

#if defined (GUIX_5_4_0_COMPATIBILITY)
    INT scaled_angle = angle << 8;
#else
    GX_FIXED_VAL scaled_angle = GX_FIXED_VAL_MAKE(angle);
#endif

my_angle_cosine = gx_utility_math_cos(scaled_angle);

/* “my_angle_cosine” contains the cosine of “my_angle”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_acos
- gx_utility_math_asin
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_math_sin"></a>gx_utility_math_sin

Вычисление синуса

### <a name="prototype"></a>Прототип

```C
GX_FIXED_VAL gx_utility_math_sin(GX_FIXED_VAL angle);
```

### <a name="description"></a>Описание

Эта служба вычисляет синус заданного угла.

Входное значение является типом данных с фиксированной точкой; вызовите GX_FIXED_VAL_MAKE для преобразования из INT в GX_FIXED_VAL. Например, если необходимо вычислить синус 90, укажите входные данные GX_FIXED_VAL_MAKE(90). 

Возвращаемое значение является типом данных с фиксированной точкой; вызовите GX_FIXED_VAL_TO_INT для преобразования из GX_FIXED_VAL в INT.

В версии GUIX 5.4.0 или более ранней значение входного значения и тип возвращаемого значения — INT, входное значение и возвращаемое значение увеличено на 256. Поэтому приложение должно масштабировать значение угла на 256 перед вызовом этой службы. Если имеется проект с версией GUIX 5.4.0 и более ранней и вы хотите обновить проект с помощью последней библиотеки guix, у вас есть два варианта.

1. Исправить входное значение и обработку возвращаемого значения этого вызова API, чтобы использовать значение типа данных GX_FIXED_VAL.
1. Определить GUIX_5_4_0_COMPATIBILITY.

### <a name="parameters"></a>Параметры

- **angle**: угол, для которого следует вычислить синус

### <a name="return-values"></a>Возвращаемые значения

- **sine**: синус указанного угла

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
INT my_angle = 80;

/* Compute sine of “my_angle”. */

#if defined(GUIX_5_4_0_COMPATIBILITY)
    INT scaled_angle = my_angle << 8;
#else
    GX_FIXED_VAL = GX_FIXED_VAL_MAKE(my_angle);
#endif

my_angle_sine = gx_utility_math_sin(scaled_angle);

/* “my_angle_sine” contains the sine of “my_angle”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_acos
- gx_utility_asin
- gx_utility_math_cos
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_math_sqrt"></a>gx_utility_math_sqrt

Вычисление квадратного корня

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_math_sqrt(UINT value);
```

### <a name="description"></a>Описание

Эта служба вычисляет квадратный корень заданного значения.

### <a name="parameters"></a>Параметры

- **value**: значение, для которого следует вычислить квадратный корень

### <a name="return-values"></a>Возвращаемые значения

- **square root**: квадратный корень заданного значения

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
/* Compute square root of “my_value”. */
my_square_root = gx_utility_math_sqrt(my_value);

/* “my_square_root” contains the square root of “my_value”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_bidi_paragraph_reorder"></a>gx_utility_bidi_paragraph_reorder

Переупорядочение двунаправленного текста в порядке его отображения

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_bidi_paragraph_reorder(GX_BIDI_TEXT_INFO *input_info, 
                                       GX_BIDI_RESOLVED_TEXT_INFO **resolved_info_head)
```

### <a name="description"></a>Описание

Эта служба переупорядочивает двунаправленный текст в порядок его отображения. Если указаны шрифт текста и ширина дисплея, то сначала применяется перенос строк, а процесс изменения порядка будет применен уже на основе каждой строки. Если указанный текст содержит более одного абзаца, то служба будет разбивать текст на абзацы, а результат разбиения на строки и переупорядочивания каждого абзаца будет связан как список.

### <a name="parameters"></a>Параметры

- **input_info**: указатель на сведения о перестановке разрывов строк и текста
- **resolved_info_head**: указатель на связанный список, который содержит разбиение на строки и результаты переупорядочения каждого абзаца указанного текста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — двунаправленный текст успешно переупорядочен.
- **GX_PTR_ERROR** (0x07) — недопустимые указатели ввода.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Например, .
#### <a name="draw-single-line-bidi-text-to-canvas"></a>Вывод однострочного двунаправленного текста на холст
```C
VOID custom_widget_draw(GX_WIDGET *widget)
{
    GX_BIDI_TEXT_INFO input_info;
    GX_BIDI_RESOLVED_TEXT_INFO *resolved_info;
    GX_CHAR bidi_text[] = "Single Line String";
    
    input_info.gx_bidi_text_info_text.gx_string_ptr = bidi_text;
    input_info.gx_bidi_text_info_text.gx_string_length = sizeof(bidi_text) - 1;
    input_info.gx_bidi_text_info_font = GX_NULL;
    input_info.gx_bidi_text_info_display_width = -1;
    
    /* Reordering BiDi text. */
    status = gx_utility_bidi_paragraph_reorder(&input_info, &resolved_info);
    
    if(status == GX_SUCCESS)
    {
        /* Draw reordered bidi text to canvas. */
        gx_canvas_text_draw_ext(widget->gx_widget_size.gx_rectangle_left,
                                widget->gx_widget_size.gx_rectangle_top,
                                resolved_info.gx_bidi_resolved_text_info_text);

        /* Delete resolved information list. */
        gx_utility_bidi_resolved_text_info_delete(&resolved_info);
    }
}
```
#### <a name="draw-multi-line-bidi-text-to-canvas"></a>Вывод многострочного двунаправленного текста на холст
```C
VOID custom_widget_draw(GX_WIDGET *widget)
{
    GX_BIDI_TEXT_INFO input_info;
    GX_BIDI_RESOLVED_TEXT_INFO *resolved_info;
    GX_CHAR bidi_text[] = "Multi Line BiDi Text Display Example";
    GX_FONT *font;
    GX_RECTANGLE client;
    GX_VALUE display_width;
    INT index;
    GX_VALUE xpos;
    GX_VALUE ypos;
    
    /* Pickup the font. */
    gx_context_font_get(font_id, &font);
    gx_widget_client_get(widget, 2, &client);
    
    display_width = (GX_VALUE)(client.gx_rectangle_right - client.gx_rectangle_left + 1);
    
    input_info.gx_bidi_text_info_text.gx_string_ptr = bidi_text;
    input_info.gx_bidi_text_info_text.gx_string_length = sizeof(bidi_text) - 1;
    input_info.gx_bidi_text_info_font = font;
    input_info.gx_bidi_text_info_display_width = display_width;
    
    /* Reordering BiDi text. */
    status = gx_utility_bidi_paragraph_reorder(&input_info, &resolved_info);
    
    if(status == GX_SUCCESS)
    {
        xpos = client.gx_rectangle_left;
        ypos = client.gx_rectangle_top;
        
        /* Draw reordered bidi text to canvas. */
        for(index = 0; index < resolved_info.gx_bidi_resolved_text_info_total_lines; index++)
        {
            gx_canvas_text_draw_ext(xpos, ypos, &resolved_info.gx_bidi_resolved_text_info_text[index]);
        
            ypos += font->gx_font_line_height;
        }
        
        /* Delete resolved information list. */
        gx_utility_bidi_resolved_text_info_delete(&resolved_info);
    }
}
```

#### <a name="draw-multi-paragraph-bidi-text-to-canvas"></a>Рисование многоабзацного двунаправленного письма на холст
```C
VOID custom_widget_draw(GX_WIDGET *widget)
{
    GX_BIDI_TEXT_INFO input_info;
    GX_BIDI_RESOLVED_TEXT_INFO *resolved_info;
    GX_BIDI_RESOLVED_TEXT_INFO *next;
    GX_CHAR bidi_text[] = "Multi Paragraph\r BiDi Text Display Example";
    GX_FONT *font;
    GX_RECTANGLE client;
    GX_VALUE display_width;
    INT index;
    GX_VALUE xpos;
    GX_VALUE ypos;
    
    /* Pickup the font. */
    gx_context_font_get(font_id, &font);
    gx_widget_client_get(widget, 2, &client);
    
    display_width = (GX_VALUE)(client.gx_rectangle_right - client.gx_rectangle_left + 1);
    
    
    input_info.gx_bidi_text_info_text.gx_string_ptr = bidi_text;
    input_info.gx_bidi_text_info_text.gx_string_length = sizeof(bidi_text) - 1;
    input_info.gx_bidi_text_info_font = font;
    input_info.gx_bidi_text_info_display_width = display_width;
    
    /* Reordering BiDi text. */
    status = gx_utility_bidi_paragraph_reorder(&input_info, &resolved_info);
    
    if(status == GX_SUCCESS)
    {
        xpos = client.gx_rectangle_left;
        ypos = client.gx_rectangle_top;
        
        next = resolved_info;
        
        /* Draw reordered bidi text to canvas. */
        while(next)
        {
            for(index = 0; index < next.gx_bidi_resolved_text_info_total_lines; index++)
            {
                gx_canvas_text_draw_ext(xpos, ypos, &next.gx_bidi_resolved_text_info_text[index]);
            
                ypos += font->gx_font_line_height;
            }
            
            next = next->gx_bidi_resolved_text_info_next;
        }
        
        /* Delete resolved information list. */
        gx_utility_bidi_resolved_text_info_delete(&resolved_info);
    }
}
```

### <a name="see-also"></a>См. также:

- gx_utility_bidi_resolved_text_info_delete

## <a name="gx_utility_bidi_resolved_text_info_delete"></a>gx_utility_bidi_resolved_text_info_delete

Удаление разрешенного списка с информацией о двунаправленном тексте

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_bidi_resolved_text_info_delete(GX_BIDI_RESOLVED_TEXT_INFO **resolved_info_head)
```

### <a name="description"></a>Описание

Эта служба удаляет разрешенный список с информацией, возвращенный gx_utility_bidi_paragraph_reorder.

### <a name="parameters"></a>Параметры

- **resolved_info_head**: указатель на разрешенный список с информацией о двунаправленном тексте

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — разрешенный текст успешно удален.
- **GX_PTR_ERROR** (0x07) — недопустимые указатели ввода.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример
```C
VOID custom_widget_draw(GX_WIDGET *widget)
{
    GX_BIDI_TEXT_INFO input_info;
    GX_BIDI_RESOLVED_TEXT_INFO *resolved_info;
    GX_CHAR bidi_text[] = "Single Line String";
    
    input_info.gx_bidi_text_info_text.gx_string_ptr = bidi_text;
    input_info.gx_bidi_text_info_text.gx_string_length = sizeof(bidi_text) - 1;
    input_info.gx_bidi_text_info_font = GX_NULL;
    input_info.gx_bidi_text_info_display_width = -1;
    
    /* Reordering BiDi text. */
    status = gx_utility_bidi_paragraph_reorder(&input_info, &resolved_info);
    
    if(status == GX_SUCCESS)
    {
        /* Draw reordered bidi text to canvas. */
        gx_canvas_text_draw_ext(widget->gx_widget_size.gx_rectangle_left,
                                widget->gx_widget_size.gx_rectangle_top,
                                resolved_info.gx_bidi_resolved_text_info_text);

        /* Delete resolved information list. */
        gx_utility_bidi_resolved_text_info_delete(&resolved_info);
    }
}
```

### <a name="see-also"></a>См. также:

- gx_utility_bidi_paragraph_reorder

## <a name="gx_utility_pixelmap_resize"></a>gx_utility_pixelmap_resize

Изменение размера карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_pixelmap_resize(
    GX_PIXELMAP *src,
    GX_PIXELMAP *destination, 
    INT width, 
    INT height);
```

### <a name="description"></a>Описание

Эта служба изменяет размер карты отображения пикселей и возвращает указатель на новую карту отображения пикселей, которая является результатом изменения размера карты отображения пикселей.

Эта служба требует предварительного использования gx_system_memory_allocator_set, чтобы выделить память для хранения карты отображения пикселей данных с измененным размером.

### <a name="parameters"></a>Параметры

- **src**: указатель на карту отображения пикселей для изменения размера
- **destination**: буфер назначения для результирующей карты отображения пикселей
- **width**: ширина результирующей карты отображения пикселей (в пикселях)
- **height**: высота результирующей карты отображения пикселей (в пикселях)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — размер карты отображения пикселей успешно изменен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на исходную или целевую карту отображения пикселей.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение ширины или высоты.
- **GX_NOT_SUPPORTED** (0x28) — исходная карта отображения пикселей имеет сжатый формат.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
GX_PIXLEMAP *des_pixelmap;

/* Resize “src_pixelmap” with specifiy width and height. */
status = gx_utility_pixelmap_resize(src_pixelmap,
    &des_pixelmap, 100, 200);

/* If status is GX_SUCCESS. “des_pixelmap” successfully load the
resulting pixelmap of resize. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift
- gx_canvas_pixelmap_rotate

## <a name="gx_utility_pixelmap_rotate"></a>gx_utility_pixelmap_rotate

Поворот карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_pixelmap_rotate(
    GX_PIXELMAP *src, INT angle,
    GX_PIXELMAP *destination,
    UINT *rot_cx, 
    UINT *rot_cy);
```

### <a name="description"></a>Описание

Эта служба поворачивает карту отображения пикселей и возвращает указатель на новую карту отображения пикселей, которая является результатом вращения карты отображения пикселей. Чтобы повернуть карту отображения пикселей непосредственно на холсте, используйте gx_canvas_pixelmap_rotate().

Эта служба требует предварительного использования gx_system_memory_allocator_set, чтобы выделить память для хранения повернутой карты отображения пикселей данных.

### <a name="parameters"></a>Параметры

- **src**: карта отображения пикселей для поворота
- **angle**: угол поворота (в градусах)
- **destination**: буфер назначения для результирующей карты отображения пикселей
- **rot_cx**: полученная координата центра вращения на оси X относительно целевой карты отображения пикселей Должно быть инициировано с координатой центра вращения на оси X относительно исходной карты отображения пикселей. Если rot_cx является GX_NULL, значение не будет извлекаться.
- **rot_cy**: полученная координата центра вращения на оси Y относительно целевой карты отображения пикселей Должно быть инициировано с координатой центра вращения на оси Y относительно исходной карты отображения пикселей. Если rot_cy является GX_NULL, значение не будет извлекаться.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно повернута.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на исходную или целевую карту отображения пикселей.
- **GX_INVALID_VALUE** (0x22) — значение угла равно 0.
- **GX_INVALID_FORMAT** (0x28) — исходная карта отображения пикселей находится в сжатом формате, что не поддерживается.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
rot_cx = source_rotate_center_x;
rot_cy = source_rotate_center_y;

/* rotate “src_pixelmap” by 30 degree in clockwise direction. */
status = gx_utility_pixelmap_rotate(src_pixelmap, 30,
    &des_pixelmap, &rot_cx, &rot_cy);

/* If status is GX_SUCCESS. “des_pixelmap” successfully load the
resulting pixelmap of rotation. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift
- gx_canvas_pixelmap_rotate

## <a name="gx_utility_pixelmap_simple_rotate"></a>gx_utility_pixelmap_simple_rotate

Поворот карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_pixelmap_simple_rotate(
    GX_PIXELMAP *src,
    INT angle,
    GX_PIXELMAP *destination,
    UINT *rot_cx,
    UINT *rot_cy);
```

### <a name="description"></a>Описание

Эта служба поворачивает карту отображения пикселей на 90, 180 или 270 градусов.

### <a name="parameters"></a>Параметры

- **src**: карта отображения пикселей для поворота
- **angle**: угол поворота (в градусах)
- **destination**: буфер назначения для результирующей карты отображения пикселей
- **rot_cx**: полученная координата центра вращения на оси X относительно целевой карты отображения пикселей Должно быть инициировано с координатой центра вращения на оси X относительно исходной карты отображения пикселей. Если rot_cx является GX_NULL, значение не будет извлекаться.
**rot_cy**: полученная координата центра вращения на оси Y относительно целевой карты отображения пикселей Должно быть инициировано с координатой центра вращения на оси Y относительно исходной карты отображения пикселей. Если rot_cy является GX_NULL, значение не будет извлекаться.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно повернута.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель на исходную или целевую карту отображения пикселей.
- **GX_INVALID_VALUE** (0x22) — значение угла равно 0 или не является простым углом (например, 90, 180, 270 градусов).
- **GX_INVALID_FORMAT** (0x28) — исходная карта отображения пикселей находится в сжатом формате, что не поддерживается.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
rot_cx = source_rotate_center_x;
rot_cy = source_rotate_center_y;

/* rotate “src_pixelmap” by 90 degree in clockwise direction. */
status = gx_utility_pixelmap_simple_rotate(src_pixelmap, 90,
    &des_pixelmap,
    &rot_cx, &rot_cy);

/* If status is GX_SUCCESS. “des_pixelmap” successfully load the
resulting pixelmap of rotation. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_center"></a>gx_utility_rectangle_center

Центрирование прямоугольника внутри другого прямоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_rectangle_center(
    GX_RECTANGLE *rectangle,
    GX_RECTANGLE *within_rectangle);
```

### <a name="description"></a>Описание

Эта служба размещает прямоугольник внутри другого прямоугольника по центру.

### <a name="parameters"></a>Параметры

- **rectangle**: прямоугольник для центрирования
- **within_rectangle**: прямоугольник, в котором выполняется центрирование

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — прямоугольник успешно размещен по центру.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода прямоугольника.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер прямоугольника.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
UINT status;

/* Center “my_inner_rectangle” inside of “my_outer_rectangle”.*/
status = gx_utility_rectangle_center(&my_inner_rectangle,
    &my_outer_rectangle);

/* Is status is GX_SUCCESS, “my_inner_rectangle” is centered
within “my_other_rectangle”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_center_find"></a>gx_utility_rectangle_center_find

Поиск центра прямоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_rectangle_center_find(
    GX_RECTANGLE *rectangle,
    GX_POINT *return_center);
```

### <a name="description"></a>Описание

Эта служба находит центр прямоугольника.

### <a name="parameters"></a>Параметры

- **rectangle**: прямоугольник
- **return_center**: указатель на центральную точку

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — центр прямоугольника успешно найден.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер прямоугольника.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status;

GX_RECTANGLE my_rectangle;

GX_POINT my_center_point;

gx_utility_define(&my_rectangle, 0, 0, 100, 100);

/* Find center of “my_rectangle””. */

status = gx_utility_rectangle_center_find(&my_rectangle,
    &my_center_point);

/* If status is GX_SUCCESS, “my_center_point” is the center point
of “my_rectangle” (50, 50). */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_combine"></a>gx_utility_rectangle_combine

Объединение двух прямоугольников в первый

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_rectangle_combine(
    GX_RECTANGLE *first_rectangle,
    GX_RECTANGLE *second_rectangle);
```

### <a name="description"></a>Описание

Эта служба объединяет первый и второй прямоугольники в первый прямоугольник. Первый прямоугольник расширяется для включения второго.

### <a name="parameters"></a>Параметры

- **first_rectangle**: первый прямоугольник и комбинированный прямоугольник
- **second_rectangle**: второй прямоугольник

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — два прямоугольника успешно объединены.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT status;

GX_RECTANGLE rect_a;

GX_RECTANGLE rect_b;

gx_utility_rectangle_define(&rect_a, 0, 0, 100, 100);
gx_utility_rectangle_define(&rect_b, 50, 50, 200, 200);

/* Combine “my_rectangle_a” to “my_rectangle_b”. */
status = gx_utility_rectangle_combine(&rect_a, &rect_b);

/* If status is GX_SUCCESS, “rect_a” is (0, 0, 200, 200) the merger
of the original “rect_a” and “rect_b”. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_compare"></a>gx_utility_rectangle_compare

Сравнение двух прямоугольников

### <a name="prototype"></a>Прототип

```C
GX_BOOL gx_utility_rectangle_compare( 
    GX_RECTANGLE *first_rectangle,
    GX_RECTANGLE *second_rectangle);
```

### <a name="description"></a>Описание

Эта служба сравнивает первый и второй прямоугольники. Если они равны, возвращается значение GX_TRUE.

### <a name="parameters"></a>Параметры

- **first_rectangle**: первый прямоугольник
- **second_rectangle**: второй прямоугольник

### <a name="return-values"></a>Возвращаемые значения

- **result** — GX_TRUE, если прямоугольники равны, в противном случае возвращается GX_FALSE.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Compare “my_rectangle_a” to “my_rectangle_b”. */
result = gx_utility_rectangle_compare(&my_rectangle_a,
    &my_rectangle_b);

/* If result is GX_TRUE, the two rectangles are equal. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_define"></a>gx_utility_rectangle_define

Определение прямоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_rectangle_define(
    GX_RECTANGLE *rectangle,
    GX_VALUE left,
    GX_VALUE top, 
    GX_VALUE right, 
    GX_VALUE bottom);
```

### <a name="description"></a>Описание

Эта служба определяет прямоугольник, как указано.

### <a name="parameters"></a>Параметры

- **rectangle**: блок управления прямоугольником
- **left**: самая левая координата
- **top**: самая верхняя координата
- **right**: самая правая координата
- **bottom**: самая нижняя координата

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — прямоугольник успешно задан.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель прямоугольника.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
UINT status;

GX_RECTANGLE my_rect;

/* Define “my_rect”. */

status = gx_utility_rectangle_define(&my_rect, 10, 5, 200, 100);

/* If status is GX_SUCCESS, “my_rect” is defined. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_overlap_detect"></a>gx_utility_rectangle_overlap_detect

Обнаружение наложения прямоугольников

### <a name="prototype"></a>Прототип

```C
GX_BOOL gx_utility_rectangle_overlap_detect(
    GX_RECTANGLE *first_rectangle,
    GX_RECTANGLE *second_rectangle,
    GX_RECTANGLE *return_overlap_area);
```

### <a name="description"></a>Описание

Эта служба обнаруживает наложение прямоугольников. Если обнаружено наложение, служба возвращает GX_TRUE и накладывающийся прямоугольник.

### <a name="parameters"></a>Параметры

- **first_rectangle**: первый прямоугольник
- **second_rectangle**: второй прямоугольник
- **return_overlap_area**: область наложения прямоугольников

### <a name="return-values"></a>Возвращаемые значения

- **result**: GX_TRUE, если прямоугольники накладываются, в противном случае GX_FALSE

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
/* Detect overlap of “my_rectangle_a” and “my_rectangle_b”. */
result = gx_utility_rectangle_overlap_detect(&my_rectangle_a,
    &my_rectangle_b,
    &my_overlap_area);

/* If result is GX_TRUE, “my_overlap_area” specifies the area the
rectangles overlap. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_point_detect"></a>gx_utility_rectangle_point_detect

Определение того, находится ли точка в прямоугольнике

### <a name="prototype"></a>Прототип

```C
GX_BOOL gx_utility_rectangle_point_detect(
    GX_RECTANGLE *rectangle,
    GX_POINT point);
```

### <a name="description"></a>Описание

Эта служба обнаруживает, находится ли указанная точка в прямоугольнике. Если точка находится в прямоугольнике, служба возвращает GX_TRUE.

### <a name="parameters"></a>Параметры

- **rectangle**: прямоугольник
- **point**: точка

### <a name="return-values"></a>Возвращаемые значения

- **result** — GX_TRUE, если точка находится в прямоугольнике, в противном случае GX_FALSE.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```c
GX_RECTANGLE my_rectangle;

GX_POINT my_point;

gx_utility_rectangle_define(&my_rectangle, 0, 0, 100, 100);

my_point.gx_point_x = 20; my_point.gx_point_y = 20;

/* Detect if point “my_point” is within “my_rectangle””. */
result = gx_utility_rectangle_point_detect(&my_rectangle,
    &my_point);

/* If result is GX_TRUE, “my_point” resides in the rectangle. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_resize"></a>gx_utility_rectangle_resize

Увеличение прямоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_rectangle_resize(
    GX_RECTANGLE *rectangle,
    GX_VALUE adjust);
```

### <a name="description"></a>Описание

Эта служба увеличивает размер прямоугольника, как указано.

### <a name="parameters"></a>Параметры

- **rectangle**: указатель на прямоугольник
- **adjust**: размер для корректировки прямоугольника

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — размер прямоугольника успешно изменен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода прямоугольника.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
UINT status;

/* Adjust “my_rectangle” by increasing 20 pixels on four sides */
status = gx_utility_rectangle_resize(&my_rectangle, 20);

/* If status is GX_SUCCESS, “my_rectangle” is 20 pixels larger. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect
- gx_utility_rectangle_shift

## <a name="gx_utility_rectangle_shift"></a>gx_utility_rectangle_shift

Смещение прямоугольника

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_rectangle_shift(
    GX_RECTANGLE *rectangle,
    GX_VALUE x_shift,
    GX_VALUE y_shift);
```

### <a name="description"></a>Описание

Эта служба смещает прямоугольник на указанные значения.

### <a name="parameters"></a>Параметры

- **rectangle**: прямоугольник для смещения
- **x_shift**: количество пикселей для смещения по оси X
- **y_shift**: количество пикселей для смещения по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — прямоугольник успешно смещен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода прямоугольника.

### <a name="allowed-from"></a>Допустимые источники

Все

### <a name="example"></a>Пример

```C
UINT status;

/* Shift “my_rectangle”. */

status = gx_utility_rectangle_shift(&my_rectangle, 10, 20);

/* If status is GX_SUCCESS, “my_rectangle” has been shifted. */
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect

## <a name="gx_utility_string_to_alphamap"></a>gx_utility_string_to_alphamap

Преобразование строки в карту отображения пикселей типа альфа-карты 8bpp (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_string_to_alphamap(
    const GX_CHAR *text, const
    GX_FONT *font, 
    GX_PIXELMAP *return_map);
```

### <a name="description"></a>Описание

Эта служба устарела. Вместо нее используется gx_utility_string_to_alphamap_ext().

Эта служба преобразует текстовую строку в альфа-карту, которая представляет собой особую форму карты отображения пикселей 8bpp, содержащую только альфа-значения. Эта служба обычно используется вместе с gx_utility_pixelmap_rotate и gx_canvas_pixelmap_draw для рисования повернутого текста на холсте.

Эта служба вычисляет размер памяти, необходимый для результирующей альфа-карты, и вызывает функцию gx_system_memory_allocator(), определенную приложением для динамического выделения памяти. В какой-то момент приложение должно вызвать gx_system_memory_allocator_set(), как правило во время запуска программы, прежде чем использовать эту службу.

Если текстовую строку следует повернуть и вывести на холст только один раз, то в качестве альтернативы предоставляется служба gx_canvas_rotated_text_draw(). gx_canvas_rotated_text_draw() вызовет gx_utility_string_to_alphamap(), gx_utility_pixelmap_rotate() и gx_canvas_pixelmap_draw() для отрисовки повернутого текста за одну операцию. Однако, если один и тот же текст будет отображаться несколько раз повернутым на разные углы, более эффективно создать альфа-карту с помощью API gx_utility_string_to_alphmap, а затем повернуть полученную альфа-карту несколько раз по мере необходимости.

### <a name="parameters"></a>Параметры

- **text**: текстовая строка для преобразования в альфа-карту
- **font**: шрифт для преобразования текста
- **return_map**: указатель на GX_PIXELMAP, возвращаемый вызывающему объекту

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текстовая строка успешно преобразована в альфа-карту.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определены распределитель памяти или функция освобождения памяти.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_PIXELMAP alphamap;

GX_PIXELMAP rotated_text;

INT xpos;

INT ypos;

gx_widget_font_get(widget, GX_FONT_ID_SCREEN_LABEL, &font);

/* render string to alphamap once */

gx_utility_string_to_alphamap("Hello World", font, &alphamap);

/* rotate and render the alphmap at multiple angles */

gx_utility_pixelmap_rotate(&alphamap, 45, &rotated_text,
    &xpos, &ypos);

gx_canvas_pixelmap_draw(10, 10, &rotated_text);

gx_utility_pixelmap_rotate(&alphamap, 135, &rotated_text,
    &xpos, &ypos);

gx_canvas_pixelmap_draw(100, 100, &rotated_text);

gx_utility_pixelmap_rotate(&alphamap, 300, &rotated_text,
    &xpos, &ypos);

gx_canvas_pixelmap_draw(200, 200, &rotated_text);
```

### <a name="see-also"></a>См. также:

- gx_utility_string_to_alphamap_ext

## <a name="gx_utility_string_to_alphamap_ext"></a>gx_utility_string_to_alphamap_ext

Преобразование строки в карту отображения пикселей типа альфа-карты 8bpp

### <a name="prototype"></a>Прототип

```C
UINT gx_utility_string_to_alphamap_ext(
    GX_CONST GX_STRING *string,
    GX_CONST GX_FONT *font, 
    GX_PIXELMAP *return_map);
```

### <a name="description"></a>Описание

Эта служба преобразует текстовую строку в альфа-карту, которая представляет собой особую форму карты отображения пикселей 8bpp, содержащую только альфа-значения. Эта служба обычно используется вместе с gx_utility_pixelmap_rotate и gx_canvas_pixelmap_draw для рисования повернутого текста на холсте.

Эта служба вычисляет размер памяти, необходимый для результирующей альфа-карты, и вызывает функцию gx_system_memory_allocator(), определенную приложением для динамического выделения памяти. В какой-то момент приложение должно вызвать gx_system_memory_allocator_set(), как правило во время запуска программы, прежде чем использовать эту службу.

Если текстовую строку следует повернуть и вывести на холст только один раз, то в качестве альтернативы предоставляется служба gx_canvas_rotated_text_draw(). gx_canvas_rotated_text_draw() вызовет gx_utility_string_to_alphamap(), gx_utility_pixelmap_rotate() и gx_canvas_pixelmap_draw() для отрисовки повернутого текста за одну операцию. Однако, если один и тот же текст будет отображаться несколько раз повернутым на разные углы, более эффективно создать альфа-карту с помощью API gx_utility_string_to_alphmap, а затем повернуть полученную альфа-карту несколько раз по мере необходимости.

### <a name="parameters"></a>Параметры

- **string**: текстовая строка для преобразования в альфа-карту
- **font**: шрифт для преобразования текста
- **return_map**: указатель на GX_PIXELMAP, возвращаемый вызывающему объекту

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — текстовая строка успешно преобразована в альфа-карту.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель ввода.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определены распределитель памяти или функция освобождения памяти.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING string;

GX_PIXELMAP alphamap;

GX_PIXELMAP rotated_text;

INT xpos;

INT ypos;

gx_widget_font_get(widget, GX_FONT_ID_SCREEN_LABEL, &font);

string.gx_string_ptr = “Hello World”;

string.gx_string_length = strlen(string.gx_string_ptr);

/* render string to alphamap once */
gx_utility_string_to_alphamap_ext(&string, font, &alphamap);

/* rotate and render the alphmap at multiple angles */
gx_utility_pixelmap_rotate(&alphamap, 45, &rotated_text,
    &xpos, &ypos);

gx_canvas_pixelmap_draw(10, 10, &rotated_text);

gx_utility_pixelmap_rotate(&alphamap, 135, &rotated_text,
    &xpos, &ypos);

gx_canvas_pixelmap_draw(100, 100, &rotated_text);

gx_utility_pixelmap_rotate(&alphamap, 300, &rotated_text,
    &xpos, &ypos);

gx_canvas_pixelmap_draw(200, 200, &rotated_text);
```

### <a name="see-also"></a>См. также:

- gx_utility_ltoa
- gx_utility_math_cos
- gx_utility_math_sin
- gx_utility_math_sqrt
- gx_utility_pixelmap_rotate
- gx_utility_pixelmap_simple_rotate
- gx_utility_rectangle_center
- gx_utility_rectangle_center_find
- gx_utility_rectangle_combine
- gx_utility_rectangle_compare
- gx_utility_rectangle_define
- gx_utility_rectangle_grow
- gx_utility_rectangle_overlap_detect
- gx_utility_rectangle_point_detect

## <a name="gx_vertical_list_children_position"></a>gx_vertical_list_children_position
### <a name="position-children-for-the-vertical-list"></a>Размещение дочерних элементов для вертикального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_children_position(
    GX_VERTICAL_LIST *vertical_list)
```

### <a name="description"></a>Описание

Эта функция размещает дочерние элементы для вертикального списка.

### <a name="parameters"></a>Параметры

- **vertical_list**: указатель на блок управления вертикальным списком

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — дочерние элементы успешно размещены для вертикального списка.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Position children in the vertical list */
status = gx_vertical_list_children_position (&vertical_list);

/* If status is GX_SUCCESS the children in the vertical list are positioned.. */
```

### <a name="see-also"></a>См. также:

- gx_vertical_list_create
- gx_vertical_list_event_process
- gx_vertical_list_page_index_set
- gx_vertical_list_selected_index_get
- gx_vertical_list_selecgted_widget_get
- gx_vertical_list_selected_widget_get
- gx_vertical_list_selected_set
- gx_vertical_list_total_rows_set

## <a name="gx_vertical_list_create"></a>gx_vertical_list_create
### <a name="create-vertical-list"></a>Создание вертикального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_create(
    GX_VERTICAL_LIST *vertical_list,
    GX_CONST GX_CHAR *name, 
    GX_WIDGET *parent, 
    INT total_rows,
    VOID (*callback)(GX_VERTICAL_LIST *, GX_WIDGET *, INT),
    ULONG style, USHORT vertical_list_id,
    GX_CONST GX_RECTANGLE *size);
```
### <a name="description"></a>Описание

Эта служба создает вертикальный список.

GX_VERTICAL_LIST является производным от GX_WINDOW и поддерживает все службы API gx_window.

### <a name="parameters"></a>Параметры

- **vertical_list**: блок управления мини-приложением вертикального списка
- **name**: имя вертикального списка
- **parent**: указатель на родительское мини-приложение
- **total_rows**: общее количество строк в вертикальном списке
- **callback**: функция, которая будет вызываться вертикальным списком при прокрутке списка Вызывающий объект должен изначально создать достаточное количество дочерних элементов на основе GX_WIDGET, чтобы заполнить видимые строки списка. При прокрутке списка эта функция вызывается для повторного создания дочерних элементов списка, соответствующих указанному индексу списка.
- **style**: стиль мини-приложения полосы прокрутки Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **vertical_list_id**: определяемый приложением идентификатор вертикального списка
- **size**: размеры вертикального списка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — вертикальный список успешно создан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_VALUE** (0x22) — недопустимое количество строк.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create vertical list “my_list” with 20 rows. */
status = gx_vertical_list_create(&my_list, “my_list”, &my_parent,
                    20, callback, GX_STYLE_WRAP, MY_LIST_ID,
                    &size);

/* If status is GX_SUCCESS the vertical list “my_list” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_event_process
- gx_vertical_list_page_index_set
- gx_vertical_list_selected_index_get
- gx_vertical_list_selected_widget_get
- gx_vertical_list_selected_set
- gx_vertical_list_total_rows_set

## <a name="gx_vertical_list_event_process"></a>gx_vertical_list_event_process
### <a name="process-vertical-list-event"></a>Обработка события вертикального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_event_process(GX_VERTICAL_LIST *list,
                                                      GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для вертикального списка.

### <a name="parameters"></a>Параметры

- **list**: блок управления мини-приложением вертикального списка
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие вертикального списка успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Process “my_event” for vertical list “my_list”. */
status = gx_vertical_list_event_process(&my_list, &my_event);

/* If status is GX_SUCCESS the event for vertical list “my_list” has been processed. */
```

### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_create
- gx_vertical_list_page_index_set
- gx_vertical_list_selected_index_get
- gx_vertical_list_selected_widget_get
- gx_vertical_list_selected_set
- gx_vertical_list_selected_set

## <a name="gx_vertical_list_page_index_set"></a>gx_vertical_list_page_index_set
### <a name="set-starting-page-index"></a>Задание начального индекса страницы

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_page_index_set(
    GX_VERTICAL_LIST *list,
    INT index);
```
### <a name="description"></a>Описание

Эта служба задает начальный индекс для вертикального списка.

### <a name="parameters"></a>Параметры

- **list**: блок управления мини-приложением вертикального списка
- **index**: новый начальный индекс

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — начальный индекс страницы для вертикального списка успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение индекса.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the starting page index of vertical list “my_list” to 4. */
status = gx_vertical_list_page_index_set(&my_list, 4);

/* If status is GX_SUCCESS the starting page index of “my_list” has
been set to 4. */
```
### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_create
- gx_vertical_list_event_process
- gx_vertical_list_selected_index_get
- gx_vertical_list_selected_widget_get
- gx_vertical_list_selected_set
- gx_vertical_list_total_rows_set

## <a name="gx_vertical_list_selected_index_get"></a>gx_vertical_list_selected_index_get
### <a name="get-selected-index-from-vertical-list"></a>Получение выбранного индекса из вертикального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_selected_index_get(
    GX_VERTICAL_LIST *vertical_list,
    INT *return_index);
```
### <a name="description"></a>Описание

Эта служба возвращает выбранный индекс вертикального списка

### <a name="parameters"></a>Параметры

- **vertical_list**: блок управления мини-приложением вертикального списка
- **return_index**: назначение для возврата выбранного индекса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запись вертикального списка успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
INT current_selected_index;

/* Get the list entry at the current index of vertical list “my_list”. */
status = gx_vertical_list_selected_index_get(&my_list, &current_selected_index);

/* If status is GX_SUCCESS, “current_list_index” contains the index of the selected list item. */
```
### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_create
- gx_vertical_list_event_process
- gx_vertical_list_page_index_set
- gx_vertical_list_selected_widget_get
- gx_vertical_list_selected_set
- gx_vertical_list_total_rows_set

## <a name="gx_vertical_list_selected_set"></a>gx_vertical_list_selected_set
### <a name="assign-the-selected-entry-in-a-vertical-list"></a>Назначение выбранной записи в вертикальном списке

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_selected_set(
    GX_VERTICAL_LIST *vertical_list,
    INT index);
```

### <a name="description"></a>Описание

Эта служба назначает выбранную запись в вертикальном списке. При необходимости вертикальный список будет прокручиваться, чтобы сделать выбранную запись видимой.

### <a name="parameters"></a>Параметры

- **vertical_list**: блок управления мини-приложением вертикального списка
- **index**: позиция новой записи в списке на основе индекса

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запись вертикального списка успешно задана.
- **GX_FAILURE** (0x10) — не удалось найти входной индекс.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение вертикального списка или записи списка.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the list entry of “my_list” to the child in line 12. */
status = gx_vertical_list_selected_set(&my_list, 12);

/* If status is GX_SUCCESS, the list entry of “my_list” has been successfully set to 12. */
```
### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_create
- gx_vertical_list_event_process
- gx_vertical_list_page_index_get
- gx_vertical_list_selected_index_get
- gx_vertical_list_selected_widget_get
- gx_vertical_list_total_rows_set

## <a name="gx_vertical_list_selected_widget_get"></a>gx_vertical_list_selected_widget_get
### <a name="get-selected-widget-from-vertical-list"></a>Получение выбранного мини-приложения из вертикального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_selected_widget_get(
    GX_VERTICAL_LIST *vertical_list,
    GX_WIDGET **return_list_entry);
```
### <a name="description"></a>Описание

Эта служба возвращает выбранное мини-приложение из вертикального списка. Обратите внимание, что, если список содержит больше строк, чем дочерние мини-приложения, а выделенное дочернее мини-приложение было прокручено из представления, эта функция возвратит GX_NULL в качестве указателя GX_WIDGET, поскольку мини-приложение было использовано для отображения новой записи списка.

### <a name="parameters"></a>Параметры

- **vertical_list**: блок управления мини-приложением вертикального списка
- **return_list_entry**: назначение для возвращаемого мини-приложения записи списка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — запись вертикального списка успешно получена.
- **GX_FAILURE** (0x10) — выбранное мини-приложение было прокручено из представления.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_WIDGET *current_selected_widget;

/* Get the list entry at the current index of vertical list “my_list”. */
status = gx_vertical_list_selected_widget_get(&my_list, &current_selected_widget);

/* If status is GX_SUCCESS, “current_list_entry” contains a pointer to the currently selected widget. */
```

### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_create
- gx_vertical_list_event_process
- gx_vertical_list_page_index_set
- gx_vertical_list_selected_index_get
- gx_vertical_list_selected_set
- gx_vertical_list_total_rows_set

## <a name="gx_vertical_list_total_rows_set"></a>gx_vertical_list_total_rows_set
### <a name="set-total-number-of-vertical-list-rows"></a>Задание общего числа строк вертикального списка

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_list_total_rows_set(
    GX_VERTICAL_LIST *vertical_list,
    INT count);
```
### <a name="description"></a>Описание

Эта служба назначает или изменяет общее число строк в списке.

### <a name="parameters"></a>Параметры

- **vertical_list**: блок управления мини-приложением вертикального списка
- **count**: число строк в новом списке

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — количество строк вертикального списка успешно задано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимое значение количества строк.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set the list row count to 20 items. */
status = gx_vertical_list_total_rows_set(&my_list, 20);

/* If status is GX_SUCCESS, the total rows of “my_list” has been set to 20. */
```
### <a name="see-also"></a>См. также:

- gx_vertical_list_children_position
- gx_vertical_list_create
- gx_vertical_list_event_process
- gx_vertical_list_page_index_set
- gx_vertical_list_selected_index_get
- gx_vertical_list_selected_widget_get
- gx_vertical_list_selected_set

## <a name="gx_vertical_scrollbar_create"></a>gx_vertical_scrollbar_create
### <a name="create-vertical-scrollbar"></a>Создание вертикальной полосы прокрутки

### <a name="prototype"></a>Прототип

```C
UINT gx_vertical_scrollbar_create(
    GX_SCROLLBAR *scrollbar,
    GX_CONST GX_CHAR *name, 
    GX_WINDOW *parent,
    GX_SCROLLBAR_APPEARANCE *appearance,
    ULONG style);
```
### <a name="description"></a>Описание

Эта служба создает вертикальную полосу прокрутки.

### <a name="parameters"></a>Параметры

- **scrollbar**: блок управления мини-приложением полосы прокрутки
- **name**: имя полосы прокрутки
- **parent**: указатель на родительское мини-приложение
- **appearance**: внешний вид мини-приложения вертикальной полосы прокрутки
- **style**: стиль полосы прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — вертикальная полоса прокрутки успешно создана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Create vertical scrollbar “my_scrollbar”. */
status = gx_vertical_scrollbar_create(&my_scrollbar,
                          “my_vertical_scrollbar”,
                          &my_parent, &scrollbar_appearance,
                          GX_STYLE_ENABLED);

/* If status is GX_SUCCESS the vertical scrollbar “my_scrollbar” has been created. */
```
### <a name="see-also"></a>См. также:

- gx_horizontal_scrollbar_create
- gx_scrollbar_draw
- gx_scrollbar_event_process
- gx_scrollbar_limit_check
- gx_scrollbar_reset

## <a name="gx_widget_allocate"></a>gx_widget_allocate
### <a name="allocate-a-widget-control-block"></a>Выделение блока управления мини-приложением

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_allocate(
    GX_WIDGET **control_block,
    ULONG memsize);
```
### <a name="description"></a>Описание

Эта служба динамически выделяет блок управления мини-приложением, вызывая функцию выделения памяти, определенную приложением. Эта служба в основном используется функциями, созданными GUIX Studio, для динамического выделения блока управления, когда в представлении свойств GUIX Studio выбрано свойство "Динамическое выделение".

### <a name="parameters"></a>Параметры

- **control_block**: указатель на возвращаемый указатель блока управления
- **memsize**: размер блока управления (в байтах)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно выделено.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — распределитель памяти не определен, или не удалось выделить память.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_MEMORY_SIZE** (0x29) — недопустимый размер памяти.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_TEXT_BUTTON *button;

/* Attach “my_widget” to “my_parent”. */
status = gx_widget_allocate(&button, sizeof(GX_TEXT_BUTTON));

/* If status is GX_SUCCESS the button widget control block is allocated. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_attach"></a>gx_widget_attach
### <a name="attach-widget-to-its-parent"></a>Присоединение мини-приложения к его родительскому элементу

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_attach(
    GX_WIDGET *parent, 
    GX_WIDGET *widget);
```
### <a name="description"></a>Описание

Эта служба присоединяет мини-приложение к указанному родительскому элементу. Если мини-приложение уже присоединено к другому родительскому элементу, оно сначала отсоединяется от него. Если мини-приложение уже присоединено к тому же родительскому элементу, функция не выполняет никаких действий.

Мини-приложение становится самым первым дочерним элементом родительского элемента в Z-порядке. Если одноуровневые элементы перекрываются, это мини-приложение рисуется поверх одноуровневых элементов. Чтобы поместить новое мини-приложение в конец Z-порядка, используйте gx_widget_back_attach или gx_widget_back_move.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **widget**: указатель на дочернее мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно присоединено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимый родительский элемент или мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Attach “my_widget” to “my_parent”. */
status = gx_widget_attach(&my_parent, &my_widget);

/* If status is GX_SUCCESS the widget “my_widget” is attached to “my_parent”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_background_draw"></a>gx_widget_background_draw
### <a name="draw-a-widget-background"></a>Вывод фона мини-приложения

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_background_draw(GX_WIDGET *widget);
```
### <a name="description"></a>Описание

Эта служба выполняет заливку фона мини-приложения сплошным цветом. Эта служба вызывается автоматически функцией gx_widget_draw, но может также вызываться приложением как часть настраиваемой функции рисования мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение для вывода

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom widget draw function. */

VOID my_widget_draw(GX_WIDGET * widget)
{
    /* Call default widget background draw. */
    gx_widget_background_draw(widget);

    /* Add your own drawing here. */

    /* Draw child widgets. */
    gx_widget_children_draw(widget);
}
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_back_attach"></a>gx_widget_back_attach
### <a name="attach-widget-to-its-parent"></a>Присоединение мини-приложения к его родительскому элементу

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_back_attach(
    GX_WIDGET *parent, 
    GX_WIDGET *widget);
```
### <a name="description"></a>Описание

Эта служба присоединяет мини-приложение к указанному родительскому элементу. Если мини-приложение уже присоединено к другому родительскому элементу, оно сначала отсоединяется от него. Если мини-приложение уже присоединено к тому же родительскому элементу, функция не выполняет никаких действий.

Мини-приложение становится самым последним дочерним элементом родительского элемента в Z-порядке. Если одноуровневые элементы перекрываются, это мини-приложение рисуется позади этих одноуровневых элементов. Чтобы поместить новое мини-приложение в начало Z-порядка, используйте gx_widget_attach или gx_widget_front_move.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **widget**: указатель на дочернее мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно присоединено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимый родительский элемент или мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Attach “my_widget” to “my_parent”. */
status = gx_widget_back_attach(&my_parent, &my_widget);

/* If status is GX_SUCCESS the widget “my_widget” is attached to “my_parent”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_back_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_back_move"></a>gx_widget_back_move
### <a name="move-widget-to-back"></a>Перемещение мини-приложения на задний план

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_back_move(
    GX_WIDGET *widget,
    GX_BOOL *return_widget_moved);
```
### <a name="description"></a>Описание

Эта служба перемещает мини-приложение на задний план в Z-порядке дочерних мини-приложений родительского элемента.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **return_widget_moved**: указатель на назначение для флага, указывающего, что мини-приложение было перемещено

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно перемещено на задний план.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_NO_CHANGE** (0x08) — нет изменений.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move “my_widget” to the back. */
status = gx_widget_back_move(&my_widget, &moved_flag);

/* If status is GX_SUCCESS and “moved_flag” is GX_TRUE, the widget “my_widget” was moved to the back. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_block_move"></a>gx_widget_block_move
### <a name="move-a-rectangular-block-of-pixels"></a>Перемещение прямоугольного блока пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_block_move(
    GX_WIDGET *widget,
    GX_RECTANGLE *block,
    INT xshift, INT yshift);
```

### <a name="description"></a>Описание

Эта служба перемещает прямоугольный блок пикселей. Эта служба чаще всего используется для реализации быстрой прокрутки.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение, запрашивающее перемещение блока
- **block**: прямоугольный блок для перемещения
- **xshift**: величина смещения по оси X (в пикселях)
- **yshift**: величина смещения по оси Y (в пикселях)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно перемещено на задний план.
- **GX_INVALID_CANVAS** (0x20) — холст мини-приложения не найден.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move a block of pixels 20 pixels to the right. */
status = gx_widget_block_move(&my_widget, &size, 20, 0);

/* If status is GX_SUCCESS the block of pixels was moved. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_border_draw"></a>gx_widget_border_draw
### <a name="draw-widget-border"></a>Вывод границы мини-приложения

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_border_draw(
    GX_WIDGET *widget,
    GX_COLOR border_color,
    GX_COLOR upper_fill, 
    GX_COLOR lower_fill, 
    GX_BOOL fill);
```

### <a name="description"></a>Описание

Эта служба рисует границу мини-приложения. Эта служба обычно вызывается как часть функции рисования мини-приложения. Эта служба интерпретирует флаги стиля границы мини-приложения, чтобы нарисовать его без границы, с тонкой границей, с высокой границей, с низкой границей или с толстой границей.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **border_color**: цвет границы В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **upper_fill**: цвет верхней заливки В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **lower_fill**: цвет нижней заливки В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.
- **fill**: этот логический флаг указывает, должна ли область мини-приложения заполняться указанными цветами заливки Если это значение равно GX_FALSE, рисуется только граница мини-приложения.

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom widget draw function. */

VOID my_widget_draw(GX_WIDGET * widget)
{
      /* Call widget border draw. */
      gx_widget_border_draw(widget, GX_COLOR_BLACK,
                            GX_COLOR_GREEN, GX_COLOR_BLUE,
                            GX_TRUE);

      /* Add your own drawing here. */

      /* Draw child widgets. */
      Gx_widget_children_draw(widget);
}
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_border_style_set"></a>gx_widget_border_style_set
### <a name="set-widget-border-style"></a>Задание стиля границы мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_border_style_set(
    GX_WIDGET *widget, 
    ULONG style);
```

### <a name="description"></a>Описание

Эта служба задает стиль границы мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **style**: стиль границы Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль границы мини-приложения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set border style of “my_widget”. */
status = gx_widget_border_style_set(&my_widget,
                                    GX_STYLE_BORDER_RAISED);

/* If status is GX_SUCCESS the widget “my_widget” border style has been set. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_border_width_get"></a>gx_widget_border_width_get
### <a name="get-widget-border-width"></a>Получение ширины границы мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_border_width_get(
    GX_WIDGET *widget,
    GX_VALUE *return_width);
```

### <a name="description"></a>Описание

Эта служба возвращает ширину границы мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_width**: указатель на место назначения для ширины границы мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина границы успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE my_width;

/* Get border width of “my_widget”. */
status = gx_widget_border_width_get(&my_widget, &my_width);

/* If status is GX_SUCCESS, “my_width” contains the border width of the widget “my_widget”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_canvas_get"></a>gx_widget_canvas_get
### <a name="get-widget-canvas"></a>Получение холста мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_canvas_get(
    GX_WIDGET *widget,
    GX_CANVAS **return_canvas)
```
### <a name="description"></a>Описание

Эта служба возвращает указатель на холст, на котором отображается это мини-приложение.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_canvas**: указатель на место назначения для холста мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — холст мини-приложения успешно получен.
- **GX_FAILURE** (0x10) — холст мини-приложения не найден.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Get canvas associated with “my_widget”. */
status = gx_widget_canvas_get(&my_widget, &my_canvas);

/* If status is GX_SUCCESS, “my_canvas” contains the canvas of the widget “my_widget”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_child_detect"></a>gx_widget_child_detect
### <a name="detect-widget-child"></a>Обнаружение дочернего элемента мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_child_detect(
    GX_WIDGET *parent, 
    GX_WIDGET *child,
    GX_BOOL *return_detect);
```

### <a name="description"></a>Описание

Эта служба определяет, является ли мини-приложение дочерним по отношению к родительскому мини-приложению. Эта служба вложена в поиск потомков дочерних элементов и возвращает значение TRUE, если родительское мини-приложение находится на любом уровне предка дочернего мини-приложения.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **child**: указатель на дочернее мини-приложение
- **return_detect**: указатель на назначение для обнаружения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — обнаружение дочерних элементов мини-приложения успешно выполнено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское или дочернее мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BOOL detected;

/* Determine if “my_child” is a child of “my_widget”. */
status = gx_widget_child_detect(&my_widget, &my_child, &detected);

/* If status is GX_SUCCESS and “detected” is GX_TRUE, “my_child” is a child of widget “my_widget”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_children_draw"></a>gx_widget_children_draw
### <a name="draw-widget-children"></a>Вывод дочернего элемента мини-приложения

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_children_draw(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба рисует все дочерние элементы родительского мини-приложения. Эта служба обычно вызывается всеми стандартными функциями рисования мини-приложений для рисования дочерних мини-приложений и должна вызываться любыми настраиваемыми функциями рисования, чтобы допускать присоединение дочерних мини-приложений к настраиваемому типу родительского мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom widget draw function. */

VOID my_widget_draw(GX_WIDGET * widget)
{
        /* Call default widget background draw. */
        gx_widget_background_draw(widget);

        /* Add your own drawing here. */

        /* Draw child widgets. */
        gx_widget_children_draw(widget);
}
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_client_get"></a>gx_widget_client_get
### <a name="get-widget-client-area"></a>Получение клиентской области мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_client_get(
    GX_WIDGET *widget,
    GX_VALUE border_width,
    GX_RECTANGLE *return_client_area);
```
### <a name="description"></a>Описание

Эта служба вычисляет клиентскую область мини-приложения, вычитая толщину границы мини-приложения из общего размера мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **border_width**: ширина границы мини-приложения
- **return_client_area**: назначение для возвращаемой клиентской области

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — клиентская область мини-приложения.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимая граница мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RECTANGLE client_area
/* Get client area of widget “my_widget”. */
status = gx_widget_client_get(&my_widget, my_widget_width,
                              &client_area);

/* If status is GX_SUCCESS, the “client_area” is the client area of “my_widget”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_color_get"></a>gx_widget_color_get
### <a name="get-color"></a>Получение цвета

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_color_get(
    GX_WIDGET *widget,
    GX_RESOURCE_ID resource_id,
    GX_COLOR *return_color);
```

### <a name="description"></a>Описание

Эта служба получает цвет, связанный с переданным идентификатором ресурса. Эту службу следует вызывать только отображаемым мини-приложениям.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением
- **resource_id**: идентификатор ресурса цвета В **приложении B** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **return_color**: указатель на назначение цвета В **приложении A** указаны предварительно определенные цвета. Обратите внимание, что приложение также может добавлять настраиваемые цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет успешно задан.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст мини-приложения, или мини-приложение не отображается.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_COLOR actual_color;

/* Get color for resource ID MY_FIRST_COLOR_ID. */
status = gx_widget_color_get(my_widget, MY_FIRST_COLOR_RESOURCE_ID,
                            &actual_color);

/* If status is GX_SUCCESS the actual color is contained in “actual_color”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_font_get
- gx_widget_pixelmap_get

## <a name="gx_widget_create"></a>gx_widget_create
### <a name="create-widget"></a>Создание мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_create(
    GX_WIDGET *widget, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent,
    ULONG style, 
    USHORT widget_id,
    GX_CONST GX_RECTANGLE *size);
```
### <a name="description"></a>Описание

Эта служба создает мини-приложение.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **name**: логическое имя мини-приложения
- **parent**: указатель на родительское мини-приложение
- **style**: стиль Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **window_id**: определяемый приложением идентификатор мини-приложения
- **size**: размер мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое родительское мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_WIDGET my_widget;
GX_RECTANGLE size;

gx_utility_rectangle_define(&size, 0, 0, 100, 100);

/* Get client area of widget “my_widget”. */
status = gx_widget_create(&my_widget, "my widget",
                          &my_parent_window, GX_STYLE_BORDER_RAISED, MY_WIDGET_ID, &size);

/* If status is GX_SUCCESS, the widget “my_widget” has been created. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_created_test"></a>gx_widget_created_test
### <a name="test-if-widget-created"></a>Проверка создания мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_created_test(
    GX_WIDGET *widget,
    GX_BOOL *return_test);
```
### <a name="description"></a>Описание

Эта служба проверяет, было ли ранее создано мини-приложение. Если ошибки не обнаружены, эта функция возвращает GX_SUCCESS, независимо от того, создано ли мини-приложение еще или нет. Результат проверки находится в указателе return_test.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_test**: назначение для результата проверки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — проверка успешно выполнена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BOOL was_created;

/* Test to see if widget “my_widget” is created. */
status = gx_widget_created_test(&my_widget, &was_created);

/* If status is GX_SUCCESS, no error occurred. If “was_created” is
GX_TRUE, the widget “my_widget” has been created. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_delete"></a>gx_widget_delete
### <a name="delete-widget"></a>Удаление мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_delete(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба удаляет мини-приложение. Если блок управления мини-приложением выделяется динамически, вызывается служба gx_system_memory_free для высвобождения динамически выделяемого хранилища.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно удалено. GX_CALLER_ERROR (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определена функция освобождения памяти.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete widget “my_widget”. */
status = gx_widget_delete(&my_widget);

/* If status is GX_SUCCESS the widget “my_widget” has been deleted. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_detach"></a>gx_widget_detach
### <a name="detach-widget-from-parent"></a>Отсоединение мини-приложения от родительского элемента

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_detach(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба отсоединяет мини-приложение от своего родительского элемента.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно отсоединено. GX_CALLER_ERROR (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Detach widget “my_widget” from its parent. */
status = gx_widget_detach(&my_widget);

/* If status is GX_SUCCESS the widget “my_widget” has been detached. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_draw"></a>gx_widget_draw
### <a name="draw-widget"></a>Вывод мини-приложения

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_draw(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба рисует мини-приложение. Эта функция обычно вызывается внутри службы механизмом обновления холста GUIX, но предоставляется приложению для помощи в реализации настраиваемых функций рисования.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom widget draw function. */

VOID my_custom_widget_draw(GX_WIDGET *widget)
{
      /* Call default widget draw. */
      gx_widget_draw(widget);

      /* Add your own drawing here. */
}
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_draw_set"></a>gx_widget_draw_set
### <a name="assign-the-widget-drawing-function"></a>Назначение функции рисования мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_draw_set(
    GX_WIDGET *widget,
    VOID (*drawing_function) (GX_WIDGET *));
```

### <a name="description"></a>Описание

Эта служба переопределяет функцию рисования мини-приложения, используемую по умолчанию.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **drawing_function**: указатель на функцию рисования

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — функция рисования мини-приложения успешно переопределена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Define a custom drawing function. */
VOID my_drawing_function(GX_WIDGET *widget)
{
      /* Add your own drawing here. */
}

/* Set the drawing function of widget “my_widget” to “my_drawing_function”. */
status = gx_widget_draw_set(&my_widget, my_drawing_function);

/* If status is GX_SUCCESS the widget “my_widget” has the drawning function “my_drawing_function”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_event_generate"></a>gx_widget_event_generate
### <a name="generate-widget-event"></a>Создание события мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_event_generate(
    GX_WIDGET *widget, 
    USHORT event_type, LONG value);
```

### <a name="description"></a>Описание

Эта служба создает тип GX_SIGNAL события, который является определенным типом или классом GX_EVENT. gx_widget_event_generate() кодирует 16-разрядный идентификатор мини-приложения вместе с переданным значением event_type в одно 32-разрядное значение GX_EVENT.gx_event_type. Параметр value кодируется в созданный gx_event. Поле gx_event_payload.gx_event_longdata.

Созданное поле event.gx_event_target всегда загружается с вызывающим родительским элементом мини-приложения, то есть созданное событие всегда сначала отправляется родительскому элементу создающего мини-приложения.

Обратите внимание, что gx_widget_event_generate следует использовать только для отправки типов событий из диапазона GX_SIGNAL. Для всех других типов событий, включая определяемые пользователем типы событий, используйте API gx_system_event_send(), который предоставляет полный контроль над каждым полем события, отправленного в очередь событий GUIX.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **event_type**: тип события Предварительно заданные события GUIX см. в **приложении E**. Приложение может добавлять дополнительные события.
- **value**: дополнительные сведения о событии

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие мини-приложения успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Generate a redraw event for widget “my_widget”. */
status = gx_widget_event_generate(&my_widget, GX_EVENT_REDRAW, 0);

/* If status is GX_SUCCESS the redraw event for widget “my_widget” has been generated. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get
- gx_system_event_send

## <a name="gx_widget_event_process"></a>gx_widget_event_process
### <a name="process-widget-event"></a>Обработка события мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_event_process(
    GX_WIDGET *widget, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Это функция обработки событий по умолчанию для всех мини-приложений. При написании настраиваемой функции обработки событий действие по умолчанию для любого типа событий всегда должно быть передано в тип мини-приложения, на котором основано мини-приложение. Мини-приложения, основанные на самом основном типе GX_WIDGET, используют gx_widget_event_process в качестве функции обработки событий по умолчанию.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие мини-приложения успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Process event “my_event” for widget “my_widget”. */
status = gx_widget_event_process(&my_widget, &my_event);

/* If status is GX_SUCCESS the event “my_event” for widget “my_widget” has been processed. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_event_process_set"></a>gx_widget_event_process_set
### <a name="set-event-processing-function-of-widget"></a>Задание функции обработки событий мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_event_process_set(
    GX_WIDGET *widget,
    UINT (*event_processing) (GX_WIDGET *, GX_EVENT *));
```

### <a name="description"></a>Описание

Эта служба переопределяет функцию обработки событий мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **event_processing**: указатель на новую функцию обработки событий

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — обработка событий мини-приложения успешно переопределена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
UINT my_event_process(GX_TREE_VIEW *tree_view,
                      GX_EVENT *event)
{
      UINT status = GX_SUCCESS;

      switch(event->gx_event_type)
      {
      case xyz:
            /* Insert custom event handling here */
            break;

      default:
            /* Pass all other events to the default tree view
               event processing */
            status = gx_tree_view_event_process(tree_view, event);
            break;
      }
      return status;
}

/* Use “my_event_process” to process events for widget “my_tree_view”. */
status = gx_widget_event_process_set((GX_WIDGET *)&my_tree_view,
                    (VOID (*)(GX_WIDGET *))my_event_process);

/* If status is GX_SUCCESS all event processing for widget “my_tree_view” is handled by “my_event_process”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_event_to_parent"></a>gx_widget_event_to_parent
### <a name="send-event-to-widgets-parent"></a>Отправка события родительскому элементу мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_event_to_parent(
    GX_WIDGET *widget,
    GX_EVENT *event);
```
### <a name="description"></a>Описание

Эта служба отправляет событие родительскому элементу мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **event**: указатель на событие

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие успешно отправлено родительскому элементу мини-приложения.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Send my_event to the widget’s parent */
status = gx_widget_event_to_parent(&my_widget, my_event);

/* If status is GX_SUCCESS the event has been delivered to the parent of my_widget. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_fill_color_set"></a>gx_widget_fill_color_set
### <a name="set-widget-background-color"></a>Задание цвета фона мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_fill_color_set(
    GX_WIDGET *widget,
    GX_RESOURCE_ID normal_color_id,
    GX_RESOURCE_ID selected_color_id,
    GX_RESOURCE_ID disabled_color_id);
```
### <a name="description"></a>Описание

Эта служба задает цвета фона мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **normal_color_id**: идентификатор ресурса цвета заливки в нормальном состоянии В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **selected_color_id**: идентификатор ресурса цвета заливки, когда фокус перемещается на мини-приложение В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.
- **disabled_color_id**: идентификатор ресурса цвета заливки, если не задан стиль GX_STYLE_ENABLED В **приложении A** указаны предварительно определенные идентификаторы ресурсов цвета. Обратите внимание, что приложение также может добавлять настраиваемые идентификаторы ресурсов цвета.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — цвет заливки мини-приложения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set background of “my_widget”. */
status = gx_widget_fill_color_set(&my_widget,
                    GX_COLOR_ID_NORMAL_FILL,
                    GX_COLOR_ID_SELECTED_FILL,
                    GX_COLOR_ID_DISABLED_FILL);

/* If status is GX_SUCCESS the widget “my_widget” background has been set. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_create
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_find"></a>gx_widget_find
### <a name="find-child-widget-of-parent-widget"></a>Поиск дочернего мини-приложения родительского мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_find(
    GX_WIDGET *parent, 
    USHORT widget_id,
    INT search_depth, 
    GX_WIDGET **return_widget);
```
### <a name="description"></a>Описание

Эта служба выполняет поиск мини-приложения с запрошенным значением идентификатора в дочерних элементах указанного родительского элемента.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение, с которого начинается поиск
- **widget_id**: идентификатор мини-приложения для поиска
- **search_depth**: определяет рекурсивный уровень вложенности, который функция будет использовать для поиска дочерних мини-приложений Если это значение <= 0, выполняется поиск только непосредственных дочерних элементов каждого родительского мини-приложения. Если это значение равно GX_SEARCH_DEPTH_INFINITE, поиск выполняется по всем дочерним элементам всех дочерних мини-приложений. Для любого другого значения > 0 это значение ограничивает уровень вложенности этой функции, который будет использоваться для поиска запрошенного идентификатора мини-приложения по дочерним мини-приложениям.
- **return_widget**: указатель на место назначения для найденного мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно найдено.
- **GX_NOT_FOUND** (0x09) — не удалось найти мини-приложение.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_WIDGET *widget_found;

/* Find widget “my_widget”. */
status = gx_widget_find(&my_widget, GX_SEARCH_DEPTH_INFINITE
                        MY_WIDGET_ID, &widget_found);

/* If status is GX_SUCCESS, the pointer “widget_found” contains the pointer to the widget found. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_first_child_get"></a>gx_widget_first_child_get
### <a name="return-pointer-to-first-child-widget"></a>Возврат указателя к первому дочернему мини-приложению

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_first_child_get(
    GX_WIDGET *parent,
    GX_WIDGET **widget_return);
```
### <a name="description"></a>Описание

GUIX хранит древовидный список родительских и дочерних мини-приложений. Эта служба возвращает указатель на первое дочернее мини-приложение родительского элемента.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **widget_return**: указатель на возвращаемый указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатель возвращен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Retrieve child widget pointer. */

GX_WIDGET *get_child_widget(GX_WIDGET *parent)
{
      GX_WIDGET *child;
      UINT status;

      status = gx_widget_first_child_get(parent, &child);
      if (status == GX_SUCCESS)
      {
            return child;
      }
      return GX_NULL;
}
```
### <a name="see-also"></a>См. также:

- gx_widget_last_child_get
- gx_widget_next_sibling_get
- gx_widget_parent_get
- gx_widget_previous_sibling_get
- gx_widget_top_visible_child_find

## <a name="gx_widget_focus_next"></a>gx_widget_focus_next
### <a name="move-focus-to-next-widget-in-navigation-order"></a>Перемещение фокуса на следующее мини-приложение в порядке навигации

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_focus_next(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба перемещает фокус на следующий одноуровневый элемент в связанном списке мини-приложений, которые принимают фокус.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — фокус перемещен.
- **GX_FAILURE** (0x00) — фокус не перемещен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move focus to next widget in navigation order. */ status =
gx_widget_focus_next(&my_widget);

/* If status is GX_SUCCESS the focus has been moved to the next
widget in the navigation order */
```
### <a name="see-also"></a>См. также:

- gx_widget_focus_previous

## <a name="gx_widget_focus_previous"></a>gx_widget_focus_previous
### <a name="move-focus-to-previous-widget-in-navigation-order"></a>Перемещение фокуса на предыдущее мини-приложение в порядке навигации

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_focus_previous(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба перемещает фокус на предыдущее мини-приложение в порядке навигации.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение, на котором в настоящее время находится фокус

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — фокус перемещен.
- **GX_FAILURE** (0x00) — фокус не перемещен.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Move focus to previuos widget in navigation order. */ status =
gx_widget_focus_previous(&my_widget);

/* If status is GX_SUCCESS the input focus has been moved to the
previous widget. */
```
### <a name="see-also"></a>См. также:

- gx_widget_focus_next

## <a name="gx_widget_font_get"></a>gx_widget_font_get
### <a name="get-font-for-specified-resource-id"></a>Получение шрифта для указанного идентификатора ресурса

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_font_get(
    GX_WIDGETG *widget,
    GX_RESOURCE_ID resource_id,
    GX_FONT **return_font);
```
### <a name="description"></a>Описание

Эта служба получает шрифт, связанный с указанным идентификатором ресурса, из таблицы шрифтов дисплея, на котором отображается это мини-приложение. Эта функция должна вызываться только отображаемым мини-приложением.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением
- **resource_id**: идентификатор ресурса шрифта
- **return_font**: указатель на назначение для указателя шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — шрифт успешно получен.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст мини-приложения, или мини-приложение не отображается.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_FONT *my_font;
/* Get font for MY_FONT_ID. */
status = gx_widget_font_get(widget, MY_FONT_RESOURCE_ID, &my_font);
/* If status is GX_SUCCESS the font pointer has been retrieved in “my_font”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_color_get
- gx_widget_pixelmap_get

## <a name="gx_widget_free"></a>gx_widget_free
### <a name="release-memory-associated-with-a-widget"></a>Освобождение памяти, связанной с мини-приложением

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_free(GX_WIDGETG *widget);
```

### <a name="description"></a>Описание

Эта служба освобождает память, связанную с блоком управления мини-приложением.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением
- **resource_id**: идентификатор ресурса шрифта
- **return_font**: указатель на назначение для указателя шрифта

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — память мини-приложения успешно освобождена.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определена функция освобождения памяти.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_WIDGET widget;
UINT status;

status = gx_widget_allocate(&widget, sizeof(GX_WIDGET))

/* Free a runtime allocated widget. */
if (status == GX_SUCCESS)
{
      status = gx_widget_free(widget);
}

/* If status is GX_SUCCESS the memory that allocated to the widget has been released. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_front_move"></a>gx_widget_front_move
### <a name="move-widget-to-front"></a>Перемещение мини-приложения на передний план

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_front_move(
    GX_WIDGET *widget, 
    GX_BOOL *return_moved);
```

### <a name="description"></a>Описание

Эта служба перемещает мини-приложение на передний план в родительском списке Z-порядка дочерних мини-приложений.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение для перемещения
- **return_moved**: указатель на место назначения для индикации перемещения мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно перемещено на передний план.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_NO_CHANGE** (0x08) — мини-приложение уже находится на переднем плане.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BOOL widget_moved;

/* Move widget “my_widget” to the front. */
status = gx_widget_front_move(&my_widget, &widget_moved);

/* If status is GX_SUCCESS and “widget_moved” is GX_TRUE, the
widget “my_widget” was moved to the front . */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_height_get"></a>gx_widget_height_get
### <a name="get-widget-height"></a>Получение высоты мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_height_get(
    GX_WIDGET *widget,
    UINT *return_height);
```

### <a name="description"></a>Описание

Эта служба получает высоту мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_height**: указатель на место назначения для высоты мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — высота мини-приложения успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE widget_height;

/* Get height for widget “my_widget”. */
status = gx_widget_height_get(&my_widget, &widget_height);

/* If status is GX_SUCCESS the height of the widget is contained in
“widget_height” . */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_hide"></a>gx_widget_hide
### <a name="hide-widget"></a>Скрытие мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_hide(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба скрывает мини-приложение. Это мини-приложение все еще присоединено к родительскому элементу, но его не разрешается рисовать на холсте.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно скрыто.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```c
/* Hide widget “my_widget”. */
status = gx_widget_hide(&my_widget);

/* If status is GX_SUCCESS the widget “my_widget” is hidden. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_style_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_last_child_get"></a>gx_widget_last_child_get
### <a name="return-pointer-to-last-child-widget"></a>Возврат указателя к последнему дочернему мини-приложению

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_last_child_get(
    GX_WIDGET *parent,
    GX_WIDGET **widget_return);
```
### <a name="description"></a>Описание

GUIX хранит древовидный список родительских и дочерних мини-приложений. Эта служба возвращает указатель на последнее дочернее мини-приложение родительского элемента.

### <a name="parameters"></a>Параметры

- **parent**: указатель на родительское мини-приложение
- **widget_return**: указатель на возвращаемый указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатель возвращен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Retrieve child widget pointer. */

GX_WIDGET *get_last_child_widget(GX_WIDGET *parent)
{

      GX_WIDGET *child;
      UINT status;

      status = gx_widget_last_child_get(parent, &child);
      if (status == GX_SUCCESS)
      {
              return child;
      }
      return GX_NULL;
}
```
### <a name="see-also"></a>См. также:

- gx_widget_first_child_get
- gx_widget_next_sibling_get
- gx_widget_parent_get
- gx_widget_previous_sibling_get
- gx_widget_top_visible_child_find

## <a name="gx_widget_next_sibling_get"></a>gx_widget_next_sibling_get
### <a name="return-pointer-to-next-sibling-of-current-widget"></a>Возврат указателя к следующему одноуровневому элементу текущего мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_next_sibling_get(
    GX_WIDGET *current,
    GX_WIDGET **widget_return);
```
### <a name="description"></a>Описание

GUIX хранит древовидный список родительских и дочерних мини-приложений. Эта служба возвращает указатель на следующий элемент того же уровня, что и текущее мини-приложение.

### <a name="parameters"></a>Параметры

- **current**: указатель на текущее мини-приложение
- **widget_return**: указатель на возвращаемый указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатель возвращен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Retrieve next sibling widget pointer. */

GX_WIDGET *get_next(GX_WIDGET *current)
{
      GX_WIDGET *sibling;
      UINT status;

      status = gx_widget_next_sibling_get(current, &sibling);
      if (status == GX_SUCCESS)
      {
            return sibling;
      }
      return GX_NULL;
}
```
### <a name="see-also"></a>См. также:

- gx_widget_first_child_get
- gx_widget_last_child_get
- gx_widget_parent_get
- gx_widget_previous_sibling_get
- gx_widget_top_visible_child_find

## <a name="gx_widget_parent_get"></a>gx_widget_parent_get
### <a name="return-pointer-to-parent-of-current-widget"></a>Возврат указателя к родительскому элементу текущего мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_parent_get(
    GX_WIDGET *current,
    GX_WIDGET **widget_return);
```
### <a name="description"></a>Описание

GUIX хранит древовидный список родительских и дочерних мини-приложений. Эта служба возвращает указатель на родительский элемент текущего мини-приложения.

### <a name="parameters"></a>Параметры

- **current**: указатель на текущее мини-приложение
- **widget_return**: указатель на возвращаемый указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатель возвращен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Retrieve parent widget */

GX_WIDGET *get_parent(GX_WIDGET *current)
{
      GX_WIDGET *parent;
      UINT status;

      status = gx_widget_parent_get(current, &parent);
      if (status == GX_SUCCESS)
      {
            return parent;
      }
      return GX_NULL;
}
```
### <a name="see-also"></a>См. также:

- gx_widget_first_child_get
- gx_widget_last_child_get
- gx_widget_next_sibling_get
- gx_widget_previous_sibling_get
- gx_widget_top_visible_child_find

## <a name="gx_widget_pixelmap_get"></a>gx_widget_pixelmap_get
### <a name="get-pixelmap"></a>Получение карты отображения пикселей

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_pixelmap_get(
    GX_WIDGET *widget,
    GX_RESOURCE_ID resource_id,
    GX_PIXELMAP **return_pixelmap);
```

### <a name="description"></a>Описание

Эта служба получает карту отображения пикселей, связанную с переданным идентификатором ресурса. Эту службу следует вызывать только для отображаемых мини-приложений.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением
- **pixelmap_id**: идентификатор ресурса карты отображения пикселей
- **return_pixelmap**: указатель на назначение карты отображения пикселей

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — карта отображения пикселей успешно получена.
- **GX_INVALID_RESOURCE_ID** (0x33) — недопустимый идентификатор ресурса.
- **GX_INVALID_CANVAS** (0x20) — недопустимый холст мини-приложения, или мини-приложение не отображается.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```c
GX_PIXELMAP *my_pixelmap;

/* Get the pixelmap associated with MY_PIXELMAP_ID. */
status = gx_widget_pixelmap_get(widget, MY_PIXELMAP_RESOURCE_ID,
                                &my_pixelmap);

/* If status is GX_SUCCESS, “my_pixelmap” contains the pixemap pointer. */
```
### <a name="see-also"></a>См. также:

- gx_widget_color_get
- gx_widget_font_get

## <a name="gx_widget_previous_sibling_get"></a>gx_widget_previous_sibling_get
### <a name="return-pointer-to-previous-sibling-of-the-current-widget"></a>Возврат указателя к предыдущему одноуровневому элементу текущего мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_previous_sibling_get(
    GX_WIDGET *current,
    GX_WIDGET **widget_return);
```

### <a name="description"></a>Описание

GUIX хранит древовидный список родительских и дочерних мини-приложений. Эта служба возвращает указатель на предыдущий элемент того же уровня, что и текущее мини-приложение.

### <a name="parameters"></a>Параметры

- **current**: указатель на текущее мини-приложение
- **widget_return**: указатель на возвращаемый указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатель возвращен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Retrieve previous sibling widget */

GX_WIDGET *get_previous(GX_WIDGET *current)
{
      GX_WIDGET *sibling;
      UINT status;

      status = gx_widget_previous_sibling_get(current, &sibling);
      if (status == GX_SUCCESS)
      {
          return sibling;
      }
      return GX_NULL;
}
```
### <a name="see-also"></a>См. также:

- gx_widget_first_child_get
- gx_widget_last_child_get
- gx_widget_next_sibling_get
- gx_widget_parent_get
- gx_widget_top_visible_child_find

## <a name="gx_widget_resize"></a>gx_widget_resize
### <a name="resize-widget"></a>Изменение размера мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_resize(
    GX_WIDGET *widget, 
    GX_RECTANGLE *new_size);
```
### <a name="description"></a>Описание

Эта служба изменяет размер мини-приложения. Если мини-приложение отображается, оно автоматически становится недействительным и помещается в очередь для перерисовки.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **new_size**: новый размер мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — размер мини-приложения успешно изменен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RECTANGLE new_size;

gx_utility_rectangle_define(&new_size, 0, 0, 100, 100);

/* Resize widget “my_widget”. */
status = gx_widget_resize(&my_widget, &new_size);

/* If status is GX_SUCCESS the widget “my_widget” has been resized. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_shift"></a>gx_widget_shift
### <a name="shift-widget"></a>Смещение мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_shift(
    GX_WIDGET *widget, 
    GX_VALUE x_shift,
    GX_VALUE y_shift, 
    GX_BOOL mark_dirty);
```

### <a name="description"></a>Описание

Эта служба смещает мини-приложение и при необходимости помечает его как "грязное".

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **x_shift**: количество пикселей для смещения по оси X
- **y_shift**: количество пикселей для смещения по оси Y
- **mark_dirty**: GX_TRUE, чтобы указать мини-приложение как "грязное", в противном случае GX_FALSE

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложение успешно смещено. GX_CALLER_ERROR (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Shift widget “my_widget”. */
status = gx_widget_shift(&my_widget, 10, 20, GX_FALSE);

/* If status is GX_SUCCESS the widget “my_widget” has been shifted. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_show"></a>gx_widget_show
### <a name="show-widget"></a>Отображение мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_show(GX_WIDGET *widget);
```

### <a name="description"></a>Описание

Эта служба отображает мини-приложение. Мини-приложение становится видимым только в том случае, если оно присоединено к родительскому элементу, а также является видимым для родительского элемента.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — мини-приложения успешно отображено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Show widget “my_widget”. */
status = gx_widget_show(&my_widget);

/* If status is GX_SUCCESS the widget “my_widget” has been shown. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_status_add"></a>gx_widget_status_add
### <a name="add-widget-status"></a>Добавление состояния мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_status_add(
    GX_WIDGET *widget, 
    ULONG status)
```

### <a name="description"></a>Описание

Эта служба добавляет любое сочетание флагов состояния к указанному мини-приложению.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **status**: состояние для добавления

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — состояние мини-приложения успешно добавлено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Add status to widget “my_widget”. */
status = gx_widget_status_add(&my_widget, status_to_add);

/* If status is GX_SUCCESS the widget “my_widget” status was. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_status_get"></a>gx_widget_status_get
### <a name="get-widget-status"></a>Получение состояния мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_status_get(
    GX_WIDGET *widget,
    ULONG *return_status)
```

### <a name="description"></a>Описание

Эта служба получает флаги состояния от мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_status**: указатель на возвращаемое состояние

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — состояние мини-приложения успешно получено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
ULONT get_status;

/* Retrieve status flag from widget “my_widget”. */ status =
gx_widget_status_get(&my_widget, &get_status);

/* If status is GX_SUCCESS the status from widget “my_widget” is
saved to “get_status”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_status_remove"></a>gx_widget_status_remove
### <a name="remove-widget-status"></a>Удаление состояния мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_status_remove(
    GX_WIDGET *widget, 
    ULONG status)
```

### <a name="description"></a>Описание

Эта служба удаляет указанные флаги состояния из внутренней переменной состояния мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **status**: состояние для удаления

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — состояние мини-приложения успешно удалено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Remove status of widget “my_widget”. */
status = gx_widget_status_remove(&my_widget, status_to_remove);

/* If status is GX_SUCCESS, the status flags are removed from the
widget “my_widget”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_status_test"></a>gx_widget_status_test
### <a name="test-widget-status"></a>Тестирование состояния мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_status_test(
    GX_WIDGET *widget, 
    ULONG status,
    GX_BOOL *return_test);
```
### <a name="description"></a>Описание

Эта служба проверяет флаги состояния указанного мини-приложения и сохраняет результат в памяти, на которую указывает return_test.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **status**: состояние для проверки
- **return_status**: указатель на назначение для результата теста

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — состояние мини-приложения успешно проверено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_BOOL test_result;

/* Test status of widget “my_widget”. */
status = gx_widget_status_test(&my_widget, status_to_test,
                              &test_result);

/* If status is GX_SUCCESS the widget “my_widget” status was tested
and the result in “test_result”. */
```
### <a name="see-also"></a>См. также:

- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set

## <a name="gx_widget_string_get"></a>gx_widget_string_get
### <a name="retrieve-string-associated-with-a-visible-widget-and-string-id-deprecated"></a>Получение строки, связанной с отображаемым мини-приложением и ИД строки (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_string_get(
    GX_WIDGET *widget,
    GX_RESOURCE_ID string_id,
    GX_CONST GX_CHAR **string);
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_widget_string_get_ext().

Эта служба возвращает запись таблицы строк для заданного значения идентификатора строки. Эта служба аналогична gx_display_string_get, за исключением того, что активный дисплей определяется автоматически, а не передается вызывающей стороной. Эту службу можно использовать только для тех мини-приложений, которые видимы, т. е. известен дисплей, связанный с этим мини-приложением.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **string_id**: значение идентификатора строки из заголовка ресурсов
- **string**: адрес переменной для возврата строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — состояние мини-приложения успешно проверено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_CONST GX_CHAR *string;

/* Test status of widget “my_widget”. */
status = gx_widget_string_get(&my_widget, GX_STRING_ID_SHUTDOWN,
      &string);

/* If status is GX_SUCCESS the string has been retrieved */
```
### <a name="see-also"></a>См. также:

- gx_display_string_get
- gx_display_active_langauge_set

## <a name="gx_widget_string_get_ext"></a>gx_widget_string_get_ext
### <a name="retrieve-string-associated-with-a-visible-widget-and-string-id"></a>Получение строки, связанной с отображаемым мини-приложением и ИД строки

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_string_get(
    GX_WIDGET *widget,
    GX_RESOURCE_ID string_id,
    GX_CONST GX_STRING *string);
```

### <a name="description"></a>Описание

Эта служба возвращает запись таблицы строк для заданного значения идентификатора строки. Эта служба аналогична gx_display_string_get, за исключением того, что активный дисплей определяется автоматически, а не передается вызывающей стороной. Эту службу можно использовать только для тех мини-приложений, которые видимы, т. е. известен дисплей, связанный с этим мини-приложением.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **string_id**: значение идентификатора строки из заголовка ресурсов
- **string**: адрес переменной для возврата строки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — состояние мини-приложения успешно проверено.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_STRING string;

/* Test status of widget “my_widget”. */

status = gx_widget_string_get_ext(&my_widget,
                          GX_STRING_ID_SHUTDOWN, &string);

/* If status is GX_SUCCESS the string has been retrieved */
```

### <a name="see-also"></a>См. также:

- gx_display_string_get
- gx_display_active_langauge_set

## <a name="gx_widget_style_add"></a>gx_widget_style_add
### <a name="add-widget-style"></a>Добавление стиля мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_style_add(
    GX_WIDGET *widget, 
    ULONG style)
```

### <a name="description"></a>Описание

Эта служба добавляет стиль для мини-приложения. Кроме того, выполняются следующие действия.

Если добавленный стиль имеет значение GX_STYLE_TRANSPARENT, будет добавлено состояние GX_STATUS_TRANSPARENT.

Если добавленный стиль имеет значение GX_STYLE_ENABLED, будет добавлено состояние GX_STATUS_SELECTABLE.

Если мини-приложение отображается, оно автоматически становится недействительным и помещается в очередь для перерисовки.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **style**: новый стиль для добавления Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль мини-приложения успешно добавлен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Add style to widget “my_widget”. */
status = gx_widget_style_add(&my_widget, GX_STYLE_BORDER_RAISED);

/* If status is GX_SUCCESS, the style was successfully applied to the widget “my_widget”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_style_get"></a>gx_widget_style_get
### <a name="get-widget-style"></a>Получение стиля мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_style_get(
    GX_WIDGET *widget, 
    ULONG *return_style)
```

### <a name="description"></a>Описание

Эта служба получает флаг стиля из мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_style**: указатель на возвращаемый стиль

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль мини-приложения успешно получен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки


### <a name="example"></a>Пример

```C
/* Retrieve style from widget into “style”. */
status = gx_widget_style_get(&my_widget, &style);

/* If status is GX_SUCCESS the style flag from widget is saved in “style”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_remove
- gx_widget_style_add
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_style_remove"></a>gx_widget_style_remove
### <a name="remove-widget-style"></a>Удаление стиля мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_style_remove(
    GX_WIDGET *widget, 
    ULONG style)
```

### <a name="description"></a>Описание

Эта служба удаляет стиль из мини-приложения. Кроме того, выполняются следующие действия.

Если удаленный стиль имеет значение GX_STYLE_TRANSPARENT, будет удалено состояние GX_STATUS_TRANSPARENT.

Если удаленный стиль имеет значение GX_STYLE_ENABLED, будет удалено состояние GX_STATUS_SELECTABLE.

Если мини-приложение отображается, оно автоматически становится недействительным и помещается в очередь для перерисовки.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **style**: удаляемый стиль Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль мини-приложения успешно удален.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Remove style from widget “my_widget”. */
status = gx_widget_style_remove(&my_widget,
                                GX_STYLE_BORDER_RAISED);

/* If status is GX_SUCCESS the GX_STYLE_BORDER_RAISED style was removed from the widget “my_widget”.*/
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_style_set"></a>gx_widget_style_set
### <a name="set-widget-style"></a>Задание стиля мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_style_set(
    GX_WIDGET *widget, 
    ULONG style)
```

### <a name="description"></a>Описание

Эта служба задает стиль для мини-приложения.

Если стиль включает GX_STYLE_TRANSPARENT, будет добавлено состояние GX_STATUS_TRANSPARENT, в противном случае состояние будет удалено.

Если стиль включает GX_STYLE_ENABLED, будет добавлено состояние GX_STATUS_SELECTABLE, в противном случае состояние будет удалено.

Если мини-приложение отображается, оно автоматически становится недействительным и помещается в очередь для перерисовки.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **style**: задаваемый стиль Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — стиль мини-приложения успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set style GX_STYLE_TRANSPARENT to the widget “my_widget”. */
status = gx_widget_style_set(&my_widget, GX_STYLE_TRANSPARENT);

/* If status is GX_SUCCESS the widget “my_widget” style is set to GX_STYLE_TRANSPARENT. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_set
- gx_widget_width_get

## <a name="gx_widget_text_blend"></a>gx_widget_text_blend
### <a name="blend-text-assigned-to-widget-deprecated"></a>Смешивание текста, назначенного мини-приложению (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_text_blend(
    GX_WIDGET *widget, 
    UINT *tColor,
    UINT font_id, 
    GX_CHAR *string,
    INT x_offset, 
    INT y_offset, 
    UCHAR alpha)
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_widget_text_blend_ext().

Эта служба смешивает указанный текст с мини-приложением, используя текущие кисть и параметры выравнивания текста.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **tColor**: цвет текста
- **font_id**: идентификатор шрифта
- **string**: рисование строки
- **x_offset**: настройка позиции рисования
- **y_offset**: настройка позиции рисования
- **alpha**: значение смешивания (0–255)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина мини-приложения успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Blend “my_string” over “my_widget” given alpha value 120. */
status = gx_widget_text_blend(&my_widget, my_text_color, my_font_id,
                              &my_string, xoffset, yoffset, 120);

/* If status is GX_SUCCESS “my_string” has been blend to “my_widget”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_text_blend_ext

## <a name="gx_widget_text_blend_ext"></a>gx_widget_text_blend_ext
### <a name="blend-text-assigned-to-widget"></a>Смешивание текста, назначенного мини-приложению

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_text_blend(
    GX_WIDGET *widget, 
    UINT *tColor,
    UINT font_id, 
    GX_CONST GX_STRING *string,
    INT x_offset, 
    INT y_offset, 
    UCHAR alpha)
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_widget_text_blend_ext().

Эта служба визуализирует строку поверх указанного мини-приложения, используя текущую кисть и выравнивание текста, а также заданный цвет, шрифт и смещение по осям X, Y.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **tColor**: цвет текста
- **font_id**: идентификатор шрифта
- **string**: рисование строки
- **x_offset**: настройка позиции рисования
- **y_offset**: настройка позиции рисования
- **alpha**: значение смешивания (0–255)

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина мини-приложения успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_STRING_LENGTH** (0x34) — недопустимая длина строки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
gx_string render_string;
render_string.gx_string_ptr = “Hello”;
render_string.gx_string_length =
    strlen(render_string.gx_string_ptr);

/* Blend “my_string” over “my_widget” given alpha value 120. */
status = gx_widget_text_blend_ext(&my_widget,
        my_text_color,
        my_font_id,
        &render_string, xoffset, yoffset, 120);

/* If status is GX_SUCCESS “my_string” has been blend to “my_widget”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_text_draw_ext

## <a name="gx_widget_text_draw"></a>gx_widget_text_draw
### <a name="draw-text-assigned-to-widget-deprecated"></a>Вывод текста, назначенного мини-приложению (не рекомендуется)

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_text_draw(
    GX_WIDGET *widget, 
    UINT *tColor,
    UINT font_id, 
    GX_CHAR *string,
    INT x_offset, 
    INT y_offset)
```

### <a name="description"></a>Описание

Эта служба является устаревшей, и вместо нее следует использовать gx_widget_text_draw_ext().

Эта служба выводит указанный текст поверх мини-приложения, используя текущие кисть и параметры выравнивание текста.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **tColor**: цвет текста
- **font_id**: идентификатор шрифта
- **string**: рисование строки
- **x_offset**: настройка позиции рисования
- **y_offset**: настройка позиции рисования

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom widget draw function. */

VOID my_custom_widget_draw(GX_WIDGET *widget)
{
    /* Add your own background raw here. */

    /* Call widget text draw. */
    gx_widget_text_draw(widget, my_text_color, my_font_id
                        &my_string, xoffset, yoffset);
}
```

### <a name="see-also"></a>См. также:

- gx_widget_text_blend
- gx_widget_text_id_draw

## <a name="gx_widget_text_draw_ext"></a>gx_widget_text_draw_ext
### <a name="draw-text-assigned-to-widget"></a>Вывод текста, назначенного мини-приложению

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_text_draw(
    GX_WIDGET *widget,
    UINT *tColor,
    UINT font_id, 
    GX_CONST GX_STRING *string,
    INT x_offset,
    INT y_offset)
```

### <a name="description"></a>Описание

Эта служба выводит указанный текст поверх мини-приложения, используя текущие кисть и параметры выравнивание текста.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **tColor**: цвет текста
- **font_id**: идентификатор шрифта
- **text_id**: идентификатор текста
- **x_offset**: настройка позиции рисования
- **y_offset**: настройка позиции рисования

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
gx_string render_string;
render_string.gx_string_ptr = “Hello”;
render_string.gx_string_length =
    strlen(render_string.gx_string_ptr);

/* Write a custom widget draw function. */
VOID my_custom_widget_draw(GX_WIDGET *widget)
{
    /* Add your own background draw here. */

    /* Call widget text draw. */
    gx_widget_text_draw_ext(widget, my_text_color, my_font_id
                        &render_string, xoffset, yoffset);
}
```

### <a name="see-also"></a>См. также:

- gx_widget_text_blend
- gx_widget_text_id_draw

## <a name="gx_widget_text_id_draw"></a>gx_widget_text_id_draw
### <a name="draw-text-assigned-to-widget"></a>Вывод текста, назначенного мини-приложению

### <a name="prototype"></a>Прототип

```C
VOID gx_widget_text_id_draw(
    GX_WIDGET *widget, 
    UINT *tColor,
    UINT font_id, 
    UINT text_id,
    INT x_offset, 
    INT y_offse)
```

### <a name="description"></a>Описание

Эта служба выводит текст с указанным идентификатором текста поверх мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **tColor**: цвет текста
- **font_id**: идентификатор шрифта
- **text_id**: идентификатор текста
- **x_offset**: настройка позиции рисования
- **y_offset**: настройка позиции рисования

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Write a custom widget draw function. */

VOID my_custom_widget_draw(GX_WIDGET *widget)
{
    /* Add your own background raw here. */

    /* Call widget text id draw. */
    gx_widget_text_id_draw(widget, my_text_color, my_font_id
                            &my_string_id, xoffset, yoffset);
}
```

### <a name="see-also"></a>См. также:

- gx_widget_text_blend
- gx_widget_text_draw

## <a name="gx_widget_top_visible_child_find"></a>gx_widget_top_visible_child_find
### <a name="return-pointer-to-visible-child-that-is-top-of-z-order"></a>Возврат указателя к видимому дочернему элементу, который находится в верхней части Z-порядка

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_top_visible_child_find(
    GX_WIDGET *current,
    GX_WIDGET **widget_return);
```

### <a name="description"></a>Описание

GUIX хранит древовидный список родительских и дочерних мини-приложений.
Эта служба возвращает указатель на самый верхний видимый дочерний элемент текущего мини-приложения.

### <a name="parameters"></a>Параметры

- **current**: указатель на текущее мини-приложение
- **widget_return**: указатель на возвращаемый указатель мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — указатель возвращен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Retrieve topmost visible widget on the display */

GX_WIDGET *get_top_window(GX_WINDOW_ROOT *root)
{
    GX_WIDGET *top_window;
    UINT status;

    status = gx_widget_top_visible_child_find(root, &top_window);
    if (status == GX_SUCCESS)
    {
        return top_window;
    }
    return GX_NULL;
}
```

### <a name="see-also"></a>См. также:

- gx_widget_first_child_get,
- gx_widget_last_child_get,
- gx_widget_next_sibling_get,
- gx_widget_parent_get,
- gx_widget_previous_sibling_get

## <a name="gx_widget_type_find"></a>gx_widget_type_find
### <a name="search-for-a-widget-of-the-requested-type"></a>Поиск мини-приложения запрошенного типа

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_type_find(
    GX_WIDGET *parent, 
    USHORT widget_id,
    GX_WIDGET **return_widget)
```

### <a name="description"></a>Описание

Эта служба выполняет поиск мини-приложения запрошенного типа.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_width**: указатель на место назначения для ширины мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина мини-приложения успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_WIDGET *retrieved_widget;

/* Find horizontal scrollbar under “parent_widget”. */
status = gx_widget_type_find(&parent_widget,
                GX_TYPE_HORIZONTAL_SCROLL_BAR, &retrieved_widget);

/* If status is GX_SUCCESS, the horizontal scrollbar widget is retrieved. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set

## <a name="gx_widget_width_get"></a>gx_widget_width_get
### <a name="get-widget-width"></a>Получение ширины мини-приложения

### <a name="prototype"></a>Прототип

```C
UINT gx_widget_width_get(
    GX_WIDGET *widget,
    GX_VALUE *return_width)
```

### <a name="description"></a>Описание

Эта служба получает ширину мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на мини-приложение
- **return_width**: указатель на место назначения для ширины мини-приложения

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина мини-приложения успешно получена.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE my_widget_width;

/* Get width of widget “my_widget”. */
status = gx_widget_width_get(&my_widget, &my_widget_width);

/* If status is GX_SUCCESS the width of widget “my_widget” is in “my_widget_width”. */
```

### <a name="see-also"></a>См. также:

- gx_widget_attach
- gx_widget_back_move
- gx_widget_background_set
- gx_widget_border_draw
- gx_widget_border_style_set
- gx_widget_border_width_get
- gx_widget_canvas_get
- gx_widget_child_detect
- gx_widget_children_draw
- gx_widget_client_get
- gx_widget_created
- gx_widget_created_test
- gx_widget_delete
- gx_widget_detach
- gx_widget_draw
- gx_widget_draw_set
- gx_widget_event_generate
- gx_widget_event_process
- gx_widget_event_process_set
- gx_widget_event_to_parent
- gx_widget_find
- gx_widget_front_move
- gx_widget_height_get
- gx_widget_hide
- gx_widget_resize
- gx_widget_shift
- gx_widget_show
- gx_widget_status_add
- gx_widget_status_get
- gx_widget_status_remove
- gx_widget_status_test
- gx_widget_style_add
- gx_widget_style_get
- gx_widget_style_remove
- gx_widget_style_set

## <a name="gx_window_client_height_get"></a>gx_window_client_height_get
### <a name="get-window-client-height"></a>Получение высоты клиента окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_client_height_get(
    GX_WINDOW *window,
    GX_VALUE *return_height);
```

### <a name="description"></a>Описание

Эта служба возвращает высоту клиента окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **return_height**: указатель на место назначения для высоты клиента

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — высота клиента окна успешно получена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RECTANGLE my_client_height;

/* Get client height of “my_window”. */
status = gx_window_client_height_get(&my_window,
                                     &my_client_height);

/* If status is GX_SUCCESS the window “my_window” client height is contained in “my_client_height”. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- x_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_client_scroll"></a>gx_window_client_scroll
### <a name="scroll-window-clients"></a>Прокрутка клиентов окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_client_scroll(
    GX_WINDOW *window, 
    GX_VALUE x_scroll,
    GX_VALUE y_scroll);
```

### <a name="description"></a>Описание

Эта служба прокручивает клиенты окна на указанный размер.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **x_scroll**: размер прокрутки по оси X
- **y_scroll**: размер прокрутки по оси Y

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — клиент окна успешно прокручен.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_VALUE** (0x22) — недопустимые значения прокрутки.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Scroll clients of “my_window”. */
status = gx_window_client_scroll(&my_window, 10, 0);

/* If status is GX_SUCCESS the clients of window “my_window” have been scrolled. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_client_width_get"></a>gx_window_client_width_get
### <a name="get-window-client-width"></a>Получение ширины клиента окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_client_width_get(
    GX_WINDOW *window,
    GX_VALUE *return_width);
```

### <a name="description"></a>Описание

Эта служба получает ширину клиента указанного окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **return_height**: указатель на место назначения для ширины клиента

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — ширина клиента окна успешно задана.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_VALUE my_client_widthl

/* Get client width of “my_window”. */
status = gx_window_client_width_get(&my_window, &my_client_width);

/* If status is GX_SUCCESS “my_client_width” contains the client width of window “my_window”. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_close"></a>gx_window_close
### <a name="close-modal-window"></a>Закрытие модального окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_close(GX_WINDOW *window);
```

### <a name="description"></a>Описание

Эта служба принудительно отсоединяет модальное окно от его родительского окна и возвращает его из модального цикла выполнения.

### <a name="parameters"></a>Параметры

- **window**: указатель на блок управления окном

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — окно успешно закрыто.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Close window “my_window”. */
status = gx_window_close(&my_window);

/* If status is GX_SUCCESS window “my_window” has been closed. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_create"></a>gx_window_create
### <a name="create-window"></a>Создание окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_create(
    GX_WINDOW *window, 
    GX_CONST GX_CHAR *name,
    GX_WIDGET *parent, 
    ULONG style,
    USHORT window_id, 
    GX_CONST GX_RECTANGLE *size);
```

### <a name="description"></a>Описание

Эта служба создает окно.

GX_WINDOW является производным от GX_WIDGET и поддерживает все службы API gx_widget.

### <a name="parameters"></a>Параметры

- **window**: указатель на блок управления окном
- **name**: логическое имя окна
- **parent**: указатель на родительское мини-приложение
- **style**: стиль окна Предварительно заданные общие стили для всех мини-приложений, а также стили для конкретного мини-приложения см. в **приложении D**.
- **window_id**: определяемый приложением идентификатор окна
- **size**: размер окна

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — окно успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_WINDOW my_window;

/* Create window “my_window”. */
status = gx_window_create(&my_window, "my window",
                          &my_parent_window, GX_STYLE_BORDER_RAISED,
                          MY_WINDOW_ID, &size);

/* If status is GX_SUCCESS window “my_window” has been created. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_draw"></a>gx_window_draw
### <a name="draw-window"></a>Вывод окна

### <a name="prototype"></a>Прототип

```C
VOID gx_window_draw(GX_WINDOW *window);
```

### <a name="description"></a>Описание

Эта служба рисует окно. Эта служба обычно вызывается внутри во время обновления холста, но также может вызываться из настраиваемых функций рисования окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на блок управления окном

### <a name="return-values"></a>Возвращаемые значения

- **None**

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Write a custom window draw function. */

VOID my_custom_window_draw(GX_WINDOW *window)
{
    /* Call default window draw. */
    gx_window_draw(window);

    /* Add your own drawing here. */
}
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_event_process"></a>gx_window_event_process
### <a name="process-window-event"></a>Обработка события окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_event_process(
    GX_WINDOW *window, 
    GX_EVENT *event);
```

### <a name="description"></a>Описание

Эта служба обрабатывает событие для окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на блок управления окном
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие окна успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic window event processing as part of custom event processing function. */

UINT custom_window_event_process(GX_WINDOW *window,
                                 GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
        case xyz:
            /* Insert custom event handling here */
            break;
        default:
            /* Pass all other events to the default window
            event processing */
            status = gx_window_event_process(window, event);
            break;
        }
        return status;
}
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_execute"></a>gx_window_execute
### <a name="modally-execute-a-window"></a>Модальное выполнение окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_execute(
    GX_WINDOW *window,
    ULONG *return_ptr)
```

### <a name="description"></a>Описание

Эта служба модально выполняет окно. Любые входные данные пользователя (события пера и т. д.) за пределами клиентской области окна будут игнорироваться. Обратите внимание, что эта функция входит в непрерывный цикл выполнения блокировки и не возвращает вызывающему объекту до завершения выполнения модели.

Модальное выполнение завершается, когда обработка события для любого полученного события возвращает значение состояния, отличное от нуля, или когда вызывается функция API gx_window_close. Код возврата обработки ненулевого события возвращается вызывающему объекту через return_ptr, переданный в этот API.

### <a name="parameters"></a>Параметры

- **window**: указатель на блок управления окном
- **return_ptr**: расположение для сохранения состояния завершения модального выполнения Может быть GX_NULL.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — успешно выполнено.
- **GX_SYSTEM_EVENT_RECEIVE_ERROR** (0x05) — сбой получения события из очереди событий.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Execute a modal window. */
status = gx_window_execute(&my_window, &return_code);

/* If status is GX_SUCCESS the window was executed, and return_code holds the execution return code. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_root_create"></a>gx_window_root_create
### <a name="create-a-root-window"></a>Создание корневого окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_root_create(
    GX_WINDOW_ROOT *root_window,
    GX_CONST GX_CHAR *name,
    GX_CANVAS *canvas, 
    ULONG style,
    USHORT id,
    GX_CONST GX_RECTANGLE *size)
```

### <a name="description"></a>Описание

Эта служба создает корневое окно.

### <a name="parameters"></a>Параметры

- **root_window**: указатель на блок управления корневым окном

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — корневое окно успешно создано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_SIZE** (0x19) — недопустимый размер блока управления мини-приложения.
- **GX_ALREADY_CREATED** (0x13) — мини-приложение уже создано.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_ROOT_WINDOW root_window;
GX_CANVAS canvas;

/* Create canvas here. */

/* Create a root window */
status = gx_window_root_create(&root_window, “root”, &canvas,
GX_STYLE_BORDER_NONE, GX_NULL, &size);

/* If status is GX_SUCCESS, the “root_window” is successfully created. */
```

### <a name="see-also"></a>См. также:

- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find

## <a name="gx_window_root_delete"></a>gx_window_root_delete
### <a name="destroy-a-root-window"></a>Уничтожение корневого окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_root_delete(GX_WINDOW_ROOT *root_window)
```

### <a name="description"></a>Описание

Эта служба удаляет корневое окно.

### <a name="parameters"></a>Параметры

- **root_window**: указатель на блок управления корневым окном

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — корневое окно успешно удалено.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_SYSTEM_MEMORY_ERROR** (0x30) — не определена функция освобождения памяти.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Delete a root window */
status = gx_window_root_delete(&root_window);

/* If status is GX_SUCCESS the “root_window” is destroyed. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_root_event_process"></a>gx_window_root_event_process
### <a name="process-event-for-the-root-window"></a>Обработка события для корневого окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_root_create(
    GX_WINDOW_ROOT *root_window,
    GX_EVENT *event)
```

### <a name="description"></a>Описание

Эта служба обрабатывает события для указанного корневого окна.

### <a name="parameters"></a>Параметры

- **root_window**: указатель на блок управления корневым окном
- **event**: указатель на событие для обработки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — событие корневого окна успешно обработано.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.

### <a name="allowed-from"></a>Допустимые источники

Потоки

### <a name="example"></a>Пример

```C
/* Call generic root window event processing as part of custom event processing function. */

UINT custom_root_window_event_process(GX_ROOT_WINDOW *root,
                                      GX_EVENT *event)
{
    UINT status = GX_SUCCESS;

    switch(event->gx_event_type)
    {
    case xyz:
        /* Insert custom event handling here */
        break;
    default:
        /* Pass all other events to the default root window
            event processing */
        status = gx_window_root_event_process(root, event);
        break;
    }
    return status;
}
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_root_find"></a>gx_window_root_find
### <a name="find-root-window"></a>Поиск корневого окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_root_find(
    GX_WIDGET *widget,
    GX_WINDOW_ROOT **return_root_window);
```

### <a name="description"></a>Описание

Эта служба выполняет поиск корневого окна для указанного мини-приложения.

### <a name="parameters"></a>Параметры

- **widget**: указатель на блок управления мини-приложением
- **return_root_window**: указатель на место назначения для найденного корневого окна

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — корневое окно успешно найдено.
- **GX_FAILURE** (0x00) — корневое окно не существует.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Find root window associated with window “my_window”. */
status = gx_window_root_find(&my_window, &root_window);

/* If status is GX_SUCCESS the “root_window” contains the root window for window “my_window”. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_scroll_info_get"></a>gx_window_scroll_info_get
### <a name="get-window-scroll-info"></a>Получение сведений о прокрутке окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_scroll_info_get(
    GX_WINDOW *window, 
    ULONG style,
    GX_SCROLL_INFO *return_scroll_info);
```

### <a name="description"></a>Описание

Эта служба получает сведения о прокрутке окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **style**: GX_SCROLLBAR_HORIZONTAL или GX_SCROLLBAR_VERTICAL
- **return_scroll_info**: указатель на назначение для сведений о прокрутке Родительское окно инициализирует эту структуру для информирования полосы прокрутки об общем размере родительского окна, области просмотра, а также о шаге приращения и ограничениях прокрутки. Реализация по умолчанию использует клиентскую область окна в качестве видимой области и прокручивается по пикселям, но настраиваемая реализация окна может использовать параметры прокрутки. Определение структуры GX_SCROLL_INFO см. в **приложении I**.

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — сведения о прокрутке окна успешно получены.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_TYPE** (0x1B) — недопустимый тип.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_SCROLL_INFO scroll_info;

/* Get scroll information for window “my_window”. */
status = gx_window_scroll_info_get(&my_window,
                    GX_SCROLLBAR_HORIZONTAL, &scroll_info);

/* If status is GX_SUCCESS the “scroll_info” contains the scroll information for window “my_window”. */
```
### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scrollbar_find
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_scrollbar_find"></a>gx_window_scrollbar_find
### <a name="find-window-scrollbar"></a>Поиск полосы прокрутки окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_scrollbar_find(
    GX_WINDOW *window, 
    USHORT type,
    GX_SCROLLBAR **return_scrollbar);
```

### <a name="description"></a>Описание

Эта служба выполняет поиск полосы прокрутки для указанного окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **type**: GX_TYPE_VERTICAL_SCROLL или GX_TYPE_HORIZONTAL_SCROLL
- **return_scrollbar**: указатель на место назначения для полосы прокрутки

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — полоса прокрутки окна успешно найдена.
- **GX_NOT_FOUND** (0x09) — полоса прокрутки не найдена.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.
- **GX_INVALID_TYPE** (0x1B) — недопустимый тип.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Find horizontal scrollbar for window “my_window”. */
status = gx_window_scrollbar_find(&my_window,
                 GX_SCROLLBAR_HORIZONTAL, &my_scrollbar);

/* If status is GX_SUCCESS the “my_scrollbar” contains the horizontal scrollbar for window “my_window”. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_wallpaper_get
- gx_window_wallpaper_set

## <a name="gx_window_wallpaper_get"></a>gx_window_wallpaper_get
### <a name="get-window-wallpaper"></a>Получение фонового рисунка окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_wallpaper_get(
    GX_WINDOW *window,
    GX_RESOURCE_ID *return_wallpaper_id);
```

### <a name="description"></a>Описание

Эта служба получает фоновый рисунок для указанного окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **return_wallpaper_id**: указатель на назначение для идентификатора ресурса фонового рисунка

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — фоновый рисунок окна успешно получен.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
GX_RESOURCE_ID my_window_wallpaper;

/* Get wallpaper for window “my_window”. */
status = gx_window_wallpaper_get(&my_window, &my_window_wallpaper);

/* If status is GX_SUCCESS the “my_window_wallpaper” contains the wallpaper resource ID for window “my_window”. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
- gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_set

## <a name="gx_window_wallpaper_set"></a>gx_window_wallpaper_set
### <a name="set-window-wallpaper"></a>Задание фонового рисунка окна

### <a name="prototype"></a>Прототип

```C
UINT gx_window_wallpaper_set(
    GX_WINDOW *window,
    GX_RESOURCE_ID wallpaper_id,
    GX_BOOL tile);
```

### <a name="description"></a>Описание

Эта служба задает фоновый рисунок для указанного окна.

### <a name="parameters"></a>Параметры

- **window**: указатель на окно
- **wallpaper_id**: идентификатор ресурса фонового рисунка
- **tile**: если GX_TRUE, применяется мозаичное заполнение фоновым рисунком, в противном случае фоновый рисунок не мозаичный

### <a name="return-values"></a>Возвращаемые значения

- **GX_SUCCESS** (0x00) — фоновый рисунок окна успешно задан.
- **GX_CALLER_ERROR** (0x11) — недопустимый вызывающий объект для этой функции.
- **GX_PTR_ERROR** (0x07) — недопустимый указатель.
- **GX_INVALID_WIDGET** (0x12) — недопустимое мини-приложение.

### <a name="allowed-from"></a>Допустимые источники

Инициализация и потоки

### <a name="example"></a>Пример

```C
/* Set wallpaper for window “my_window”. */
status = gx_window_wallpaper_set(&my_window,
                                 MY_WALLPAPER_RESOURCE_ID,
                                 GX_TRUE);

/* If status is GX_SUCCESS the wallpaper for window “my_window” is set. */
```

### <a name="see-also"></a>См. также:

- gx_window_canvas_set
- gx_window_client_height_get
- gx_window_client_scroll
- gx_window_client_width_get
- gx_window_create
- gx_window_draw
- gx_window_event_process
- gx_window_root_create
- gx_window_root_delete
- gx_window_root_event_process
- gx_window_root_find
-  gx_window_scroll_info_get
- gx_window_scrollbar_find
- gx_window_wallpaper_get
